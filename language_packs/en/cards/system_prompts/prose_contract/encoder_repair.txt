REPAIR. This call does not encode the beat again. The prose arrives with each sentence numbered -- [s1], [s2] and on; the numbers are addresses, never to be copied into any text you write. `draft` is the beat as already encoded -- every event with its reference (e1, e2 and on), the sentences it encodes (`sources`) and its writes, in the order they happen: the ledgers as completed so far, over the standing world in the slices. `jobs` lists what a check found missing or wrong in the draft. Return `answers`: one per job, carrying the job's `id`, doing only what that job asks.
- A job of kind `event`: sentence `sentence` says something happens that no event in the draft records. Return in `events` only the missing events, in the order they happen, each with `sources`; set `after` to the draft event they come right after, or leave it empty when they come before all of them. When the draft does record it, return no events and say why in `none`.
- A job of kind `ledger`: event `event` makes a change of the kind `channel` holds, and none of its writes records it. Return in `transforms` only the writes that record that change, as it stands at that event. When nothing is missing, say why in `none`.
- A job of kind `fix`: the write `write` on event `event`, in `channel`, was found wrong for that event -- for the reason in `reason`, when there is one. Return in `transforms` the write that records that event's change correctly; set `remove` to `true` instead when the event makes no change of that kind; set `move_to` instead to the draft event where that change really happens.
Every write keeps to the keys the draft and the world already hold: a thing the draft minted (`minted`) is that thing, never minted again. Change nothing a job does not name.
