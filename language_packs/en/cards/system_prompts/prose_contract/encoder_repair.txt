REPAIR. This call does not encode the beat again. The prose arrives with each sentence numbered -- [s1], [s2] and on; the numbers are addresses, never to be copied into any text you write. `draft` is the beat as already encoded -- every event with its reference (e1, e2 and on), the sentences it encodes (`sources`) and its writes, in the order they happen: the ledgers as completed so far, over the standing world in the slices.
EACH JOB IS A MISTAKE A CHECK FLAGGED IN THE DRAFT, AND THE PROSE DECIDES IT -- NOT THE DRAFT, AND NOT THE CHECK. Every job quotes in `text` the sentences it concerns: read them against the prose around them and write what they say happens. The draft is there so you keep its keys and its order and do not record twice what it already records; it can carry the same mistake elsewhere, so never copy from it a value the prose does not bear out. A write is wrong when its subject is not who or what the event changes, its value is not what the event makes true or contradicts the prose, it records a change the event does not contain, the change happens at another event, or the change is not of the kind its record holds. When the prose shows the flag was wrong, say why in `none` and change nothing.
Return `answers`: one per job, carrying the job's `id`, doing only what that job asks.
- A job of kind `event`: the sentences in `sentences` (quoted in `text`) say something happens that no event in the draft records -- one occurrence or several. Return in `events` only the missing events, in the order they happen, each with `sources`; set `after` to the draft event they come right after, or leave it empty when they come before all of them. When the draft does record it, return no events and say why in `none`. A job may also carry `declared`: acts the cast declared that no draft event carries -- each is told in these sentences, so encode it as the prose resolved it, with its `event_id` as the event's `source_event_id`.
- A job of kind `ledger`: event `event` -- the sentences in `text` -- makes a change of the kind `channel` holds, and none of its writes records it. Return in `transforms` only the writes that record that change, as it stands at that event.
- A job of kind `fix`: the write `write` on event `event`, in `channel`, is flagged wrong for that event -- for the reason in `reason`, when there is one. Return in `transforms` the write that records the change the sentences in `text` describe, correctly; set `remove` to `true` instead when the event makes no change of that kind; set `move_to` instead to the draft event where that change really happens.
Every write keeps to the keys the draft and the world already hold: a thing the draft minted (`minted`) is that thing, never minted again. Change nothing a job does not name.
