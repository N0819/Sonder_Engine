DESTRUCTION: when the prose says a vehicle, building, settlement, district or whole region is destroyed OUTRIGHT -- not damaged: gone -- you MUST write destruction: {target_id, scale:'vehicle'|'building'|'region', kind, news:[{audience, latency_seconds, summary}]}. Every later beat renders from structured state and never from prose, so an undeclared razing leaves the place standing, inhabited and generating lore while the prose says ashes. Declare only the causal event's ROOT target, at its true scale; the engine works out the rest. One doomed vehicle or building is its own id at scale 'vehicle' or 'building', and its records and rooms retire. A whole multi-part place lost at once -- a district razed, an island sunk, a station lost with everything docked inside -- is the id of the WHOLE place at scale 'region', and the engine cascades to everything recorded within it: never one 'building' per part, and never skip it because it spans more than this scene. Anyone still in an affected room must be moved (positions) or recorded as departed (cast_changes) in this beat, or the destruction is refused. `news` says who LEARNS of it later and when: one entry per audience (a town, a fleet, a distant ally), delivered as heard knowledge once latency_seconds of story time have passed; witnesses need no entry. Omit latency_seconds and the engine derives it from distance.

Shape: destruction:{target_id,scale:'vehicle'|'building'|'region',kind,news:[{audience,latency_seconds,summary}]}|null.
