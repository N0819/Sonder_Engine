# Director prompt clarity stress — 2026-09-15

## Latest measured result

The new flask scene and known coat regression execute their checked physical
sequences correctly. The first new key capture exposed a routing ambiguity; a
separately labeled key regression after a targeted clarification now preserves
the insertion, failed turn and withdrawal in both the world timeline and
observer perception. Every capture, including the initial failure, is retained.
The final key capture passes all 30 predefined checks and the manual topology
and delivery checks; its zero-call replay reproduces the compared world fields
and event log exactly.

## First-pass result

The rewritten prompts preserve all **36 prelisted event propositions and 9
spoken lines with their correct speakers** across two new scenes and one
previously observed coat regression. All **96 predefined structural and quote
assertions pass**. Manual review of that first pass finds an important failure: three key
handling actions are present in the log and their physical contact topology is
correct, but their completion status is unresolved and they do not reach the
other observer's perception. Passing the finite state checklist does not make
that failure a pass.

The [frozen corpus](../../tests/data/causal_prompt_clarity_stress.json) was
written before any calls for this revision, including manual checks for the
key's actual interior contact, its withdrawal, simultaneous reciprocal
transfer, intermediate states, and unchanged supports. Its SHA256 is
`9a5d841c6a91594758a6bb38f541b140a1b9da589eff4a32658a1b243a263e5c`.
The two new cases contain 22 propositions and 6 quotes. The coat case contains
14 propositions and 3 quotes and is explicitly a **known regression**, not new
holdout evidence.

The [evidence bundle](data/causal_prompt_clarity_2026_09_15.json) retains exact
prompts, scoped model inputs, accepted and rejected replies, intermediate
worlds, completion receipts, final worlds, event logs, observer views, reviewed
chronology alignments, and manual findings. Prompt bodies are deduplicated by
SHA256. [Readable event logs](CAUSAL_PROMPT_CLARITY_2026_09_15_EVENTS.md) expose
execution statuses as well as prose accounts. Scratch databases and provider
credentials are excluded; exports are checked against every scratch database's
configured secrets.

| Scene | Group | Expected propositions | Quotes | Log rows | Calls | Seconds | Structural/quote checks |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| Two flasks | New first pass | 12 | 3 | 13 | 5 | 44.25 | 33 pass |
| Partial key insertion | New first pass | 10 | 3 | 10 | 5 | 36.80 | 30 pass; manual delivery failure |
| Coat | Known regression | 14 | 3 | 14 | 7 | 63.95 | 33 pass |

### Two flasks: chronology, identity and supports

Pickup, opening and closing occupy distinct boundaries. The reciprocal
exchange occupies one boundary containing both inventory transfers and both
new grips: Eren receives the striped flask while Mara receives the dotted
flask. Later pickup, return handover and final set-down also remain separate.
The flask identities never merge; old grips and old support contacts clear;
both flasks end released on the stool with closed caps.

The counter and stool are references/supports, not spurious required fixture
mutations. All physical actions receive applied receipts and reach the other
participant's perception. The remembered quoted command does not open either
flask. All three lines retain their speaker and words in the event log.

One extra row describes rain sound. Its `sensory_events` effect has no scene
postcondition verifier, so the receipt is pending; the sound nevertheless
arrives through ambient perception. The event-log actor defaults to Mara even
though the account and sensory source identify rain. This exposes a remaining
environmental-source attribution limitation. The perception renderer also
strips the closing inner quotation glyph from Eren's remembered quotation;
the original event log preserves it. These are separate from the correct
flask transformations.

### Key: correct topology, incomplete observer delivery

The insertion creates an actual object-to-object relation:
`short_key` tip → `lock` keyway, with an interior relation. Neri retains the
hand-to-key grip and held possession. The relation persists through the failed
turn and speech, ends at the withdrawal boundary, and the key remains held
until its later set-down. The short and long keys stay distinct, the long key
never enters the lock, and the cashbox stays locked and closed.

The Director also assigns `contact_action_ops` to insertion, attempted turning
and withdrawal. That channel describes persistent tactile dynamics; these
spans need a contact change or a transient unsuccessful act. The contact hand
correctly supplies the insertion/removal through `contact_ops`, and explicitly
settles the no-change failed attempt `not_mine`. The engine still has the
Director's literal `contact_action_ops` obligation, so all three spans become
unresolved. Their accounts remain in the event log, but Olan does not perceive
them. This is a routing/completion mismatch rather than the earlier missing
key-to-lock physical relation.

### Coat: the previous conflict does not recur

Removal combines attire removal, a real hand-to-coat contact and an inventory
transfer to Noa in held mode. No containment clear erases the new held relation.
Handovers and set-downs keep the same garment identity and remove old grips.
Re-wearing agrees across worn containment, Noa's wardrobe list and garment
regions; her hands are free, and the same coat follows her onto the landing.
Every physical row is applied or unchanged and reaches the other participant's
view.

The body hand initially returned one result for two rows. The normal validation
repair supplied both results; both replies remain in the capture. There was no
full-scene reroll.

## Separately labeled key regression

After reviewing the three frozen first captures, the Director prompt was
clarified: `contact_ops` changes contact topology, `contact_action_ops` describes
persistent tactile dynamics, and a visible unsuccessful act with no record
change may use `categories: []`. The contact-action chunk was clarified to the
same effect. Hand-specific worked examples were moved into their gated channel
chunks, and item allocation was clarified to number only first appearances in
item lists. No runtime behavior changed during these prompt captures.

The one additional key capture uses **6 calls and 35.58 seconds**. Insertion and
withdrawal now route only to contact topology and receive applied receipts. The
failed turn is a separate recorded act with no invented world mutation. Olan's
view contains all three actions in order. The actual key-tip-to-keyway relation
and retained hand grip still agree, the lock remains locked, and the cashbox
remains shut. All ten propositions, three correctly attributed quotes, 30
predefined assertions and the manual topology/delivery checks pass. The
Director's item handles are dense and no allocation warning occurs.

The objects hand initially returned one result for four input rows; the normal
validation repair supplied four. Both responses remain in the evidence. This
is one explicitly labeled revision regression following a measured failure,
not a replacement for the original capture or new holdout evidence. The flask
and coat captures were not repeated after this last clarification.

All four captures replay with **zero provider calls**, with network and DNS
blocked, no replay errors, no unused accepted replies, and exact equality of
the compared entity/position/station/pose/contact/containment/attire/substance/
room fields and the entire event log. The first group's replay records prompt
drift because it uses the final prompt sources. In the final key replay, four
of five accepted-call prompt/payload pairs match exactly. The objects answer
was originally a repair response with a repair wrapper; replay delivers that
accepted response at the ordinary call site, so its wrapper differs while the
current contract validates it. Replay establishes deterministic application of
the captured replies, not a fresh model success.

## Scope and usage

The first-pass group uses **17 provider calls, 129,985 input tokens and 37,896
output tokens**, totaling 145 seconds across the three sequential captures.
All calls use OpenRouter `z-ai/glm-5.2` with the preceding experiment's copied
role configuration. Token counts are provider-reported and may include
reasoning; monetary cost was not captured. No specialist ledger exposes a
private item or chronology ID.

Each case runs in its own synthetic scratch story through the real Director,
specialists, chronological composition and deterministic perception. The runner
does not simulate autonomous character decisions or a narrator and does not
commit a production story. Configuration is read-only at its source. There is
one full capture per case; normal validation repairs and complementary
specialist dispatches remain part of that capture. These are three examples,
not a reliability estimate or a controlled isolation of each prompt change.

The additional key regression uses **43,171 input and 9,921 output tokens**.
Across the four paid captures, totals are **23 calls, 173,156 input tokens and
47,817 output tokens**. The repeated key scene adds three preserved quoted
lines, for **12 correctly attributed lines across all four captures**. All
captured specialist payloads, including repair wrappers, were checked for
private item/chronology ID fields and contain none.

## Reproduce the final captured run without a provider

```bash
.venv/bin/python tools/causal_stress.py unpack \
  --bundle docs/experiments/data/causal_prompt_clarity_2026_09_15.json \
  --run regression --out /tmp/prompt-clarity-captured
.venv/bin/python tools/causal_stress.py run \
  --corpus tests/data/causal_prompt_clarity_stress.json \
  --case partial_key_insertion_failed_turn \
  --replay-root /tmp/prompt-clarity-captured \
  --out /tmp/prompt-clarity-replayed
```

The bundle's `alignments.regression` contains the reviewed event-to-chrono map
for intermediate checks. Use it with the runner's `score --alignment` command;
replay does not infer these correspondences from event numbering.

## Automated verification and final wording review

- 84 focused prompt/Director checks passed with the project runtime after the
  final wording review. Both complete orchestration/contact-onset test files
  also pass (290 checks), as do the four updated attire/contact wording checks.
- The 30 specialist-example tests include real scoped prompt assembly: each
  example appears only with its channel grant, and both language packs'
  outputs satisfy the current strict schema and item accounting. These overlap
  the focused total and are not additional independent trials.
- Full suite under the project runtime (Pydantic 2.11.7): **15,432 passed,
  30 failed, 3 skipped**, in 12m17s. All 30 failure names also occur in the
  earlier completion audit's full run. This is not a fully green repository.
- Regenerated the code map; project structure checks passed. The existing
  deferred Japanese protocol-parity notice remains. `git diff --check` passes.

An initial broad validation run loaded the user's older dependency ahead of
the project environment and overlapped test-example relocation. It is not
the final result quoted above. The final complete run uses the project
dependency first and the finished source tree. An additional combined focused
run stalled and was stopped; its partial progress is not counted. The final
84-check run and the complete 290-check and four-check runs above completed
successfully under the intended environment.

After the final live capture, review clarified two shared-core sentences:
a mixed result retaining any valid transforms uses `encoded`, with other
items' unresolved identities in `settled`; an empty `not_mine` response
reports no applicable effect without certifying assigned work complete.
These match existing validation and completion behavior and do not change
runtime code. This final wording refinement has static contract coverage,
not an additional provider capture. The evidence retains the exact prompt
version used for every recorded call.
