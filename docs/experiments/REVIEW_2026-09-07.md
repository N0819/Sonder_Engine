# Sonder Engine review synthesis — 319 verified findings

Read-only review, 2026-09-07. 24 readers (18 subsystem slices, 6 lens agents), 319 findings surviving adversarial verification, 4 rejected. Every line number below was checked against the working tree by a verifier; where the verifier corrected a line, the corrected one is given.

## Executive summary

1. **The identity floor is one rule applied at some sites and not their siblings.** `observer_label_fn`/`_recognizes` is the engine's answer to "may this observer name that body"; the outcome pass (perception.py:5231/5272/5312), the loop-warmed `awaiting_your_answer` cache (character.py:539), the micro-round (loops.py:263), every background-presence field except `present_others` (background.py:252/1031/1573), the narrator's `spatial_frame.ahead_entity` (narration.py:1769) and `player_said_nothing` (character.py:790) each re-derive it narrower or skip it. Six firewall leaks from one missing call.
2. **Script-blind guards fail open.** `\b`, `[a-z]+`, `[^a-z0-9]` and English word tuples sit beside the repaired `name_boundary_pattern`/`fold_identity_key`/pack-`_ling` siblings: observer_name_scrub and `_strip_identity_tokens` (common.py:4364/4704), the disguise-disclosure guard (scene.py:865), `_scene_has_subject`, `_asks_player`, `rekey_place_claims`, `is_player_speaker`, commit_attire's introduction gate, every commit_background matcher, lore `_fts_query`, history routing, card warnings, the `_origin_on_drift` mood lists. A Japanese story gets a firewall that does not fire and guards that never match.
3. **Two representations of one fact, free to disagree.** perception_outcome's scene mirror vs. commit's composition (five passes behind); `classify_movement` exact-match vs. `dedup_minted_rooms` fold; `remove_adjacent` one-sided vs. reciprocal readers; vitals exact key vs. casefold readers; `appearance_of` bare `.get` vs. `entry_for`; three registry writers vs. "one chokepoint"; judgments keyed by stranger label vs. canonical subject; routines' private 24h day vs. `day_cycle`; ruin flag vs. `room_registry` retirement; NSFW overlay decided three ways; ATTIRE_REGIONS retyped in JS; the JS translator copied twice.
4. **Retirement residue and half-wired channels.** The offscreen hand survives in both stage sheets (dispatch reports the key unrouted and no hand runs), "six specialists" in ~25 comments and three UI sentences, interpret specialists are dispatched BY a note and shown no note, the specialist repair merge is a hand list that drops 14 owned channels, `source_manifest` (tells) is computed per observer and read by nobody, `stable_facts` is solicited and discarded, `fed_by` is read by four systems and written by none, `medium:'comm'` is stripped by the schema before its five readers.
5. **Wall-clock.** Nothing on the turn is memoised at the PipelineContext: the scene blob is SELECT+`json.loads`'d at ~50 sites per turn (only `charters` has the `world_read_token` cache built for exactly this), the charter registry is re-normalized and re-saved several times per commit under the write lock (0.95–2.5 s each on the 307-body town), the per-turn checkpoint re-serialises the 41 MB registry and hashes+INSERTs every memory vector from a function documented read-only, `compile_world_context` pays an embedding call serially ahead of a stage that never reads it, the streaming path 400s on every schema-bearing call for the life of the process after one rejection, a reasoning-only empty reply costs two or three extra model calls where one retry would do, `visual_level_between`/`effective_station`/`proximity_rel` are O(N²) with no per-scene memo, and one full scene deep-merge happens twice (outcome mirror, commit) plus six more times for route checks inside resolve.

---

## Section A — BUGS and GAPS (deduplicated, high → low)

Format: **title** — `file:line` — what happens — evidence — fix as a class rule — readers.

### High

**A1. Single-step reroll rehydrates side channels from the steps AFTER the rerolled one** — `agents/runtime.py:1112` — The `only_key` branch hydrates every stored step (`ORDER BY ord`, no cut) and rehydrates each; for `perception_outcome` the handler runs against the OLD `director_resolve` and writes `ctx["_player_room"]` from the discarded outcome scene (perception.py:2766-2768). A rerolled `director_resolve` then reads that room at 3367/3426/4590 and grades the player from the room the discarded ruling moved her to; the checkpoint restore at 1153 exists to prevent exactly this. — `from_key` (1317-1330) stops at `start_i`; this path has no `ord < s["ord"]` filter. — Rule: no stage may read a later stage's output; hydrate and rehydrate only ords below the rerolled step. — agents-runtime+plan.

**A2. Identity floors match names with ASCII boundaries, so an unspaced-script name is never scrubbed or stripped** — `agents/common.py:4364, 4704, 4531`; `story/scene.py:865/941/970` — `observer_name_scrub` uses `\b{form}\b`, `_strip_identity_tokens` uses `(?<!\w)…(?!\w)`, `_unknown_actor_label` splits on whitespace; the disguise-disclosure guard's `_ABSENCE`, `_part_tokens` (`[a-z]+`) and concealed-term scrub (`\b`) are English/ASCII. In ja a particle is a word char, so the guard passes the canonical name/denial through with no warning and the fallback label is the English literal "a person whose appearance is unremarkable". — `character_schema.name_boundary_pattern` (1484) documents this exact fail-open; siblings at common.py:4907/4790 are already repaired; tests use Latin names only; ja `linguistics.json` has no `story.scene` section. — Rule: every identity-form match goes through `name_boundary_pattern`/`name_boundary_regex`; every English vocabulary lives in the pack under `linguistic(module, name)`. Sibling instances (medium/low): `_scene_has_subject` (366), `_asks_player` (2757), `_PHRASE_BREAK`/`_word_shingles`/`_check_prose_quote_authority`/`_narration_person_counts` (7754/7922/6130/8294), `rekey_place_claims` (theory_of_mind.py:334), `is_player_speaker` (scene.py:1961 ×3 plus 11 more `[^a-z0-9]` sites), `_garment_named_in` (commit_attire.py:245, also refuses garments under four letters), all commit_background matchers (1087/1135/3005/2781/95/2740), `_fts_query` (memory_common.py:183), `_origin_on_drift` mood tuples (memory_context.py:193), `history_routing` patterns (24-58, reports .9 confidence on an unread card), `character_card_warnings` literals and `_PART_WORDS` (character_schema.py:2127/2290), `_declares_rapid_movement` regex (director_movement.py:509). — agents-common, story-cards+scene, mind, persist-memory+archive, persist-scene.

**A3. Outcome pass names a speaker/actor through an identity-concealing disguise** — `agents/perception.py:5231, 5272, 5312` — `if _recognizes(speaker, recognized): display = speaker` runs BEFORE `display_map`, and `recognized` is the bare `known` ledger, so an acquaintance not in `disguise_known_to` receives the canonical name for speech, communication and acts. The act stage reads the map (4519); `_composer_company` reads the map; the same view calls the body unrecognised and names it. — `composer.observer_display_map` (502-508) applies `disguise_breaks_recognition`; `_composer_unknown_sources` (3099) reads `known` only; no outcome-pass disguise test. — Rule: one label per (observer, body) per beat, read from `display_map` first in every branch. — agents-perception+composer.

**A4. Authored-prose gate is bypassed for every sensory event carrying `detail`** — `agents/perception.py:3553` — `_gated_ambient_percepts` writes the gated text to `desc/description/text` but not `detail`; `composer.ambient_percepts` (2151) reads `detail` FIRST; every beat sound is `dict(record, desc=record["detail"])` (4939) and the normaliser emits `detail`, so every beat sound, charter noise and `heard_events` row reaches the percept ungated. — No test names `_gated_ambient_percepts`. — Rule: a gate rewrites the key the reader reads; extract text with the reader's precedence and hand back a single gated `detail`. — agents-perception+composer.

**A5. Interpret-side specialists never receive the ruling that dispatched them** — `agents/director_fanout.py:408` — `director_note` is attached only when `view["source"] == "resolved_beat"`; the interpret branch sets `player_declaration` alone though `_interpret_beat_view` carries `ledger_notes` "so `_note_for` routes an interpret-side ruling". Every hand's sheet ends with director_note.txt: no note + no `changes_asserted` (interpret manifest is `[]`) = encode nothing. A player's asserted coat-off/sits-down reaches the onset preview only if a model disobeys its sheet. — `_note_for` has one caller (fanout:408); test at test_director_orchestration.py:1039 never asserts `director_note`. — Rule: the note lookup lives above the source branch. — agents-director-core.

**A6. Specialist repair patches in 14 owned channels are silently dropped by `_merge_repair_into_diff`** — `agents/director_evidence.py:515` — The merge is a hand list (rooms, entities, attire, overlays, conditions, positions, poses, stations, ten list fields, time). `scales`, `containment`, `vitals`, `destruction`, `artifact_ops`, `sensory_events`, `comms_ops`, `crowd_ops`, `courier_ops`, `telling_ops`, `charter_ops`, `public_evidence`, `ratified_claims`, `contradicted_claims` never reach `sd`; the report says encoded, the re-check (director.py:2307) finds nothing, the beat ships a staleness warning for a repair that was made and thrown away. `_normalize_diff_shape` (196) has the same hand list. — The block comment at 297-315 derives the two READER tables from `schemas._fields(StateDiff)` for exactly this drift; only positions/rooms/remove_adjacent are tested. — Rule: every writer of a diff channel is derived from the schema, dict channels upsert, list channels union. — agents-director-support.

**A7. A mint standing in a room with no figures is bound to a post-holder in some OTHER room** — `agents/director_floors.py:1561` — `pool = by_room.get(room) if room else None; if not pool: pool = list(people)`: an empty list is falsy, so a KNOWN empty room widens to every figure in the player's ambient scope. "The innkeeper" minted in the empty square binds to Tam Ashwell of the inn. — Docstring 1516-1533 promises widening only when the room is unknown; test_identity_binding.py:206 uses a room with figures. — Rule: widen only on `room is None`. — agents-director-support.

**A8. `awaiting_your_answer.from` leaks the asker's canonical name through the loop-warmed cache** — `agents/character.py:539` — Cache key omits `label`; `interaction_loop` pre-warms it with no label (loops.py:744-747); `character_step` then asks with `label=_contact_label` (3916) and receives the cached raw-name dict. Every interaction-loop step hits the warmed key; only bare `character:<id>` steps get the gate. — test_decision_names_are_gated.py:115 asserts only the source text contains `label=`. — Rule: cache the raw record, apply the label at return. Sibling floor bypasses (medium): micro-round `actor_name in known.get(...)` (loops.py:263); background `_beat_for_presence` speaker attribution, `_manager_events` speaker/intended_target, `addressed_by` gated for the player only (background.py:250/252/270/1031/1573); narrator `spatial_frame` built without `label_for` though `_view_label` exists twenty lines up (narration.py:1769, 2230); `player_said_nothing` asserts presence on co-location only (character.py:790). — agents-minds+narration, lens-firewall.

**A9. `passable_route_next_step` searches the DIRECTED passable graph backwards** — `world/spatial_routing.py:396` — BFS from destination over `neighbor_map(directional=True)` reverses every `passage_from` edge: a chute A→B refuses next_step(A,B) and walks B→A UP the chute; comment still says "the graph is undirected". Callers: director_movement.py:1059/1164/1343 (leg-at-a-time travel). — `passable_route_exists` and `passable_path` search forward and are correct; test_travel_continues.py has zero `passage_from`. — Rule: one BFS (forward from `from_room`, return first hop); delete the third private one. — world-spatial.

**A10. `perform_merge` drops every away-frame ledger except rooms and positions** — `world/spatial_frames.py:1077` — `{**parent_scene, rooms, positions}`; attire, entities, contacts, stations, poses, contained, scales, orientation, comms, substances, passages, scents are the parent's copy from the split. `perform_split` (911) handed the child the full ledgers. A persona who changed clothes, shrank or minted away loses all of it on reunion. — Not in UNBUILT. — Rule: for every subject-keyed ledger take the child's rows for subjects positioned in the child scene; union entities/passages/comms/scents; symmetric partition on split. — world-spatial.

**A11. `step()` discards every `settle_commitment` an institution rule fires** — `world/charter_run.py:1310` — Rules settle into `commitments_now` (a normalized COPY) and rebind `charter`; the tail writes `after_charter["commitments"] = commitments`, the local from `advance_commitments` at 902. The `commitment_<state>` event IS emitted and witnessed, so heads learn a bargain was repudiated that the ledger still holds open; the rule refires after refractory. — tests pin only `fire_institution_rules`' return shape. — Rule: one local for the advanced store; settle into it. — world-charter.

**A12. A private comm is delivered to every body whose alias contains the target string, in any room** — `world/charter_observe.py:141` — `medium == "comm" and _names_body(target, forms)` returns True before any spatial test; `_names_body` is bidirectional substring and `forms` includes a formal alias for EVERY rank/post title. A comm to "Lieutenant" or "Venn" is acquired verbatim by every lieutenant / every Venn two towns away. Verified by pure call. `_is_concealed` has the mirror defect. — `resolve_target_body` (331) exists and is unused here. — Rule: a partial string is never an address; resolve the endpoint through `resolve_target_body` and deliver to that one body. — world-charter.

**A13. A walk continued across a window boundary is priced at the flat town rate** — `world/charter_move.py:312` — `continue_walks` has no `scene` parameter, so `_advance` prices every leg at 1.0 room (ten minutes) instead of `edge_cost` (seconds); beat windows are 6-45 s so nearly every walk is a continuation, slowing ~30×. The carried-credit cap at 310 (1.0 room) discards many doorway-priced legs. — charter_run.py:637 has `scene` in scope and unused (the sole caller). — Rule: one pricing regime; thread `scene` and express the credit cap in edge units. — world-charter.

**A14. Reciprocal prestory rows truncate each participant's experience store to 16** — `world/charter_history.py:691` — `experiences[participant] = rows[-16:]`, unsorted, while `EXPERIENCE_CAP = 4000` (charter_model.py:420) was raised for exactly this; every coworker of a featured resident loses a month of presim. — `charter_run._remember_experience` is the sorted, capped writer. — Rule: one bound, one writer. — world-charter.

**A15. Writers' Room reads the PRESENT frame's scene against a named frame's plans, packages and needs** — `story/room_slice.py:104` — `read_scene(cid)` → `get_scene` → `wget` routes by the ambient `active_frame_id`, which room routes never set and the streamed worker thread starts empty; every sibling reader (`planned_entities`, `registry_for`, `packages`, `open_planning_needs`) honours the explicit `frame_id`. Callers: room_tools.py:277/325/617/702/805, room_frontier.py:112, plot_packages.py:503/1786/1860/2146 (inside apply functions that then write frame X). — `wget_for_frame` exists (db.py:2596) and is unused by them. — Rule: one frame-aware `read_scene(cid, frame_id)`; every slice reader threads its frame. — story-room+plot.

**A16. Archive import and branch truncate every Planner/Dramaturge line to 4,000 chars** — `story/room_conversation.py:526` — `restore_room_messages` calls `_clean(r.get("text"))` with the default limit; the write path uses `ROLE_MESSAGE_CHARS` (80,000 for planner/dramaturge). Callers: chat_archive.py:1034, app.py:6008. — Rule: restore uses the writer's per-role ceiling. — story-room+plot.

**A17. Checkpoint remap never rescopes `fixed_points`/`scheduled_events` frame ids** — `web/app.py:1106` — `_remap_cp_blob` remaps frames/chat_personas/world_events/relationship_events but calls neither `_remap_fixed_points_frames` nor `_remap_scheduled_event_frames` (both called on the LIVE world at 5944/5982 and chat_archive.py:1068/1115); `restore_checkpoint` rewrites `world` and `scheduled_events` from the blob, so the first reroll after a branch/import restores SOURCE-chat frame ids. The two remaps also disagree on unparseable strings (`pass` vs `world_id_remap.get`). — Rule: one `remap_world_state(world, tables, …)` called from all three sites. — web+dressing.

**A18. Raw JSON tab caches the world blob for the dialog's lifetime; saving reverts every other-tab edit** — `static/js/world_browser.js:1516` — `if (cache.raw) return render(cache.raw)`; `refresh`/`replaceCard` never invalidate; Save PUTs the stale whole world (app.py:4744 `DELETE FROM world WHERE chat_id=?`). The dialog is not busy-gated so it can also overwrite a committed turn. — Rule: no client-side cache of a server row without invalidation on write; or per-entry writes. Sibling (medium): the attire editor writes the WHOLE ledger from a stale index and `attire_put` has no `_require_chat_idle` (world_browser.js:1285, app.py:4777). — js-play.

**A19. Renaming a reusable character card rekeys the in-story identity of every attached story without a per-story card** — `static/js/editors.js:716`, `web/app.py:2898` — `char_edit` has no name/uid guard; `chat_character_sheet` resolves `COALESCE(cc.sheet, ch.sheet)` and attach (app.py:3865) snapshots no sheet, so 103 of 116 attachments read the library sheet live and a rename rekeys positions/known/memories — the rekey the story-card route refuses with 400 (4336). UNBUILT §1.104 asserts characters carry a story-local copy; they do not. — Rule: the reusable card is a template; snapshot on attach, edit in-story through the story card. — js-play.

**A20. `director_establish` tells the model to open a crowd with `op:'open'`, which `crowds.apply_ops` rejects** — `language_packs/{en,ja}/…/director_establish.txt:60` — `_OPS` has no 'open'; `_op_word` returns '' → "unknown crowd op", dropped. Every opening that follows its own sheet opens an empty square. — commit.py:364-372 intercepts only charter emerge/absorb. — Rule: the sheet spells the op the engine owns (`set`, empty `crowd_id`); a closed set may tolerate its own sheet's spelling. — prompts.

**A21. Both stage sheets name the retired `offscreen` hand as a valid `ledger_notes` key** — `director_interpret.txt:54/56`, `prose_author_sheet/28.txt:1`, `prose_author_output_shape.txt`, ja mirrors — `_unrouted_rulings` (director_scopes.py:784) knows only `SPECIALISTS` + channels, so a crowd/courier/telling ruling keyed as instructed is reported unrouted and the social hand does not run. Interpret says "six scoped specialists". — Rule: derive the roster in the sheet from `SPECIALISTS` at assembly; give the dispatcher a registry-level successor map (retired name → owner) so a stored preset or model habit routes rather than un-dispatches. — prompts, agents-director-core.

**A22. `remove_adjacent` severs one end of a doorway readers resolve from both ends, and a passage record re-mints it in the same merge** — `world/spatial_merge.py:1698` — Only `room`'s edge is filtered; `spatial_geometry` walks the far room's back-edge (379-389); `sync_scene_passages` (1813) re-mints the missing edge for every passage record, so a doorway ever touched by the passage editor cannot be severed even when both sides are named. Replayed in-process on both shapes. No `remove_passage` channel. — Rule: a doorway is ONE object with two ends; removal drops both edges and the passage record before sync. — lens-two-representations.

**A23. `dialogue_log.medium` is not a schema field, so `medium:'comm'` is dropped before any of its five readers** — `llm/schemas.py:1868` — `DialogueLogEntry` lacks `medium`; pydantic `extra='ignore'` strips it on the strict path (`_dump`, 5600). Readers: composer.py:645, perception.py:2816, director_fanout.py:151, director.py:2943, director_scopes.py:598. A transmitted line reaches its remote addressee only via the by-name rescue, which the composer refuses whenever the sound field has a `signal` — i.e. in every mapped building. Reproduced under .venv. — Rule: every key a reader consumes is declared on the model; `medium` as an engine-owned class (voice/comm, room for sign/text/mind). — lens-universality.

**A24. A one-beat sound in the NEXT room reaches nobody once the listener's room has geometry** — `agents/perception.py:5146` — `_sound_field_for` never passes `events`; own-room events go through `ambient_percepts` (drops other rooms), far events through `distant_sounds` (skips `near_rooms=field.grid.offsets`). The only near-field event reader, `heard_events`, is called once — in `_composer_establish` (4395). Appears exactly in the 336/580 rooms the field was widened to cover. — Rule: one field per perceiver per stage built with the beat's sources; the near field delivers what its composite places. Sibling (medium): `far_path_gain` returns 0.0 for rooms no edge chain joins, so `signal=0.0` withdraws the device-hail rescue the composer says is "earned and stays" (sound_field.py:1685, composer.py:671). — lens-stagecraft.

**A25. No generated charter ever sets a need's `fed_by`** — `world/charter_generate.py:1021` — `seed_needs(bodies)` with no template; `DEFAULT_NEEDS` has no `fed_by`; four readers depend on it (supply factor, own-stake appraisal, "where the bread is" errands, "the post heals the need"). Every town services sustenance at supply 1.0 whatever its stocks; the economy has no body at the end of it. Only tests set it. — Rule: derive `fed_by` at generation from the plan's flows/stores and a medical post; accept a per-charter `needs_template`. — lens-simulation.

**A26. perception_outcome's mirror of commit's scene composition has drifted again** — `agents/perception.py:2733` vs `persist/commit_scene_state.py:1461-1467, 1959-1967` — The mirror calls `merge_scene_with_diff(sc, diff, clock_seconds=)` only; commit also runs `_refuse_unheld_transfers`, passes `sleeping`/`carriers`/report sinks, then `protect_planned_edges`, `settle_developed_stubs`, `prepare_frontier_expansion`, `materialize_planned_fringe`. The narrator (via `outcome_scene`) and every memory episode see a scene commit will not write (a refused handover shown as done). Third recorded drift. — Rule: one `compose_beat_scene(ctx, prev_scene, res)` in commit_scene_state, two callers; commit consumes `ctx.outcome_scene` when built from the same inputs. — lens-inefficiency.

### Medium

**A27. Streaming json_schema rejection is memoised as a json_object rejection** — `llm/providers.py:2721` (sync), `3126` (async stream), `3156` (async blocking) — Stage 1 pops `response_format` whatever its type and calls `_note_json_object_rejected`; `_json_schema_supported` stays True so every later call sends the schema, 400s, retries — one failed round trip per call for the process life, then no shape constraint after two stalls. Blocking path (2770-2786) has the correct stage 0. Tests exercise only json_object rejection on the stream. — Rule: one three-stage ladder parameterised by transport. — llm-providers+schemas.

**A28. A reasoning-only reply on the streaming path returns "" with no typed failure** — `llm/providers.py:2183`, `3240` — `_sse_openai` returns `text` without checking `reasoning`; `ReasoningBudgetExhausted` is raised only by `_message_content` on the blocking branch, so `chat_complete`'s reasoning-off/candidate-switch remedy never fires for character, narrator, interpret or the prose author; the ladder buys a temp-0 repair and fallbacks instead of one retry. Measured case in schemas.py (chat 95 t18). Sibling: streaming reasoning capture is a two-key list (`reasoning`/`reasoning_content`) while `_reasoning_text` reads any reason/think key and flattens blocks; `reasoning_details` is dropped on streams (2165/3222). — Rule: one empty/reasoning-only check and one reasoning reader for both transports. — llm-providers+schemas.

**A29. Interpret's `state_assertions` gets none of the resolve diff's preprocess or field-level pruning** — `llm/schemas.py:4544, 5537` — `_fill_entity_names`, `_coerce_conditions`, `_coerce_optional_time`, `_hoist_misplaced_entity_siblings`, `_coerce_empty_list_to_dict` run only for resolve/establish; `_prunable_diff_fields` roots on `state_diff` only. One malformed field fails the whole interpret and buys a repair; `validated_player_state_assertions` (common.py:9780) then discards the whole diff in a bare except. — Rule: `_prepare_state_diff(target)` called for every StateDiff-typed field, prune keyed by root. — llm-providers+schemas.

**A30. Repair examples omit fields the prompts ask for** — `llm/schemas.py:4789, 4870, 5080` — interpret example lacks `state_assertions`, `contact_assertions`, `ledger_notes`, `follow_op`; resolve lacks `ledger_notes`, `thoughts_omitted`, `travel_interrupted`, `world_pressure`; spatial example teaches `mode:'voice'` which `COMMS_MODES` silently folds. The file's own argument: a key absent from the imitated object "reads as not part of the answer", so a repaired interpret drops the player's asserted state. — Rule: a structure test that every prompt-requested top-level key appears in the step's example. — llm-providers+schemas.

**A31. `_rehydrate_side_channels` rebuilds five channels; perception writes at least four more** — `agents/runtime.py:1011` — `_player_room`, `_sound_crowds`, `_composer_turn_ledger`, `_background_beats_cache` are not rebuilt; on a resume from `director_resolve`, `_player_room` is None so 3367 skips the player's room from `_fig_rooms` and 3426/4590 receive None. — Rule: make the onset player room a FIELD of `perception_act`'s saved content read via `ctx.player_room()`; a project_check that every `ctx["_…"] =` writer is a declared field or listed in the rehydrator. — agents-runtime+plan.

**A32. `classify_movement` and director.py:1402 match the destination by exact string while every sibling folds** — `agents/mapping.py:176` — `to_room: "Lantern Path"` for scene room `lantern_path` is `unplanned`, files a `declared_destination_unplanned` need, spends a Planner pass, and commit's `dedup_minted_rooms` folds it anyway. — Rule: one room-identity helper (lift `_room_display_slug` + slug compare into `spatial_identity`) shared by classify, query status, interpret and dedup; carry the resolved id as `to_room`, the spelling as `declared_as`. Sibling: `_location_query_status` uses raw substring (`sp in folded`) so room `inn` answers `beginning_of_the_road` (mapping.py:224); token-boundary match. — agents-runtime+plan.

**A33. A Dramaturge pass launched from the panel runs with `base_turn=None`, skipping the rewind guard** — `agents/story_planner.py:973` — Both guards (834, 1126) start `if base_turn is not None`; the commit tail passes `turn_idx`. — Rule: every job carries its base turn; import `room` in that scope. — agents-runtime+plan.

**A34. `_scene_has_subject` exempts nine English self words and ignores the cast key tolerance** — `agents/common.py:366` — A participant written あなた/自分, or by uid/`character:<id>`, is "missing", the phase is `blocked`, `prune_blocked_phase_changes` deletes its records and `adjudicated_player_action_text` returns ''. — Rule: blocking is a statement about presence, never spelling; read the exemption set from the pack and resolve through `cast_spelling_policy`. Sibling: `_asks_player` (2757) with English aliases and ASCII `?`, so the interaction loop never stops on a question in ja. — agents-common.

**A35. Rear-arc "heard, not seen" act percept is dead on the outcome pass** — `agents/perception.py:5291` — `or actor in behind: continue` precedes `composer.act_percept` (which mints the hearing percept for the rear arc); live only at onset. UNBUILT PM5 still says the substitute does not exist. — Rule: let `act_percept` decide the arc in both passes; delete the pre-skip and `behind_sources`. — agents-perception+composer.

**A36. `source_manifest` (tells, surface demeanor) is computed per observer and read by nothing** — `agents/perception.py:2887/2914/2946`, `_delivered_manifest` 1870 — Three writes, no reader; no cue percept kind; the F6 tell-grounds ledger pays off cues nobody received; character.txt:66 and FEATURES.md:133/225 promise delivery. Gates have drifted unread (reads `character_results` not `_settled_character_result`; `visible` skips `_sense_graded`; `audible` requires `same_room`; `_tell_acuity` matches channel names literally). — Rule: deliver it (cue percept, see D1) or stop computing it. — agents-perception+composer, lens-literature.

**A37. A standing disguise re-earns the player's full description and a memory episode every beat** — `agents/perception.py:5040` — `if p_disguise: appearance_changed.add(p_name)` on every beat the disguise is ACTIVE; `render_episode` treats `force` as a change so every NPC mints an identical episode per beat (view dedupe holds). — Rule: re-earn on the TRANSITION (a `physical_disguise` condition row written/deactivated THIS beat). — agents-perception+composer.

**A38. ledger_notes routing ignores the manifest category vocabulary and its plural tolerance fails on -y/-ies** — `agents/director_scopes.py:700` — `inventory`, `substance(s)`, `comms`, `artifacts`, `contact_action(s)`, `entity` never meet `inventory_ops`…`entities`; `_CATEGORY_CHANNELS` owns exactly this vocabulary and is consulted for the manifest only. Measured 8/11 notes keyed by channel. — Rule: normalise a note key with `_normalize_omission_category` → `_CATEGORY_CHANNELS` before hand/channel spellings; match the channel stem. — agents-director-core.

**A39. At interpret the contact hand's `phase_sources` lands on the top-level output and `contact_assertions` is never pruned for blocked phases** — `agents/director.py:2795` — Container chosen by `channels[0]` (`contact_ops` → `(out, "contact_assertions")`), so provenance goes to `out["phase_sources"]` (no reader); the prune runs over `state_assertions` only; a continuation contact whose onset was deferred is applied to `resolve_sc` as standing. — Rule: container by STAGE; prune a view including `contact_assertions`. — agents-director-core.

**A40. Repair-pass specialists read the pre-beat scene while dispatch-pass specialists read the previewed one** — `agents/director.py:4113` — `ctx["_orch_repair"]` stashes view/extras, no scene; `_specialist_repairs` → `_specialist_payload(name, ctx, sc, …)` with pre-beat `sc`; only `extras["contacts"]` is previewed. The chat 117 t82 shape the fix addressed. — Rule: stash `resolve_sc` beside view/extras. — agents-director-core.

**A41. `_fold_derived_manifest_events` folds an unrelated inventory/entity change into an attire event on a name substring** — `agents/director_evidence.py:1267` — Second match is `handle in attire.change` prose; "Hinami picks up the lantern" folds into "Hinami removes her cloak"; lantern loses its event id, dispatches no hand, gets no evidence check; `also_described_as` has no reader. — Rule: fold only on `resolve_garment` proof, never on prose containment. — agents-director-support.

**A42. The manifest is clamped to 8 for dispatch, specialist views and every evidence check** — `agents/director_evidence.py:1213` — Items 9+ reach no hand's slice and are never verified; the comment justifies bounding repairs, not visibility. Not in UNBUILT. — Rule: bound REPAIR calls, never what is dispatched or checked. — agents-director-support.

**A43. `_standing_pressure` reads `row["state"]`; cast rows carry `cstate`** — `agents/loops.py:541` — sqlite3.Row raises, except returns 0.0; the opening speaker order is pure jitter on every live story. Test fixtures are dicts with `state`. — Rule: read through `sheet_state(row)`; fixture uses the real projection. — agents-minds+narration.

**A44. Silence note fires on beats where the player communicated without a quote** — `agents/character.py:3811, 749` — `_p_spoke`/`_player_quiet_beats` read only the scalar `speech`, set from `type == "speech"` elements; `_unanswered_question_note` and `_narrator_player_declared` already count `communication` as speaking. — Rule: one "the player spoke this beat" helper. — agents-minds+narration.

**A45. Ledger-form enclosure inside a body with no attire/scale never yields `source_enclosed`** — `world/spatial_containment.py:568` — `_body_interior_holder` requires `_is_body_entity` (attire/scales only) for the ledger form; a minted creature with `contained[x] = {in: creature, mode: interior}` returns None, `_hear_level` falls to `same_room` → `full`; gate 3 of `materialize_enclosure_interiors` has the same test. `_mass_holder`'s docstring argues the opposite. — Rule: `mode == "interior"` is affirmative evidence of a mass; exempt only what `_mass_holder` exempts. — world-spatial.

**A46. `light_sources` places an unstationed source at `_centre(grid_side(...))`, off-grid in any non-square room** — `world/spatial_light_field.py:559` — The only remaining caller of the pair `room_centre` was written to replace (measured chat 117 t13); a corridor lamp lights nothing. — Rule: `room_centre(scene, room_id)`. — world-spatial.

**A47. `sprint_reach` prices rooms by the raw `size` word** — `world/spatial_routing.py:856` — `_ROOM_COST.get(room["size"], 1)` ignores `extent`/name hints that `effective_room_size` derives; a planned 20×20 hall costs one unit. — Rule: every size reader goes through `effective_room_size`. — world-spatial.

**A48. The watching side of a `one_way_window` hears nothing at any volume** — `world/spatial_senses.py:1162` — `spatial_rel` rewrites the blind side to `wall`, leaves the watcher's barrier `one_way_window`; `_hear_level` has no branch for it → `none` at shout; `APERTURE_PASS` treats it as `window`. — Rule: sight's asymmetry is a sight fact; fold to `window` for hearing. — world-spatial.

**A49. `presence_view` deep-copies the whole charter twice and normalizes it three times per voiced presence** — `world/charter_runtime.py:3701` — see C. **A50. Judgments formed from on-screen conduct are keyed by the stranger label while the claim's subject is canonical** — `world/charter_social.py:304` — `_signals_in_claim` reads `public.actor or claim.about` (the label), never `claim.subject`; every reader keys by canonical figure key, so on-screen evidence never moves a tie or affordance toward the figure it was about. — Rule: key by `subject`, render by label. — world-charter.

**A51. A creature standing still on its prey is heard "moving" every window** — `world/charter_predation.py:675` — `moves[body] = here` is "stay"; `_noise(…, "moving")` fires for every entry and suppresses `idle`; `walk` skips it. — Rule: noise follows a place that changed. — world-charter.

**A52. A caravan loses every freight good the local market's economy does not define** — `world/charter_economy.py:419` — Wagon stock is loaded into `out["stocks"]`; every `trade` normalizes and keeps only goods `in goods`; salt vanishes into a grain-and-wool town whenever any trade succeeds. — Rule: keep the wagon out of the normalized economy; rebuild freight from original minus sold plus bought. — world-charter.

**A53. Inherited news/relationship memories read `last_seen`/`at_hours` fields no claim writes** — `world/charter_promote.py:264, 347` — Every claim writes `as_of_hours`/`happened_at`; every inherited row lands at hour 0. — Rule: read the keys the writers write. — world-charter.

**A54. Three registry writers, two bypassing the documented "one write chokepoint"** — `world/charter_runtime.py:2719, 1843` — `land_snapshot`/`land_presim` `wset_for_frame` the JOINED shape with no reservation pass; the stored shape alternates. — Rule: one `_write_registry`. — world-charter.

**A55. A ruined room's registry retirement is undone by the next commit's projection** — `persist/commit_room_registry.py:337`, `world/region_events.py:300` — `apply_wave` retires the row and leaves the room in the scene with `ruined: True`; `_prepare_room_registry` skips only live rows and upserts `retired_turn_id=NULL`; nothing reads `ruined`. Sibling: room `hazard` has an onset and no end (region_events.py:296; only reader `_hazard_rooms`, `since_elapsed` read by nothing). — Rule: the registry projection respects ruin; a hazard is a room-subject `world_conditions` row so expiry/ticks/the Director's view apply. — world-rest.

**A56. `positions` is read as a body roster by the condition tick and hazard floor** — `world/mechanics.py:721, 1059` — Docked vehicles and `infer_vehicle_zones` entities are in `positions`; a cart gets "no body of that name in vitals" every beat, its air denied, a boat warned about in a flooded quay. — Rule: one body predicate (`comfort._is_body`); commit passes `cast_names` as `bodies`. — world-rest.

**A57. `apply_vitals_diff` keys by exact spelling while every other vitals reader is casefold** — `world/survival.py:419` — `mirela` creates a second row; `vitals_of` returns the old one; both tick. — Rule: resolve to the existing row key (`_vitals_entry_key`, moved into survival). — world-rest.

**A58. Routines keep a private 24-hour day from raw elapsed seconds** — `world/routines.py:104` — `elapsed % 86400`; `day_cycle` owns anchor and `day_length_hours`; the docstring "no day anchor" is stale. — Rule: watch from `hour_of_day / day_length_hours`. — world-rest.

**A59. `search_lore`'s keyword term is scored across the WHOLE `lore_fts` table with LIMIT 50** — `mind/memory_common.py:206` — No book predicate (the FTS has no lorebook_id column); on a multi-story library the 50 slots and the normaliser come from books this chat cannot read and every in-scope entry gets 0 for the 0.35 term. — Rule: scope with `rowid IN (SELECT id … WHERE lorebook_id IN …)` as `_lexical_memory_ranking` already does. — mind.

**A60. Embedding repair lane covers memories and summaries but not lore; `update_memory` never queues a fallback** — `mind/memory_lore_entries.py:67`, `mind/memory_read.py:219` — `_embed_lore_document` discards `got.fallback`; `_REPAIR_PENDING` has two tables; a lore write under rate-limit stays hashed until restart. — Rule: every embedding writer checks `fallback` and queues. — mind.

**A61. `dedupe_regions` rebuild drops the region's `uncovered` flag** — `story/attire.py:1404` — The fork rebuild writes `{garments, beneath}` + `beneath_zones`; `advance`/`apply_flat_change` carry the flag; `release_removed_garments` removed the corpse fallback, so `beneath` is withheld on every read while the store keeps it. — Rule: every rebuild carries every flag; one region constructor. — story-cards+scene.

**A62. `appearance_of` reads the attire ledger with a bare `.get(name)`** — `story/scene.py:521` — The fifth tolerant-lookup site and the only one with no fallback; `visible_body_text` beside it was fixed; 10 callers. — Rule: `entry_for` (or heal keys once on read in `get_scene`). — story-cards+scene.

**A63. The "standing surfaces" arrival window is global, not per room** — `story/carriers.py:160`, `couriers.py:632` — Twelve newer located events anywhere push a quiet room's surface out of the window forever; only index is `(chat_id, occurred_at)`. — Rule: per-room query behind one shared helper; index `(chat_id, frame_id, location_id, occurred_at)`. — story-cards+scene.

**A64. Two of the three card fill surfaces cannot reach a per-story card** — `story/importers.py:1063, 963` — `fill_appearance`/`fill_character_psychology` read `characters` only; only `fill_body_interior` grew `chat_id`. — Rule: all three siblings take `chat_id` and get chat-scoped routes. — story-cards+scene.

**A65. A fold that names a setup as paid marks it paid before the payoff line can be refused** — `story/room_bible.py:247` — `_save` then `add_entry` raises; setup leaves eviction protection with no `paid` line. — Rule: validate the payoff before mutating. — story-room+plot.

**A66. `prepare_scene_commit` performs durable autocommit writes before the transaction** — `persist/commit_common.py:645` — `add_engine_notice` `wset`s regardless of ctx from six prepare-path helpers, plus `sight_contradictions_told`/`layout_lint_told`; outside `transaction()` each is its own commit and survives a rollback; the sweep rewrites the key from `ctx.engine_feedback` anyway. — Rule: staging path is exclusive (`ctx.tell_director` only); flags ride the prepared bundle. — persist-scene.

**A67. `_fold_duplicate_mints` drops the mint from the scene but not the prepared diff or merged ledgers** — `persist/commit_scene_state.py:782` — Fold runs after merge; `commit_world_entities` iterates `diff.entities`, finds no merged entity, INSERTs a phantom row that `_entity_alias_map` may pick as canonical. — Rule: fold on the DIFF before the merge with an `_apply_entity_renames` sibling of `_apply_room_renames`. — persist-scene.

**A68. `vacuum_blobs` collects payload blobs live capture rows still reference** — `persist/llm_capture.py:222` — NOT IN unions three hash columns; payload digests live in the `payload_hashes` JSON. — Rule: a normalized refs table, or compute the live set in Python. — persist-memory+archive.

**A69. `turn_branch` takes cast membership from the source chat NOW** — `web/app.py:5671` — Copies every current `chat_chars` row, overlays blob state by UPDATE only; a post-branch promotion arrives hollow, a post-branch detachment is missing. `restore_checkpoint` sweeps by `blob["chars"]`. — Rule: one membership helper for both paths. — persist-memory+archive.

**A70. Four scene/state writers skip the idle guard every sibling holds** — `web/app.py:4777 attire_put, 3854 chat_add_char, 4074 chat_add_persona, 4303 chat_del_char (500s on missing row), 4625 ph_put` — Rule: a `_scene_write(cid, frame_id, mutate)` helper holding era + idle + wset. — web+dressing.

**A71. Guest input is keyed to the chat-global next idx while turn creation/folding are per-frame** — `web/app.py:4269` — A guest stationed in a non-current frame loses every declaration; the "already resolved" guard checks steps only. — Rule: compute `next_idx` from the persona's stationed frame; store frame on the input row. — web+dressing.

**A72. Backdrop/ambience resolve the player's room by exact positions key plus a dead `player_room` fallback** — `dressing/backdrops.py:146` (also `agents/background.py:687`) — `room_of` is imported in the same file and used at 543. — Rule: `room_of`. — web+dressing.

**A73. Reroll-nav state survives a story switch; run-scoped UI writes are not gated on the run's chat** — `static/js/chat.js:1099, 942, 1154` — Arrow keys POST a narration select on another story's turn; early narration appends A's prose into B and refreshes B. — Rule: every run-scoped write checks `run.chatId === S.chatId`. — js-play.

**A74. Room messages, live text and reasoning are passed through the UI translator** — `static/js/writers_room.js:489/564/569` — story text through `t()` (349 space-free keys). — Rule: `txt()` + `translate="no"` for story text; translate only the fixed Planner lines by membership. — js-play.

**A75. Ambience crossfades ride `requestAnimationFrame`, stopped in a hidden tab** — `static/js/ambience.js:262, 198` — A handover begun while hidden promotes an already-ended spare; the layer is silent until the token changes. — Rule: fades on a clock that runs while hidden (`timeupdate` or `performance.now()` interval); self-heal on completion. — js-settings.

**A76. Weather-fx re-arms lightning on tab return regardless of effects level; layers built while hidden; thunder timers survive stop** — `static/js/weather-fx.js:536, 253, 504` — Rule: `weatherFxApply` is the single gate; track every timer. — js-settings.

**A77. Cast › Lorebooks tab renders the retrieval graph and drops any attached book whose parent is not attached** — `static/js/settings.js:1352` — Reads `chat_get.lorebooks` (the walk lorebooks.js moved off for this reason); orphan loop is an empty body; attach dropdown lists library children so the muted/detach controls become unreachable. — Rule: `GET /api/chats/{cid}/lorebooks` and the shared missing-parent-as-root rule. — js-settings.

**A78. Character stage writes `access_count` on every roll, including rerolls** — `mind/memory_retrieval.py:785`, `memory_context.py:333/399/569` — A durable write in a read-only stage; `_internal.retrieved_ids` already reaches the step boundary. — Rule: stage proposes, commit persists. — lens-firewall.

**A79. Structured sight fields in the character payload bypass the senses gate the view honours** — `agents/character.py:3861/3732/3319` — `here_affords`, `corridor_sight`, `in_sight` cue take no senses; a sightless card receives them. — Rule: a structured echo of a sense may not exceed the sense. — lens-firewall.

**A80. Private-history authoring reads/writes the base `chat_chars` row while the mind reads the frame override** — `web/app.py:4624, 4614, 4347` — Every committed turn in a frame creates the override row; the host's edit lands where the frame never reads. — Rule: `frame_id` on the routes, read `COALESCE`, write `set_char_state(frame_id=)`. — lens-two-representations.

**A81. `elapsed_psych_units` relaxes every ledger MORE for an asserted zero-duration beat than for a ten-second one** — `mind/psychology_runtime.py:144` — Equal readable clock → the ≥1-unit turn fallback; the 10 s unclaimed floor → 0.167 units; drive strain got a per-beat floor, mood/undercurrent did not; `decay_affect` docstring still says TURNS. — Rule: fallback only when the clock is unreadable; one unit contract for the interior. — lens-simulation.

**A82. Director-declared `consequences` are asked for every beat and dropped by an off-by-default gate** — `persist/commit_mechanics.py:207` — Chunk 22 has no gate key; the mint requires `living_world_allows(…, "scheduled_consequence")` (default off); approach D one screen later says "settings gate surfaces, never truth". — Rule: mint every declared fuse; gate the surface. — lens-simulation.

**A83. perception_outcome places a positionless player at the DECLARED `movement.to_room` even when resolve refused it** — `agents/perception.py:2766` — `_resolve_player_room`'s second rung returns `interp.movement.to_room` directly under the comment saying that is the failure; nothing outside director reads `movement_refused`. — Rule: a declaration is evidence of intent before resolve and of nothing after; pass `interp=None` and consult the resolved diff. — lens-inefficiency.

**A84. Debug capture records only the ACCEPTED exchange and misattributes repair calls** — `llm/llm_quality.py:449` — `_captured` closes over the outer system/payload and hardcodes `ok: True`; patch and fallback successes record nothing. — Rule: one exchange per PROVIDER CALL with what was sent, `ok=False` for failed attempts. — core+llm-support.

**A85. `init()` re-runs the chat-copy→overlay migration on every start and nulls `origin_id` on books the runtime still keys by** — `core/db.py:2499` — Unlike `backfill_regions`, not version-gated; `kept_own` branch rewrites rows that `copy_lorebook_tree`, branch, import and restore still mint; readers at app.py:3484 and checkpoints.py:278 lose the key. — Rule: gate on a schema-version crossing. — core+llm-support.

**A86. Memory consolidation never sees a character's re-readings** — `mind/memory_summaries.py:411` — `disputed` is on `_row_memory` and not in the consolidator payload; a revised memory is summarised from the abandoned reading. — Rule: `now_reads` per row + one card sentence. — lens-literature.

**A87. Sight is hard-wired to light: no authored sense can see in the dark** — `world/spatial_senses.py:1596`, `spatial_light.py:466` — `sight_level` discards WHICH `none` (barrier vs light); `sense_adjusted` refuses to lift sight from `none`; `_sense_channel` maps only onto three ladders. A night-vision/echolocation card is deterministically blind. — Rule: a sense declares what it depends on (`needs_light`, `equivalent`); darkness stays a property of the place, who it blinds a property of the perceiver. — lens-universality.

**A88. Weather is a closed Earth vocabulary and the drift invents rain from any storm** — `world/weather.py:754` — `sandstorm`/`ashfall` → `sky: storm`, particulate lost, `_SKY_FALL['storm']` rolls rain hourly; `cold=` at commit_scene_state.py:1912 is redundant. — Rule: `precipitation = {kind: none|liquid|frozen|particulate, name}`; the drift preserves declared kind/name and rolls intensity; a style-guide `climate` table. — lens-universality.

**A89. An authored outfit region outside `REGIONS` is dropped silently** — `story/attire.py:550` — `regions: {"tail": …}` on an extra-parts card loses the garment; the coverage path beside it warns; `attaches: true` at the part's `at` region already exists as the representation. — Rule: re-home to the part's region with `attaches`, else `DEFAULT_REGION` + card warning. — lens-universality.

### Low

- **`_ambient_blocked_slugs`/`_keys_reference_blocked` are stubs (`blocked = set(); return blocked`) with no caller** — common.py:3333/3361 — decide or delete; add to UNBUILT dead-family table. [agents-common]
- **`Engagement` model reachable from nothing** — schemas.py:1587 — delete per the file's own rule. [llm-providers+schemas]
- **`_persona` PipelineContext field has no user** (`_player_room`/`_books` ARE used via mapping-style access) — pipeline_context.py:282. [core+llm-support]
- **Host preset override disables specialist scope gating** — prompts.py:328 — store presets per PART. [core+llm-support]
- **Research fetch leaves streamed response open at the byte cap** — research_providers.py:178 — `with` block. [core+llm-support]
- **`_run_pipeline` builds the plan from `cast_rows` read before the checkpoint restore** — runtime.py:1291 — pass `ctx.cast`; reload `extra_players` in `_restore_and_refresh`. [agents-runtime+plan]
- **Presentational-tail reroll re-runs orphaned steps from the TABLE and clears their stale flag** — runtime.py:1178 — tail from `build_plan`. [agents-runtime+plan]
- **Four step registries agree only by a partial test** — runtime.py:253 — one project_check rule. [agents-runtime+plan]
- **Destruction gate reads free-text `kind` against five words** — director_scopes.py:634 — gate on existence. [agents-director-core]
- **`_declares_rapid_movement` inline regex omits `charge` and is English-only** — director_movement.py:509 — build from `_RAPID_FOLLOW_VERBS`. [agents-director-support]
- **Player-declaration awareness rules protect only the primary player** — director_floors.py:775, 182 — per declaring player. [agents-director-support]
- **All-bodies concealment note says "dropped" for a line that is merely unheard by registered minds** — director_floors.py:1933 — wording only; the behaviour is deliberate (PC2 test). [agents-director-support]
- **`movement.mover` spelling accepted four different ways** — director_movement.py:627/1240/774/1007 — one `mover_subject` helper. [agents-director-support]
- **`_travel_in_flight_view` routes on `sc`, `_travel_continues` on the merged scene** — director_movement.py:1059/1140 — carry the announced leg. [agents-director-support]
- **`_conditions_view` cap of 40 hides the NEWEST live rows** — director_floors.py:593 — an unasked ceiling on a view whose purpose is ending rows; remove or bound by bytes keeping beat-relevant rows. [agents-director-support]
- **Interaction-loop speakers never hear what the reaction loop declared this beat** — loops.py:815 — micro-deliver reactors' speech (uncontested channel). [lens-firewall]
- **Silence where there was sound reaches only the Director** — composer.py:2893 — real only for the single-source/room-goes-even case; see D. [lens-stagecraft]
- **Two proposals accepted in one round are both marked implemented by the first package** — story_planner.py:1064 — default only when exactly one of each. [lens-stagecraft]
- **`_apply_region_event` has two identical due-wave branches** — plot_packages.py:2150 — decide which the docstring means. [story-room+plot, world-rest]
- **Proposal `filed_at` passed and dropped by normalisation** — room_proposals.py:163. [story-room+plot]
- **A clock reading with an AM marker at twelve reads as noon; readings above 23:59 refused on a 30-hour day; `_hour_bucket` is a second 24h phase table** — day_cycle.py:208/206, backdrops.py:110. [world-rest, lens-universality]
- **`_HAZARD_ANSWER_CHANNELS` declared and unread; `clock_elapsed` re-spelled inline** — mechanics.py:981/1141. [world-rest]
- **`inherit_regions` picks first sorted id, comment says most bodies** — regions.py:377. [world-rest]
- **Origin window sent twice with one summary window** — memory_context.py:229. [mind]
- **Off-screen consolidation mints a duplicate summary row with a paid embedding** — memory_summaries.py:636 — cursor as its own field. [mind]
- **BM25 top-60 taken before visibility filters** — memory_retrieval.py:34. [mind]
- **Rebuild progress omits lore and takes max across kinds** — memory_vectors.py:697. [mind]
- **`STYLE_GUIDE_AUTO` and the `genre` branch are dead** — scene.py:2542. [story-cards+scene]
- **Attire seeding walks the base roster under a frame** — scene.py:97 — not reachable today; hygiene. [lens-two-representations]
- **`_record_sensory_events` silently discards past 8, charter noises cut first** — commit_scene_state.py:1124 — at minimum report through the existing warning path. [persist-scene]
- **`commit_world_entities` still DELETEs from decommissioned `world_placements`** — commit_entities.py:414. [persist-scene]
- **Hash-only turn debug export carries story text through warnings/decisions/errors** — pipeline_trace.py:526 — digest them. [persist-memory+archive]
- **Two Ctrl+Enter handlers both click Send; wizard stacks twice** — app.js:1050. [js-play]
- **`Sonder.state()` promises a copy, hands live `S.boot`/`S.chat`** — extensions.js:371. [js-settings]
- **chime.js/ambience.js touch localStorage unguarded at top level** — ambience.js:1039, chime.js:36. [js-settings]
- **Character phase line concatenated before translation** — chat.js:747 — one template literal. [lens-two-representations]
- **`06.txt` asks for volume/visibility/conceal_from on every line then forbids it** — prose_author_sheet/06.txt:11. [prompts]
- **`communication` and `event` element types taught by no first-pass sheet** (medium) — director_interpret.txt:9 — the type is recovered only by the repair call. [prompts]
- **ja character sheet documents `repeat_correction` fields the engine never delivers** — ja character.txt:70. [prompts]
- **Resting/social phases are engine-global; a nocturnal institution cannot be authored** — charter_move.py:410 — optional per-charter `resting_phases`/`social_phases` defaulting to today's constants. [lens-universality]
- **Unknown light values default `full` in corridor sight, `none` in `here_affords`** — place_purpose.py:243 — unreachable today (`effective_light` only returns table keys); hygiene. [lens-firewall]

---

## Section B — DUPLICATION / WRONG HOME / TWO REPRESENTATIONS

Each: title — locations — which representation is authoritative — readers.

**B1. perception_outcome scene mirror vs. commit composition** — perception.py:2733 vs commit_scene_state.py:1461-1467/1959-1967 — Authoritative: commit's composition as ONE function (`compose_beat_scene`) called by both; the mirror ceases to exist. (Also A26.) [lens-inefficiency]

**B2. Room identity: `classify_movement`/director.py:1402 exact string vs `dedup_minted_rooms` fold vs `_location_query_status` fold** — mapping.py:176/224, director.py:1402, commit_room_registry.py:193 — Authoritative: one helper in `world/spatial_identity.py` (lift `_room_display_slug` + slug compare); carry resolved id in `to_room`. [agents-runtime+plan]

**B3. Doorway: one-sided `remove_adjacent` vs reciprocal `effective_adjacent`, plus `visible_adjacent_rooms`/`_onward_exits`/`spatial_frames._room_graph` hand-rolling the reverse walk without `resolve_edge`** — spatial_merge.py:1698, spatial_routing.py:1064/943 — Authoritative: `effective_adjacent`; removal drops both ends and the passage record. [lens-two-representations, world-spatial]

**B4. Vitals key: exact in `apply_vitals_diff`, casefold everywhere else** — survival.py:419 vs 190/204, mechanics.py:784 — Authoritative: the existing row's key via `_vitals_entry_key` moved into survival. [world-rest]

**B5. Attire lookup: `appearance_of` bare `.get` vs `entry_for`** — scene.py:521 vs 510 — Authoritative: `entry_for`; long-term heal keys once on read. [story-cards+scene]

**B6. Registry write: `save_registry` split shape vs `land_snapshot`/`land_presim` joined shape** — charter_runtime.py:590/2719/1843 — Authoritative: split, through one `_write_registry`. [world-charter]

**B7. Judgment subject: label (`_signals_in_claim`) vs canonical (`evidence_claim.subject`, every reader)** — charter_social.py:304 vs charter_observe.py:212 — Authoritative: canonical `subject`; relabel on render. [world-charter]

**B8. Ruin: scene `ruined` flag vs `room_registry.retired_turn_id`; hazard: scene block vs `world_conditions`** — region_events.py:296-308, commit_room_registry.py:337 — Authoritative: the registry for retirement; `world_conditions` for a standing hazard of a place. [world-rest]

**B9. The story day: routines' `elapsed % 86400`, backdrops' `_hour_bucket` 5/11/17/21, `clock_reading_hour`'s `> 23`, mapping's inline `sun_light` copy — vs `world/day_cycle`** — routines.py:104, backdrops.py:110, day_cycle.py:206, mapping.py:256 — Authoritative: `day_cycle` (`phase_at`, `sun_light`, `day_length_hours`). [world-rest, lens-universality]

**B10. Sound-field cache demands object identity on top of a content key narrower than what `SoundField` reads; light-field keys on content alone** — spatial_sound_field.py:1610 vs spatial_light_field.py:906 — Authoritative: content key widened to every field read (poses/orientation/scales/crossings/entity state); identity test then unnecessary. Note: single caller today, so cross-call waste is unmeasured. [lens-two-representations]

**B11. NSFW overlay decided three ways** — prompts.py:339 (`spec["nsfw"]`), 366 (unconditional), 505 (`nsfw_prompt_ids`) — Authoritative: `nsfw_prompt_ids`; delete the per-spec flag or derive one from the other; project_check that both cannot coexist. [core+llm-support]

**B12. Aliases: `cast_scene_context` raw sheet vs normalized identity (`carriers._carriers`, `character_scene_keys`)** — scene.py:2684 vs carriers.py:412 — Authoritative: normalized once at the top of the loop (also removes seven of eight redundant normalizations). [story-cards+scene]

**B13. Claim-disposition scan in `_claim_realized` and `adjudicated_player_action_text`** — common.py:333 vs 632 — one `_claim_rows_for`. [agents-common]

**B14. Communication ask vocabulary `("ask","question","request","instruct")` in character.py:644 and common.py:2777** — one engine-owned constant taught to the schema. [agents-minds+narration]

**B15. Orphan placement: `_subject_is_somewhere` refuses (routes a repair the objects hand cannot perform) while commit's `_place_orphan_mints` places deterministically** — director_evidence.py:780 vs commit_scene_state.py:1008 — Authoritative: place in the Director's deterministic passes so evidence, warning, mid-turn merge and commit see one answer. [agents-director-support]

**B16. Specialist scene: dispatch pass reads `resolve_sc`, repair pass reads pre-beat `sc`** — director.py:4106 vs 1718 — Authoritative: `resolve_sc`, stashed on `_orch_repair`. (A40.) [agents-director-core]

**B17. Familiarity: `FAMILIAR_SATURATION = 250` vs `TIE_SATURATION = 200` (hoisted "because two copies drift")** — charter_practice.py:150 vs charter_social.py:97 — Deliberate second scale per its comment; route through `familiarity()` with the occasion term folded in. [world-charter]

**B18. `_entity_exterior_room` exact-string vs `room_of`** — spatial_transit.py:275 — Authoritative: `room_of` (importable without a cycle). [world-spatial]

**B19. Reserved names: `_package_checks` (cast+charter+plans+aliases) vs `_t_inspect_contradictions` (cast only)** — plot_packages.py:2600 vs room_tools.py:942 — one `reserved_names(cid, frame_id)` honouring the plan_entity exemption. [story-room+plot]

**B20. Operation rooms: `_op_rooms` vs `_rooms_named_by`, neither reads `plan_creature.lair`/`hunts`** — room_slice.py:356 vs plot_packages.py:2629 — one `operation_rooms(op)` beside `OPERATION_FIELDS` with a `ROOM_FIELDS` marker. [story-room+plot]

**B21. Three room graphs with divergent containment rules** — room_slice.py:215 `room_graph`, room_tools.py:294 `_route_graph`, plot_packages.py:2650 `_reach_warning` — Authoritative: `room_graph(cid, scene, extra_edges=)`. [story-room+plot]

**B22. Two variant digests: trace hashes with `_engine_notes`, turn debug without** — pipeline_trace.py:139 vs 525 — one `_variant_digest`. [persist-memory+archive]

**B23. `web/story_view._step_content` re-implements `agents.storage.active_content`, keeps `ENGINE_NOTES_KEY`, and its import-graph justification is moot** — story_view.py:224 — Authoritative: `active_content` moved to a runtime-free home. [web+dressing]

**B24. `ATTIRE_REGIONS`/`ATTIRE_REGION_ZONES` retyped in components.js:657 while world_browser reads `vocab.attire_regions`** — ship both on bootstrap (the `MEM_CATS_FALLBACK` pattern); nothing ships `REGION_ZONES` today. [js-play]

**B25. `i18n.js` is a second hand-synchronised translator (diverged once); `roomStream` re-implements `streamPost` without the 401 redirect or parse guard** — i18n.js:11, writers_room.js:312 — one `i18n-core.js`; replace roomStream body with `streamPost`. [js-play]

**B26. Micro-round attribution (`known` membership, no aliases) vs perception's `_recognizes` + alias strip vs background `_present_others`** — loops.py:263, background.py:1497 — Authoritative: `observer_label_fn` per observer. (A8 siblings.) [agents-minds+narration, lens-firewall]

**B27. Prompt leaves stored twice: `prose_author_sheet/11` and `15` (same `planning_need` duty, both load), `28.txt` and `prose_author_output_shape.txt` (byte-identical, held equal by a test), establish sheet's free copies of comms/poses/entities/sensory chunks (four-rung sound ladder vs six; `{…distance,intensity}` vs `{…db,detail}`; `open` vs `set`)** — one leaf per contract, `{{fragment:…}}` references. [prompts]

**B28. Sensory manifest re-delivers every unchanged standing contact verbatim after the view stopped** — narration.py:530 vs composer.py:2845 — Authoritative: the per-observer standing ledger; ship `{clause, verdict, since_turn}` and drop the template sentence on `unchanged`. [lens-literature]

**B29. Prompt word lists narrower than the engine's closed tables** — director_interpret.txt:3 (six cue words vs 16-entry `ATTEMPT_CUES`), :27 (eight relocation verbs), narrator.txt:88/91 (banned tells also in `_CRAFT_TELLS`) — Authoritative: the linguistics tables; sheets state the class and mark examples as illustrations. [prompts]

**B30. `_origin_on_drift` English mood tuples vs pack-backed `_mood_axis`; the numeric sign-flip already decides** — memory_context.py:193 vs memory_retrieval.py:344 — delete the lists. [mind]

**B31. Streaming reasoning two-key read vs `_reasoning_text`** — providers.py:2165/3222 vs 2876 — one reader. (A28.) [llm-providers+schemas]

**B32. `unresolved_threads` carried twice in the memory payload** — memory_context.py:598/615. [lens-firewall]

**B33. `cast_changes.status` free string coerced by a ~20-word English table; two readers disagree on an unrecognised word** — scene.py:246, commit_mechanics.py:406 vs spatial_frames.py:342 — the schema has no `Literal` precedent; at minimum one reader rule. [lens-two-representations]

**B34. `is_player_speaker` uses the `[^a-z0-9]` fold `fold_identity_key` was written to retire; 14 live sites** — scene.py:1961 — project_check refuses the literal outside character_schema. [story-cards+scene]

**B35. `_delivery_ok` docstring claims every deterministic delivery site calls it; only loops.py does** — common.py:4632 — the two-family drift is UNBUILT §3.8; the docstring is new. [agents-common]

**B36. Three separate `_beat_scene`/scene reads in `background_react`; `_player_room` read in three orders across the three perception stages (`_player_room_in` scene→cache→resolver vs outcome resolver→cache)** — background.py:106…, perception.py:2766/2138 — one ordering (`_player_room_in`). [agents-minds+narration, agents-perception+composer]

---

## Section C — INEFFICIENCY (ordered by estimated wall-clock saved per turn)

Each: cost basis → better mechanism.

**C1. Charter registry re-normalized and re-saved several times per locked commit** — charter_runtime.py:3210 — `sight_figures_in_scene`, `ingest_public_evidence`, `apply_scene_placements`, `settle_rendered_surfaces`, `apply_presence_conduct`, `save_carrier_state` each `registry_for_update` (parse+normalize) and `save_registry` (normalize, `story_identity_reservation` query, `_stored_shape` deepcopy of 13 stores per body, JSON dump), each invalidating the cache; `_would_change` fires whenever `strength < 1.0`, true after any `decay_minds`. Measured 0.95 s parse / 2.3 s split / 2.5 s deepcopy on the 307-body town → several seconds per beat under the write lock. → ONE private registry per PipelineContext, mutated by every writer, saved once at transaction end; compute the reservation once per turn; `_would_change` compares `as_of_hours`. [world-charter]

**C2. Per-turn checkpoint is a full copy of the story and its "read-only" snapshot INSERTs per memory row** — checkpoints.py:16/191, memory_snapshot.py:186 — `json.loads` + `json.dumps` of every world row (41 MB charters ≈ 0.95 s parse ×2/beat), `SELECT *` of every memory row with both 20 KB blobs, sha1 per row, `INSERT OR IGNORE` per row inside a transaction — every turn, twice on a data_version race; comments at 1329/1421 say "does no writes". → file `vkey` at mint time in `_upsert_memory` (column + archive/checkpoint field); snapshot selects non-blob columns and writes nothing; snapshot large world keys by content hash into a side table. [persist-memory+archive, lens-inefficiency]

**C3. Streaming json_schema 400 per call for the process life** — providers.py:2721 — one failed round trip + retry per schema-bearing call after the first rejection. → A27. [llm-providers+schemas]

**C4. Reasoning-only empty stream costs a temp-0 repair plus fallbacks** — providers.py:2183 — two or three model calls where one retry would do. → A28. [llm-providers+schemas]

**C5. `compile_world_context` pays an embedding round trip (docstring: "no provider call") serially ahead of `perception_act`, which never reads it** — mapping.py:357, runtime.py:787 — one provider call on the critical path; the pairing the runtime comments still cite was lost. → `_run_parallel_group` the two; cache the query vector by text hash. [lens-inefficiency]

**C6. Two full scene deep-merges per turn (outcome mirror + commit) plus six `merge_scene_with_diff` for route checks inside resolve (+ a seventh partial)** — perception.py:2733, director_movement.py:272/597/618/1140, director.py:4231/4536, director_floors.py:1374 — each is `copy.deepcopy(scene)` + derivation passes; between the route merges only `sd["positions"]` changes. → one `compose_beat_scene` (B1); one `route_scene` memoised on ctx after rooms/entities settle. [agents-director-support, lens-inefficiency]

**C7. `_llm_resolve_player_room` paid twice per turn on affected beats; outcome consults the cache only after paying** — common.py:9715, perception.py:2766 — one utility model call. → `_player_room_in` ordering everywhere; memoise on (positions, to_room, input). [agents-perception+composer, lens-inefficiency]

**C8. Interpret builds every specialist extra eagerly (three DB-backed condition views, sightlines, couriers, carried_reports→get_scene, unratified claims) on beats that dispatch no hand** — director.py:1187 — → build extras only when any hand runs, or lazy thunks. [agents-director-core]

**C9. Four world views computed twice per stage (`_gate_facts` + payload), `_sightlines_view` ×3, `_egocentric_exits` ×2; `_carried_reports_view` → `get_scene` + `carriers._carriers` (two `json.loads` per extant cast row) — and carriers rebuilt SEVEN times per turn across director views, `advance_carriers`, `apply_tellings`, `run_couriers`, `run_artifacts`** — director_scopes.py:641, carriers.py:395 — → `_gate_facts` takes the rows; memoise the carrier index per PipelineContext with a dirty flag. [agents-director-core, story-cards+scene]

**C10. `auto_promote_background_characters` mints a sheet with a synchronous LLM call in the commit tail** (opt-in `auto_promote`) — commit_background.py:4270 — → `core/jobs` like consolidation. [lens-inefficiency]

**C11. Scene blob SELECT + `json.loads` at ~50 sites per turn; `background_presences` 24, `known` 14; only `charters` uses the `world_read_token` cache built for this** — db.py:2528, scene.py:368; commit_mechanics.py:87 re-reads inside the transaction what `prepared["prev_scene"]` holds; commit_mapping.py:442/668, commit_common.py:567, commit_background.py ×8-9, background.py ×3, perception ×4 — → token-validated parsed-row memo on the PipelineContext (`registry_for_update` precedent for mutators); thread `prepared["scene"]`/`prev_scene` through the commit domains. Note: charters-size measurement (13.5-20 s) does not transfer; scene cost unmeasured. [core+llm-support, persist-scene, persist-memory+archive, lens-inefficiency, lens-simulation, lens-two-representations, agents-minds+narration]

**C12. Spatial derivation leaves recomputed O(N²) per pass with no per-scene memo** — `visual_level_between` ~5-9× per (observer, body) per stage (composer.py:512/1056/1820, perception.py:4000/3670/861/5358/1783/1902), each doing `room_of`×2, `spatial_rel`, `crossing_visible_from`, `containment_conceals`, `light_at`, glare; `effective_anchors`/`effective_station`/`proximity_rel` (21/15 callers, `normalize_scene_stations` calls anchors per station); light/sound `_cache_key` `json.dumps` the whole scene on every lookup (hit or miss) and `anchor_cells` computes `effective_anchors` before consulting its cache; `far_path_gain` Dijkstra per pair (documented deliberate, 0.16-3.8 ms). → one sight map per observer per stage handed to the builders; identity-keyed memo (`far_field_graph` pattern) for anchors/stations/derived bearings; content key computed once per scene object. [agents-perception+composer, world-spatial]

**C13. `active_disguises`/`active_transformations` are SQL queries per body, several times per stage (~4N+6 per stage)** — perception.py:1978, scene.py:602 — → read once per turn onto ctx like `_composer_extra_parts_cache`. [agents-perception+composer]

**C14. Character sheet normalization repeated: `normalize_character_data` (~0.5 ms) at 39 accessor sites, `cast_scene_context` ×8 per row ×2/turn, `_sense_card`/`senses_of` paired per body per stage, `json.loads(c["sheet"])` at ~80 sites, nine common.py helpers re-parsing every cast sheet (`cast_spelling_policy` ≥7×/turn), character_step building the identity floor 4× per character, `_micro_body_forms` per action event** — character_schema.py:1712, common.py:5241, character.py:3361, loops.py:380 — → memoise `normalize_character_data` on sheet text (the `character_name_from_text` precedent); a parsed-sheet CastRow on ctx; build label/scrub once per observer on `character_turn_snapshot`. [story-cards+scene, agents-common, lens-firewall, lens-universality, lens-inefficiency]

**C15. `is_memory_visible` issues 2-6 SELECTs per memory row in any framed chat; `search_memories` reloads and BLOB-decodes the whole bank up to 4× per character per beat and its MMR recomputes pair similarity every round (~49k evaluations at k=24)** — frames.py:126, memory_retrieval.py:733 — → load frames once per call; memoise the decoded bank per (chat, char, cutoff, frame) on ctx; precompute token sets / one matmul per round. [core+llm-support, mind]

**C16. `room_registry` re-scanned and JSON-parsed ~15× per turn by `_planned_specs` and siblings** — structure.py:592 — → parse once per PipelineContext, derive every `planned_*` reader from it. [world-rest]

**C17. `_advance_ground` BFS per room per beat with nothing falling; `room_exposure` ~110 substring scans per visit** — commit_scene_state.py:347 — → early return when precipitation none and ground empty; exposure per room once, depth per component. [world-rest]

**C18. Rulebook walks the first 24 rooms in dict order (can miss the player's own room on a >24-room plan) and `present_charter_figures` is walked again at resolve for the right set** — mapping.py:292, director.py:3366 — → compute `rooms_in_view` once in the compiler, record on the step, resolve reads it; drop `RULEBOOK_ROOMS_CAP`. [agents-runtime+plan]

**C19. `presence_view` deep-copies the whole charter twice and normalizes three times per voiced presence; `advance_snapshot` re-reads clock+style guide per charter; `charter_economy` normalizes 14× per window; six registry passes rebuild roles/display names per body** — charter_runtime.py:3701/2289/3848, charter_economy.py:283 — → copy only mutated stores; hoist `_story_day`; private `_band/_quote/_trade`; identity index per cached registry. [world-charter, lens-simulation]

**C20. Commit hygiene: `commit_mapping` writes `known`/`lore_cache`/`active_books` byte-identical every beat; `planning_needs` read-normalize-write per need and re-queue a no-op job while any room need is open; `_beneath_visible` SQL per body per observer (N²); presence readers re-`wget` crowds/couriers/artifacts per perceiver; per-character ledger queries (6+12+8 variants) per step; `_delivered_manifest` dead work** — commit_mapping.py:635, planning_needs.py:262, common.py:1156/1642, character.py:3263 — → write on change; load once; `inputs` pattern; per-turn `prior_variants` map. [persist-scene, world-rest, agents-common, agents-minds+narration]

**C21. Out-of-band but repeated: Planner `_payload` rebuilds frontier/minds/clock/packages/status every step (5-40 per reply; `frontier_report` reads `planned_topology` and `containment` twice); Dramaturge `_payload` per step (13 queries for 12 beats); `publish_package` takes the world snapshot N+2 times and `validate_package` saves the store 3×; `fit_result` re-encodes per dropped item** — story_planner.py:445, dramaturge.py:217, plot_packages.py:2768, room_tools.py:1607 — → memoise the static half per reply keyed by a write-tool counter; one snapshot per publish. [story-room+plot, lens-stagecraft]

**C22. Frontend/route: `chat_get` runs one variants query per turn and parses every committed `state_diff` for `dialogue_log` on every open (~2,500 queries on the owner's chats); every turn completion re-fetches and rebuilds the whole transcript plus one GET per render; `guest_state` 2N queries polled every 10 s; backdrop/ambience GETs rebuild the request 2-3× (json_extract over a multi-MB blob, `room_projection` the payload never carries, `backdrop_status` twice, `acoustic_fingerprint` twice); `SelectiveGZip` level-9 compresses every PNG/audio file; `boot()` (~60 call sites) re-downloads the 256-306 KB catalog and every default prompt; Writers' Room re-renders 60 messages through 393 template regexes per token; World Browser 3-4 full re-reads per field commit** — app.py:3700/4235/6577/433, chat.js:1160, app.js:9, writers_room.js:365, world_browser.js:2784 — → the narration.py:1177 JOIN and `json_extract`; `?since_turn_id=`; `data_version` 304; a cheap resolver for the poll path; compress text types only; per-list refresh; rAF-coalesced room render. [web+dressing, js-play, js-settings]

---

## Section D — IMPROVEMENTS FROM OTHER ARTS

Grouped by plug-in point. Each: source art → reader-visible problem → plug-in → data needed. Merged where the lens agents proposed one idea.

### Composer

**D1. Tells reach the page (Wood/Stanislavski: the actor's body).** Problem: 101 beats of a character with no glance, grip or catch in the voice that was not a declared action; deception and dramatic irony have no channel. Plug-in: `_composer_outcome` mints a `cue` percept (hearing or sight per admission) from `source_manifest[source]` and a standing `demeanor` percept keyed `standing_key("demeanor", (body,), (demeanor,))`; observations derive from the render so `because` cannot cross. Data: already computed (A36); add the kind to `PERCEPT_KINDS`, `_STANDING_ORDER`, ja adapter, masked-floor test. [lens-literature, agents-perception+composer]

**D2. Establish a voice once (Wood; theatre gives a vocal quality once).** Problem: "in a flat, clinical monotone" on 63-98 of 79 lines; the ban list exempts what the view itself contains. Plug-in (merged from lens-stagecraft "manner as standing state" and lens-literature "voice baseline"): (a) card — `tone` names departure from `self.voice`; (b) composer — on a speaker's first line to THIS observer render `voice.register` as manner (set-membership on a `voice:<body>` ledger key), thereafter only when `tone` is present; keep `tone` in `Percept.data` so minds receive it every time. Never compare tone strings. Data: `self.voice.register`, per-observer ledger. [lens-stagecraft, lens-literature]

**D3. Silence where there was sound (sound design's room tone dropping out).** Problem: the player wrote "nothing hummed" themselves. Plug-in: a `ceased` verdict from the observer's OWN previous ledger for a hearing-channel standing key with no percept this beat, in the same room — `as_beat` "the <label> has stopped". Real gap only for the single-source/room-goes-even case (multi-source rooms already yield `changed`). Data: `prev_standing`. [lens-stagecraft]

**D4. Blocking by depth (stage direction).** Problem: narrator invented "on my right" for a body at the other door (UNBUILT 1.149). Plug-in: `_render_presence_group` orders bodies near→far by cell distance where both cells exist, tier ladder otherwise; a `depth` word on `co_present_positions`. Ordering only, never withholding. Data: `body_cell`, `proximity_rel` tiers (already stamped). [lens-stagecraft]

**D5. Key light says where to look (cinematography).** Problem: the player aims the lamp on five beats; the page names the source, never what the beam is on. Plug-in: `light_shape` gains `aimed`: visible anchors/bodies whose nearest cell has `cone_factor >= 1` on the observer's own held cone, distance-ordered; one clause in `render_light_shape`. Data: `src["axis"]`, `cone_factor` (exist). [lens-stagecraft]

**D6. Subtext: the unanswered address as a fact.** Problem: "no answer came" sometimes fabricated about a body within reach. Plug-in: in `_outcome_event_stream`, for each player line with `intended_target` T delivered above `none` where T's merged sequence has no speech, a `speech` percept `fidelity='silence'` ("T says nothing") into the PLAYER's view only; state the fact, never the reason. Data: `intended_target`, delivered level, merged results (exist). [lens-stagecraft]

**D7. Planted detail / Chekhov's gun.** Problem: the most loaded object in the story planted at beat 18, never touched in 83 beats; the narrator remembers 12 turns. Plug-in: the player's standing ledger becomes `{key: {first_turn, last_rendered_turn, sentence}}`; `long_established` (still present, delivered ≥N beats ago, not rendered since) handed to the narrator as REFERENCE. Firewall-safe: every entry was already delivered. Data: `standing_keys` (today a bare set). [lens-literature]

**D8. Reveal economy (Le Carré): the room's registry name as perceived text.** Problem: "Upper Service-Core Riser 10" printed 30 times on a level established as having no signage. Plug-in: player-mode `environment_percept` renders the name only when earned (a sign anchor, a name heard/read, an authored party name), else description and features; ids stay for checks; character-mode and `_annotate_known_exits` untouched. This is an unregistered design ruling, not a defect — ask the owner. [lens-literature]

### Narrator prompt / payload

**D9. Scene vs summary; ellipsis (Burroway; film cuts).** Problem: four consecutive beats of one bare corridor at full scene length; ten-minute windows rendered at two-minute grain. Plug-in (merged from lens-stagecraft and lens-literature): `beat_time: {mode, duration_seconds}` from `director_resolve.state_diff.time` (absent when unset) + one sheet paragraph: `time_skip` is SUMMARY, `action` is scene; never license events inside the span. Note: `display_advance` IS read (commit_scene_state.py:1877) — UNBUILT 1.87 is stale; the datum a writer needs is mode/duration, not the phrase. [lens-stagecraft, lens-literature]

**D10. Register per story.** Problem: `style_guide.tone` reaches the Director's unseen `resolved_event` and never the page; exemplars are one install-wide list. Plug-in: `register` in the narrator payload (absent when unset, the `narration_tense` routing); optional per-story `exemplars` on the style guide. No genre switch — a register statement. [lens-literature]

**D11. Computed rhythm report instead of a banned-word list.** Problem: the tell migrated to unlisted words ("steady" 48, "unyielding" 11). Plug-in: `rhythm_report` beside `overused_phrases` from the last four proses (sentence-length spread, trailing participial share, front-loaded attribution share, recurring closer class); cut the lists, keep the class sentence. Payload and warning only. [lens-literature]

**D12. Focalization is the player's in every person (Genette).** Problem: rule 4 licenses the player's feeling for second person only; 101 first-person beats with exterior-only prose. Plug-in: one sheet sentence — person governs REFERENCE, focalization is the player's; their sensation and declared private thought may be free indirect discourse in first and third. Data: `player_declared.private_thought`, `sensory_channels.interoception` (in payload). [lens-literature]

### Dramaturge / Writers' Room

**D13. Intensity-driven pacing (Left 4 Dead's AI Director).** Problem: proposals arrive on a fixed beat cadence whether the last twelve beats were a siege or a supper. Plug-in: an `intensity` scalar per beat in `record_measure` (committed strain/charge/drive-strain maxima, threat-class evidence count, fired events) kept as a trail; `schedule_room_work` runs a pass when flat for the budget OR as a peak begins to fall. Author layer; the dial still governs what. [lens-simulation]

**D14. Scenes and acts, not beats (dramaturgy's unit).** Problem: a hundred-beat descent through five spaces reads to the Dramaturge as one flat stretch of 12 truncated beats. Plug-in: deterministic scene index from the player's own room history and clock, stamped on each stream entry (`scene`, `room`, `elapsed`), plus `since_last` per proposal kind. Data: turn scenes, `simulation_clock`, proposals ledger (all player-visible). [lens-stagecraft]

**D15. Progress clocks on typed triggers (Blades in the Dark).** Problem: package clocks fire on turns/hours only; consequences arrive because the calendar turned. Plug-in: optional `segments` + `advance_on: [{event_kind, location_id?, actor?}]` on a clock, ticked in `fire_due_clocks` from fired `scheduled_events`/`world_events` and `public_evidence` kinds — the shape `_reactive_triggered` already matches. Time-due clocks unchanged. [lens-simulation]

**D16. Fail forward (tabletop GM practice).** Problem: a refused declared act can resolve to nothing changing. Plug-in: a `cost` prose duty gated on a deterministic fact (interpret carried a physical/communicative declaration AND resolve's dispositions refuse or block it), stating the class once — a refused act leaves the world changed by the attempt through `consequences`/`world_pressure`/`obligations`; `_PROSE_DUTY_SHIPPED` backstop. No outcome forced. [lens-simulation]

### Charter

**D17. Needs with a supply chain (see A25 `fed_by`).** The largest single fidelity change the module names; unreachable until generation writes `fed_by`. [lens-simulation]

**D18. A social need serviced by practice (The Sims).** Problem: a town that "met itself and had nothing further to do"; nothing pulls a body toward company. Plug-in: per-role `company` need serviced by this window's converse/greeting acts in `advance_needs`; `errands` treats low `company` as a pull to the nearest occupied commons; feeds `charter_feel` through the existing unmet-need pain path. Depends on D17. [lens-simulation]

**D19. Authorable rest/social phases (see A low: nocturnal institutions).** Optional `resting_phases`/`social_phases` on the charter record defaulting to today's constants. [lens-universality]

### Psychology

**D20. Weather reaches the body as discomfort (Dwarf Fortress / The Sims exposure).** Problem: bare-armed in freezing rain feels what the hearth feels unless the model says so. Plug-in: `world/exposure.py` as comfort's negative twin — `weather_for_room` temperature/precipitation/intensity at the body's room, scaled by bare `attire` regions and stamina → bounded `ambient_discomfort` into `resolve_hedonic` as a PAIN floor (habituating, never charge); same reading in `charter_feel.appraise_window`. Closed enums only. [lens-simulation]

**D21. A deterministic, evidence-cited signal floor for registered characters' stances (Comme il Faut / Prom Week).** Problem: charter bodies move five axes deterministically from witnessed speech-act kinds; registered characters only when the model emits ops; a promoted body stops obeying the rules that formed it. Plug-in: after the model's ops in the relationship domain, apply `charter_social.DEFAULT_SIGNALS` (with `_room`) for every `public_evidence` act whose observation id was DELIVERED to that mind, one `relationship_events` row per axis, `provenance="witnessed"`. Source set is the character's own delivered observations. [lens-simulation]

**D22. Hand a mind its own relationship history (Prom Week's legible social facts).** Problem: the payload carries five numbers plus one overwritten `salient_event`; the reasons are stored and never read by the character. Plug-in: `relationships_for_payload` attaches the top-|delta| `relationship_history` rows per target as `because`. No new state. [lens-simulation]

**D23. Dialogue subtext (Mamet).** Problem: `suggested_lines` is a seeded RNG and the card says "write that many"; a character never withholds an answer she has. Plug-in: derive `suggested_lines` from `voice.verbosity`, `awaiting_your_answer`, whether the enacted want is the suppressed one; replace "write that many" with the class rule (what you withhold shapes the line). Keep `min_lines`/`hard_max`/`may_stay_silent`. [lens-literature]

### Memory

**D24. Consolidation sees re-readings and turning points (memory as story-shaped reconstruction).** Problem: a disputed memory is summarised from the abandoned reading; a summary can only list that trust changed, not the moment. Plug-in: `now_reads`, `reread_on_turn`, `importance`, `importance_revised` per consolidation row; ask for `turning_points: [{when, what_changed_for_me, memory_refs}]` delivered as `memory.what_changed_me`. Never move a claim across provenance scopes. [lens-literature]

**D25. `stable_facts` gets a reader (or goes).** Persist as a column delivered as `memory.settled`; the natural reinforcement source for `learned_beliefs`. [lens-literature]

---

## Section E — STALE DOCS

| doc:line | what it says | code:line that contradicts it |
|---|---|---|
| core/db.py:596; core/pipeline_context.py:69; llm/llm_quality.py:457; persist/llm_capture.py:11,146; persist/pipeline_trace.py:432,440,537; agents/runtime.py:416,425; agents/director_fanout.py:62; agents/director_scopes.py:63,165,467; llm/prompts.py:337; llm/schemas.py:1184,2366,4865; tools/project_check.py:994; static/js/chat.js:1338; static/js/settings.js:2406,2706,2709,2830; AGENTS.md:603 | "six specialists" / "six hands" | agents/director_scopes.py:96-149 `SPECIALISTS` has five keys; llm/providers.py:1261 `NO "director_offscreen"` |
| core/jobs.py:120 | "Clear the six turn-scoped vars" | jobs.py:142-148 tuple clears nine |
| core/db.py:2474, 2054 | "DDL autocommits, so a crash mid-list leaves earlier statements applied" | db.py:2400 default isolation; `_set_schema_version` INSERT opens a transaction; lists after the first run inside it until `executescript(LATE_SCHEMA)` at 2490 |
| core/db.py:1027; docs/guides/DATABASE.md:167 | `transit_edges` named "in the chat-deletion sweep in web/app.py" | persist/chat_delete.py:32; web/app.py:47 only imports |
| core/pipeline_context.py:89 | `note_step_decision` "no-op when debug capture is off" | pipeline_context.py:492-500 always appends; runtime.py:355 sets the sink unconditionally; 411-414 persists per variant |
| core/pipeline_context.py:490 | "perception fans out across a thread pool" | agents/perception.py and composer.py contain no executor; pools are director.py:2719, narration.py:2287, character.py:106 |
| llm/providers.py:220-223, 565 | "use the 300s default", "REQUEST_TIMEOUT's read deadline — 300 seconds" | providers.py:191 `REQUEST_TIMEOUT = (30, 90)`; 284 `BLOCKING_READ_TIMEOUT = 300.0` |
| agents/runtime.py:267; language_packs/*/ui.json:1354 | STEP_LABELS commit = "Mapping & memory · commit-up" | docs/guides/PIPELINE.md:902 no lore filed since 2026-09-03 (commit_mapping still files needs; "commit-up" is the stale half) |
| agents/runtime.py:585-586, 1351-1352 | "three overlapping pairings … mapping/perception_act pairing above" | runtime.py:787-788 sequential append; loop has two `_run_parallel_group` calls (1337, 1353) |
| agents/mapping.py:341; docs/guides/PIPELINE.md:38 | `compile_world_context` "no provider call" | mapping.py:357 → `search_lore` → memory_lore_entries.py:448 `embed_texts_meta` |
| agents/common.py:4632 | `_delivery_ok` "the one predicate all of them call" | only agents/loops.py:309, 362 |
| agents/common.py:8409, 8643, 9646 | `_ENFORCEABLE_PREFIXES` "buys a full narrator rewrite" | agents/narration.py:2009-2091 detect-and-report only |
| agents/narration.py:289-291; docs/guides/PIPELINE.md:851-852, 857-860; Design.md:227 | fidelity strip / craft screen "asks for one direct rendering" / "rewrites every inflection" | narration.py:2009 "blocks on being parseable JSON and on nothing else", 2014 "THE CRAFT REWRITE IS GONE", 2033 warnings only |
| agents/narration.py:1275 | `_render_current_events` "the plain chronological package written from" | narration.py:1846 uses `_render_observed_events`; only reader is tools/narrator_package_bench.py |
| agents/perception.py:1136-1145 | "nothing gives a BODY a smell … a gate with nothing behind it" | perception.py:3579 `_body_scents` reads `scent_of`; 3666 `_scent_sources` grades it; `scent_channel_to_sources` has only a test reader |
| agents/perception.py:2269 | a stopped source "is heard as silence where there was noise" | composer.py:2893 no verdict for an absent percept; commit_scene_state.py:617 files a Director notice only |
| agents/README.md:53-55 | only `loops→character` and `background→perception` cross-import | agents/director_movement.py:110 imports composer; agents/character.py:3174 imports composer |
| agents/README.md:56 / agents/runtime.py:253 | four registries agree (asserted) | tests/test_step_labels.py:17 plans no loop/character/narrator_extra; tools/project_check.py has 0 hits for STEP_LABELS/STEP_HANDLERS |
| persist/checkpoints.py:1329-1330, 1421; web/app.py:5551 | `snapshot_state` "does no writes … read-only" | mind/memory_snapshot.py:186-193 `with transaction(): put_memory_vector(...)` per row |
| persist/commit_common.py:626 | "So no writer touches the key" | commit_common.py:645-648 `wset(cid, ENGINE_NOTICES_KEY, …)` on every call with cid |
| persist/commit_background.py:1695-1697 | "the merge commits later in this same turn" | persist/commit.py:484 scene domain runs before 553 background_presences |
| persist/commit_entities.py:216 | "(and legacy placement cleanup)" | 414 DELETE from decommissioned `world_placements` |
| persist/commit_scene_state.py:1205 | "without mutating durable state" | 653/758/785/1072/1194/2077 `add_engine_notice`, 2079/2116 `wset` flags, before the transaction |
| world/spatial_routing.py:397 | "the graph is undirected" | spatial_routing.py:367 `neighbor_map(..., directional=True)` |
| world/spatial_barriers.py:797-798; world/spatial_merge.py:1342 | a passage "whose barrier reads as nothing is dropped" | barriers.py:812-836 drops only on the rooms test; `normalize_barrier` never returns empty |
| world/spatial_sound_field.py:2002-2003 | "a wall is on this graph, at 45 dB" | sound_field.py:1941 `WALL_LOSS_DB = 16.0` |
| world/routines.py:25-27, 41-44 | "The engine's clock has no day anchor" | world/day_cycle.py:13-16, 129-139 |
| world/weather.py:26-29; world/place_purpose.py:178-181 | keyword exposure fallback "must not be used for anything a mind could act on" | world/spatial_light.py:102-113 `room_light` darkens via `room_exposure`; `_LIGHT_SIGHT` gates sight (UNBUILT 10078 records the light use as a ruling; retract the docstrings, tokenise the haystack) |
| world/regions.py:376-379 | "the one the most bodies stand in" | regions.py:358 set with no counts; 380 `standing[0]` alphabetical |
| world/mechanics.py:981 | `_HAZARD_ANSWER_CHANNELS` is "the whole vocabulary" the floor checks | mechanics.py:1036-1039 hardcodes `dice` + three names; constant unread |
| world/charter_needs.py:224-234 | `mood` "NOT WIRED IN … read by nothing" | world/charter_run.py:508, 539-553 folds `mood_weight * mood(...)` |
| world/charter_runtime.py:573-574 | "The one write chokepoint for the registry" | charter_runtime.py:1843, 2719 `wset_for_frame(cid, CHARTERS_KEY, …)` |
| world/charter.py facade docstring | 12 siblings | 41 `charter*.py` files; charter_runtime omitted |
| mind/affect.py:665-673 | `decay_affect` "every half_life TURNS — the caller counts beats" | commit_memory.py:1297-1314 passes psych units (minutes) |
| mind/memory_inference.py:29-32 | `_cos` "recomputes both norms (~4x)"; "See UNBUILT §1.4" | memory_common.py:218-233 plain dot product; no §1.4 heading exists |
| mind/memory_vectors.py:313-316 (+ following para) | `lore_entries` "has no embedding_model/embedding_dim columns" | memory_vectors.py:338-358 selects and writes both; memory_lore_entries.py:112-129 inserts them |
| mind/memory_context.py:349-350; tools/benchmark_recall_review.py:51 | replacement "lives in agents/character.py (`_attach_recall_review`)" | no such function; mind/memory_judge.py:408 `review_recall` inside `review_minted_memories` |
| story/room_research.py:50-57 | "nothing can grant it … that module's owner wires it" | story/mandates.py:82 `"research"`; mind/canon_provenance.py:62 `"web_reference"` |
| story/scene.py:2464-2468, 2542 | free-text style fields reach "the Director and the mapping agent"; "Pinning a genre is the new capability" | no mapping agent; `STYLE_GUIDE_FIELDS = ("tone","avoid")` (2492), the `genre` branch (2614) unreachable |
| story/room_slice.py:76-81 | `_op_rooms` reads "the closed field set of OPERATION_FIELDS" | room_slice.py:356 vs plot_packages.py:2629 disagree; neither reads `lair`/`hunts` |
| web/story_view.py:224-232 | `active_content` "lives behind the pipeline's import graph" | agents/storage.py:5-8 imports only json/time/core.db; web/app.py:51-55 imports it at load |
| language_packs/*/…/director_interpret.txt:54,56; prose_author_sheet/28.txt; prose_author_output_shape.txt | "six scoped specialists … offscreen" | director_scopes.py:96 five keys |
| language_packs/*/…/interpret_delegation_note.txt | OBJECTS lacks `sensory_events`, GEOGRAPHY lacks `comms_ops` | director_scopes.py:133-134, 147-148 own them |
| language_packs/*/…/specialists/*/core.txt:7 (spatial :9) | "shared with the full Director sheet" | no `DEFAULT_PROMPTS["director_resolve"]` (AGENTS.md § Director orchestration) |
| language_packs/*/…/narrator_fidelity_correction.txt, narrator_craft_correction.txt | describe a rewrite pass | narration.py:2001 "THE FIDELITY CORRECTION PASS IS GONE"; 0 code refs |
| language_packs/ja/…/character.txt:70 | `repeat_correction`/`move_correction`/`intention_correction` in the input | character.py:4124-4141 built after output, "read by nothing" |
| static/js/settings.js:2250-2254 | caching "only applies to Claude models" | providers.py:768 `_CACHE_PASSTHROUGH_KINDS = ("openrouter","nanogpt")`, 800-826 |
| static/js/settings.js:1525; editors.js:947; lorebooks.js:1390 | "updated by the mapping agent" / "agents/mapping.py reads it as canon" | agents/mapping.py has no `canon`/`lorebook`; commit_mapping.py:550-563, background_claims.write_canon |
| static/js/chat.js:913 | "commit's mapping call" in the tail | commit_mapping.py:6,110 mapping model retired |
| static/js/extensions.js:369-370; docs/guides/EXTENSIONS.md:1481 | `state()` returns "a copy" | extensions.js:371-377 returns live `S.boot`/`S.chat` |
| docs/guides/PIPELINE.md:822-825; docs/UNBUILT.md:2818 | "Active sensations are never presentation-compressed"; `ACTIVE_STANDING_KINDS` "already exempts" | composer.py:2845 `ACTIVE_STANDING_KINDS = frozenset()` since 2026-09-07 |
| docs/guides/PIPELINE.md:499-502; Design.md:220; AGENTS.md:276 | `_strip_onset_rendering`, `_inject_onset_sequence`, `_inject_onset_speech`, `_self_cannot_see_own_surface` "dead code kept" | deleted in 6d843e2 (UNBUILT §1.45); only `_inject_visible_actor`/`_inject_action` survive (common.py:7300/7355) |
| docs/guides/PIPELINE.md:881, 883-918, 921; Design.md:530 | commit "prepares all lore … embeddings"; 14 domains, scene second; "Two things run after" / "Only autobiographical consolidation" | commit_mapping.py:1-2 no lore; persist/commit.py:473-580 21 domains (world_events second); 604-756 eight+ post-transaction schedulers |
| docs/guides/ENGINEERING.md:46-48, 109-110, 117-118, 130, 150, 181 | every stage a model call; perception one call per observer; `mapping_stage`/`mapping_quick` handlers and diagrams | providers.py:1235 `NO "perception"`; runtime.py:920-927 `compile_world_context`; 0 hits for `mapping_stage` in runtime.py |
| docs/guides/FEATURES.md:133-135, 225-227 | "Body language you can catch", "Tells that get paid off" | `source_manifest` has no reader (A36) |
| docs/guides/RESEARCH.md:46, 77-78, 86 | `memory.py:51/977/1013-1016/1046` | mind/memory.py is a 137-line facade; `_rrf_add` at memory_retrieval.py:357, `MEMORY_PROVENANCE` at memory_common.py:92 |
| docs/guides/LIVING_WORLD.md (51 `.py:N` citations, ~30 mismatched, e.g. :85, :92, :101, :137, :344, :405) | line-cited gates | e.g. story/scene.py:1992 is a BehaviorController comment; persist/commit.py:453 is blank |
| docs/README.md:186; docs/UNBUILT.md:8717-8722 (§2.9) | Story Planner/Dramaturge "None is built" / "Build the Story Planner" | agents/story_planner.py, agents/dramaturge.py exist; Design.md:276, 278 **Built** |
| AGENTS.md:228 | `_at_post_within_earshot` is the at-post gate | tests/test_crowds.py:241-242 asserts it does not exist; four triggers only |
| AGENTS.md:263, 265, 266 | `PLANNER_MAX_TOKENS`/`CHARTER_PLANNER_MAX_TOKENS` = 20000, `DRAMATURGE_MAX_TOKENS` = 3000, `BIBLE_FOLD_MAX_TOKENS` = 2500 | all four = 40_000 (story_planner.py:93,134; dramaturge.py:53; room_bible.py:68) |
| AGENTS.md:603 | `interpret_delegation_note` at `agents/director.py:595` | director.py:1038; line 595 is `planned_topology` |
| AGENTS.md:692-696, 706; CLAUDE.md:109; Design.md:753 | "fourteen" / "thirteen" `spatial_*` modules | world/spatial.py imports 19 (fov, lint, light_field, sound_field, scent_field missing from every list) |
| AGENTS.md:711, 716-729; CLAUDE.md:129 | "twelve `memory_*` siblings" | mind/memory.py imports 13 (memory_time missing); memory_judge imported directly by character.py:39, commit_memory_write.py:208 |
| Design.md:392 | paradox rungs `warn → hazard → toll → force_restore`, `paradox_policy.max_severity` clamp | world/paradox.py:71 MODES dread/hazard/toll/warden/bureau; 126-133 policy has mode/escalation_rate/toll_in_radius; `max_severity` 0 hits |
| Design.md:1091 | Hard mode "(UNBUILT §2.4)" | no §2.4; Design.md:639 "### Hard mode (built)" |
| Design.md:1020-1031; docs/UNBUILT.md:8572-8573 | relationships have "no change log" | mind/memory_relationships.py:84-121 `relationship_events` per axis with provenance |
| docs/UNBUILT.md:1304-1330 (§1.33), 2157-2159 (§1.62) | `mapping_stage`/`mapping_quick` as live stages | runtime.py:921 `compile_world_context` |
| docs/UNBUILT.md:5553-5557 (§1.116 PC3) | open-edge floor "is not built" | spatial_sound_field.py:747 `open_edge_floor`; senses.py:980; sound_field.py:1547, 1714 |
| docs/UNBUILT.md:6326-6335 (PX5 third floor) | micro-loop "no third-body admission"; helper "is `agents.perception._act_surface_admission`" | agents/common.py:4729; loops.py:26, 378-383 |
| docs/UNBUILT.md:6338-6346, 6784-6795 (PM5 b) | `act_percept` refuses the rear arc with no substitute | composer.py:2688-2712 mints the hearing percept (dead only on the outcome pass, A35) |
| docs/UNBUILT.md:9050-9051 (§2.18) | "The `offscreen` specialist ships the ops surface only" | hand retired 2026-09-04 |
| docs/UNBUILT.md:5971 (§1.124), 6864 (§1.137), 8263 (§1.147) | headers say BUILT/FIXED, no residual named | register rule: delete on landing, add Design.md row |
| docs/UNBUILT.md:1060 | "A room cannot carry a hazard" | region_events.py:296 writes `room["hazard"]` |
| docs/UNBUILT.md:3261 (§1.87) | `display_advance` "reaches NO reader" | commit_scene_state.py:1877 → `_advance_day_cycle` 144-146 |
| docs/UNBUILT.md § 1.104 | characters "carry a story-local copy" of the card | web/app.py:3865 attach writes no `chat_chars.sheet` (A19) |
| docs/UNBUILT.md:1543-1549 | dead-family table (six helpers) | omits `_ambient_blocked_slugs`/`_keys_reference_blocked` (common.py:3333/3361, stubs) and `_render_current_events` |

---

## Section F — PROPOSED BUILD ORDER (≤15, for the main session now)

Chosen for (page/world-truth impact) × (confidence) / (blast radius). No caps, no genre switches. Each: files → test file.

1. **Streaming provider ladder.** Give the streaming recovery (sync 2721, async 3126/3156) the blocking path's stage 0 (json_schema → json_object → nothing; `_note_json_schema_rejected` when the dropped field was a schema); raise `ReasoningBudgetExhausted`/empty-content `LLMError` at the end of `_sse_openai`/`_sse_openai_async`; read stream reasoning through `_reasoning_text`. — `llm/providers.py` → `tests/test_json_object_400_fallback.py` (add a streamed json_schema rejection), `tests/test_reasoning_budget_stream.py` (new).

2. **Reroll hydrates only ords below the rerolled step.** Cut the `only_key` hydration and rehydration at `s["ord"] < rerolled ord` (the `from_key` rule); pass `ctx.cast` to `build_plan`; tail from `build_plan` not the table. — `agents/runtime.py` → `tests/test_reroll_side_channels.py` (new: commit a turn whose resolve moved the player, reroll `director_resolve`, assert the Director payload's player room is the onset room).

3. **Outcome-pass identity and gating (three one-line classes in `_composer_outcome`).** `display_map` first for speaker/communication/actor (5231/5272/5312); `_gated_ambient_percepts` writes `detail` (3553); delete the `actor in behind` pre-skip (5291); `_composer_unknown_sources` honours `disguise_conceals_identity`. — `agents/perception.py` → `tests/test_perception_disguise.py` (outcome speech through a disguise), `tests/test_gated_ambient_detail.py` (new), `tests/test_what_is_behind_you.py` (through `_composer_outcome`).

4. **Interpret dispatch actually reaches the hands.** Move `_note_for` above the source branch; route note keys through `_normalize_omission_category` → `_CATEGORY_CHANNELS` before hand/channel spellings; add a retired-hand successor map (`offscreen` → social); phase_sources container by STAGE and prune over `contact_assertions`; rewrite the roster in `director_interpret.txt:54/56`, `28.txt`, `prose_author_output_shape.txt` (en+ja) from `SPECIALISTS` and fix `director_establish.txt:60` `op:'set'` (accept `'open'` in `_op_word`). — `agents/director_fanout.py`, `agents/director_scopes.py`, `agents/director.py`, `world/crowds.py`, language packs → `tests/test_director_orchestration.py` (interpret payload carries `director_note`; category-keyed note dispatches), `tests/test_crowds.py` (establish example through `apply_ops`), `tests/test_offscreen_hand_retired.py` (no sheet names a hand outside SPECIALISTS).

5. **Schema-derived repair merge; no manifest clamp; mint binding by room.** `_merge_repair_into_diff` and `_normalize_diff_shape` iterate `schemas._fields(StateDiff)`; remove `_RECONCILE_MAX_MANIFEST_ITEMS` from `_manifest_items` (bound repair CALLS if anything); `pool = by_room.get(room, []) if room else list(people)`; stash `resolve_sc` on `_orch_repair`. — `agents/director_evidence.py`, `agents/director_floors.py`, `agents/director.py` → `tests/test_director_diff_channels.py` (channel-by-channel merge), `tests/test_identity_binding.py` (empty-room mint), `tests/test_resolve_reconciliation.py`.

6. **One identity floor.** `_unanswered_question_note` caches the raw record and labels at return; `deterministic_micro_perception` and `background._present_others`/`_beat_for_presence`/`_manager_events`/`addressed_by` use one label fn built from `observer_label_fn`/`_presence_recognizes`; `spatial_digest(..., label_for=_view_label)` in narrator and narrator_extra; `player_said_nothing` gated on `_micro_seen_bodies`. — `agents/character.py`, `agents/loops.py`, `agents/background.py`, `agents/narration.py` → `tests/test_decision_names_are_gated.py` (warm the cache via the loop, assert the label), `tests/test_unknown_actor_label.py`, `tests/test_narrator_spatial_frame_labels.py` (new).

7. **Script-aware boundaries at the firewall sites.** `observer_name_scrub`, `_strip_identity_tokens`, `_unknown_actor_label` (strip before tokenising) via `name_boundary_pattern`; move `_ABSENCE`, the generic label and `_part_tokens` behind `linguistic("story.scene", …)` with entries in both packs; `rekey_place_claims` and `_garment_named_in` onto the shared boundary (`story.attire.garments_named_in`). — `agents/common.py`, `story/scene.py`, `mind/theory_of_mind.py`, `persist/commit_attire.py`, `language_packs/*/cards/linguistics.json` → `tests/test_lore_identity_leak.py` and `tests/test_unknown_actor_label.py` (CJK cases), `tests/test_disguise_disclosure_ja.py` (new).

8. **Spatial truth pack.** `passable_route_next_step` = first hop of `passable_path`; `remove_adjacent` drops both edges and the passage record before `sync_scene_passages`; `_hear_level` folds `one_way_window` → `window`; `light_sources` uses `room_centre`; `sprint_reach` uses `effective_room_size`; `_body_interior_holder` treats `mode == "interior"` as a mass (and gate 3). — `world/spatial_routing.py`, `world/spatial_merge.py`, `world/spatial_senses.py`, `world/spatial_light_field.py`, `world/spatial_containment.py` → `tests/test_travel_continues.py` (directed chute), `tests/test_spatial_merge_remove_adjacent.py` (new, both replayed shapes), `tests/test_hearing_barriers.py`, `tests/test_light_field.py` (4×12 room), `tests/test_containment_enclosure.py`.

9. **The comm channel and the near field.** Declare `medium` on `DialogueLogEntry`; build one sound field per perceiver per stage with `events=beat_sounds` and add the establish stage's `heard_events` step to `_composer_outcome` before `distant_sounds`; `far_path_gain` returns None when no edge chain joins the rooms. — `llm/schemas.py`, `agents/perception.py`, `world/spatial_sound_field.py` → `tests/test_comm_channel_hearing.py` (through `validate_llm_output`), `tests/test_near_field_beat_events.py` (new), `tests/test_sound_field.py`.

10. **Charter correctness (five one-function fixes).** Settle commitments into the advanced local; `continue_walks(…, scene)`; `_record_shared_recent_history` through `_remember_experience`; comm delivery through `resolve_target_body` with exact-form `_names_body`; `charter_generate` derives `fed_by` from the plan's flows/stores and medical post; `charter_promote` reads `as_of_hours`/`happened_at`. — `world/charter_run.py`, `charter_move.py`, `charter_history.py`, `charter_observe.py`, `charter_generate.py`, `charter_promote.py` → `tests/test_charter_creature_rules.py` (step-level settle), `tests/test_charter_move.py` (two-window walk == one-window), `tests/test_charter_public_evidence.py` (title comm reaches one body), `tests/test_charter_generate_needs.py` (new: every generated need has `fed_by`).

11. **One registry per commit.** `ctx.charter_registry` taken with `registry_for_update` on first mutation, every writer mutates it, `save_registry` once at transaction end through the single `_write_registry` also used by `land_snapshot`/`land_presim`; `_would_change` on `as_of_hours`; `presence_view` copies only `figures`/`practices`. — `world/charter_runtime.py`, `persist/commit_background.py`, `persist/commit_scene_state.py`, `story/carriers.py` → `tests/test_charter_runtime_commit.py` (new: assert one `CHARTERS_KEY` write per commit and split shape on disk).

12. **Checkpoint without writes, remap without holes.** `vkey` filed at mint time (`_upsert_memory`, column + archive/checkpoint field per DATABASE.md), `dump_chat_memories(inline_vectors=False)` selects non-blob columns and writes nothing; `_remap_cp_blob` calls `_remap_fixed_points_frames`/`_remap_scheduled_event_frames` via one shared `remap_world_state`; `turn_branch` membership from `blob["chars"]` through the restore path's helper. — `mind/memory_snapshot.py`, `mind/memory_write.py`, `persist/checkpoints.py`, `web/app.py`, `persist/chat_archive.py`, `core/db.py` → `tests/test_checkpoints.py` (snapshot performs no INSERT), `tests/test_branch_frames.py` (new: two-frame branch, reroll, frame ids hold), `tests/test_branch_membership.py` (new).

13. **Writers' Room and editors read and write the row they mean.** `read_scene(cid, frame_id)` via `wget_for_frame` threaded through room_tools/room_frontier/plot_packages apply functions; `restore_room_messages` per-role limit; `_require_chat_idle` on `attire_put`/`chat_add_char`/`chat_add_persona`/`chat_del_char`/`ph_put` (404 on missing row); world_browser clears `state.cache.raw` on every write and the attire editor writes one body; `char_edit` refuses a name/uid change when any attachment lacks `chat_chars.sheet` (or attach snapshots the sheet). — `story/room_slice.py`, `story/room_tools.py`, `story/room_frontier.py`, `story/plot_packages.py`, `story/room_conversation.py`, `web/app.py`, `static/js/world_browser.js`, `static/js/editors.js` → `tests/test_room_frames.py` (new: room planted in frame 2 only), `tests/test_room_archive_roundtrip.py` (10,000-char planner line), `tests/test_card_rename_guard.py` (new).

14. **Frame merge carries the away party's ledgers.** `perform_merge` takes the child's rows for every subject-keyed ledger (`_SUBJECT_KEYED` + attire + list ledgers) for subjects positioned in the child scene, unions entities/passages/comms/scents; `perform_split` partitions symmetrically. — `world/spatial_frames.py` → `tests/test_spatial_frames.py` (dress/shrink/mint away, merge, assert survival).

15. **Wall-clock pack, low blast radius.** `compile_world_context` ‖ `perception_act` as one `_run_parallel_group` (fix the mapping.py:341 docstring); interpret extras built only when a hand runs; `perception_outcome` and establish use `_player_room_in` (scene → cache → resolver) and pass `interp=None` after resolution; `_gate_facts` takes the four views' rows; `route_scene` memoised once per resolve for the six route checks; `active_disguises`/`active_transformations` read once per turn onto ctx; `_advance_ground` early return. — `agents/runtime.py`, `agents/mapping.py`, `agents/director.py`, `agents/director_scopes.py`, `agents/director_movement.py`, `agents/director_floors.py`, `agents/perception.py`, `persist/commit_scene_state.py` → `tests/test_runtime_plan_parallel.py` (new), `tests/test_director_orchestration.py` (no extras evaluated when `_idispatch` is empty), `tests/test_player_room_resolution.py` (new: refused movement leaves the positionless player where she was; the utility resolver is called at most once per turn).

Deferred from this order because blast radius exceeds confidence today: B1/A26 (one `compose_beat_scene` for outcome and commit — the right fix, but it moves five structure passes into a stage that runs before the Director's own resolve and needs the perception_outcome ↔ commit contract restated first); C11 (scene-blob memo on the PipelineContext — most `get_scene` callers mutate in place, so it needs the read/for-update split and a measurement before it); D1 (cue percept — new percept kind touching composer, ja adapter and memory; build after item 3 lands so the outcome pass is clean).

## Section G — LIVE VERIFICATION (2026-09-07, after commit `ad260f1d`)

Run on database copies only: the descent (chat 117, beats 115–118, Gemini
3.8 Flash) and the charter town (chat 114 beat 13, plus a fresh Japanese
chat 122 with a disguised body, GLM 5.2). Every beat landed on the first
attempt with no domain error (70–137 s).

**Verified live, from the fifteen.** Interpret-side hands dispatched BY
note (`addressed_by=['note']` on `contact`/`spatial`, beats 115–118); the
objects hand dispatched by `pressure` and the tremor reaching the page;
`compile_world_context` recorded `parallel_with: perception_act` on every
beat; a moved player (118) committed `positions` and minted the room through
the typed planning need, with two needs recorded. `charter_registry_writes`
read 0 on the town beat because the charter run was still `running` — the
once-per-commit property is held by `test_registry_session`, not re-measured.

**Fixed from the run (this commit).** Each is the same class the review named:
one rule applied at some sites and not its siblings.

- *A room's fixture is a contact endpoint at every site.* `spatial_contacts`
  resolved an unplaced endpoint through the room declaring it as an anchor;
  the player-authority guard in `director_contact` compared bare `room_of`
  and discarded a palm on the access door (115) and a shoulder against its
  leaf (117), and `composer._pose_referent` dropped the anchor id before
  asking a room, rendering "You feel something's steel plate". Beat 118, with
  the fix: the onset contact kept, no warning.
- *A room nobody has named is shown by its placeholder, never its id.*
  The spatial hand minted `sub_level_three_utility_core` with `name: ""`;
  the view read "You are in sub_level_three_utility_core." Five sites each
  spelled the placeholder inline and six perception fallbacks applied none.
  One producer (`derived_room_name`), a merge floor for a nameless new room,
  a reader floor (`room_display_name`).
- *The one player line the world fills.* The player read a stencil aloud
  (115); the interpret carried the act, the sheet's absolute rule against
  authoring a player line left the number unspoken until the character
  repeated it a beat later. Clause added to `prose_author_sheet/06.txt` in
  both packs: a text the world holds is quoted, in the player's voice,
  because what it says is a fact about the world.

- *Referent text speaks the story language.* Chat 122 (a fresh Japanese
  story, GLM 5.2, a disguised body): the disguise held — the Doctor's view
  showed the presented appearance, no concealed term, no name — but the
  composed Japanese view read "the 砂浜の上に", "youの向かってに" and
  "見知らぬごく普通の人間の若い女性。は…". `_pose_referent` and `_noun_phrase`
  read the ENGLISH card regardless of story language and the adapter
  rendered their output verbatim; the adapter translated the self sentinel
  on a percept's own label but not inside a pose's `relative_to`/`support`;
  three terminal-stop strips knew the ASCII full stop alone. Pack-aware
  templates (`_t`), pack determiner tables read at use time, a
  `_SENTENCE_ENDS` cue for `agents.composer` in both packs, and the sentinel
  translated at every field of the Japanese pose.

- *D1 LANDED (commit after D2).* The delivered manifest — tells and
  surface demeanor, gated per observer since Phase 4 and read by nothing
  (A36) — is minted into `cue` events and a `demeanor` standing state at the
  outcome stage, named through the observer's display map and scrubbed like
  an act surface; only the cue text and the demeanor cross. Measured on the
  descent copy beat 123, the first beat with the fix: Sarah's manifest
  carried a heard tell ("A faint, measured exhalation against his ear before
  speaking", subtlety 0.85, with a private `because`); the player's view
  read "Sarah Moon: A faint, measured exhalation against his ear before
  speaking." and the page "a faint, measured exhalation brushed against my
  ear before she spoke" — the first tell to reach this story's page in 123
  beats. Her demeanor was correctly withheld: the lantern was off. Open: a
  cue is written in the character's frame ("against his ear") and, unlike
  an act surface, is not run through `resolve_action_referents`; the
  narrator corrected it this beat.

**Measured, not fixed — for the owner.**

- *Japanese Director output in the pose enum slots.* GLM wrote `constraint:
  "なし"` (none) and `relation: "向かって"` (facing) — a null word and free
  prose where the English schema expects empty and an enum — so the view
  read "…の向かってにthe 砂浜の上になし立っている". The English path drops
  `""`; a Japanese "none" is a word list to recognise or a schema to
  tighten (empty string, enum keys in English), and the latter is the fix
  that does not grow.
- *An English card in a Japanese story.* The Doctor's appearance summary is
  English, so the stranger label read "見知らぬa tall slim youthful-looking
  man". This is the registered PD11/PE5 ruling (authored words keep their
  language); noted only because a card written in one language attached to
  a story in another is now a measured, common case.
- *Planning needs recorded for setting facts.* The Japanese opening turn
  recorded three needs "the beat reached for setting fact '…'" — the
  world-fact channel routed through the planning-need path with a prose
  sentence in the key slot, the same shape as the lore-title-as-room need
  below.

- *D2 measured, then LANDED (commit after `ff3bfad3`).* Every Sarah line on
  the page carried a manner tag ("In a flat, hushed monotone voice, she
  said"): 6 of 7 lines over beats 115–117; the tag originated in the composed
  view and the narrator copied it. Built as the review specified: the
  speaker's `social.voice.register` renders once per observer through a
  `voice:<body>` key in the standing ledger (`composer.speech_percept`,
  both renderers), afterwards a manner only where the line's own `tone`
  names a departure, and the character prompt (en + ja) tells the character
  to leave `tone` empty in its ordinary voice. Tone strings are never
  compared. Measured over beats 119–122 on the descent copy: 119 rendered
  the register once ("in a clinical, deadpan monotone"); 120 and 121
  re-established it, each time because a change to the key between beats
  made the voice new again — which exposed that the ledger is rolled
  forward a turn at a time and a voice key was re-filed only on a beat the
  speaker spoke, so voice keys are now carried forward unconditionally (a
  known voice survives silence); 122 rendered no register and only the
  declared departures ("in a flat whisper"), with the character's own tone
  field empty on the ordinary lines of 120. The UNBUILT entry and the
  Design.md row await the owner's committed UNBUILT split.
- *`_noun_phrase` keeps a sentence capital.* Anchor descriptions written as
  sentences without a determiner splice mid-sentence with their capital:
  "You can see Overhead conduit banks …, and Massive cast concrete pillars"
  (118). The function's own ruling is that a capital may be a proper name.
  A decidable alternative: lower a multi-word phrase's first word when no
  other word is capitalised and the first word names no record in the scene.
  Owner's call — it reverses a stated ruling.
- *`fed_by` derives for none of the four real charters.* The rule (A25) feeds
  sustenance from the upkeep whose PRODUCE flow makes a good the same charter
  CONSUMES. In chat 114 no charter both produces and consumes one good — the
  guesthouse produces `meals` and consumes `groceries`; the hotel produces
  nothing. The test fixture's shape (bread produced and consumed) is one the
  generator never emits. 198 stored needs carry `fed_by: ""`. Needs a
  structural signal that matches generated economies (an upkeep with both a
  produce and a consume flow; or the town-wide chain across charters).
- *Self-pose detail in the third person.* "You are standing … as he sweeps
  the beam" (118): the Director wrote the player's pose detail in the third
  person and the composer splices it under "You". A pronoun rewrite is a
  literal guard; the payload's own instruction is the better site.
- *A planning need named a lore title as a room.* Beat 118 recorded a need
  for room `Site-17 Sub-Level Three utility core layout and threshold` — a
  plan/lore title in a room-id slot.
- *Two ledger warnings worth a look:* `obligation discharge matched no open
  ledger entry: 'ob:voss_stencil_check'` (a question asked and answered in
  one beat, discharged without an open); `intent 'i9': already at full
  progress` (repeats every beat).
