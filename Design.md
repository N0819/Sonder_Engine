# Sonder Engine — Design

## What this document is

The architectural record: what the engine is for, what is actually built, what
is partly built, what is not built, and what should be built next.

**The honesty rule.** The previous edition of this file grew past 1,500 lines
and drifted far enough from the code that `CLAUDE.md` had to warn readers to
verify it before trusting it — which made it a liability rather than a
reference. This edition was written by checking claims against source, and it
carries a conformance table precisely so drift becomes visible instead of
buried.

Every "Built" claim below was verified against code, not against another
document. Where verification was partial, it says so. When you change
behaviour, change the status row in the same commit; a row that is wrong is
worse than a row that is missing. The previous edition is in git history.

`AGENTS.md` is the operational guide (edit routing, invariants, workflow).
`docs/guides/PIPELINE.md` is the stage-by-stage execution reference. `docs/guides/DATABASE.md`
is the schema and change checklist. `docs/UNBUILT.md` is the single register of
everything not yet built. This file is the *why* and the status — do not
duplicate the other four here.

---

## Thesis

The engine produces long-form interactive fiction by **simulating people and a
world honestly, and letting story be the residue rather than the target**. No
agent is trying to be entertaining.

One layer computes what objectively happens. One computes what each mind could
register of it. One predicts what each person does given their psychology. One
renders the player's slice as prose. Secrets, betrayal, dramatic irony and false
belief are authored nowhere — they fall out of the simulation the moment the
bookkeeping is honest enough to make *absence of knowledge* computable.

The north star is **coherence without omniscience, not realism**. Realism is
expensive and often anti-dramatic. What the engine guarantees is narrower and
more valuable: a world that holds together *and* contains no mind that knows
more than it should. That second property is the soil dramatic irony grows in,
and it is the thing a single context window structurally cannot provide.

The player **is** the protagonist. No agent models the player's interior — the
player's own mind supplies it live, every turn. The engine exists to give that
mind a believable, causally honest, selectively ignorant world to act in, and to
hand the player the dials to shape it.

### Genre-agnostic substrate, world-specific law

The engine should be able to serve any genre without teaching its core that one
genre's interpretation is universal. Core state describes reusable facts:
identity, space, time, contact, motion, containment, visibility, perception,
knowledge, authority, conditions and causality. It should not hard-code what a
wound, spell, transformation, technology, supernatural weakness or social
custom means in every world.

Genre-specific mechanics belong in lorebooks when they are authored or already
canonical. When canon is silent, the Director infers the local rule from the
fiction model, established facts and immediate circumstances. The ordering is:
explicit canon first, established story facts second, Director inference last.
An inference may fill a gap but never silently override a lorebook rule. The
engine's job is to represent, validate, persist and deliver the resulting facts
without losing their causal or epistemic boundaries.

This is also the quality bar. Sonder Engine is not successful merely because it
can generate plausible prose in many genres; a raw LLM call already does that
with fewer seams and often smoother local writing. The orchestration must earn
its cost by producing demonstrably better long-form continuity, causality,
character agency, epistemic integrity, memory and world-specific coherence.
The target is eventually to handle any genre to a higher standard than an
unmediated model call, while being honest that every structured boundary can
lose nuance until it is made reliable.

That advantage is not wholly hypothetical. Long-term character memory has
already proved more durable than raw model context: it survives context-window
pressure, process restarts and story restoration as character-owned,
provenance-bearing evidence, then retrieves earlier experience into a bounded
current prompt. This proves durability, not perfection—selection, consolidation
and interpretation can still fail—but it is a concrete case where structure has
already delivered something an unmediated context cannot sustain.

### The two principles everything else serves

1. **Structure over instruction.** Anything you want guaranteed must be
   *impossible to violate*, not merely instructed against. A prompt cannot
   un-write its own context.
2. **Auditability.** Every numeric change should be event-linked, every
   scheduled effect seeded and logged, every resolution recorded. Silent drift
   is the failure mode being prevented at every layer.

[Structural debt](#structural-debt) is an honest account of where the engine
currently falls short of principle 1. It is the most useful section here.

### What the firewall is, and what it is not

**The firewall restricts the FLOW of knowledge, not knowledge itself.** A mind
may know anything it has a channel to. What it may not do is come into a fact
that reached it through no channel at all. Everything the engine does about
information is that single rule applied to a different pathway — sight, sound,
scent, touch, memory, frame, identity.

It is a GAP, not a rule imposed on top of one. Two people genuinely do not
share a head; the engine is not restraining a character, it is declining to
collapse a distance that already exists in the fiction. That is why nearly
every guard in the codebase SUBTRACTS — `composer.presence_percepts` does not
forbid describing somebody, it declines to invent a channel, so a body
`visual_level_between` answers `none` for yields no percept to describe;
`visible_memory_rows` does not withhold memories, there is no path from that
mind to those rows.

Three things follow, and each has been got wrong at least once (see
`AGENTS.md` § Information boundaries for the operational form):

- **Inference is the product, not the risk.** A conclusion drawn from
  legitimately perceived material is exactly what this engine exists to
  produce. Never harden a guard by making minds conclude less.
- **A leak is an engine failure, never a model's.** The deterministic floor
  must not depend on a model cooperating, so no model behaviour excuses a
  crossing. A warning, by contrast, is the system working — nothing crossed.
- **Firewall integrity is an invariant, not a model-selection criterion.**

And the gap is kept because it is GENERATIVE, not because it is safe. Dramatic
irony, deception, misidentification, a mind acting confidently on a belief the
world has since contradicted — all of it requires the distance between minds to
be real. Collapse it and the result is not a freer story but one where nobody
can be surprised, deceived, or wrong.

### What it fixes

Three failures recur in single-model storytelling, and they are one bug: a
single context window where everything is epistemically flat, so the model
conditions on all of it, because that is the only thing a model can do.

- An NPC references a fact it was never told.
- An NPC treats the player's private thought as spoken dialogue.
- An NPC reacts to something that happened while it was not present.

None is a discipline failure a model can be instructed out of, because the
forbidden information is *in the context*. The fix is to never place the
forbidden thing in the slot: **each mind runs in its own context containing only
what it legitimately earned, and a filter decides what crosses.**

---

## Status at a glance

Conformance against the founding architecture, re-verified against source at
alpha 6.1.

| Founding commitment | Status | Evidence / gap |
|---|---|---|
| Contact dynamics are state, not improvised prose | **Built** | Every normalized contact has a stable opaque id derived from its two owned endpoints. `scene.contact_actions` attaches ongoing tactile effects such as vibration, pressure pulses, or suction to that id; effects persist through model silence and end automatically with the parent contact. Perception admits them only to a contact participant and renders a cause-blind tactile clause, while the Director may use an exact endpoint selector for a contact created in the same beat. |
| Firewall as plumbing — each mind gets only its perception object | **Built** | `agents/perception.py` composes per-observer views deterministically through `agents/composer.py` — percept builders decide admission on typed data (delivery gates, hear/sight levels, containment, rear-arc, concealment, recognition labels — scent is graded upstream and admitted by nobody, see below), and `render_view`/`render_episode` realise them from percepts alone, taking no scene and no DB, so a rendering path structurally cannot ADD information. No model call is involved and there is no `perception` model role to configure. Characters receive their view, never the event stream; stored event rows are per-observer redacted via `recent_events_for_observer` when loaded into character context |
| Two perception passes per turn (onset, outcome) | **Built** | `perception_act` before resolution, `perception_outcome` after |
| Player-leads loop; characters declare blind to each other | **Built** | Plan built from `director_interpret.flow`; character steps run in parallel |
| Memory provenance | **Built, exceeds spec** | Six kinds (`witnessed/heard/told/read/inferred/remembered`) against the specified three, plus `turn_idx`, bound at commit |
| A mind remembers what it said and did | **Built** | The firewall means nobody else can remember it FOR them: deterministic perception excludes a mind's own conduct from its own view (correct — a view is what was perceived), so the `category: self` row `prepare_memory_commit` mints from the character's own declared sequence is the ONLY durable record of their own speech and acts. Decision-framed by `_own_sequence_memory` ("I said … Then I tried to …") so it composes with the perceived outcome instead of replaying the act; bounded to every spoken beat plus silent acts the mind itself appraised at `salience >= 0.7`. This row was silently suppressed for eight days when d290ca4's when-a-view-exists gate met 3a82657's always-present, conduct-free views — 0 self rows across chats 69–80's 240 turns, a doctor with no memory of a promise she made, the same three propositions restated five beats running. Chats 69–80 are not backfilled (`docs/UNBUILT.md` §1.49); `tests/test_own_conduct_memory.py` pins the repair |
| Action visibility posture | **Built** | `visibility` + `conceal_from` + `targets` on every declaration; targets the model leaves empty are bound deterministically, because the seams that ask "does this land on someone?" all read that field |
| A beat opens with one character, and causality builds | **Built** | The interaction loop used to open with a simultaneous wave of two, on the argument that everyone in the initial queue answers the same fixed thing and so could not have seen each other. That is right about a beat aimed at the ROOM and wrong about one aimed at a person, which is most of them: the others are bystanders to an exchange that has not finished, and deciding blind they answer a question that is already answered. Live, chat 59 t161 — the player asked the Doctor a direct question, he was correctly ranked first and answered, and Tamamo in the same blind instant said "Doctor?", prompting a man who had just spoken; the narrator's own fidelity check caught it as dialogue rendered out of order. The stranding the wave was introduced for is now fixed where it was caused (the beat-ending exit is gated on `commitment: "contestable"`), so `initial_parallel_reactors` defaults to 1 and survives as a knob. Order is the beat's own causality: spoken to, then acted upon, then merely present — speech outranking action because an action's `targets` is a looser field that the deterministic binder fills from whoever the act plausibly lands on, and a live beat had "sits back down at the table" targeting both people in the room. Inside the untargeted band it is standing want-urgency plus a seeded jitter (`_untargeted_order`), because cast-REGISTRATION order was deciding and that is not a fact about the fiction — the same character opened every untargeted beat for the life of a story. Seeded on turn AND nonce, so a rerun from a stage replays and a reroll may land differently, the rule the dice already follow. **Interruption is declarative, not scheduled** — a character later in the chain has already HEARD the line they want to cut off, which is how interruption works in life, so `interrupts:"<name>"` on a speech or action element says the beat landed DURING that line rather than after it. Resolved deterministically: the named party must have spoken this beat and the interrupter must have been able to hear them. `cut_short_speech` breaks the line at a breath point rather than a word count — whole sentences survive, the last breaks near 60%, and that cut slides to the nearest comma or conjunction, because a flat halfway cut lands mid-phrase. A line too short to get inside is left whole. An interrupted ACTION is marked and not rewritten, because what happens to a reach that got grabbed is causality and causality belongs to the Director — which does not yet read the flag; see `docs/UNBUILT.md` § 1.29. **And an early exit no longer strands the summoned cast**: both exits drain every initial reactor first, since being summoned to the beat is what earns a character their turn, not being flagged — the failure the wave existed to paper over came straight back when the wave did (chat 59 t161–t162, Tamamo never called, two beats running) |
| A name is learned by hearing it said | **Built** | `known` gates every identity the engine will let a mind use — perception scrubs an unearned name out of a view, memory stores "a voice" instead of a speaker, the narrator will not name a person to somebody who has not met them. It was written in exactly two places: `story/greetings.py` seeds the one greeting character against the player, and commit seeds everyone when a background presence is PROMOTED. A third path exists, `validated_introductions`, and needs the mapping model to declare an explicit introduction event. Nothing recorded a name learned in play, so a character attached the ordinary way never entered the map and nobody ever learned anybody by being told. **Measured: 19 of 42 played stories held fewer recognitions than a fully-acquainted cast**; chat 59 — 162 turns, two cast, a mother and her daughter — held ONE directed pair, so every beat scrubbed both names out of both views. The failure that surfaces is not a missing name but a wrong one: a view holding one surviving name and one anonymous body invites the model to join them, and the Doctor answered a question the player asked as though Tamamo had asked it. `commit._names_heard_in` learns a name when it is SPOKEN in a hearer's own delivered view AND the person it names is in the room with them — no model call, riding a channel the firewall already governs. Two refusals keep it from becoming a leak: a name for somebody absent or in another room teaches nothing (hearing about someone elsewhere teaches you a name, not a face, and would otherwise license recognising a stranger who walks in later), and your own name teaches you nothing. Accumulated in `prepare_memory_commit`, which runs before the write lock, and applied by `commit_memories` inside the transaction, merged rather than assigned so an explicit introduction earlier in the same turn is not overwritten. **One writer since 2026-08-28.** The three paths that create a cast membership from nothing — greeting launch, the attach route, background promotion — held three recognition semantics with opposite defaults and nothing naming the difference, so a programmatic attach produced a mutual stranger with no warning at all: chat 95 played nine turns with `known` empty, the player's own commanding officer composed for the narrator as "a figure, backlit, indistinct, the face unreadable", and the stranger phrasing still arriving as remembered_past on turn 11 after recognition had filled — recognition seeded late does not rewrite the memories written before it. All three now seed through `commit_common.seed_mutual_recognition`, which takes the roster it is to close over rather than assuming the player is the only edge, and an attach that states no answer seeds nothing and says so in its response and its log (an omitted key is not consent to widen a channel). Character-to-character acquaintance is a second, independent answer (`already_known_cast`, 2026-08-29): it closes the arriving member against every other active cast member through the same seeder, is asked once for the group by the story-builder, and is reported unasked only when somebody is already there to be a stranger to. Neither answer is inferred from the other. And ONE ROSTER answers who exists: `commit_common.charter_recognition_projection` is read by the hearing channel and the introduction channel alike, so a Charter body — a placed identity `chat_chars` does not contain — can be learned and can hold a row of its own; an introduction resolves address forms rather than substrings, refuses a row whose two ends are one body, and requires the same room wherever the engine can place both. Measured on chat 98, that gate had been dropping 9 of the 11 `ok` introductions authored across forty turns, every Charter one among them, so no presence ever held a row and the player was "the unfamiliar person" to a man who had spoken to her twice. What the DEFAULT should be when neither question is answered stays the owner's: `docs/UNBUILT.md` §1.95 |
| A standing contact is a continuous percept | **Built** | The engine had two categories for anything physical — `event` (rendered once, on the beat it happens) and `state` (mentioned, then inert) — and a contact between two bodies is neither. It is true every beat AND felt every beat, until the ledger drops it. The consequence of having no third category was that the perception contract specified the tactile channel only as a SUBSTITUTE for sight: every mandatory clause is conditioned on sight being absent (in the dark, behind a wall, sealed inside something), so two bodies in continuous contact in a lit room had a wide-open tactile channel and nothing requiring a word of it. A view written under a token budget renders what is seen and drops what is felt. Measured over 7,508 corpus observations before the fix: **46.8% classified as `mixed` because no sensory cue matched them at all**, `interoception` accounted for 2.4%, and in the story that surfaced it the ACTING view of a character in three standing contacts was a median 460 characters against 812 for the outcome view of the same character — she chose her conduct with no sensation from her own body and was told what she had felt after the choice was made. `spatial.contact_sensation` renders the percept from one party's side in plain physical terms (pressure, weight, movement, friction, warmth; fullness and stretch for an interior contact), asymmetric where the contact is — the enclosing party feels something within them, the entering party feels something closed around theirs, and rendering either side with the other's phrasing describes a body the perceiver does not have. `agents/composer.py`'s `contact_percepts` is the deterministic floor, and it is no longer a floor UNDER a model — perception writes no prose, so a standing contact is admitted as a typed percept rather than appended to what a model left out. It admits only for a party to the contact (a bystander watching two other people touch feels nothing) and carries the clause `spatial.contact_sensation` rendered from that party's own side. The earlier implementation — `_deliver_standing_sensations`, a repair pass over model prose, plus a prompt clause stating the one contact rule that does not depend on sight — is GONE, prompt and function together, and the tests that exercised the cohort now assert against `contact_percepts` |
| A speech element carries words, and only words | **Built** | `text` is the utterance; conduct is an `{type:'action'}` element beside it, and the sequence is ordered so a character interleaves them freely. The engine had no opinion about the contents of `text`, so a model writing chat-roleplay stage directions into it (`"*leans in and sets a hand on her shoulder* Sit down"`) got the movement delivered as SOUND — lost entirely to a listener who could not hear, ground into word fragments by muffling, and narrated by ear to a listener who could hear but not see (`You hear Reya say: "*leans in…*"`). **A flow defect, not a knowledge one**: the person being touched would have felt it, and nobody learned anything they had no channel to — but the channel the information travelled was wrong, and a wrong channel is an engine failure, never a model's. `agents.common.split_stage_directions` excises the span inside `norm_sequence`, before anything downstream sees it, and re-files it as the action element it should have been: placed immediately before the line it was buried in, inheriting that line's concealment (a stage direction inside a whispered aside must not become overt conduct just because it moved channel), and routed through the same mental-verb check as any other action. A one-word span is markdown emphasis and stays spoken. `tone` was considered as the home for vocal manner and rejected — the delivery layer renders tone only when the listener can SEE the speaker, so an audible laugh parked there is lost in the dark, which is the same bug one layer down |
| Two-store protagonist, never merged | **Built** | `private_voice_setting` appears only in `agents/narration.py` — verified absent from character and perception stages. `shadow_profile` is separate world state |
| Seeded, logged, replayable dice | **Built** | `director_resolve` uses `random.Random("{chat}:{turn}:{nonce}:{actor}:{attempt}")` and records `seed/roll/modifier/dc/outcome/margin` |
| Deterministic scheduling | **Built** | `scheduled_events.seed` written as deterministic strings; `stable_event_key` gives rerun idempotency |
| Per-character temperature | **Built** | `character_temperature(sheet)` passed to the provider call in `agents/character.py` |
| Narrator exemplar pool, event-amnesiac | **Built** | `exemplars` setting read in `agents/narration.py`. The row used to say "narrator receives the player view, not the event stream", which overstated the severance and contradicted the *An act that happened reaches the page* row below: the narrator receives the player's VIEW plus the engine's numbered record of what happened this beat, precisely because a view that omits an act leaves the narrator nothing to write it from. Since alpha 9.9 those arrive as three named packages in reading order — `past_narration` (the story so far as ONE unlabelled text: the player's writing and the narrator's, alternating, ending in this turn's raw input, which is exactly the page `static/js/chat.js` has been showing the player), `present_scene` (perception's render of what may legitimately be perceived now) and `current_events` (`_ordered_beat_events` rendered as a numbered chronological list, the writing material) — with the payload ordered config → craft → standing facts → packages, so generation begins immediately after the beat's own events. What it does not receive is the raw objective ledger, another mind's view, or anything the player did not perceive; `past_narration` cannot breach that, since every word in it is text the player has already read |
| Tiered cognition | **Built** | Model roles (`default`/`director`/`narrator`/`utility`) plus per-character `simulation.tier` |
| Character latency without a smaller mind | **Built; compatibility-wire removal remains experimental** | Provider JSON schemas discard annotation-only metadata while retaining every constraint and the complete local validator; the character identity line sits behind the reusable authored contract for prefix caching; immutable per-turn scene/history reads are shared; memory retrieval overlaps independent lore/relationship/frame assembly under a parent-copied context. Production still offers `considered_responses` and the legacy compatibility projections: `tools/character_wire_ab.py` is the matched cognition/agency/firewall/social-memory A/B required before removing them |
| The Director is a writer plus specialists, not one mind | **Built** | Each Director stage is ONE step that fans out inside itself (design note 19): a PROSE AUTHOR owning the beat's account, and six specialists — `body`, `social`, `contact`, `objects`, `spatial`, `offscreen` — each owning a subset of `state_diff` channels. Per-beat channel SCOPE, keyed on scene state and never on prose, drives both dispatch and sheet assembly, so a specialist that is not dispatched costs nothing at all: no prompt, no payload, no output surface. A beat dispatches a mean 1.75 of 6, each carrying a 1-4k sheet against the single unsplit sheet's ~21k. The monolithic Director is GONE rather than defaulted-off — it shipped behind `director_orchestration` while the two were measured against each other, the fan-out won on stability, tokens and wall clock, and keeping a switch for the losing path would only have preserved a way to make the engine worse. `DEFAULT_PROMPTS['director_resolve']` went with it, because nothing sent it and a preset key nothing reads is folklore; every segment it was assembled from now belongs to exactly one owner. What remains a choice is CONCURRENCY: `director_fanout_mode` runs the specialists at once (default — they hold disjoint channels of one finished beat and have nothing to say to each other, so the beat costs its slowest hand rather than their sum) or one at a time, for a provider that will not take concurrent requests. Sequential is not a fallback to the monolith: same hands, same scopes, same canonical assembly. Every cross-channel judgment — the movement backstop above all — stays with the deterministic orchestrator and judges the MERGED diff, a failed specialist costs its own channels rather than the beat, and the reconciliation seam repairs an omission at the channel's OWNER rather than re-running the author at full-core cost |
| Theory of mind, cached and event-triggered | **Built** | `mind/theory_of_mind.py`; `tom_triggers` on the flow |
| Event-grounded live psychology | **Built** | v4 character schema plus `mind/psychology_runtime.py`: stress split into aversive strain and non-distressing drive, mixed pain/pleasure outside survival with a slow-integrating unresolved `charge`, learned cue associations, and simulation-time recovery. `psychology.self_model.protected_beliefs` was credited here in error for a while, and is now live for real — see *Authored convictions are convictions the mind defends* above: it had been normalized, editable and archived while reaching no prompt, no runtime and no commit path, and a name in it now sets the per-belief `protected` FLAG that `mind/psychology_runtime.py` already read to halve the weakening step (0.05 against 0.15). Two spellings of one authoring act, and only one of them worked. Ambient comfort from surfaces (`world/comfort.py` + `resolve_hedonic`'s `ambient_comfort` floor) is built: it raises the pleasure level only — never `charge` — habituates on a sustained source, and lets `tick_vitals` derive passive rest from lying on a soft support |
| Authored initial outfit with live story attire | **Built** | Character/persona `initial_outfit` is kept separate from stable body appearance and seeds `scene.attire` once; later clothing changes remain mutable story state |
| The body a card describes, delivered and concealed | **Built** | `character_appearance` returned `embodiment.visible.summary` and nothing else, so `build`, `face`, `hair` and `eyes` — four fields the generator fills on every card — reached no view, no narrator and no memory. The authoring surface did not move: `_coerce_appearance` PROJECTS the located three into `embodiment.visible.regions.head.visible_zones` on every normalize (a projection, not a decider — which region a face is on has one answer), and `attire.uncovered_zone_text` gates delivery on the MIRROR of the `beneath` rule: what is under clothing is spelled out only once something comes off, what a body shows of itself is delivered until something goes on. Something covering the region conceals everything described on it, something displaced off it (`covered_zones` empty) conceals nothing, and something merely worn AT it never covered anything — so a hood coming down shows the hair with no vocabulary for hoods anywhere. `build` sits OUTSIDE the gate deliberately: it is not located, so nothing worn can cover it, and it belongs with `summary` and `scent` as a whole-body property with no place. The described surfaces (`attire.VISIBLE_ZONES`) are a SECOND axis beside `REGION_ZONES`, which answers what a garment can expose and whose answer is read as bareness. Delivery rides the description path only (`perception._body_descriptions` → the appearance percept), past the existing full-sight and rear-arc gates; the short stranger LABEL is still cut from the summary alone, so an observer holding a silhouette receives no face. A body whose outward form is not its own card's — disguised or transformed — delivers none of it. `distinctive_features` is preserved and still unread (`docs/UNBUILT.md` §1.78) |
| Clothing by body region | **Built** | `story/attire.py`: a garment covers one or more of head/torso/arms/hands/waist/**groin**/legs/feet, ordered outermost-first, so exposure is state rather than a sentence. `waist` and `groin` are separate because a sash covers the belt line and nothing else, while a dress covers both — conflated, a body in only an obi reports its groin covered, and a dress stopping at the waist reports it bare. `regions_covered` spans a garment across every region it covers (a kimono: torso/arms/waist/groin/legs), `_sync_spanning_garments` keeps those copies one garment so loosening a kimono at the torso does not leave its sleeves fastened, and `newly_removed` reports it once. A garment is worn OVER a region or merely AT it (`attaches`: a ribbon is in the hair, a necklace at the throat — present, visible, covering nothing, so a head wearing only a ribbon is bare and removing it uncovers nobody), and garments within a region are ordered outermost-first, so an under-layer keeps covering when the layer over it comes off and is reported as hidden while it does not. `name` is a short handle — the key the Director and `decisive_targets` match on — with `description` beside it; `split_garment_name` recovers the two from generators that write "Name — description" into one field, which used to truncate at the name limit mid-clause AND leave a hundred-word string as the matching key. The Director is given an explicit `exposed` list and told it is the whole truth about bare skin. Regions are the only authoring surface; `initial_outfit.wearing` is retired to an INPUT format (older cards, imports, generator output), migrated into regions by `character_schema._normalize_initial_outfit` on read and written back derived, so a cue-table guess lands in the region editor where an author can correct it and the two representations cannot disagree. `initial_outfit.state` is retired outright — see garment condition. `agents/common.attire_view` is the shared prompt projection, and `beneath` reaches no prompt unless the host sets `attire_beneath` — an uncovered region still reports itself uncovered |
| Garment condition, distinct from wear | **Built** | A garment carries `condition` (stained, torn, soaked) alongside `state` (how far off the body); a shirt can be soaked and fully worn. Set by the Director through `attire.<name>.conditions`, persists until something changes it, and travels with the garment when it comes off (`commit._mint_shed_garments`) so a stained shirt is a stained shirt on the floor. The resolve prompt states the asymmetry a blow creates: the garment's damage is permanent until mended, the body's is `vitals.injury`/a condition/an overlay, all of which heal, and neither may be written into stable appearance |
| Undressing as a sequence | **Built** | A garment with fastenings moves `worn → loosened → open → removed`, and the prompt teaches the staging — but the clamp is INVERTED (design note 17, second incident): a resolved `remove` LANDS by default, and `attire.advance` holds it one rung only when the beat's own prose shows the act still in progress (`attire._PROCESS`: inchoatives, conatives, explicit partiality — a smaller, closed set), because twice a wrong ledger's root cause was the completion vocabulary missing one more way English says a garment came off ("pulls the tank top off", then "shrugs the jacket off" on a live reroll). The Director owns objective causality; commit consistency-checks its resolution against the prose instead of re-deciding it, and an unrecognised phrasing now fails SAFE — a removal lands that the Director asserted anyway, instead of silently forking the ledger from the fiction. `process_targets`/`decisive_targets`/`wardrobe_change_subjects` share one attribution ladder (`_attributed_targets`: garment phrase, possessive-genitive owner, head noun, first person, sole name), so the actor is not mistaken for the target and one person's fumbling does not slow the room. The two SPEED readings re-attribute away from a sole name carrying a non-reflexive possessive; the LICENCE reading does not, because whose clothing a beat is about does not turn on which way the possessive points, and third-person self-undressing ("Sabine changed out of her uniform") is the ordinary shape of NPC prose; decisive lifts even a same-beat process reading; intermediate jumps (`worn → open`) still clamp — staged states remain the contract. A held removal is reported to the Director (`tell_director`), never silent. Getting dressed is deliberately unrestricted. A garment that comes off is minted as a real portable object in the room (`commit._mint_shed_garments`) rather than ceasing to exist — unless it arrives on ANOTHER body the same beat, which is a handover rather than a drop: "she takes off her coat and drapes it over his shoulders" used to leave two coats, one on his shoulders and one at their feet. Keyed on arriving-this-beat rather than worn-by-anyone, so two guards in the same kind of cloak still get a real cloak on the floor when one takes his off |
| Displacement, the third clothing axis | **Built** | A still-worn garment that stops covering a place it normally covers — a jacket pushed off the shoulders, a skirt hiked to the waist, trousers around the ankles — is neither a ladder move (fully worn) nor a condition (undamaged), and it had no channel: the chat-70 incident held torso/arms/waist covered while the narration wrote the body out of the jacket, because the displacement lived in condition prose that coverage never reads. Now the existing `coverage`/`covered_zones` machinery generalized to region grain (`attire.zones_of`: an unzoned region is its own single zone, torso keeps chest/midriff), because the corpus showed models already reaching for exactly that shape — 4 region-grain coverage emissions silently dropped by the torso-only validator, 37% of stored condition notes carrying displacement language, one stating coverage outright in prose. Instant and reversible, no step rule: displacement is one gesture with no middle worth a beat and is cheap to undo, which is the ladder's asymmetry inverted — while removal from a fully-displaced garment is one rung, its middle already played. The removal/displacement boundary is guarded both ways: displacement anchors keep "off her shoulders" a shove, a coverage claim emptying a garment named by a removal-directed decisive phrase escalates to the removal it was (ambiguity keeps the recoverable reading) — and the removal ladder's clamp is inverted (see the undressing row: a resolved `remove` lands unless the prose is in progress, so no completion wordlist sits on the removal path). The floor detects and feeds back, never executes prose: held removals, rung words in conditions, and prose-only displacement all reach the Director via `tell_director`; an unreadable zone list keeps the garment covering (the mildest-reading rule). Hand edits are first-class — the region editor offers the coverage toggle for every covered region, `rederive_entry`/`dedupe_regions`/`_sync_spanning_garments` all carry the override idempotently. No schema change, no migration. `design_notes/17-garment-displacement.md`; residuals (left/right asymmetry, sheer transparency) in `docs/UNBUILT.md` |
| Extra body parts as structured data | **Built** | `embodiment.extra_parts` on character and persona cards: a part (tail, wings, horns) declared once with closed menus — attachment region from `attire.REGIONS`, aspect from `character_schema.EXTRA_PART_ASPECTS` (front/back/top/underside/left/right/sides), count, `through_clothing`, bounded description — instead of living only as prose in `visible.summary` (9 of 45 corpus character sheets carried one that way). Card-level and read live like senses, so persistence/archive/branch come free with the sheet and a cast without any is byte-identical to before. Perception delivers a part on the body-region rows through the same `region_visibility` verdicts clothing uses: vantage/containment/darkness hide everything, garment coverage at the attachment region hides only a part authored `through_clothing: false` (the default tail passes through the skirt), and a body always knows its own. Part nouns are free text and were already valid contact endpoints (`_part_identity`/`_same_appendage` are structural). Authored by every path that writes a card, not only by hand: one shared `prompts.EXTRA_PARTS_NOTE` is spliced into `generator_character`, `generator_persona`, `fill_appearance`, `promote_character` and both import-reinterpret prompts, each of which also carries `extra_parts` in its JSON template. Without it the field was writable only through the editor — a kitsune generated or imported from a card arrived with nine tails in prose and none the engine could see, cover or touch, which is `psychology.drive`'s failure mode exactly: a structured field that is empty reads as complete. `fill_appearance` keeps what comes back (the merge read `visible` and `initial_outfit` only, so a proposed part was discarded), treating an omitted list as silence rather than an amputation and an explicit `[]` as a clearance, and the editor sends the author's unsaved parts with the request so a fill run after typing "tail" does not propose a second one. `character_import_warnings` says so when body prose names a part the sheet does not declare, reading only the visible-body fields — a character who "never turns tail" has no anatomy in the sentence. `design_notes/11-extra-body-parts.md`; residuals in `docs/UNBUILT.md` §6.10 (Director-driven transformation, garments covering a part) |
| Every route that hands back a card hands back its warnings | **Built** | `character_card_warnings` reports the four things that make a character read as passive fifty beats later — empty `psychology.drive`, no standing goals, unset capacity, body prose naming an undeclared part — and it ran on the import route and nowhere else. The blank-card route is the sharp one: `default_character_data` ships `{"essence": "", "expression": "", "taboo": ""}`, `"capacity": ""` and `"goals": []`, so it produces BY CONSTRUCTION the exact sheet the first three warnings exist to catch, and said nothing. AI generation, hand edit, the per-story card override, promotion confirm, psychology fill, appearance fill and the greeting launch were all equally silent. Nothing about the answer depends on where the sheet came from, so the rule is stated that way and nine routes now warn; the browser gets ONE helper (`showCardWarnings`) called at every site a card response arrives, because a copy per call site is precisely how eight of nine went quiet. The greeting launch warns AFTER the story opens rather than before, since that is the last moment before the card starts BEHAVING — which is when a driveless sheet stops looking like a missing field and starts looking like a dull character |
| Authored convictions are convictions the mind defends | **Built** | `psychology.self_model.protected_beliefs` was normalized at three default sites, edited in the browser as a line list, read back on save and carried through every archive — and reached no prompt, no runtime and no commit path, because `_authored_beliefs` looked at `self_model.beliefs` only. Measured read-only before wiring it, because the register described this as a small field: **50 of 168 live sheets carry one, averaging three convictions each, and almost every one of them has an EMPTY `beliefs` list** — so for a third of the cast the only place the sheet says what the character will not give up was the place nothing looked. That is the silent-empty-field failure `CLAUDE.md` warns about arriving from the other side: a field that is FILLED and inert, which reads as authored and behaves as unauthored. Wired to the thing that already works — `protected` on a belief entry halves the weakening step — so a name in the list sets that flag on the elaborated entry where a card states the belief in both lists (one claim made twice) and mints a flagged entry where it does not. A minted one takes confidence 0.5, which is not a number chosen here: a bare string in `self_model.beliefs` is already normalized to exactly that, and the same authoring act must not silently mean something stronger in the other list |
| Generated body and clothing | **Built** | `importers.fill_appearance` + the `fill_appearance` prompt fill body and per-region outfit from the card, what the author has typed but not yet saved, and a brief; `beneath` is a separate opt-in and is stripped from the proposal when it was not asked for. Writes nothing — the editor reopens on an unsaved proposal, as `fill_character_psychology` does |
| Approach is not arrival | **Built** | `MovementDecl.arrives` — whether the declaration covers ARRIVING or only setting off — filled by `director_interpret` and honoured deterministically by `director_resolve` (`_guard_approach_is_not_arrival`, plus the backstop refusing to commit a non-arriving move). Live failure, "The Blizzard" turn 2: "You wander towards it" of a building seen through the snow became `to_room: distant_mountain_building`, the route check passed it (the rooms genuinely were adjacent and open), and the resolve wrote her through the door into the firelight — from `exposure: open` to `exposure: sheltered`, out of a blizzard, with nobody having said she was going in. A FIELD rather than a downstream test because the distinction is not recoverable downstream: measured across 1249 live turns, no text heuristic separates "I cross the command deck toward the med bay" (an asserted crossing) from "progresses across the clearing toward the building" — both say "toward", both are staged `approach`, both are `commitment: asserted`. Four heuristics were tried against the corpus and each blocked legitimate arrivals (3, 8 and 4 false positives). Defaults true, so no existing declaration changes meaning. Carried/contained bodies are exempt; a declared VEHICLE mover is guarded like any other, since a skiff told to head for a light is as much not-there-yet as the hand on its tiller. An approach in flight is recorded on the scene per mover (`scene.approach`), and a declared walk CONTINUES: a beat that says nothing about movement no longer abandons it. Silence used to read as stopping, which made travel survive only by being re-declared every beat — the sentence nobody wants to keep writing, and wrong about the commonest thing in fiction, which is people talking while they walk. Live (chat 72): a beat spent grabbing someone by the shoulders was recorded as abandoning a walk to a hotel that was plainly still under way, while a station anchor moved her the two rooms the approach guard had refused one beat earlier — the designed channel said she stopped and an accidental one moved her. Continuing EXECUTES the player's declaration; stopping them without being told is what overrides it, so an interruption is the thing that must be established, by the Director in `travel_interrupted` (objective causality, and it needs the whole beat to answer) under a deterministic floor it cannot argue with — no passable route, carried, already there, and restraint for free because `_travel_continues` writes the leg into `state_diff.positions` BEFORE every movement backstop rather than teleporting after them. One edge per beat, two for a `far`/`remote` edge, and the leg is computed BEFORE the prose so the scenery changes on the page rather than being applied behind it. A second declaration toward the same place still ARRIVES — without that memory the feature strands anyone who keeps writing approach-flavoured text, and time spent approaching is time spent standing still (measured: six simulated hours of "trudging towards the mountain" left the walker in her starting clearing under level-12 snowdrifts). `ActionStage` — classified since the beginning, and read on the resolve path by nothing (`agents/common._requires_reaction_phase` is its one consumer anywhere, and it decides contestedness, not arrival) — is `docs/UNBUILT.md` §1.13 |
| Place purpose (what a place is FOR) | **Built (v1)** | `world/place_purpose.py` per `docs/design/DESIGN_PLACE_PURPOSE.md`: live `place_purpose.here_affords` echo (read by `agents/character.py`, not by perception — this row named it `perception.here_affords` for two releases and the module never had it); `affords` ledger on the character's own place-graph nodes — `witnessed` from own vitals/`comfort.rest_affording`, `told` mirrored from reconciled beliefs with `belief_credence`-refreshed sureness, `assumed` derived read-side from own node names (never stored); the recall half surfaces at most two walked-route options on a felt need — `place_purpose.felt_needs` + `affords_here` + `place_options`, assembled by `agents/character.py` into the payload key `recalled_places`. There is no `memory.recalled_places`; the row named a payload key as though it were a function. Witnessed drink/water/warmth, told-basis node minting, and negative entries deliberately not built |
| Durable place graph (a mind's own map of walked ground) | **Built** | `commit.update_place_graph` writes per-character nodes and edges onto `chat_chars.state` with `basis: walked\|seen`, `disproven` retraction both ways, and `PLACE_GRAPH_NODE_CAP` eviction; read back as navigational verdicts and `_frontier_hops` distance in `agents/character.py`. `basis: "told"` is an accepted value with no writer, deliberately |
| Long-term goals (the project tier) | **Built (v4); the occasion now arrives and is declined** | **v4 made it reachable, and reachable was not enough.** Re-measured read-only against the live `engine.db` on 2026-08-18, 329 turns after v4 landed: **6 of 100 banks now carry a `project_review`** — the occasion is being offered, which is exactly what v4 was for — and **0 banks hold a project and 0 hold a former project**, unchanged. 32 banks carry the field at all. So the failure has MOVED rather than closed: it was "the review beat requires a project, so a first project can never be adopted", and it is now "the review beat arrives and the character does not take it". That is a different question — an adoption deliberation that refuses everything, or a drive-serving want that always outbids the occasion — and it needs its own measurement before another gate is changed (`docs/UNBUILT.md` §1.56). The v4 diagnosis below stands as the record of what was fixed. One condition written twice: `affect.project_boundary` opened `if not projects: return None`, and the payload guarded `if isinstance(_preview, dict) and _self.get("projects")`. The prompt names the review beat as the occasion to emit `project_ops` — "That beat IS the review this tier owes you" — and both gates made the review beat require a project. The one moment a first project could be adopted was conditional on already having one. The payload-affordance rule ([Structural debt](#structural-debt) #1) from a new direction: there the payload made the asked-for thing harder to reach and the prompt lost; here it withholds the occasion entirely, and no prompt argues with a key that is never present. Arrival still needs a project to arrive at; a task closing and the scene or frame changing no longer do. (The pre-v4 measurement was 0 of 14 banks carrying the field, none of which had ever held a project or a former one.) v3: `affect.apply_project_ops` / `serves_priority` / `project_boundary` / `settle_probation`; persisted in `interior.projects` / `former_projects`. Caps at two, adoption requires a non-circular `satisfied_when`, probation weighs at intention level until served on ≥3 beats over ≥12 turns, drift surfaces as `adrift`. Per `docs/design/DESIGN_LONG_TERM_GOALS.md` |
| Memory retrieval by meaning | **Built** | An `embeddings` provider makes the two vector rankings of `search_memories` genuinely semantic, and one is configured: as of 2026-08-01 `embedding_model_key()` is `openrouter:3:perplexity/pplx-embed-v1-4b` at 2560 dimensions, with all 5,236 memory rows on it and none on the fallback — so the semantic half of retrieval is live rather than hypothetical. Without one the engine falls back to `providers.cheap_embed`, a signed character-n-gram hash (Weinberger et al. 2009) — measured against a real 441-memory story on vocabulary-disjoint paraphrase, that scores **0% recall at every k, median rank 228 of 441**, indistinguishable from random, while a real model reaches median 1–3. It is a strong LEXICAL retriever and a non-existent semantic one, which is why episodic recall worked and thematic recall never did. Deliberately no ANN index: `search_memories` applies a turn cutoff (its own comment calls it "the SOLE defence" against a mind retrieving how the turn it is deciding turned out) and frame visibility BEFORE ranking, and those are exactly the selective predicates an ANN index carries badly — the `sqlite-vec` declaration was deleted rather than wired. The exhaustive scan is also not the bottleneck: 16 ms at a real story's worst case, ~709 ms at ten thousand turns, 2.2 s at 200,000 rows — a novel series in one chat — against an LLM call in the same beat. Two optimisations sat in front of the index anyway, because `_cos` recomputed `norm(a) * norm(b)` although every stored vector is already normalised. The first landed in alpha 6.6 — `_cos` is a plain dot product, **4.4x** (4.99 ms → 1.13 ms over 442 real rows), verified rather than assumed: across 8,000 stored vectors the norms are unit to 9.2e-06, scores agree to 8.7e-06, and the only ranking differences are three adjacent-pair swaps at ranks 743, 1289 and 1808, float32 noise on effective ties far past the `[:60]` cut. Stacking the rows into one matmul is a few lines more and ~20x total, and is deliberately **not queued**: the scan after the first fix is already far below the LLM call beside it, so there is no story length at which it becomes the reason to add an index. That question is settled permanently, not provisionally |
| Changing the embedding model is safe | **Built** | A vector is only comparable with one from the same model, so a row embedded by another scores 0.0 on both vector rankings forever. Nothing re-embedded and nothing said so, so configuring a provider silently split a bank into two eras. Now: `embedding_bank_status` counts the split, retrieval warns once per situation, `rebuild_embeddings` re-reads stranded rows resumably and refuses to write the fallback over real vectors, opening a story OFFERS the rebuild rather than performing it, a checkpoint restore hands the bank back to the reconciler (a reroll had been putting 637 of 642 rows back on the fallback), and `rebuild_checkpoint_embeddings` carries a completed rebuild through saved states by SUBSTITUTION rather than computation — 99,442 saved memories repaired across 1,040 checkpoints in 98 seconds and zero API calls, because a vector is a pure function of the memory and the same memory recurs unchanged across every checkpoint. Of the MEMORY, not of its `content`: that join keyed on `(char_id, content)` until alpha 6.6 while `_memory_document` also folds in turn, location, category, key_phrases, entities, gist, provenance and emotional_context — so two rows could agree on content, hold different vectors, and be handed each other's. It now keys on the whole document, and a summary on its whole `_summary_retrieval_text` rather than its `summary` field, which had the same collision |
| A row is not stranded by one bad second | **Built** | Everything above answers "the host changed models"; none of it answered "the provider blinked", which turned out to be the commoner way a bank splits. `embed_texts_meta` degrades to the crc32 hash on ANY error and the writers store that hash under its own honest stamp — correct for a query, permanent for a write, since nothing re-embeds until somebody accepts a paid rebuild. It had no retry (the machinery at `providers.RetryConfig` covered chat completions only), and no log, so the whole event was invisible: reported live 2026-08-11 as a story created from a greeting offering, on its first beat, to rebuild the memories it had just written, then stranding four more on turn 2 — while the very same eight documents embedded fine on demand a minute later. The embeddings POST now retries on the statuses a chat call retries and on the same dropped-connection exceptions, refuses to retry a 400 (a wrong model is an answer, not a hiccup, and retrying it would trebly slow every write for as long as the role is misconfigured), and says out loud which provider failed and why when it does fall back — rate-limited to once a minute per distinct error, so an outage reports itself without flooding. `embedding_bank_status` and the live-dimension probe opt OUT with `retry=None`: they run while a host waits for a chat to open, and a degraded provider is the thing they are asking about. Greeting seeds go through `add_memories_batch`, so six seeds are one round trip rather than six independent chances to be stranded on the busiest moment a story has. **And the engine paces itself against the ceiling it finds.** Measured the same day on the live OpenRouter/Perplexity route: a burst of 12 took 4 rate limits, and 8 SEQUENTIAL requests at ~2.5/s took 3 — so it is a request RATE, not a concurrency cap, the error is Perplexity's own `request_rate_limit_exceeded` relayed by OpenRouter rather than anything about the account, and this engine's ordinary fan-out reaches it (parallel character steps, one retrieval embedding each). `_embed_pace_wait`/`_embed_pace_penalize`/`_embed_pace_relax` start unpaced, learn an interval from the provider's own 429s, and decay it away once they stop — ADAPTIVE rather than configured, because the ceiling belongs to the model (a local embedder does tens per second and must not pay a number chosen for someone else's provider) and because a ceiling learned last week is not evidence about this week's. Slots are taken under the lock and slept outside it, so N concurrent callers get N spaced departures and then wait in parallel rather than serializing into each other. `RetryConfig.delay_for` gained EQUAL jitter — half fixed, half random — because a fan-out that all sleeps exactly 1s then exactly 2s retries as the same wave that was rate limited, and full jitter can draw a delay near zero, which against a rate ceiling is not a backoff at all. **And none of that is sufficient, so the engine finishes its own write.** A beat can still spend its whole retry budget inside one depleted window — live, chat 70 turn 6 lost all four memories with every guard above already running — so `note_failed_embedding_write`/`repair_pending_embeddings` re-embed the fallback-stamped rows BY ID about thirty seconds later, once the window has refilled. Deliberately not a rebuild, and the distinction carries the justification: walking the bank is a migration a host chooses and pays for, while re-doing a write the engine fumbled seconds ago is the engine finishing its job. It touches only rows whose own write fell back (never the historical corpus, never lore, never another chat), refuses to write the hash over the hash, skips rows another path already repaired, is bounded so an hour-long outage hands the problem back to the rebuild OFFER rather than accumulating, and is a no-op when the crc32 hash is itself the configured embedding — there the queue would mean re-hashing the same text on a timer forever. **The ceiling counts requests, not tokens, so concurrent calls are coalesced into one** (`_coalesced_embed`): callers arriving while a request is in flight queue behind it and the next request serves the whole group, identical texts embedded once. No artificial batching window — a timed wait-for-companions would tax every solo call to help the crowded ones, so instead the natural contention point IS the collection window, which means no-contention behaviour is unchanged and the busier the fan-out the better it batches. Licensed by measurement rather than by the API's promise: the same document embedded alone and inside a batch of three returned BITWISE identical vectors at both first and last position, so a text's vector does not depend on its companions. The engine already depended on that in one direction — writes batched, queries single — so recall would have been broken from the start if it were false. Vectors are routed back by position in both the success and fallback paths, so a caller structurally cannot receive another's vector; the bank probe opts out with `retry=None`, since a measurement taken while a chat is opening must not queue behind somebody else's turn |
| A character can not-remember instead of confabulating | **Built (passive lane)** | `memory_retrieval.recall_confidence`: a deterministic query-performance signal (NQC/WIG shape — top-16 best-vector mean over the bank mean, in units of the bank's own standard deviation), free with the full scan the engine already pays, never an absolute cosine floor (which drops everything or nothing depending on the embedding model). Fails OPEN: under 40 comparable rows, model coverage under half, a degenerate distribution, or a fallback query vector all mean "no signal", never "empty". When it fires, `build_character_memory_context` adds `nothing_comes_back_clearly: true` beside the recalled rows — the rows still ship, since suppressing them is unmeasured behaviour. Threshold 1.7, calibrated on the frozen probe sets across BOTH corpus states (stock and post-repair): zero false abstention on all 37 measured positive hits in each, true abstention on 1 of 15 negatives — the modesty is the finding, because most authored negatives name events the bank DISCUSSED but never lived, and a score distribution cannot tell topical resonance from answer presence. Validated once organically: of the three real `ponder` queries in the corpus, it abstains correctly on the one whose answer genuinely is not in the bank and would falsely abstain on a diffuse pattern-over-session query — which is why it is deliberately NOT extended to the ponder lane (`docs/experiments/MEMORY_IMPROVEMENTS.md` §5) |
| Relevance outranks recency | **Built** | RRF's magnitude is arbitrary (~0.02 at rank 1; only its order means anything) while the bonuses after it are hand-tuned on a 0..1 scale. Summed raw, the four relevance rankings reached 0.074 combined against a recency bonus of 0.12 alone — a recent, salient memory matching nothing outranked the best match on every relevance signal there is. Invisible until real embeddings made the signal real. `_RRF_SCALE` bridges the two scales; `_RECALL_LIMIT` rose 8→16 because every result set is padded with chronological neighbours and at 8 the padding was a third of what a character saw. Mood and goal now fuse as their own rank lists instead of being concatenated onto a query 20× their length |
| A character's history is searchable by era | **Built** | `memory_summaries` was keyed `(chat_id, char_id, scope)`, so a scope held one row and every consolidation overwrote it. The cost was not storage but that the summary layer could not be SEARCHED — there was nothing to search between — while every summary already carried a maintained `embedding`, computed on write, re-embedded on a model change, carried verbatim through every archive and checkpoint, and read by no retrieval path in the engine: 67 vectors on the live bank, maintained across years of turns and never once ranked. Schema v23 completes the key with `end_turn_idx`. Consolidation needed no change at all — it was already computing bounded windows (`min`/`max` turn_idx over that pass's memories, which are already the ones after the previous summary's `end_turn_idx`) and the constraint was throwing each one away on the next write. `search_memory_summaries` ranks them under the same scoping as `search_memories`: `char_id` is the bank, `before_turn_idx` is the same exclusive cutoff, a cross-model vector is skipped rather than compared. Migrated against a copy of the live bank: 67 rows in, 67 out, byte-identical |
| A character receives the era the beat is about | **Built** | The half the row above deliberately deferred, landed once the deferral's own premise was measured and found false. It said the latest window is CUMULATIVE — consolidation folds the previous summary forward, so bounded windows in the payload would cost a character their early history. The consolidator is indeed told to merge forward; it is told just as firmly to shed low-salience detail, and shedding wins. Across the six live window pairs, successive windows share **3–16% of their text** at cosine 0.57–0.88; the Doctor's second window recaps the first in one clause and is otherwise entirely about its own ten turns. So a "cumulative" summary is in practice the latest CHAPTER — which means the pre-v23 singleton was not holding a life and overwriting nothing, it was overwriting every chapter before the last, and **53 of the 67 live banks have no summary over their opening turns at all** and never will. Windows stopped the loss; this reads them. `build_character_memory_context` ranks the earlier windows against the beat's own query and sends the best two beside the current one as `earlier_in_my_life`, first-hand scope only (three provenances in one field is the collapse the separate scopes exist to prevent), chronological oldest-first (ranking chooses which, not what order a life is read in), each dated RELATIVELY — `turn_idx` is global play order shared by every frame, so an absolute index would tell a mind where a flash-forward sits in the story's construction. Same guarantees as raw recall: one character's bank, the same exclusive turn cutoff, a cross-model vector skipped. No minimum score, deliberately: every prose vector scores every window in a compressed 0.45–0.55 band, so an absolute floor would drop everything or nothing depending on the embedding model — but the ORDER inside that band is trustworthy, a memory formed inside a window ranking that window first 97% and 82% of the time across the two live windows. Costs no extra round trip (the query batch is embedded once and shared with `search_memories`) and duplicates little: across 48 probes on the live bank, a mean **14%** of the sixteen recalled raw memories fall inside the window's own turn span. `_origin_on_drift` now surfaces the earliest first-hand window separately as `where_i_came_from` when an existing drift signal fires (goal held, project adrift, or resolved mood crossing its baseline), without spending a similarity slot every beat |
| The eras a destroyed summary took with it can be rebuilt | **Built** | `backfill_memory_summary_windows` re-consolidates the turn ranges the pre-v23 singleton overwrote, walking forward from a character's first memory to the earliest surviving window in aligned steps and chaining each result into the next exactly as the forward path would have. It never archives (the forward path retires low-salience rows it has just folded in; doing that here would retire hundreds at once on the strength of a summary written long after the fact) and never moves the consolidation cursor — `get_memory_summary` orders by `end_turn_idx DESC`, so older rows leave the newest one holding it, which is load-bearing and tested. Run against the longest story (chat 38, 118 turns, three characters, 641 memories): **27 windows in 336s**, summary coverage 8–18% → **92–98%**. It also surfaced why consolidation needed a rule it did not have. `persist/commit.py` already refuses to MINT a memory from a view that says only "You are in an unspecified area" — an absence is not an episode — but banks written before that guard still carry them (369 rows corpus-wide; 85 of one character's 101 in this story), and consolidation had no such rule, so **13 of the 27 backfilled windows spent an LLM call rendering an absence into prose** to hand a character as a stretch of life they lived. `_is_empty_view` moved to `mind/memory.py` (commit imports it back) and both consolidation paths now drop placeholder rows; the forward path, which cannot simply skip because the cursor lives on that row, advances it carrying the previous account forward and asks no model at all |
| A memory carries the mood it was formed in | **Built** | `valence/arousal` record the resolved affect carried INTO the event rather than the model's saturated self-report (measured +0.773 mean, 0% negative, against resolved +0.467 and 22% negative). Schema v24 adds `encoding_valence/encoding_arousal` from the post-appraisal surface, so the record distinguishes how the event was encountered from how it left the mind. Both pairs survive checkpoint/archive/character-bank/editor round-trips; old rows remain neutral rather than receiving invented retrospective arcs |
| Nobody may author the player's interior state | **Built on the Director; DELETED on the narrator** | The Director owns objective causality and does not own what is inside the protagonist. Live: the player typed only "W-what did you do to me!?" and the resolve wrote "the shrill, PANICKED cry" and "she takes in the GENUINE TERROR in those wide eyes" — asserted as fact, with "genuine" claiming its truth — which perception then copied into a second mind's view. `_check_player_interiority_authority` on `director_resolve` still guards that, exempting anything the player themselves wrote, and it is the half that matters: an interior state asserted there is asserted as OBJECTIVE FACT and propagates into other minds' views. THE NARRATOR-SIDE TWIN IS GONE (2026-08-23). `_check_player_interiority_prose` matched `_YOU_INTERIOR` — a named-emotion list plus "you feel/know/realise" — against second-person prose, where that is how a narrator reports SENSATION: it flagged 73 of 2,389 stored drafts (3.1%) on lines like "'you feel' in 'laps against your shoulders'", and being enforceable each firing bought a whole extra narrator call. It is the literal-guard class whose failure rate rises with how well the model writes. The owner's ruling, and it is the right one: narration suggesting emotion or sensation is not a trespass, it is the second person doing its job — the limit is proportion, which is a craft judgement and not a regex's to make. The sheet now says so, and what remains prohibited there is unchanged: deciding a mind's conduct, and another mind's interior, to which the narrator has no channel |
| A character calls the player what they may legitimately call them | **Built** | Three possible handles, and the engine was using the two wrong ones. Live in "Run!" (chat 54), three of The Doctor's four turn-0 memories read "The Doctor knows **the player** was being chased by a Dalek", "intrigued by **the player's** appearance" — the engine's own out-of-fiction word for the protagonist, inside a fictional mind, at salience 1.0. `{{PLAYER}}` was in none of them: the model wrote the literal English words, so `sub()` — which replaces only the exact token — had nothing to replace. An earlier run of the same card had the opposite failure: the token WAS present, `sub()` resolved it to "Hinami", and the character began knowing a name the launch had explicitly said he did not (`already_known=False`). `greetings.player_handle_for` now answers the question once — recognised → the persona's name, not recognised → a DESCRIPTION from the same `_unknown_actor_label` every perception path uses, so the launch cannot drift from the identity floor — and `_substitute_player_slot` rewrites the token AND the bare words, anchored on a leading article so an in-fiction "a lute player" keeps their job. The same defect had a second source: `persist/commit.py`'s dialogue-memory path rewrote the player to the literal `"the player"` and then EXEMPTED them from the recognition gate every other speaker passes, producing 68 rows across the live corpus including `the player said "My Name is Hinami." to Dr. Moon` — the memory in which the character learns her name, attributed to a word from outside the story. The player is now a body in the room like any other: the persona's real name goes in and the gate decides. `_unknown_actor_label` also stopped truncating mid-phrase on a linking participle ("the beautiful young woman appearing"), which matters once the description is the deliverable rather than a fallback |
| Nobody may author a character's conduct but that character | **Built** | The mirror of the row above, from the character's side: the Director resolves what a declared act ACHIEVES and does not decide that the character also did something else. Live in "Run!" (chat 56, t1391) The Doctor declared one act — scan "from several feet away", "while staying at distance", against a want to act "without crowding her" — and `speech: null`; the resolve had him take "a half-step closer" and say "You're alright, Hinami. Nothing broken…". `_check_character_speech_authority` was armed and blind to all of it: it read only sentences OPENING with the literal name (every fabricated sentence opened with "He") and measured its verb window three words from that name (the attribution verb sat twelve words out, in a compound predicate). The dialogue_log backstop that WOULD have dropped the line was inert because `dialogue_log` was empty — the line existed only in prose, and the speech check strips quoted spans on the assumption the dialogue path covers them. Each guard held ground the other had. Now: `_sentence_subjects` binds a pronoun subject to the most recent NAMED subject (a newer name takes it, an unanchored pronoun binds to nobody), `_predicate_heads` measures the verb window per conjunct, `_check_character_act_authority` guards acts — the full act-verb list for a character who declared none, narrowed to MOVEMENT for one who declared a non-locomotive act, because distance decides what perception delivers and what contact is possible — and `_check_prose_quote_authority` catches a spoken line in prose that no declaration supports, whoever the prose says said it. All fold into the existing one-retry loop, kept only if it reduces the total. The cost of missing it is not cosmetic: the narrator dropped both fabrications, so nothing was visible in play, and both still committed as the Doctor's own episodic memory of what he did |
| Nobody may author the player's conduct but the player | **Built** | The doing half of the row above. `_check_player_act_authority` on `director_resolve`, folded into the same one-retry loop. Two scopes, and the second was missing entirely: a player who declared NO action was protected, and `if declared_actions: return []` disarmed the guard for anyone who narrated so much as a gesture. Live in "Run!" (chat 56, t10) that player narrated one every single beat, so the guard was off for the whole story — they typed `"Heh? What are we doing what's going on?" You look genuinely confused.` and the resolve wrote "her hands coming up to grip the edge of the console, fingers finding a lever as if to steady herself", which perception copied into their OWN view as "I grip the console edge" and the narrator rendered as fact. Their very next input was "Which lever?!" — the fabricated act replayed a beat later, which is precisely the failure the guard exists to stop. Elaboration cannot be separated from addition by vocabulary, because "pushes herself upright" elaborates a declared "slowly stands up" and shares not one word with it; what CAN be separated is what the act TOUCHES. Elaboration re-describes the player's own body, fabrication reaches out and takes hold of the world, so the widened scope flags exactly one thing: a manipulation verb taking a DIRECT object that is neither the player's own body nor anything their declaration mentions. "Pressed flat against the cold metal" is a body bracing itself and is not a grip on the metal |
| A view never narrates its own perceiver | **Built** | `_strip_self_narration` drops sentences whose subject is the perceiver, in all three perception passes. Live: Elyndra's own view read "Elyndra's gaze stays fixed on the shifting lump, her teasing smile faltering" in a view that elsewhere said "You see Hinami". Perception had copied the Director's omniscient sentence rather than rendering the beat from her frame — per-observer calls do not prevent that, since each observer's call simply echoed its input. Whole sentences only, subject only, and never emptying a view entirely. Subject resolution was name-anchored on the stated ground that "a pronoun could be anyone in the beat" — true of a pronoun read in isolation, false of one read in sequence, and the reason chat 56 t6 walked through: the PLAYER's own view read "She feels her arms still wrapped tightly, her breathing slowing, the terror in her eyes beginning to recede", third person about its own perceiver, naming an interior state she never declared. Now shares `_sentence_subjects` with the Director-side guards, so a pronoun continuing a named subject is resolved; an unanchored one still binds to nobody. **The guard needed a floor and the prompt needed to agree with it**: a view written WHOLLY from outside its own perceiver puts them in the subject slot of exactly the sentences carrying what they saw, so dropping by subject removes the framing error and the observation together. Live (chat 38, t140) the Doctor stood at the genkan with `shapes` sight to both bodies, watching an embrace the resolved event says he was watching with bright interest, and his view, his structured observations and his committed memory of the beat all came out sound-only. The perception prompt carried no person discipline at all while this scrub enforced one — the Narrator has an explicit PERSON DISCIPLINE clause and perception had nothing — so it now requires each view to be written from inside its perceiver, and `_strip_self_narration` refuses a drop that would leave a view with no sight in it (`_SIGHT_ASSERTION`), reporting the refusal rather than performing it. Over-denial is the worse failure, on the same ground the row below states it: a framing slip is bounded and visible, and what a mind saw is not |
| A view describes only bodies the perceiver can reach | **Built** | The engine already knew: `visual_level_between` returns 'none' for the pair and `spatial_rel` calls the rooms `separated`, and nothing consumed either answer once the view was prose. Live (chat 58, t28) the player slammed the TARDIS doors and stood in the console room; the Dalek stood outside, two rooms off with no connecting edge, and her actions were still narrated into its view — and from there into its own next-turn context. THE GUARD MOVED FROM THE PROSE TO THE IR. `_strip_unreachable_bodies` read a finished view back and cut sentences naming an unreachable body — a repair pass, with all a repair pass's edges (possessive forms, the perceiver's own name, the view left beyond repair). The composer now BUILDS the view instead of checking it: `visual_level_between` is consulted before any sentence exists, so a body it answers `none` for yields no presence percept, no pose percept and no appearance percept, and there is nothing to cut. Deliberately the hard case only: a body the perceiver cannot see but can still hear through a shut door is left alone, because over-denial is the worse failure — silence about someone audibly present is its own lie — and sight-asserted-where-only-hearing-exists needs sense-cue analysis this guard does not attempt. The subject match itself was the other half: every subject-anchored guard read the bare registered name, so a body registered `A Dalek` and written `The Dalek's visual sensors` slipped past all of them, which is why that view narrated its own perceiver in the third person. `_subject_opener` tolerates a leading article — the three articles only, never a title, since a title is often the sole thing telling two bodies apart (§1.17's line). The scene DRIFT that put those bodies in the wrong rooms is not fixed: `docs/UNBUILT.md` §1.20 |
| A crowd, a courier and a posted notice reach the minds in the room | **Built** | `world/crowds.py`, `story/couriers.py` and `story/artifacts.py` each publish a perception seam that has already decided room scope and already reduced the thing to what a bystander takes in — the crowd's density and heading, the courier's figure and which door he makes for and NEVER his message, that a bill hangs there and never its wording. Those subtractions were being computed per observer, on every pass, and consumed by nothing since the composer replaced the per-observer model call: a player standing in a packed square was told about the square. So a dispatched courier could be moved on the simulation clock, questioned, silenced and made to arrive, and no mind in any room he crossed received a word about him — the design's own verbs (intercept, follow, question, outrun) had no perceptual event to begin from. `composer.room_content_percepts` mints them as `ambient` percepts, which is what they are: a standing feature of the room, not an event, not a body, not a sensation on this body. No new percept kind and no new pack wording — `ambient` already renders in the English reference renderer and the Japanese adapter. The dedupe key hashes the subject's own uid with the STATE its seam publishes -- a crowd's band, density and mood -- and never the sentence composed from them, so an unchanged crowd is furniture a delta view omits and a crush that thins to a scatter re-renders. It hashed the DESCRIPTION until 2026-08-28, which is the one combination that manufactures a false `changed`: a stable subject paired with prose content makes every re-rendering read as an event, and an event is an obligation the narrator must write a sentence about (measured, chat 95: seven `changed` verdicts in sixteen turns on a crowd whose band never left "a dozen or so", because the phrase is a top-two-of-tally over a membership that walks its errands -- five spellings of one unchanged fact). A courier and a posted notice publish no state and keep their description as their content, which is where the old behaviour survives |
| The room's talk reaches an observer as ground plus at most one figure | **Built** | `world/charter_chatter.py` + `agents/common.chatter_for_room`, per `docs/design/DESIGN_BACKGROUND_PRESENTATION.md` Part A. The substrate computed ambient talk and threw it away: `charter_practice.enact` returned acts every window (measured 18 median on twin_towns(40)) and `after_charter["acts"]` died at every `normalize_charter`, so perception had nothing to read. `step` now deposits the last window's acts room-stamped as `window_acts` (normalized, so it survives save/restore), and the perception seam derives two objects per observer-room-beat: a HUM band (silence / scattered talk / a steady hum / a din — thresholds set from the measured room median 4 / p90 6, graded with any crowd band standing in the room — authored ledger rows and Part B's derived charter crowds both, since in a charter-only story the authored ledger is empty and the derived crowd is the only band there is) and at most ONE overheard fragment, the who-asked-whom-about-whom triple with labels licensed by recognition (presence record → name, visible post → role noun, else anonymous; the SUBJECT is named regardless because the name was said aloud — the asymmetry that makes rumour work). Degradation inverts with crowding: packed admits only entangled/`_ACT_EVENTS` fragments, a crush is a din and zero fragments (the walla rule — prose has one channel, so anything verbatim IS foreground). Delivered as `hearing` percepts beside the crowd view, so `observations_from_render` makes character receipt legitimate with no second representation; `WITNESSABLE` deliberately unchanged (ordinary chat below observation salience — the measured alternative was ~100 claims/window into charter heads). Zero model calls; selector+hum measured 0.04ms per room-window, the once-per-stage registry read 26.6ms. `tests/test_charter_chatter.py` |
| A charter crowd is a projection, never a second ledger | **Built** | `world/charter_crowd.py` + the derived rows in `agents/common.crowds_for_room` and `agents/director_views._crowds_view`, per `docs/design/DESIGN_BACKGROUND_PRESENTATION.md` Part B. Charter bodies at the observer's room, minus everyone individually presented (bindings, live presence records), become one crowd row derived at every read and stored NOWHERE — uid from `(chat, charter, place)` under the `crowd:charter:` prefix, band from `crowds.count_band` (the one place an integer meets the band vocabulary; the reverse arithmetic still never happens), composition from the watch bill's role nouns, mood from banded `strain_of`, no heading and no `since_turn`. Below `CHARTER_CROWD_FLOOR` (3) the members stay individual figures — and the subtraction and that presentation are now the SAME predicate, which they were not until 2026-08-28. `members_of` removed a body from the crowd on the strength of "something else presents it individually" and nothing did: perception built every observer's co-present body roster out of the cast and the players alone, so a body that earned a presence record left the crowd and entered no view at all, and a sub-floor member entered none either. `agents.common.presence_figures_for_room` is the missing half — the ledger's people standing in this room, plus the charter bodies no derived crowd carries, in the co-present body shape — and `perception._presence_bodies` places them on the stage's own scene copy so the ordinary spatial guards can grade them (`room_of` fails CLOSED for a body the scene puts nowhere, which is why they were dropped rather than shown). Nothing is persisted and nothing is granted: an unseen body is subtracted exactly as a cast body is, and `observer_display_map` hands a stranger a descriptor built from the same role noun the crowd's composition uses (`charter_crowd.member_noun`, one reader for both, so a room's band and the person standing out of it cannot describe two different rooms). Measured live, chat 98 (bench.db) turns 10-13: five crew in the player's lounge read as "a handful lieutenant commanders and ensigns" until each acquired a record, whereupon `crowds_for_room` returned `[]` and every view of that room held nobody — while `background_presence_records` still returned all five; two bodies had likewise been standing unseen on the bridge since turn 0. `apply_ops` refuses `move`/`split`/`disperse`/`set` on the prefix — a second writer on where charter bodies stand is the `wearing`/`state`/`regions` scar — and `MAX_CROWDS` keeps governing the authored ledger alone. `emerge` resolves at the commit seam: `persist/commit.py` re-derives the same rows perception showed and `emerge_from_charter_crowd` persists the `with_charter_presences` overlay record (the record IS the emergence; membership excludes the body on the next read; no `emerged` list), picking by `charter_practice.entanglement` with whoever is present when `who` is empty. `absorb` deletes only a record nothing durable names — Charter never stopped simulating the body, so absorption changes nothing else. DESIGN_CROWDS §3a's "an emergence may not be re-met" is superseded for this species (amendment in that note): the person who stepped out last visit IS there next visit. Measured on twin_towns(40): derivation 0.5ms cold / 0.007ms memoized per room atop the shared once-per-stage registry read; emerge/absorb round-tripped membership 6→5→6 with the charter state byte-identical. `tests/test_charter_crowds.py` |
| The voice tier answers demand, not salience | **Built** | `persist/commit_background.pick_voice_demand` + `agents/background._demanded_presences`, per `docs/design/DESIGN_BACKGROUND_PRESENTATION.md` Part C. A background presence is voiced only when an authored mind's own conduct calls on it this beat: addressed (overt declaration, flow ref, aimed character line, or a routed Director hand-off), owed an unexpired reply, acted toward an authored mind last beat (`engaged_turns`, written at commit; charter half from `window_acts`), or emerged from a crowd this beat. Co-presence is not a trigger and never became one, but since 2026-08-29 it is a FILTER: a demand from an authored mind reaches a person through a channel or it does not reach them, so a trigger that merely claims something ARRIVED — the player's raw words, and the two carried debts — must also put an authored mind within full hearing of where the presence stands (`demand_reaches`, the same bar `_character_address_of` and the reply-debt writer already used; the Director's own judgment for this beat, a routed line or a flow address or an emerge, stays exempt, and unknown rooms fail open). It was a filter and not a priority term because the people who should have been picked were not losing on ordering — they never qualified at all. Replayed over all 40 recorded turns of chat 98: the gate spent 29 of the run's 51 voice calls on a body two decks from the player that qualified on `acting` for 14 consecutive beats and produced not one line; every pick that ever produced a line survives the filter, including one from an adjacent room through an open door. `mentioned`, bare dialogue history and `at_post` stopped qualifying — the room stays alive through the chatter and crowd tiers for zero calls (§C2) — and `max_managed` became a ceiling on overflow rather than a selector. Overflow order addressed > owed > acting > emerged with the B3 entanglement tie-break; an addressee is never dropped (a PRECISE address widens the slots — the loose significant-word match qualifies but cannot force, after the measured six-for-one "Regular" case), and addressees past the ceiling sharing one derived charter crowd answer as ONE deterministic chorus entry, nothing persisted. No tenure: `charter_crowd.PRESENTED_IDLE_BEATS` (4, measured — 25/28 resumptions after real inattention within 4 idle beats across every live chat) lapses a record's individual presentation losslessly; recognition never lapses. Measured on twin_towns(40) with six at-post regulars over 50 beats: voice calls 50→10, manager cast entries 300→60, and the old gate answered a direct address with the wrong (merely recent) regular where the demand gate answers with the person named. `tests/test_voice_demand.py` |
| A beat's transition is perceived, not just its result | **Built** | Perception ran against the OUTCOME scene alone, so any act that closed a channel erased the perception of itself. Live (chat 58, t27): the player ran through the TARDIS's open doors, the doors slammed, and the ship went `in_transit` — which correctly severs an interior's exterior edges — all in one beat, so by the time the Doctor's view was built the room she had run into was adjacent to nothing. His view records the doors closing on an empty doorway; he never saw her go in. Nothing in the transit model was wrong: a ship in flight IS cut off, and `in_transit` with no destination is a legitimate state (a destination need not be declared at departure, and the gap is room for later play). The defect was asking the perceptibility question only of the beat's last frame. `_source_channels` now takes `prev_sc` — the scene before this turn's diff — and a source counts as perceptible if it was reachable at EITHER end, marking the rel `was_reachable_at_beat_start` so a reader can tell a transition from a standing line of sight. It only ever UPGRADES: a body unreachable at both ends stays unreachable, so nothing closed for the whole beat is opened. Covers every act that shuts a channel it is seen through — a slammed door, a drawn curtain, stepping into a container, a vehicle pulling away. **Only where the SOURCE's act closed it**: keyed on the relation alone it also fired when the PERCEIVER walked out, handing her the room she had left for the whole beat with `same_room` true, so `line_hear_level` returned "full" and `speech_percept` emitted no `via` — four speeches delivered as unchannelled co-present voice from a room she was not in, and a narrator that invented a display feed to explain them two beats before any comm link existed. A body is graded from where it IS, so `_body_relocated` (room, or the enclosures shut around it) withdraws the rescue from a perceiver who moved; she still receives what a real channel carries, tagged `via` |
| A delivered line reaches the mind that heard it, whole and in order | **Built** | `_dedupe_view_sentences` has always documented "sentences containing quoted dialogue are never dropped -- quotes must survive verbatim". The splitter defeated it: the check is per-SENTENCE, and a spoken line carrying its own terminal punctuation is cut into fragments -- only the two on the ends keep a quote mark, and every fragment between them is judged naked and dropped if it echoes anything earlier in the view. Live (chat 58, t30): the player answered a direct question with "Seven? I think? There might have been more... they began to spread out..." -- four terminators, four fragments. This runs LAST in `perception_act`, after the deterministic delivery, and ate the interior of the quotation; the Doctor then asked "How many? Where exactly?", the question that had just been answered. `_mask_quoted_spans` now replaces each quoted span with an opaque token carrying no whitespace and no terminal punctuation BEFORE splitting, so a quotation cannot be cut apart. A second ordering failure surfaced in chat 38 t125: interpret correctly held `awe line -> turn -> teasing line`, but perception rendered `turn -> untoned line -> tease`, softened the smirk, added delivery metacommentary, and duplicated the appearance tail. Both failures were repairs over model prose, and both are now structural: perception writes no prose to strip or re-order. Each declared element becomes its own percept through its own hearing/sight/concealment gate (`speech_percept`, `act_percept`, `crossing_percept`), carrying the exact quote body, the intent-free observable, and the declared tone; `Percept.order_key` holds declared order, so chronology is a field rather than a pass. The repair functions that did this over prose are gone with the prose: `_strip_onset_rendering`, `_inject_onset_sequence`, `_inject_onset_speech` and `_self_cannot_see_own_surface` were deleted once the composer replaced the path that fed them. Two siblings in `agents/common.py` — `_inject_action` and `_inject_visible_actor` — outlived the deletion because they live in a different file and have tests of their own, which is the whole of `docs/UNBUILT.md` §1.45. Upstream, `_interpret_coverage_corpus` includes `tone` and `observable`, so the already represented delivery no longer triggers a redundant repair action. Chronology is therefore plumbing rather than a prompt preference |
| Appearance compression belongs to the player's page, not an NPC's mind | **Built** | Player narration may omit unchanged body/attire strings, render one garment delta instead of a wardrobe inventory, and re-earn the complete description on discovery, re-encounter or explicit examination. **The ledger is asked before `force`.** `force` is an objective judgement -- some visible-form channel was written this beat -- and it was deciding a per-observer question, so an overlay wiggle whose composed description hashed identical re-earned the whole card. What that looked like is in the corpus: chat 89's stored player views carry the same 342-character description on twenty-five consecutive beats (turns 3-27), and 211 of the corpus's 290 appearance-admitting beats admitted a description whose exact key that observer's own ledger already held. Those rows were composed by an older engine and `_attire_changed_semantically` has since closed most of the ways `force` was fired, but the CLASS was still live at HEAD: an identical card plus `force=True` re-rendered in full (`tests/test_composer_change_tier.py`). `render_view` now suppresses a description whose full dedupe key this observer already carries, whatever `force` says; a description that moved renders as the change and leads the beat. That is presentation compression. Applying it to NPC perception subtracts cognition input from a stateless character call, so character-mode rendering keeps every other visible person's complete body and attire surface every beat, even when the appearance ledger says it was described before. The sole self-side omission is the observer's own body/attire string: the updated character card and `self.attire` already carry it in the same call, while pose, contact, condition and other situational changes remain percepts. |
| A compound declaration keeps causality, identity, and communicative meaning | **Built** | The normalized sequence distinguishes exact `speech` from semantic `communication`: “I ask what happened” carries the authored proposition through hearing, memory, public evidence, Charter reporting, and narration without manufacturing a quote. Interruptible physical declarations may carry `phase_id`, `phase`, and `depends_on`; explicit `participants` and `requires_contacts` make prerequisites machine-checkable. Only onset/atomic roots reach pass 1. Resolution composes the model's contested-outcome verdicts with those structural prerequisites into engine-authored `sequence_dispositions`; blocked dependents are absent from the player-facing event order, and `phase_sources` removes their explicitly sourced state changes before commit. The sidecar is consumed rather than persisted. Typed `referents` bind repeated or same-pronoun action surfaces to observer-safe labels without guessing from prose. Player continuation/completion phases render after character reactions, preserving `onset → response → continuation`. Until 2026-08-25 the mechanism was unreachable in practice: `phase`, `phase_id`, `depends_on`, `participants`, `requires_contacts` and `referents` were read on every beat and named in no sheet, so the Director had no way to declare one. The interpret sheet now carries them. When a standing contact ends, contact-bound support/pin/grapple pose facts are invalidated while genuinely independent relations such as facing or beside remain. |
| Structured pose prose keeps the perceiver's person | **Built** | A pose is keyed by the body it describes, so its owner is known even when a spatial specialist writes a free `detail` fragment in third person. Before a pose enters the percept IR, exact handles for the receiving observer become `you/your`; owner pronouns in owner-bound posture, constraint and detail fragments do the same only until another body is explicitly named in that local fragment. This fixes mixed seams such as `You are kneeling — settled on her heels` without guessing that `steady her` after a named third party also means the perceiver. The episode renderer converts the same second-person pose referents to `I/me/my`, including another body's `astride you`. |
| A view leads with what changed | **Built** | Standing state is the BACKGROUND of a percept, not its content: what is continuously true is context, and what is different since THIS OBSERVER last perceived it is the beat. Measured over 389 replayed player views in seven stored stories (chats 86-92, the composer's own builders re-run against the checkpointed scenes), **59.3% of every composed atom and 48.0% of every composed character was standing state re-rendered because it was still true**, and 68 of those beats handed the narrator ZERO numbered observations. Across the whole stored corpus, 772 of 2,645 consecutive player-view pairs (29.2%) repeat a sentence of 60 characters or more verbatim from the beat before. The narrator was not lazy and was not badly prompted: it was handed the same scene again and asked to make it feel different. `composer.standing_key` splits a standing dedupe key into `<tag>:<subject>:<content>` and `composer.standing_verdicts` reads it against this observer's OWN previous ledger -- unchanged, changed, first sight -- so `_render_view_english` can partition a player view into a BEAT half (this beat's events in declared order, then the standing percepts that changed, a changed room last) and a BACKGROUND half (first mentions, re-encounters, sensations still being felt). That ORDER is `composer.player_view_order` and every language pack calls it, because a pack that spells it itself gets it wrong without dropping a fact: the Japanese adapter emitted the beat in the order it iterated -- changed standing state before the events -- so a Japanese player view opened on the pose where the English one opened on the act that moved it. Changed percepts are marked `beat`, so `observations_from_render` files them as obligations and they reach the narrator's numbered deliveries instead of its wallpaper. After: standing falls to 30.7% of atoms and 25.5% of characters, obligation observations per beat rise from a median of 2 to 4, beats delivering none fall from 68 to 28, and 347 of 389 views open on the beat rather than on the furniture. THE DIFF OPERANDS ARE NOT TWO SCENES -- they are this observer's admitted percepts and this observer's own last ledger, so a subject with no percept produces no verdict and no sentence; a diff of the WORLD is exactly the shape that leaks and this is not one. Old single-hash ledgers degrade to first sight, never to a false claim that something moved. Character mode is untouched IN ENGLISH -- an NPC agent is a stateless call, and what is not in its context is not in its mind. It is not byte-identical in every language: the Japanese adapter had been applying page compression in EVERY mode, so a Japanese NPC was being denied a repeated appearance the English renderer kept handing it. That copy is gone, which means a Japanese character view now carries appearance strings it did not before -- a mind receiving what it can see, which is the direction the firewall subtracts toward, not away from |
| Active sensation is present input, not delta scenery | **Built** | Player-facing compression omits unchanged rooms and descriptive standing facts, but never an admitted `sensation`: sustained pressure, friction, warmth, pain and ongoing effects attached to a contact remain in the current perspective every beat they remain active. Character mode already retained them; player mode now follows the same bodily boundary. Stable sensation keys still collapse duplicates, and memory keeps a separate sparsity rule—an unchanged sensation with no event or other changed percept does not manufacture another autobiographical episode. |
| A multi-action declaration stays multi-action at the page boundary | **Built** | Outcome perception projects every distinct overt action from the merged reaction/interaction sequence. It does not reduce a person to an actor-keyed terminal action: that discarded earlier motion while preserving all of the same person's dialogue, producing a disembodied and causally incomplete beat even though Director resolution still held the complete response. Exact delivered lines from `dialogue_log` are rebound to their original declaration slots, because a transcription list is evidence of delivery rather than authority to regroup `act → line → act`. A contestable onset is explicitly attempt-modal (`attempts to create space`), because a present-tense observable must not award the effect before reaction or contradict a later rejected disposition. The same boundary keeps a target-owned body reference on its structured target during second-person conversion, rather than changing `Mara's back ... her shoulders` into two different bodies; anatomical modifiers remain attached and a newly named third body ends the repair scope. The narrator receives the restored ordered acts through perception's own observations. Its dialogue-order check locates whole quoted spans, never a short player line as a prefix inside a later NPC quote. Standing tactile clauses remain present evidence rather than mandatory chronology, and the craft screen rewrites every inflection of sensor-ledger diction such as "the contact at your hand registers..." or "shared warmth registering..." instead of publishing the composer's bookkeeping voice. |
| Perception's recognizer tables follow the story's language | **Built** | `agents/perception.py` explains at length why its recognition tables must be read from the ACTIVE pack at use time. Six were — and five of those six fed a function with no production caller, while every table on the LIVE path was an English literal in the module: the structure was right and pointed at the dead half. Five moved, each with what it decides and what a Japanese story got instead: `_LOOK_VERBS` (an explicit look re-renders the player's whole standing state; 見回す matched none of eleven English words, so a full render was never re-earned), `_RAPID_MOVEMENT_VERBS` (disables the open-group continuity rescue; 走る matched nothing, so the rescue stayed open on the beats it exists to close), `_SIGHT_ASSERTION` (`_strip_self_narration`'s refusal floor — a cut that would leave a view with NO sight in it is refused; a view asserting 見える scored as sightless, the refusal could not fire, and the whole cut went through, which is over-denial, the failure the floor was built to prevent, reintroduced by the language it was written in), `_RAISING`/`_LOWERING` (the inverted-motion tripwire) and `_PAIN_CUES` (whether a non-awake mind's residue carries pain — hand-copied TWICE, ten lines apart, in the two residue paths, so a cause reading 負傷 matched neither). Each pack owns its own anchoring, which is the point of the seam: English verb tables anchor `^...s?$` because they are matched against a single declared verb token, and a script with no spaces has no token to anchor |
| A conversation remembers the job it already did without flattening character | **Built** | Exact-line history answered “did I reuse the words?” and missed “did I make the same offer again?” Live in chat 38, turns 126/127/136/137 repeatedly offered Saturn or dragons and handed the choice back; turn 138 substituted Calufrax while its own deliberation called that an “entirely new destination to break repetition.” `_recent_self_moves` now projects one selected response/goal per turn from immutable active variants across a twelve-turn window, so extra speech lines and fresh proper nouns cannot evict the conversational job. `_first_repeated_move` uses conservative lexical similarity, combined with any exact-line or already-spent-intention finding. **Since `e629d60` it buys no second call.** The contextual review it used to open — a re-ask of the whole character decision, gated by a small `move_repeat_screen` on the cheap lane — is gone, prompt included, along with `llm_quality.move_repeat_screen`: repetition is WEAK output, not broken output, and the redo cost a full character call (36.3s / 58.0s / 155.6s measured live) to be told the beat was fine 48 times out of the stored variants. The findings are now RECORDED on the step as `repeat_correction` / `move_correction` / `intention_correction`, each raised as a warning, and the beat stands. Two consumers read them: the intent ledger (a `progress` claim on a beat that repeated an earlier move does not advance the goal, `affect._advance_intent`) and `_unbidden_trigger`, whose `barren_goal` reason surfaces one CONTRASTING memory — "here is something else you own" — which is the only positive move available to a mind that has run out of them. A negative constraint helps a mind that has another move and does nothing for one that does not, which is why every re-ask rephrased and kept the propositions. `steering_intention_ids` closes the adjacent cause: dormant, blocked, satisfied, and abandoned intentions remain visible as autobiography but cannot authorize a fresh want or response, and commit enforces the same boundary on settled state |
| A goal the world has invalidated is closable by the world | **Built** | `nonviable` is the right verb for a goal the world has sealed and only the character may emit it — which is precisely the mind that can no longer see the goal is closed. Below the progress ceiling `affect._advance_intent` deliberately refuses to stall ("a slow intention is not a stuck one... Closing a goal still needs evidence"), so nothing acted on the contradiction at all. Measured, chat 88 char 72 intention i6: formed turn 46, the station its text names left behind at turn 54, still `active` at turn 67 with every want carrying `serves: "i6"` — while commit warned four separate times (turns 54/55/58/64) and nothing read the warning. `affect.settle_intent_world_anchors` is the floor and the evidence comes from OUTSIDE the mind: the settled scene's contact ledger, read as `_interior_relations_of` in `persist/commit_memory.py` — the same interoceptive channel the subject already feels through. An aim naming both the other party and their current station anchors there; when the relation settles somewhere the text does not name for `WORLD_DRIFT_SETTLE` (2) beats, the goal closes with the existing `nonviable` semantics, steers its collapse beat once, and hands `project_boundary`'s task-closed review a successor to invite. Three refusals bound it: absence of the relation resets the clock (an ended contact must never close a goal), only the CONTAINER's aims are in reach (the contained party may legitimately strive against the passage), and a station string that changed only because the representation changed is not the world moving — the anchor carries its `source` and a changed source discards the binding. Corpus survey, read-only over all 329 intentions in the live corpus: the rule closes exactly two, and both are that one intention in two sibling branches. `tests/test_world_closed_intent.py` |
| The character agent supplies decision mechanisms, not a default temperament | **Built** | Prompt scope constrains elapsed action and causal ownership only: one physically coherent next beat, with contested success left to the Director. It no longer describes “smallest plausible” as a brake on escalation, seeds silence and a small response into every candidate set, or forces an aid/caution candidate when something is urgent. Current evidence must enter appraisal and stale expectations cannot erase it, but urgency changes stakes and time rather than supplying values. Candidate classes are not universal; inaction, compromise, escalation, violence, mercy, restraint and their opposites appear only when this authored mind would actually entertain them. A decisive act is as valid a micro-beat as a hesitant one. This keeps perception, appraisal, alternatives and causal limits as engine mechanisms while leaving temperament to the sheet, learned state and present goals. |
| A declared destination exists, and is reachable | **Built** | Going somewhere is the strongest possible assertion that a place is there -- stronger than naming it, which is why this is keyed on MOVEMENT rather than mention: a character can talk about a city all day without the engine minting it, but the moment a body walks toward one it has to be somewhere for them to arrive. `prepare_scene_commit` used to create the destination ONLY as a side effect of lore staging -- if that turn's mapping happened to stage a `layout` entry -- so a room existed or not depending on whether the lore layer had something to say, and a mover could be sent to a room that was never created. Live (chat 58, t25): movement targeted `alley_mouth`, an ANCHOR inside `street_outside` rather than a room, nothing staged layout lore for it, and nothing was made. The room is now always created, with staged layout lore supplying its description when there is any, AND with an adjacency edge back to where the mover came from -- a room with no edges reads `separated`/`far` from everywhere, which is how an interior falls out of the world. Falls back to the busiest room when the mover cannot be named, because an unreachable destination is worse than an imperfect edge |
| A room created this turn is reachable from somewhere | **Built** | Adjacency resolves BOTH ways, so an empty `adjacent` is fine as long as something points AT the room -- which is why `alley_room` worked carrying no edges of its own. The failure is the stronger one: a room no edge reaches in EITHER direction, for which `spatial_rel` answers `separated`/`far` on every pair, leaving its occupants able to perceive nothing but each other. Live (chat 58): `northern_plaza` was minted with `adjacent: []` and nothing pointing at it, so when the player stepped out of the TARDIS into a plaza whose own description carried shuttered buildings, dripping awnings and the alley a Dalek was grinding out of, her view could offer only the police box she had just left -- the derived dock edge was the single edge the map admitted. She looked around a city and was shown a doorway. `connect_orphan_new_rooms` runs in `merge_scene_with_diff` and attaches such a room to where the bodies stood immediately before the diff. Scoped to rooms NEW in this merge and applied as they are created, because that is the only moment the context to place them still exists -- an established map is never rewired, since the justification for a guess is by then long gone. Interiors are skipped: a `parent_entity` room's doorway is derived by `apply_transit_dock_edges`, and a sealed hull is severed on purpose |
| Adjacency resolves both ways for EVERY reader | **Built** | The row above says "adjacency resolves BOTH ways", and it was true of ten readers and false of five. `egocentric_frame` read the observer's own `room["adjacent"]` and nothing else, and everything built on it inherited that -- `spatial_digest` (the narrator's and the character's exits), `room_layout`, the Director's egocentric exits, perception's rear arc -- as did `corridor_sightlines`, `sprint_reach` and `survival.is_sealed_in`. Nothing writes a reciprocal edge and nothing drops one: `_merge_room` upserts only what a diff names, `normalize_scene_bearings` fills a reciprocal BEARING but skips when the reciprocal EDGE is absent, and `connect_orphan_new_rooms` deliberately skips a room something already points AT. The spatial prompt then produced the shape by instruction -- "connect it via adjacency to the room the mover came from" puts the edge on the NEW room, and the adjacency chunk tells the model not to re-list edges an existing room already has -- so the room a story STARTS in can only gain an outbound edge by accident. Measured on the live corpus 2026-08-25: **117 of 374 room pairs (31.3%) declared from one side only**, 88 of them plain `open`/`open_door`; **20 sink rooms with no outbound edge, 9 of them occupied**, one with seven bodies in it. What it cost, reproduced on a fresh run's origin room: `spatial_digest` `{}`, `sprint_reach` `[]`, `corridor_sightlines` `[]`, a runner told `stops: 'dead_end'` about a room with two doors, and `room_layout` naming two doorways as anchors beside `exits: {}` in the same payload. Worst of all a live firewall EXPANSION: `_behind_rooms` was directed while `visible_adjacent_rooms` is not, so an observer with their back to a far-declared doorway kept receiving fresh sight of the room behind them. Fixed on the READ side, where the doctrine already lived and where `_onward_exits` and `effective_anchors` had each already written the derivation privately: `spatial.effective_adjacent` is the room's own edges plus every far-side edge naming it, bearing and verticality reversed, marked `implicit` and written nowhere, and the five readers go through it. The stored graph is NOT symmetrised -- that would repair none of the 20 rooms already on disk, would have to be sequenced against `stamp_sight_direction`, and would add a writer to a subsystem whose doctrine is that silence is never an assertion. A far-side `wall` is not derived, since a non-relation nobody on this side declared would ADD information rather than restore a doorway |
| One-way PASSAGE is a field, not an absence | **Built** | A chute, a drop, a hatch that latches behind you is legitimate fiction, and the barrier vocabulary could not say it: every member of `_PASSABLE_BARRIERS` is symmetric and `one_way_window`'s asymmetry is SIGHT only. So the reciprocal-deriving read above needed a way to tell "deliberately one-way" from "written once" -- and the absence of a reciprocal has never been the first, because writing an edge once is simply how edges get written (31.3% of live pairs, and ZERO of the 16 `one_way_window` declarations among them). `passage_from` is the direction as a FIELD, the exact mould `sight_from` was promoted into for the same reason (`sight_direction`: two declarations that AGREE are one fact written twice, where declaring-side made them cancel). `spatial.edge_passable` is the one question "may a body cross this, from here", and every path a body moves along asks it: `passable_neighbors` (hence `passable_route_exists`, the movement backstops, crowds, couriers), `passable_path`, `sprint_reach`, `_onward_room`'s deeper-interior walk and `is_sealed_in`. Sound, scent, sight and ambience stay undirected -- direction is a fact about passage, and a chute you cannot climb is still one you can see and shout up. A `passage_from` naming neither end of its own edge is dropped at `normalize_scene_barriers`, the same refusal `sight_direction` makes, because a value naming the mechanism instead of a room would otherwise seal the passage from both sides silently. This lands the vocabulary half of `docs/UNBUILT.md`'s "Interior passage is undirected"; one-way passage now has a spelling, and the reaction/dice machinery still owns contested crossings |
| Dim light is a rendering fact up close | **Built** | `dim` used to make an ADMISSION decision — conceal every region of a body — for what is, at contact range, a RENDERING choice: `_LIGHT_SIGHT` maps `dim -> shapes` and `visual_level_between` applied the verdict flat, distance only ever weakening it, so two bodies in continuous contact in a dim room saw each other as silhouettes (chat 70: kneeling over a body, both hands on it, every region `vantage`-concealed while the surfaces were computed and discarded). The ladder of KIND, not degree (design note 18): dim + measured closeness → full admission with the light as Layer-B prose ("The light is dim." already rides the standing environment percept); dim at range → shapes, unchanged; dark → sight fails at every range, deliberately — touch has its own continuous channel and a carried source is `light_at`'s business. NOT solved with a richer authored vocabulary: 349 of 395 live rooms author no light and 40 write `dim` (the `DISTANCE_TIERS` lesson — data everyone writes and nothing can read, inverted), so the rungs come from light × proximity, both computed. The strengthening evidence is CLOSED and positive-only (`spatial._measured_intimacy`): a standing contact between the pair or station-measured `within_reach` — never `proximity_rel`'s "near", which is the documented no-station-data fallback and would un-dim every ordinary room; never cross-room; never dark. One seam: `visual_level_between` composes it, `region_visibility` stays attribution-only, and every admission consumer inherits. Same beat, same pass: the stranger label's cap-cut prepositional phrase is trimmed to its content head ("the towering hooded stranger with smooth" → "…stranger"), and `contact_sensation` gained the identity floor (`label_for`) — it named the OTHER party canonically, which is what the composer tripwire had been correctly scrubbing every contact beat; the tripwire stays as backstop |
| A mind is never told about itself in the third person, by name OR by the label strangers use for it | **Built** | `_self_second_person` had rewritten a perceiver's own NAME into "you" in their own view since alpha 6.3, and a name is not the only handle the engine puts into circulation: `_unknown_actor_label` mints an appearance descriptor for every body an observer has not recognised, and every mind that receives one writes it back out in its own declarations. Live (three-model playthrough, 2026-08-12): the persona read "A young smith's apprentice with a borrowed sword", the composer minted "the young smith's apprentice", `director_resolve` took it into the OBJECTIVE account ("Bryn turns toward the young smith's apprentice" — of the player, whose name the same paragraph had already used) and the cast shortened it in their surfaces, so the player's own view read "eyes settling on the sword at the apprentice's hip" and the narrator wrote him in the third person. `common.self_reference_forms` is the other half of `self_name_forms`: the exact minted label(s) plus ONE short definite form cut from the head noun, guarded by the labels this observer already uses for other bodies (`avoid`), a generic-head stoplist ("the person" is never a shortening — it is what every stranger label is built from) and no indefinite variant. Applied at the two places engine prose is HANDED to the mind it is about: `perception._composer_self_forms` feeding `composer.act_percept`'s admission-time rewrite, and `narration._ordered_beat_events`, because `event_order` is a second delivery of the same prose to the player. `director._report_observer_epithets` reports the objective-record use back through `tell_director` without rewriting the account; the `intended_target` half is an open admission question (`docs/UNBUILT.md` §1.38). Design note 20 |
| Two identical strangers are told apart in prose, not by an index | **Built** | `assign_stranger_labels` widens colliding labels from the bodies' own appearance summaries, and when the appearances genuinely cannot distinguish them it used to fall to `the person of unremarkable appearance (2)` — an ENGINE device in a layer whose whole contract is decision-free PROSE. Live, in the player's own view, three times in one sentence; one narrator model copied it onto the page verbatim and another paraphrased it away, and neither misbehaved. Now ordinal language (`_ordinal_label`): "the second person of unremarkable appearance". Chosen because an ordinal distinguishes by NOTHING the observer has not already got — they can see three bodies, and counting them adds no attribute, no history and no identity, so the firewall constraint is met by construction. Position and posture read better in one sentence and were rejected: they change within the beat (a referring expression must be stable while its referent moves) and they are separately admitted percepts. Words to twelfth, a numeral past that; the widening ladder is untouched and the ordinal stays the last resort. Design note 20 |
| The narrator is checked against the person it was asked for | **Built** | `_narration_person_counts` was called in exactly one place — on the PLAYER's raw input, to CHOOSE `narration_person` — and nothing read the prose that came back; `_check_player_person` catches the player being NAMED, not a draft written in the wrong person outright. `_check_narration_person_match` reuses the same detector on the output (reused, not re-implemented: it already strips quoted dialogue and folds unterminated quotes). Third-person evidence from the player's NAME only — every other body on the page is legitimately "he"/"she"/"they" — and the dominant person must lead the declared one by 2, the same hysteresis `_resolve_narration_person` uses. Measured before shipping over 2,303 stored drafts with the person replayed per turn: 12 warnings, 0.52%, all real. A WARNING and deliberately not enforceable (a rewrite is a whole narrator call, and person is a whole-draft property). It would NOT have caught the epithet row above — that phrase is neither a name nor a pronoun — and says so in its own docstring. Design note 20 |
| The tense the narrator writes in is the author's to choose | **Built** | Tense was UNCHOSEN, not defaulted: measured over 2,803 stored narrator drafts, a story's tense is settled by whatever turn 0 happened to land on and then self-reinforces, and one story ran 41 of 41 drafts in the past without anything ever asking for it. `narration_tense` is a closed-vocabulary `style_guide` field (`present`/`past`) on the `weather_severity` model — normalized rather than trimmed, because it GATES a stage rather than describing it — read per turn, so it can be turned mid-story. UNSET IS THE DEFAULT AND IS A THIRD STATE: no payload key, no instruction, no warning, byte-identical to the behaviour every existing story has. It reaches the opening turn, which is the beat with no earlier prose to inherit from. `_check_narration_tense_match` is the verification half, mirroring the person check: pack-scoped markers resolved at USE time, quoted dialogue stripped first (speech is present tense inside a past narrative by convention), and WARNING-ONLY for the same reason person is — a rewrite costs a whole narrator call and tense is a whole-draft property a correction note cannot patch. Measured before shipping: 57 of 57 correct on a hand-labelled sample of 60, 91.9% of the corpus decided, 1.55% disagreeing with their own chat's dominant tense against a measured real drift rate of 2-4%. Scope is narration alone — characters, memory's own past-tense register and the Director's present-tense state labels are untouched |
| One being has one name (subject identity) | **Built** | A character can be registered cast AND present as a scene entity with its own id, with nothing joining the two records, so the Director writes whichever it reaches for and both are correct. Five separate defects in one investigation were the same consequence — some function compared `"Elyndra"` against `"elyndra_succubus"` with `==`, got False, and did nothing, silently, because every spatial query answers an unresolved subject with its safe-closed default and that is indistinguishable from distance. `same_subject` closed those five and is a FLOOR: it only helps where somebody remembered to route through it, and every new comparison site is a fresh chance to write `==` again. `spatial.normalize_scene_subjects` is the fix — run at merge, before anything resolves one ledger against another, it folds `positions`, `scales`, `attire`, `stations`, `contained` (keys and `in`), `contacts` (actor/target) and `following` onto one spelling per being, after which plain equality is correct because there is nothing left for it to be wrong about. **The scope was the hard part, and getting it wrong was worse than the defect**: the first version folded on identity alone and broke eleven tests, because `positions` legitimately keys objects, fixtures and unregistered presences by entity id and readers resolve them that way — carried lights, derived stations and destruction cascades all stranded. Two rules keep it narrow: fold only when the canonical name is ALREADY live as a subject spelling elsewhere in the scene (the defect is two records for one being, not "ids ought to be names"), and an id differing from its name only by case (`tardis` for `TARDIS`) is its own evidence and does not count. Ambiguity folds nothing — two entities named "A Dalek" are two Daleks, and merging them would be a worse error than either spelling |
| A body sealed inside another body is IN it, not far from it | **Built** | Investigated from a live story with one character fully enclosed in another. The measurement is the whole finding: the relation to the body around her came back `{"same_room": false, "barrier": "separated", "distance": "far"}` — byte-identical to the relation for a window across the room. The engine could not tell the mass she was inside from a draught outside, and every channel therefore read as distance. Four causes. (1) A `positions` value naming an ENTITY rather than a room was accepted silently, because every spatial query resolves an unknown room to its safe-closed default rather than raising, so she sat in a room that does not exist for the rest of the story; `repair_entity_positions` now places the body in that entity's room and records a station AT it, and never infers containment from a typo. (2) `derive_contained_positions` looked its carrier up by SPELLING, so a record naming the entity id found nothing in a map keyed by the display name and skipped, silently. (3) `_body_interior_holder` read only the interior-room form of enclosure while `_hiding_holders` read both, so an occupant got concealment and none of its compensations — conducted sound, flooding scent — and the one voice she was closest to in the world arrived through a wall. (4) `concealed` is symmetric and was standing in for two opposite situations. Now three directions: `inside_source` (perceiver within this source: scent full, voice conducted, sight none), `enclosed_from_source` (perceiver within something else: the room beyond is GONE, not faint), `source_enclosed` (source within something the perceiver is outside: muffled outward, which the barrier rules used to be trusted with and could not do once both sides derived into one room). A perceiver is never sealed from themselves; co-occupants of one enclosure perceive each other normally; and the whole thing is scoped to BODIES via `_is_body_entity`, because a crate is not a mass and opaque is still not soundproof |
| A beat that says nothing about time still costs the clock | **Built** | The story clock is what every windowed thing in the engine compares against -- condition ticks, scheduled events, weather drift, the off-screen epoch, affect decay, strain windows, belief provenance -- and a resolved beat whose `state_diff.time` carried no readable claim advanced it by nothing at all, so they all went quiet together. Measured read-only against the author's corpus 2026-08-25: of 2,614 active non-stale `director_resolve` outputs, **130 (5.0%) assert no time this engine can act on**, and 109 of them sit in six chats of one story line (88:24, 89:24, 90:20, 86:15, 87:15, 85:11). Chat 89 turns 58-62 are five consecutively, with empty player input, while its Director wrote its own note about a transit in progress -- the clock stood at 1098.0 for nine turns and the world with it. `world.mechanics.beat_end_elapsed` is now THE ONE PLACE the end-of-beat clock is computed, and it charges `UNCLAIMED_BEAT_SECONDS = 10.0` for silence -- the p25 of the 2,479 beats that DO declare a positive duration (p10 5 / p25 10 / median 25 / p75 30 / p90 45), taking the quartile rather than the median on `time_diff_duration`'s own stated doctrine that under-ageing is recoverable and over-ageing is not. ASSERTION IS THE OFF SWITCH, so a beat keeps the authority to say no time passed by SAYING it: `duration_seconds: 0`, a re-asserted current position, any claim the reader can act on turns the floor off completely, and the prompts now teach the canonical zero. Only silence and an UNREADABLE claim are charged -- and an unreadable one is charged AND still reported, because a refusal that also froze the world was the double defect; the warning names the floor that advanced instead of reporting a stop. **Three readers, one number**: the scene commit writes `simulation_clock`, `commit_common._monotonic_elapsed` stamps the memory commit's affect/strain/belief windows, and `perception_outcome` merges the mirror the narrator composes -- all three go through the helper, because flooring only the first would put a story's psychology ten seconds behind its world on every one of those 130 beats, which is exactly the three-readers-three-subsets class the shared helper was created to close for backwards beats. `commit_memory`'s `if isinstance(_time_diff, dict)` guard went with it: that guard WAS the silent beat, sending it to the raw previous clock. A deterministic floor rather than a prompt clause, on the standing rule that the floor must not depend on a model cooperating -- the Director that produced the frozen stretch was writing prose about the transit while asserting no time, five beats running. Establish turns never floor. The two read-only `director_floors` VIEWS are a fourth reader and stay unfloored, bounded and registered in `docs/UNBUILT.md`: they lag at most one floor per silent beat and the only decision they gate is a 28,800-second sleep threshold |
| The engine owns the clock's position; a beat contributes a span | **Built** | Measured live, chat 95 second pass beat 2: four people talking on a bridge declared `{start_seconds: 20520, duration_seconds: 45, end_seconds: 20565}` against a simulation clock standing at 20.0, the forward absolute won under the old contract, and a conversation moved the story clock FIVE AND A HALF HOURS while the one correct number in the block -- the 45-second duration -- was discarded. Nothing a model emits MEASURES where the story clock stands: the payload prints the clock to it, but a model is free to run its arithmetic from another zero entirely (this one was working in a time of day the engine never held), and an absolute produced there is a guess wearing the authority of a measurement. So `world.mechanics.read_time_diff` reads every absolute IN THE FRAME THE BLOCK ITSELF DECLARES: `start_seconds` is the block's own anchor, and a beat begins where the clock stands by definition. Anchored at the clock, or declaring no anchor, the claimed end is adopted verbatim -- every canonical corpus row and the chat-88 bare absolutes behave byte-identically to before. Anchored anywhere else, the block is internally coherent and externally meaningless, so only the SPAN crosses -- the declared duration, else end-minus-start -- from where the ENGINE stood, and the commit warns ("anchored away from the engine clock", worded apart from "ran backwards", which becomes the same rule's other face: both are positions the engine did not adopt, reported through one `displaced` slot). A time skip is a duration, so a night lands whole from any frame; `duration_seconds: 0` still says no time passed; the `UNCLAIMED_BEAT_SECONDS` floor still charges only silence. Sleep still measures because `_sleep_elapsed` subtracts its stored start from a beat end that is now always engine-framed -- the three tests that pinned the old contract (a triple anchored at 1000 against a clock at 0.0 yielding 1060) encoded the defect and were rewritten to worlds whose clock and triple agree, each saying so at the site. The residual is the sleep subtraction's OTHER operand: a condition row's `started_at_seconds` is still model-authored (`docs/UNBUILT.md` 1.84a) |
| A mind remembers in TIME, not in beats | **Built** | Recall reached characters stamped "about 2 beats ago" -- 94 such stamps across 54 character calls in one instrumented run -- and they reasoned in the unit they were given ("the same declaration about the door lock ten beats ago"). A beat is a turn index: global play order, the engine's own vocabulary, a frame of the story's CONSTRUCTION that nobody inside a fiction can feel. They used it because it was the only working unit on offer -- across three instrumented runs `simulation_clock.display` read "now" on every call of every beat and `scene.time` ended as the empty string, while `elapsed_seconds` moved correctly the whole time, so the one channel carrying real duration reached nothing that dated a memory. `memories.encoded_at_seconds` (v34) stores the clock reading each row was written at, from the value `persist/commit_memory` already computed for affect decay, strain windows and belief provenance; `mind/memory_time.py` is the ONE ladder that names an interval -- seconds, minutes, hours, days, weeks, months, years, largest unit only, built to the top rung now because a ladder that stops where today's clock stops is found the hard way by the first time skip. STORED AND NOT DERIVED is the load-bearing choice: an age computed as `(now_turn - turn_idx) * (now_elapsed / now_turn)` reads off a MOVING denominator, so re-running one turn with a different declared duration silently re-ages every memory in the bank including rows from beats that never changed, and a branch reads the same memory at two ages. A stored reading is local and rolls back with its own row -- the write path already deletes and re-mints a re-run turn's rows, and the clock is a frame-scoped `world` key not on the checkpoint preserved-list. FOUR PAYLOAD SITES, ONE RULE: the single-memory projection, the summary-window span, the unbidden intrusion and the out-of-band tension pass each carried their own "about N beats ago" arithmetic and now share `MemoryClock`. Two questions come BEFORE the arithmetic and either refuses it: a row from the deciding turn or later is never stamped (the cutoff stays in TURN INDICES, because the clock does not advance inside a beat and so cannot tell this beat's own outcome from the one before it), and a row from another frame gets no number at all, because a clock reading is a per-frame fiction time and subtracting across a frame is arithmetic on two unrelated clocks. A refusal is a qualitative phrase, never a confident wrong number. The v34 backfill charges existing rows `turn_idx * UNCLAIMED_BEAT_SECONDS` -- the rate the live clock already charges a beat that claimed no duration, so old rows come out consistent with new ones by construction rather than by an invented constant -- and it is the one estimate here, with `memory_time.window_clock_readings` the named seam a stored per-turn clock history replaces it at (`docs/UNBUILT.md` 1.85) |

| What time it is, and how long a beat took, are different fields | **Built** | `scene.time` held both at once and could hold only one. A standing TIME OF DAY was written to it once, by `director_establish`; a per-beat PASSAGE PHRASE ("moments later") was written over it by every resolved beat, and a phrase spelled EMPTY -- a beat saying it carried no phrase -- deleted the time of day outright. The phrase won because it was written every beat while the time of day was written once. Measured read-only against the author's 81-chat corpus 2026-08-25: **63 of 80 openings named a time of day a reader could act on and 6 live scenes still held one**, so 57 stories lost theirs; one chat holds the bare boolean `False` there. `simulation_clock.display` is the same field's other half and had the same single per-beat writer, so after beat 0 it could hold a duration phrase or the `wget` default and nothing else -- chats 95 and 96 read "now" on every call to every role for 20 and 15 beats, which is why characters fell back to counting BEATS ("the same declaration about the door lock ten beats ago", 94 recall stamps of "about N beats ago" across 54 character calls). The split is three-way and mostly SUBTRACTION: `scene.time_of_day` holds the standing property and every one of the six readers moved to it; `simulation_clock.display` restates it, so the two cannot drift (they already had -- five corpus chats hold the scene's empty beside the clock's "moments later"); and the passage phrase gets NO field, because it reached no reader anywhere in the tree and survives as the beat's own words on its persisted resolve variant. `director_establish.simulation_clock` was an ORPHAN -- solicited by the schema, filled by 80 of 80 openings, read by nothing -- and is now what the label is taken from, since where the two disagree it is `time` that drifts into circumstance ("Immediate aftermath of a containment breach" beside an "08:42:15 AM"). Two class-level repairs ride along: `time_bucket` reads a clock reading as well as a synonym, guarded so a countdown and a year are not times (17 unbucketable openings -> 2), and the field is normalized on write, so a non-string claims nothing. Existing stories are repaired from their own openings at `db.init` (`_recover_scene_time_of_day`, idempotent, keyed on the key so a story that has moved on is never dragged back): 7 of 84 scene rows carried a readable time of day before, 81 after |
| A body that takes another body inside becomes a place | **Partial** | The engine has always had two accounts of one body being within another, and the row above is about the first: a `contained` record, from which the occupant's position is DERIVED to the holder's own room every merge. The second is a room whose `parent_entity` is the body around it — the occupant stands somewhere, and that somewhere travels. Everything the second needs was built and tested years earlier: the interior/exterior doorway is derived from the holder's own state (`apply_transit_dock_edges`), `membrane` is opaque open or shut, `ambient_scope` stops at the boundary, `_body_interior_holder` and `_hiding_holders` read both forms, and the composer renders the inside like any room. What did not exist was the ROUTE from the first into the second, so an interior a beat declared stayed a one-line ledger entry. Measured over fifteen consecutive audited turns of one story: the holder carried `interior_rooms: []`, `scene.rooms` held ONE room with `adjacent: []`, `positions` put both bodies in it, the entire interior was four free-text keys on the entity blob that no spatial query reads, and the beat's `engine_notices` was literally `["spatial specialist: not_mine"]` — no specialist owned the most important physical fact of the beat. The occupant's composed view ran four sentences, because there was no room to compose. Now: `spatial.place_enclosed_bodies` converts a `mode: interior` record into a real position inside the holder whenever the holder HAS interior rooms — preferring the station a standing interior contact already names, else the entry room the dock rewrite would use — and drops the record, because a body cannot be both standing inside and carried by the same holder. `sync_entity_interior_rooms` keeps the room's own `parent_entity` claim and the entity's `interior_rooms` index in step, which is what makes a declared body interior opaque by default rather than see-through — **scoped to bodies**, because that index is not only an index: `agents/director_scopes.py` gates a Director specialist on it and `persist/commit_scene_state.py` folds it into the set of rooms the mapping stage may not prune, so filling it for an entity nobody had indexed is a behaviour change rather than a repair. Measured read-only against the author's corpus: 53 rooms carry `parent_entity`, 15 of them across 13 chats were unindexed, and all 15 belong to NON-bodies (lift cars, turbolifts, a ship, a police box) — indexing those would have switched the destruction specialist on in five stories and made six stories' interiors permanently un-prunable, silently and untested. Contact hygiene admits an enclosure-joined pair (`enclosure_joins_rooms`) so the touch channel — the ONE channel the firewall permits across an enclosure — survives two bodies holding different room ids, and an interior relation's `target_interior` is re-derived from the room the occupant is actually in rather than frozen at the one they entered through (measured stuck for eight turns). `containment_facts` states the place form beside the ledger form, so the occupant is still told where they are and the holder is still told what it contains. `is_sealed_in` reads `_PASSABLE_BARRIERS` instead of a hand-written two-member copy of it: a body that can walk out through a membrane is not suffocating, however little it can see. **EXISTENCE NOW HAS A PRODUCER**, which for one landing it did not: every consequence of the place form was built and exactly one thing made the rooms -- a clause in a prompt -- so the road had no entrance and the feature fired in no story. `materialize_enclosure_interiors` (`world/spatial_containment.py`) mints the inside from the record itself, at BOTH `place_enclosed_bodies` sites in the merge, under five gates that are each a skip: `mode: interior` exactly, an unambiguous holder, a BODY, a holder the scene places, and no interior rooms already. With no card it mints ONE room -- named from the region the standing interior contact had already declared, dark, prose-free, `dock_exit` stamped at mint so the merge is a fixpoint immediately -- because the record entails that an inside exists and entails nothing about its structure, so a second room would be invented anatomy. Body-scoped for a FIREWALL reason and not a taxonomic one: minting for a non-body yields an `open_door` dock edge and turns an occupant `containment_hides` was concealing into one visible from outside. TOPOLOGY is still authored, and now has a card surface: `embodiment.interior`, a chain of stations outermost-first, normalized in `story/character_schema.py`, carried to the scene entity as `interior_spec` by `agents/common.stamp_authored_interiors` at commit (the merge has no cast to ask), and minted as rooms by the same materializer. **The migration was measured twice, not asserted**: all 80 of the author's stored scene rows (77 plain plus three frame-scoped) were re-merged with an empty diff on this branch and on `main` and compared whole. 76 are byte-identical; FOUR differ -- chats 86, 87, 88 and 89, one story's branches -- and each gains exactly one room named from its own ledger (`mouth`, `mouth`, `stomach`, `throat`), the holder's `interior_rooms` index and `enclosure: membrane`, the occupant moved inside with `is_sealed_in` False, the containment record consumed, and the two bodies' `near` station link dropped by the merge's own non-co-located hygiene. Two of the four store `contained: {}` and mint their record from a standing interior contact at the SECOND merge site, which is why a producer at the first alone would have left them a beat behind. All 15 one-way non-body `parent_entity` rooms across 13 rows are in the byte-identical set. **Partial** for the residuals: no persona interior, no engine-minted interior for a non-body, one-way interior passage, room hazards, and scale-release for the place form -- all registered in `docs/UNBUILT.md` §1.28. **The passage now RUNS, which is the half that made it a place rather than a label.** A room may declare `transit_seconds` -- how long crossing it takes, in story seconds -- and `advance_room_transits`, called inside `merge_scene_with_diff` between contact settlement and station hygiene, carries its occupants one station deeper whenever the simulation clock says the crossing time has elapsed, remainder carried rather than discarded. Derived at merge like every sibling fact (`infer_body_enclosures`, `derive_contained_positions`, `place_enclosed_bodies`), so it is a pure function of (scene, clock) and correct under reroll, branch, checkpoint restore and replay by construction. CONDUIT AND CHAMBER ARE DERIVED, NOT DECLARED: the magnitude IS the declaration, on the three precedents `_tick_interval`, `_schedule_new_arrivals` and news latency already set -- a place that kneads its occupant for hours authors the hours and is crossed slowly, and a `role` enum with no magnitude would give the crosser nothing to cross. Where no magnitude is authored the CONTACT LEDGER decides, in its own two words: a station inside a body is bounded at `_UNSTATED_CROSSING_SECONDS` only while the occupant's standing interior contact says `motion: moving`, which is what makes one corpus branch reading `settled` beside a card measured in hours come out differently from its sibling reading `moving`. Direction is DERIVED and refuses to guess: strictly farther from the way in, by breadth-first walk over passable barriers RESTRICTED to the holder's own interior, so a branch, a shut door, a deep end or a room outside an enclosure moves nobody -- and the walk cannot eject anyone, because the exterior room is excluded by construction rather than by a check. A bound reached with nowhere onward reports the missing fact to the Director ONCE and then SUBTRACTS the claim the world cannot support, settling the motion: a crossing nothing is advancing is not running, and inventing the next station would be invented anatomy. `materialize_named_stations` is the on-ramp -- and it does not graft onto an AUTHORED chain mid-way, because a declared chain is an ordered document and a region it does not name has no position in it: measured on a scratch corpus copy with the card filled, one branch's ledger named an omitted region, the occupant was placed at the entry station, and the next merge minted that region DEEPER than the entry and walked her outward into it for fourteen beats. Past the last authored station deeper is the only place a new one can go, so a story that continues past its card still grows a chain. Placed before contact hygiene because `_restation_interior_contact` re-derives `target_interior` from position and was therefore erasing every newly named region: a body ALREADY inside whose beat names a region no room matches gains that station, chained by `membrane`. Migration measured, not asserted: all 78 stored scenes re-merged with an empty diff on the branch and at the base commit hash byte-identical 78 of 78, because the pass is a hard no-op without a clock. **AN AUTHOR DECLARES THE ANATOMY ONCE; THE ENGINE DERIVES EVERY TRANSITION FROM IT** -- the sentence the structure and the clock were both built for, and which nothing could reach until the structure had a filler and the mint had a successor. `story.importers.fill_body_interior` (`POST /api/characters/{cid}/fill_interior` and `POST /api/chats/{cid}/characters/{ch}/fill_interior`, prompt chunk in both language packs, the editor widget the field never had) reads a card's OWN prose ONCE at authoring time and proposes the chain with its magnitudes: the third sibling of `fill_character_psychology` and `fill_appearance`, writing nothing, INSTRUCTED not to invent an inside the prose does not document (most bodies have none, and an empty list is the complete answer -- a prompt clause under author review, since nothing deterministic tells an extracted chain from an invented one; the floor under it is the siblings' own, that the proposal is visible row by row in the widget and the author's ordinary Save is the only write), and REFUSING any proposal that would drop, rename, reorder or untime an authored station -- the first fill surface whose output is a MECHANISM rather than a description, so a silent loss re-plumbs a body rather than degrading a paragraph. **IT IS OFFERED ON BOTH CARDS, because the fill has to land in the sheet the story actually reads**: `scene.active_cast` resolves `chat_chars.sheet` OVER `characters.sheet`, and measured read-only 2026-08-25, 13 of 116 `chat_chars` rows carry a per-story sheet -- including every story of the one line whose card documents a route, so a reusable-card-only surface would have been pressed, reviewed, saved and read by nothing. The story-card editor's Save already PUT the field; what was missing was the read. Neither rejected alternative was built: no runtime prose-duration parser (shaped by whichever card it was written against) and no per-beat model choosing the next station (the guessing that crossed three stations in four turns in one chat and stalled nine beats in its sibling on identical input). `replace_engine_minted_interiors` is gate 5's successor rather than a hole in it: gate 5 walks `contained`, which `place_enclosed_bodies` has already popped in exactly the scenes that need the chain, so the replacement walks ENTITIES beside the mint. It fires only where the standing interior is EXACTLY ONE room carrying exactly the key set `_mint_minimal_interior` writes -- a whole-key-set test, not a field-by-field one, because chat 91's stub carries Director-declared `exposure` and `size` and a looser check would delete them -- mints the authored chain FIRST so nobody can land in nowhere, relocates each occupant by casefolded station name, retargets the contact ledger, and SKIPS ENTIRELY when an occupied mint's name matches no authored station, because carrying a body outward to the way in is worse than leaving the mint standing. `stamp_authored_interiors` now runs in the perception mirror as well as at commit, on the same two scopes: it ran at commit alone, so the mirror would have composed the beat a chain lands on from the interior the commit was replacing -- composed and committed disagreeing about which room a body is standing in, on the one beat that matters. Migration measured again on both sides of the change: every stored scene re-merged with an empty diff and every stored sheet re-normalized, byte-identical, because the pass keys on `interior_spec` and 0 of the 79 stored sheets carry a non-empty `embodiment.interior` until an author runs the fill |
| The enclosure directions fire on the paths that deliver | **Built** | The row above built the vocabulary; nothing in production spoke it. `spatial_rel_between` was the ONLY setter of `enclosed_from_source` and `source_enclosed` and had zero non-test callers — the purest form of AGENTS.md invariant 4, a guard that cannot fire — so a voice sealed inside a body reached the whole room at full clarity through both injection floors, an enclosed perceiver heard and smelled the room it was sealed away from, and the comments on `hear_level`'s enclosure branches cited measured live failures the branches could never again catch. It is now THE body-to-body relation builder (with `observer_room`/`target_room` overrides so callers keep their uid-tolerant room resolution): `_source_channels`, the onset perceiver build, the outcome dialogue fallback rel, the micro-loop, `_delivered_manifest` (a voice/breath tell no longer crosses a body seal that muffles the voice itself), and background hearing all route through it. Four masked siblings closed in the same pass, each the same anatomy — a graded fact computed in `world/spatial.py`, consumed as a coarser projection at the delivery site. The outcome dialogue floor gained the `proximity` the onset floor always passed, via `measured_proximity_rel`, which forwards a tier only when it is a MEASUREMENT — "near" is the fallback in the ~91% of rooms with no stations, and a default must not silence a conversation (the whisper lesson). Sight of an actor is graded by the light in the ACTOR's room: `spatial_rel` stamps the light of the room being looked at, and the onset build and micro-loop passed the arguments backwards, granting a full visual channel to an actor standing in darkness. `visible_adjacent_rooms` gates on `effective_light` — a pitch-dark neighbour's authored description is no longer delivered as literal sight, while spill keeps a lit doorway's cellar visible as dim. And `_audience_map` fails CLOSED on unresolvable geometry, as its sibling `_beat_for_presence` always did and as its own comment falsely claimed it already did. Every fix subtracts; the by-name comm rescue is the one narrowing worth naming — the shape floor no longer pierces an enclosure, because being named by a voice beyond the mass around you creates no channel through it (explicit `medium:'comm'` still does). `tests/test_masked_floor_leaks.py`: 17 assertions fail on the pre-fix tree, 8 over-subtraction guards pass on both |
| Something smells, and a nose finds out | **Built** | Scent was a permission system with nothing to permit. `scent_level` graded reach as `none|muffled|full` off a barrier TABLE, cards could author a nose from anosmic to preternatural, `sense_adjusted` shifted the grade, perception computed `scent_channel_to_sources` for every perceiver on every beat, `CHANNELS` declared `"smell"` and the narrator gave it a manifest row — and no card, entity, substance or diff anywhere said what anything SMELLED of, so no builder ever emitted the channel and the manifest row read "open air; nothing ledgered rides this channel" on every beat of every story. The fact now lives on the record the thing already has, with no fourth ledger: `embodiment.scent` beside a body's stable appearance, `SceneEntityDef.scent` beside `light_source` (what a thing emits on the other channel), and `scent` on the substance record, which is the commonest smell in play and needed no new op — the contact specialist already owns `substance_ops`, so "the Director can emit a scent" is one more key on a record it already writes. Each carrier had a trap already sprung on the field beside it: `scent` is DECLARED on the entity (an undeclared field does not survive `model_dump()`) and listed in `_ENTITY_DEFAULT_FIELDS` (outside it, the validation `None` on every beat that did not re-declare the oven overwrites its smell instead of reading as silence — what made `enclosure` settable only at creation), and it sits beside `amount`/`detail` on the substance record rather than among the identity fields, so a re-description updates one pool instead of filing drying blood beside fresh. **`muffled` is finally spent**: the three-value grade had two values that were the same value downstream, and what a half-open door withholds is not the smell — the material crosses — but WHICH BODY it belongs to, so a muffled scent is delivered unattributed and so is a full one from a body in the dark. **A scent does not defeat a disguise**, because it carries a material and never a name: the label is whatever `observer_display_map` already earned, so an identity-concealing disguise yields the stranger's descriptor here exactly as it does for presence and pose, and the mind may still conclude from its own memories whose smell it is — legitimately, and defeasibly, since two bodies can wear one perfume. An entity's smell is never attributed at all (the composer admits no percept for the objects in a room, so naming the oven would be this channel delivering what no channel gated) and a substance is attributed to what it is ON, never the body it came FROM. Fixed in the same pass: `ambient_percepts` read `channel`/`room` while both packs' prompts ask for `kind`/`source_room`, so all 199 stored sensory events fell to `mixed` and were delivered to every observer in the scene regardless of room. Decay, drift, trails and multi-hop reach are deliberately unbuilt and argued in `docs/design/DESIGN_SCENT.md` |
| An actor sealed inside something is not shown themselves from outside | **Built** | `observable` is the intent-free surface of an act as seen FROM OUTSIDE, and the actor normally receives it in their own view — rewritten to second person by `_self_second_person` — because people can see themselves doing things. Sealed inside something that stops being true: the surface then describes the outside of the enclosure and there is no channel from inside to it. Live: the declared observable described cloth shifting and bulging around a small body, and the actor's own view came back naming the shape she made, in darkness, two clauses after her own narration said she could see nothing. The property holds, but **not by the function this row used to credit**: `_self_cannot_see_own_surface` was reachable only from `_inject_onset_sequence`, which had no production caller, and both have since been deleted. What enforces it now is the composer path, by a stronger and blunter mechanism — an actor never receives their own act surface in their own view at all, enclosed or not, because `_composer_act` builds no perceiver for the acting player and `_composer_outcome`'s act loop skips the observer's own act. The retired guard was keyed on being ENCLOSED and deliberately not on darkness or a failed sight check — proprioception is not sight, and suppressing an actor's own conduct every time the lights went out would be a worse error than the one this fixes. Every other perceiver's view is untouched: the surface is exactly what they can see, which is what it is for |
| The touch-only firewall does not fail on a spelling | **Built** | `_touch_only_sources` decides who a perceiver can feel but not see, and its answer is what triggers `_surface_translate_event` — which replaces the omniscient `resolved_event` WHOLESALE, because free prose cannot be security-matched. It compared names with casefolded equality, and one being routinely carries two names at once: a cast display name and a scene entity id. An enclosing character matched as `Elyndra` against a holder recorded as `elyndra_succubus` was not matched at all, so no translation fired and the unfiltered omniscient event — which names the occupant's own interoceptive state in as many words — went into her payload and out into her view. That is AGENTS.md's own-body isolation rule breaking on a string comparison, and it fails OPEN and silently. Every identity test in that function goes through `spatial.same_subject`. **The rule this generalises**: an engine in which one being can carry two names cannot use `==` to decide who anybody is — five separate defects in this investigation were that one comparison |
| An interior does not show you its own outside | **Built** | An entity's `description` is its EXTERIOR -- what a body in the room around it takes in. Handed to its own occupant it reads as a thing across the way. Live (chat 58, t38): the player stood in the TARDIS console room and her view read "...while a blue police box -- its paint darkened by rain -- settles with a heavy thud on the cobbles". The plaza beyond the open doors was correct; the police box was the one she was standing in, landing, seen from inside itself. The engine already knew the relationship -- `_body_interior_holder` resolves a room-parented interior and `spatial_rel_between` sets `inside_source` from it -- but that flag only ever conducted SOUND, and nothing withheld the exterior. `_perceptible_entities` now drops `description` for an entity whose interior the perceiver is standing in. Only the outward appearance goes: the entity itself stays, because presence is not the leak and the room's own `parent_entity` already says what you are inside |
| An act that happened reaches the page | **Built** | `event_order` is described to the narrator as "the engine's numbered record of what actually happened this beat", and for everyone except the player it was speech-only: `_ordered_beat_events` collected `type == "speech"` from every character's declared sequence and silently discarded `type == "action"`, so no physical event a character performed was ever listed. Live (chat 52, t26), across three rerolls: the Director resolved one character carrying another downward, perception delivered the direction correctly ("a steady pressure that pulls you downward"), and all three narrator drafts dropped it — the beat's only physical event reached the last stage as one clause buried mid-paragraph in the view, competing with the room's furniture, carrying `fidelity: "ambiguous"`, with nothing anywhere requiring it to survive. Two of the three also *halted* an ongoing motion the view said was continuing ("pauses mid-stroke" against "moving in a slow circle"). Acts now enter the record, gated on `visibility == "overt"` and the `_player_sees_character` test `co_present_positions` already uses, carrying only the Director-authored `observable` surface and never the private `attempt` with its intent and appraisal in it. Fails CLOSED with no scene or no player room. `_check_action_direction` backs it deterministically at two confidences: a REVERSED direction is enforceable, an absent one is a warning only, because correct prose can carry a descent with no directional verb in it |
| A background presence is somewhere | **Built** | `canonicalize_positions` folds a uid position key back onto the display name only for a REGISTERED cast character, and says so — "unregistered background presences are left untouched". Correct, since they are not cast; but nothing mapped the name back the other way, so a presence placed under its entity uid was unreachable by name from the moment it was placed. Every reader asking where a background speaker stood got None, and `spatial_rel(None, room)` answers "remote, no known spatial channel". Live (chat 58, t23): a machine in the player's own alley with its weapon trained on her chest sat in `positions` under `40af0ac4bf2644a1`; `cast_room` returned None, perception's hearing gate classified it as remote and dropped its line for every observer, and the view rendered it as "something" and "the source" rather than the thing she had just thrown a rock at. Corpus-wide, 47 of 78 background lines never reached a single view. `cast_room` now falls through to `entity_room_by_name`, matching `scene.entities` on name first and aliases second so a nickname cannot outrank a real name, and resolving an ambiguous name to NOBODY — two of the same machine in two rooms is precisely the case that must not be guessed, and a wrong room is worse than the None they all used to get. A background presence's ACT also reached no record: `_ordered_beat_events` read only `dialogue_log_entry` from a reaction, never its `action`, whose shape (one prose string, no `observable`/`visibility` pair) the character path cannot see. It is now collected under the same perceptibility gate. What the presence is CALLED is still wrong — see `docs/UNBUILT.md` §1.19 |
| A background presence is ONE presence | **Built** | A scene entity carries two spellings of one identity — the opaque id keying `scene.entities`/`scene.positions` and the display name in its def — and the presence ledger tracked both: the dialogue-speaker harvest folded ids to names, the positions harvest did not, and the entity harvest looked its station up by name against an id-keyed ledger and missed. Live (chat 80): SIX presences for THREE things, every body doubled, and the id-keyed twin of a ceiling-mounted suppression device accrued its own dialogue turns and its own separately-minted human blurb while the name-keyed record held the mentions. Every harvest in `track_background_presences` now folds through the beat-aware id→name map, `_resolve_or_mint_presence` resolves before filing, and `_fold_duplicate_presences` merges id-keyed twins on load (an entity id denotes exactly one entity, so unlike the article fold there is no crowd ambiguity; the body's current display name wins the `name` attribute), so a split story heals at its next commit with no migration; the gate, the stage, the manager roster and the promotion list all read through the fold so the twin cannot be dispatched even before that. `tests/test_presence_identity_and_speech.py` |
| A presence is tracked on an id, its name is an attribute | **Built** | `background_presences` was a dict KEYED BY NAME, so two people sharing a name were ONE RECORD — one memory, one dialogue history, silently merged — and the stable identifier was deliberately discarded on the way in (the tracker resolved entity ids INTO names). Measured over the live corpus 2026-08-26, 91 records across 37 chats already held every failure mode the key produces: one shared full name away from a merge (two families of tracked surnames), the same person under two spellings (`station engineer`/`station_engineer` — actually THREE spellings of one body, provable by shared entity binding), raw entity ids stored as names (three in one chat), and objects tracked as people. The ledger now keys each record on a minted uid (`p_` + 16 hex; deterministic in its binding so pre-commit readers and the commit writer agree), `name`/`aka` are attributes, and `presence_record_for` is the PERMANENT name→record resolver — models speak names forever, so name resolution is infrastructure, not migration scaffolding, and it refuses when two records answer to one name (attribution warns and files nothing; promotion raises rather than welding two people's evidence into one sheet). A legacy bank migrates on load in three tiers — charter binding, provable single-entity binding (merging spellings on ID AGREEMENT only), fresh mint — per `docs/UNBUILT.md` §1.17's settled doctrine that ambiguity refuses to merge. What the key deliberately does NOT close: objects-as-presences (an object has a valid entity id; the speech/nature verdicts remain that guard, now resolving the entity by the record's own binding) and id-shaped display names (refused as identities via `presence_is_unnamed`, awaiting a real name). `tests/test_presence_uid_keying.py` |
| An unnamed person draws one permanent name from the story's own law | **Built** | `presence_is_unnamed` marked a record awaiting a name and nothing minted one. `story/naming.py` is the story-level naming law -- authored `naming_profile` world key (GET/PUT `/api/chats/{cid}/naming_profile`, checkpoint-preserved as SET_BY_HAND) outranking the Charters' lore-derived `naming` laws (each a separate LANE, pools never blended across cultures) outranking pools harvested from the registered cast and the lorebook's `character` entries -- and there is NO fixed default table: a story yielding no law mints nothing, per `charter_identity`'s doctrine that the engine never invents a culture. THE MINT IS A WRITE, NOT A RENDERING: `persist/commit_background._mint_missing_presence_names` runs in the ledger's one writer, names exactly the records `presence_is_unnamed` marks (empty, or an id-shaped string standing where a name should) that clear the speech verdict's person bar, writes `record["name"]` once with the former spelling kept resolving in `aka`, and never touches a role-descriptor name -- renaming "the barkeep" would be the engine reaching for the field, the exact act permanence forbids. Deterministic in (chat, presence uid), so a replayed commit re-lands the same name and a REPLACEMENT -- a new body, a new uid -- draws a new one while the old name stays with the old record; the no-fly list spans persona, `characters.name` and every tracked spelling, so the generator never introduces a name collision itself. Validated read-only on the corpus copy: chat 67's three raw-id "names" mint harvested-vocabulary names, byte-identical across processes; chat 93 draws from its Charter's lore-derived law; a story with no material mints nothing. Residuals in `docs/UNBUILT.md` §1.89. `tests/test_presence_name_minting.py` |
| A minted person never arrives under a registered mind's address | **Built** | `_refuse_name_collision` has stated the rule since promotion existed -- "Names are how this engine tells minds apart" -- and was wired to ONE of the engine's two minting paths. The other is `world/charter_identity.materialize_body_names`, which names a whole institution's population at once from a naming law the location planner derived from the SAME lorebook that supplied the cast, so its pools arrive holding the cast's own name elements. Measured 2026-08-27 over the five populated charters in the live corpus: chat 95's 42 generated bodies drew from a family pool carrying three registered surnames under a formal address of `{rank} {family}`, and two landed on them -- one story holding a captain and an ensign whom everybody calls by the same word; chat 83 held the same contamination latent under `Dr. {family}`. One answer now serves both paths: `story.naming.registered_identity_names` reads the two wells the promotion guard trusts (the persona and `characters.name`), `charter_identity.identity_reservation` turns them into the forms a mint must refuse, and `name_is_reserved` is the single predicate. A WHOLE name is always refused; a single ELEMENT is refused only where `address_components` finds the story's own law calling people by that element alone -- under `{given} {family}` the pair is the address and two people may share a family the way two people do, which is why the subtraction touched nothing in chats 84/93/94 and removed exactly `Picard`/`Crusher`/`La Forge` and `Moon` in the two that were wrong. The subtraction lands twice and never expands: `strip_reserved_pools` takes the element out of the law a charter PERSISTS (so `story/naming.py`'s Charter lane stops offering it to the presence mint too), and the allocator refuses a candidate that reaches one anyway. A body that arrives already NAMED is untouched -- a featured resident is placed under the registered character's own name on purpose. `tests/test_naming_collision.py` |
| A population larger than its authored name space stays distinct | **Built** | The naming law drew every body's family uniformly from a finite authored pool, so pool size silently became population law: measured on chat 95's 42 generated bodies, a twelve-family pool recurred across the whole population -- four bodies carried one registered character's surname between them (the crew table in `docs/UNBUILT.md` §1.84b), each recurrence reading as a household the fiction never authored, and one body doubled a single pool word into both halves of its own name. The allocator (`world/charter_identity.materialize_body_names`) is now capacity-aware: each `spread_components` element -- family, for the kinship a recurrence asserts, plus any component `address_components` finds the law using as a whole address -- is handed out breadth-first, authored pool values before the extension lane, reuse only once the whole reachable space is spent. The extension lane (`extension_profile`) is the profile's own `*_parts` syllable machinery -- previously dead code whenever a pool existed -- filled by the author, or else derived deterministically from the pool's own words (`derived_name_parts`: a word's vowel-final opening chunk plus another word's remainder, single-chunk words and unsplittable scripts contributing nothing), so every generated value stays inside the law's phonology and a story with no law still mints nothing. `components_repeat` refuses a doubled component on every path including the exhaustion disambiguator, and `story/naming.minted_presence_name` runs the same extension as a second round after its pools are spent. Names remain writes: a bank generated under the old pool-only allocator keeps every stored name, and its concentrated surnames are counted as spent so later bodies spread away from them. `tests/test_naming_capacity.py` |
| A generic name is never made out of a named person | **Built** | The mint's material was a model's. `refuse_harvested_pools` emptied a generated law's name POOLS -- a pool is a list of whole name elements and where a model wrote it from a story's lore those elements are the story's own people -- and kept its FRAGMENTS, on the stated premise that "a fragment names nobody however well a model knows a canon". Measured 2026-08-28 across three consecutive generations of one institution whose lorebook carried a large named cast: two of the three supplied `family_parts.starts` that were, entry for entry, the openings of the cast's own surnames (one list 100% so, including an element of a character REGISTERED IN THAT CHAT), and the third supplied ordinary fragments touching nobody -- variance in what a model volunteers, which is exactly why the guard cannot be the model. No whole name was reconstructed (the `NAME_RUN_FLOOR` novelty floor held) and the material was still the cast, cut up; reproduced here, one body came out as a registered person's surname EXACTLY, assembled from a three-letter opening and a two-letter ending, past every guard -- the novelty floor permits an exact share on purpose and the element rule only fires where the law addresses people by one component. Neither was wrong; the MATERIAL was. So the same rule now runs at the fragment: `charter_identity.fragment_is_name_element` refuses a fragment that opens or closes a name the story gives to somebody, or that such a name opens or closes -- anchored at the ends for the reason `_reserves_component` already anchors there, floor `NAME_ELEMENT_FLOOR` = 2 because one letter is the alphabet. **The refusal is paired with the material that replaces it**, because it is capable of taking a whole field (it takes all nine family openings of the measured law) and a naming defect turned into `generated lived location did not provide a usable naming law` is not an improvement: `_fill_empty_material` refills an emptied field from the law's own other field first (a start is a start, and it stays inside one law's phonology), then from the setting's words for places and institutions (`vocabulary_name_parts` -- a place is not a person), and a law left with nothing assemblable at all keeps its subtracted pools exactly as it did before this refusal existed, so no story that generated yesterday fails today. Both measured generations reach that branch never. The other half is that named lore people SHOULD still appear: `charter_generate.lore_cast_residents` places them as real background residents under their own names, untouched -- the rule is about the generic POOL, not about who may walk on. `tests/test_name_material_partition.py` |
| An author can fix a story's names by editing one entry | **Built** | `world/charter_runtime._record_phonology` wrote a generated law's fragments back into the story's lorebook as a `phonology` entry -- the artifact that makes the one model judgement reviewable, since the objection to model-made names is opacity at the SAMPLER and does not apply to a judgement made once, in the open, that a person can change. Nothing read it. `phonology_parts` was called only by `phonology_law_exists`, `phonology_law_exists` by nothing at all, and `story_naming_lanes` did not list the lane -- so the entry was written, never consulted, while `_record_phonology`'s own docstring asserted it "is the one source the mint may read", a sentence that was false the day it was written. `story.naming.phonology_lanes` is now a real lane, ranked authored > phonology > charters > harvested: above the stored Charter law because it is the same material in the form a person can edit, below an author's explicit `naming_profile` because a list of names is a stronger statement than a list of sounds. ONE ENTRY IS ONE LANE, never blended -- two settings' sound systems stitched together belong to neither, the same reason pools are never blended. And every fragment is re-checked against the story's reserved names on every read, because the entry is written by the generator as well as by a person and, unlike a pool, a fragment set CLAIMS to name nobody. A lane whose material does not survive that check stays silent and the next authority speaks. `tests/test_phonology_lane.py` |
| Only a person may hold a background speaking turn | **Built** | The gate read only the ledger, and the ledger says nothing about what a name DENOTES — so a `kind: "device"` record with an accrued history qualified exactly like a barkeep, and chat 80's Scranton Reality Anchors were picked at their "post" in the interview cell on turns 3 and 7 and interrogated the restrained player ("You're not restrained. You're contained."), rendered as "the unfamiliar person" because a device has no name anyone knows. `commit._presence_speech_verdict` reads the presence's own frozen answer first: `blurb_mint` already visits every newly tracked presence once, batched per room, with the place, the Director's description and the genre in front of it, so it now settles `nature` (`person`/`thing`/`voice`) at NO additional call and the answer is stored beside the blurb. That prompt used to open "you give personalities to background PEOPLE" and was handed a list assembled by name — so when it met the Anchors it wrote them a manner of talking and a standing grievance, and the phantom that interrogated the player was wearing a personality the engine had commissioned. A `thing` or `voice` is now stored with no personality at all. An unanswered `nature` is deliberately NOT a yes; it falls through to a graded fallback that asks the scene: an animate kind (whole or word-wise) or no entity record at all is a person; a bodiless voice (`is_ubiquitous_entity`) or a thing (`_is_inert_presence_candidate`) never speaks through this stage — the resolve prompt has always said bodiless voices are the Director's own mouth, and the routing guard now honours that instead of routing a thing's line to the stage that would refuse it; a kind that decides neither ("device" cannot be lexically told from a live "dalek war machine") speaks only on the Director's explicit judgment this beat (`routed_to_background`, `flow.addressed_to`). Those kind lists are the FALLBACK and nothing more — `_INERT_ENTITY_KINDS` had reached 50 entries and `_ANIMATE_ENTITY_KINDS` 35, and the space of nouns a story can invent has no end, so a list was never going to finish. The scene-manager roster admits persons only, the promotion flag refuses things, and the person-shaped common case (night clerk, barkeep, guards) is pinned unchanged. `tests/test_presence_identity_and_speech.py`, `tests/test_presence_nature.py` |
| A field name can never become an entity | **Built** | `state_diff.entities` is keyed by whatever string arrives, and the objects specialist's map arrived carrying its own sibling field names one nesting level too deep — the resolve path has hoisted misplaced siblings for ages, the specialist path never did. Live (chat 80 turn 1): six "entities" keyed `remove_entities`/`inventory_ops`/`artifact_ops`/`destruction`/`notes`/`resolved_events`, each a verbatim copy of the Interview Chair, committed to the scene as furniture. Three layers now hold the line: `preprocess_llm_output` hoists a specialist's own declared siblings out of `entities` (and DROPS entity-def-shaped debris under a sibling key — hoisting a chair copy would have turned it into a `destruction` declaration), `spatial.merge_scene_with_diff` refuses `schemas.NON_ENTITY_FIELD_KEYS` (computed from the models' own declarations) in both the entity and position ledgers and thereby heals a standing scene at its next commit, and `scene.get_scene` reads the debris out for everything in between. `tests/test_entity_sibling_keys.py` |
| A thing that exists is somewhere | **Built** | An entity carries no location: `SceneEntityDef` has no `room` field, `world_entities` has no room column, and an entity's room is `scene.positions[<id>]` and nothing else. The hand that MINTS things cannot write that channel — `entities` belongs to the objects specialist and `positions` to the spatial specialist, they run in parallel on scopes documented as having "nothing to say to each other", and a `positions` key emitted by the objects hand is dropped by pydantic before assembly sees it, silently and with no `outside_scope` note. Measured over every active Director variant on disk: single-hand `director_establish` places 96.5% of what it mints (278/288), monolithic `director_resolve` placed 35.7% (752/2108), and the orchestrated fan-out places 5.8% (85/1465) — 78 of those 85 being bodies that also carry an entity record, so genuine non-body objects placed by the fan-out across the whole corpus number about seven. The evidence was being written anyway, into `inventory_ops`, which had NO reader in `persist/` or `world/`: two instrumented runs made 67 objects calls, emitted zero `positions` keys, and filed placements like `{op: place, object_id: …, to_id: <a room>, relation: on}` into that dead channel — which simultaneously ACQUITTED the omission at the reconciliation seam, so the detector reported the placement encoded while the object was nowhere. `spatial.derive_inventory_placements` derives it at merge, the same move `derive_scene_stations` makes: the ledger the models do maintain seeds the one they do not. Three subtractive rules — a destination that does not resolve against the scene writes NOTHING (never a raw token into `positions`, which is the category error `repair_entity_positions` cleans up after); a destination that resolves to a BODY writes a CARRIER RELATION rather than a position, so `hiding_holders_of` keeps a pocketed thing concealed instead of making it nameable by the whole room; a null endpoint is silence, never an erasure. A FOURTH, added 2026-08-29, subtracts nothing and only speaks: a transfer naming an object the scene keeps no entity record for still places nothing (the pass holds a token a model reached for and no name, kind or size to mint from) but now writes a Director-facing sentence saying which handover it could not write down and what would let it. Re-merging all 2758 recorded (scene, diff) pairs on disk, that refusal fires on 26 beats across seven chats — 26 possession facts the engine resolved and then filed nowhere, every one of them previously silent. The verb is never read and body-ness is never read off `kind` — `_is_body_entity` derives it from attire/scales, the same refusal the transit code already made. `_evidence_present` no longer acquits an `entities` or `inventory` item whose subject ends the beat in no room, and `director_floors._unplaced_minted_entities` reports what is still nowhere by MERGING and asking `room_of`, so the floor cannot drift from the placement rules. The transfer ledger answers only for a beat that wrote one, and the larger class is a thing minted with NO transfer at all — measured over one audited 15-beat run and the two before it, every minted entity committed with `room: None`. `spatial.derive_minted_entity_placements` runs immediately after the transfer derivation, over the entities THIS beat named, and reads the ledgers the other hands did fill: a station's anchor (where it was set down), either endpoint of a contact naming it (who handled it, and which room they are in), and a transfer's ORIGIN where its destination resolved to nothing. It yields to a position or containment declared by name, to the carrier relation the transfer pass writes, and to any room the thing already had; it places no body and none of the three classes with no room by construction; and a handler the scene keeps out of sight lends no room at all, so a pocketed thing is never given a position its holder does not have. `tests/test_entity_placement.py` |
| A ledger outranks the prose that mentions the same thing | **Built** | `scene.poses[x]["detail"]` is free text that qualifies an arrangement, and it is the one scene field nothing re-derives: written once, rendered verbatim into every view that can see the body — the body's own interoception line included — and standing until some later beat happens to overwrite it. When that prose says a body is CARRYING something it stops being only an arrangement and becomes a possession claim, and possession is what `inventory_ops` is for; the two were reconciled by nothing. Measured (chat 98): the establishing beat minted no entity for the object the whole opening was about and wrote its entire existence into one pose detail; four beats later the Director resolved a complete transfer of it to another body; the detail did not move, and five beats after she let go the giver's own composed view still read "… — holding <it> against chest", in her interoception and in every other observer's sight line, with the narrator writing it into the prose twice. `spatial.invalidate_transferred_pose_details` retires the carriage clause when the transfer ledger says the thing left that body — the `detail` ALONE, because posture, support and the relation fields are the body's own and no transfer touches them. Subtractive throughout: the destination body keeps its own carriage prose, a detail that does not name the thing on a word boundary stands, and a detail naming it without carriage vocabulary stands (watching a thing is not holding it — the vocabulary is `_CONTACT_BOUND_POSE_WORDS`, the one list `invalidate_contact_bound_poses` already asks its own question with, rather than a second competing one). This beat's own pose declarations are checked too, unlike the scale-change retirement's `stated` exemption, because `poses` and `inventory_ops` come from different specialists on disjoint scopes so neither is the later word. Re-merging all 2758 recorded (scene, diff) pairs on disk changes exactly one scene. `tests/test_pose_possession.py` |
| The Director can see what the engine did | **Partial** | `scene.contacts` and `scene.stations` now reach the resolve payload — a stage asked to maintain a ledger it cannot see writes fresh every beat rather than re-asserting, which was the cause of the contact drift alpha 6.3 repaired downstream. `ctx.tell_director` carries the deterministic layer's READING of a beat's output onto the next beat's `engine_notices` (a re-described contact taken as the same limb moving; an attire note taken as dressing someone). `_evidence_present` is now category-aware rather than a containment check — each omission class asks whether the subject is touched in the RIGHT dimension of the diff, which is what closes the partial-encoding trap (a room whose desc was updated while its narrated adjacency change was dropped passes bare containment and fails the `adjacency` class), and every class was widened to accept every spelling the diff legitimately uses after a live census read 5 of 11 manifest items as omissions against diffs that encoded them: garment handles as well as wearers for attire, structured endpoints over the free-text subject for contacts, a `cross` op's ended endpoint, a station for a within-room drop. A wording gate turns coverage into a coin flip per reroll, because the manifest's `subject` is model-worded. Still **Partial**, and for the other reason: most silent discards elsewhere in commit say nothing at all, so the Director learns what the engine READ and not what it threw away |
| Within-room position (stations) | **Built** | `scene.stations` {name:{at:anchor_id|null, near:[]}} against per-room `anchors`, deriving `proximity_rel` (within_reach/near/across), co-located left/right (`entity_side`), the rear-arc blind spot (`entity_arc`), whisper range, and surface comfort. Designed in Phase 2 and **inert in production until alpha 6.3**: `stations` was declared on neither `StateDiff` nor `ScenePatch`, so Pydantic's `extra="ignore"` deleted the field before `merge_scene_with_diff` — which has always merged it — could see one. Measured: 0 of 45 live scenes carried a station while 17 model outputs across the same database contained one the schema had thrown away, so every co-located pair read `near`, `entity_side`/`entity_arc` returned `None` universally, and a body lying on a bed drew no warmth from it. `RoomDef` declared neither `anchors` nor `size` either, so the Director could not author, update or preserve an anchor and the only anchors any story had arrived through the mapping stage's untyped patch dicts. Because models fill contact reliably and stations essentially never, `spatial.derive_scene_stations` seeds the ledger from the one they do maintain: a contact with an anchor-backed entity stations that body there, a body-to-body contact makes a mutual `near` link. Additive, idempotent, never argues with an explicit statement, and a derived station outlives the contact that produced it — you do not leave the bed by taking your hand off the quilt — while a room change still clears it. "On the bed" is a station AT it plus a contact WITH it plus that body's own `state.posture`; no fourth ledger |
| One limb, one place (contact identity) | **Built** | A contact is keyed on (actor, actor_part, target, target_part), all free text the Director writes fresh each beat — so a re-description read as a second limb. Measured over 17 live beats: `thumb→ear` became `thumb→ear_base`, `hand→waist` became `hand→side`, `tail_spade→calf` became `tail→ankle`, and the character was told as standing truth that one woman had two hands and two tails on her. Fixed by reading an unqualified part noun as a definite description: `spatial._part_identity` splits a part into (kind, instance), `_same_appendage` recognises a refinement that repeats the limb's own word (`tail spade` is that tail; `thumb` is not a `hand`), and a fresh spot for the same limb RETIRES the old one. Two carve-outs keep the range: anything asserted in the SAME beat stands (two hands are said in one breath), and a bare noun never displaces a qualified limb nor the reverse — once the fiction has distinguished her left hand from her right, losing the distinction is worse than carrying a hold ageing will retire anyway. Contacts gained a bounded `detail` ("beneath her shift", "feather-light"), which is what the parts alone could not say and therefore what the Director had been smuggling into entity `state` |
| Contact has exactly one record | **Built** | `contacts_from_entity_state` lifts contact out of an entity's own `state` and deletes it, so the ledger is the only account — but until alpha 6.3 it required the contact verb in the KEY NAME and the value to slugify to a bare person, and every contact assertion in the measured story evaded both (`hand_position: beneath_Hinami's_shift_caressing_bare_side`, `tail_spade: curled_around_Hinami's_ankle`, `lips: trailing_kisses_along_Hinami's_jaw`). Entity state neither ages nor prunes, so all of them stood contradicting the ledger for the rest of the scene — one mouth simultaneously two inches away, trailing kisses along a jaw, and kissing. Pattern B now lifts a verb found in the VALUE when the key names the part, with a direction guard so `leaning_over_X` stays the bearing it is and `gaze` is never touched; leftover words survive as `detail` rather than being deleted with the key. Lifted holds go through `apply_contact_ops`, so they obey displacement too. Pattern C drops a relational key (`lips_distance`) only where a standing contact already speaks for that part: the ledger ages and prunes, entity state does neither, so the ledger is the record — and a key nothing contradicts is left alone, because dropping it would be inventing an absence |
| A garment is one garment (attire identity) | **Built** | The wardrobe keyed garments on `name.casefold()` and the Director writes names fresh, so a redescription forked the garment: measured live, `"sheer obsidian silk robe"` against a registered `"sheer obsidian silk robe that parts with every movement"` left one body wearing two robes across four regions, one of them halfway off because the reconciliation was adding one back while removing the other. `attire.resolve_garment` resolves every incoming handle against what the body already wears in four narrowing tiers (exact, article-stripped, phrase containment, then head noun only when exactly one worn garment carries it — an ambiguous handle resolves to nothing rather than a coin flip, so a silk robe and a cotton robe stay two robes), and `dedupe_regions` heals an existing fork on READ, so the ~49 stored scenes repair lazily rather than through a migration that would rewrite stories mid-play. The fork's actual trigger was `app.attire_put`: the region editor stored the browser's body verbatim, so a hand rename left `wearing` naming the old spelling — it re-derives all three representations now. `flat_state` names a spanning garment once, as `flat_wearing` and `newly_removed` already did. The STORED shape is now rebuilt on the write path too, not only healed on read: commit rederives `state` from `flat_state` unconditionally and keeps a prior note only when `attire.is_derived_state_note` says it was authored. It previously dropped a stale derived note only when the old string was a SUBSTRING of the new one, so the moment a garment re-covered a region the containment test failed and the stale note survived as though authored — chat 76 held three contradictory `bare at the ...` notes on one body at once, invisible to every reader (all of which pass through `rederive_entry`) and visible to the attire panel, exports and checkpoints, which read the blob raw. Unconditional because a body dressed again derives NO notes, and that is exactly the beat the last one must leave on |
| An off-schema clothing change is read, not dropped | **Built** | `StateDiff.attire` had an untyped inner dict and `persist/commit.py` reads exactly `wearing`/`add`/`remove`/`replace`/`state`/`conditions`; every other shape validated cleanly and then changed nothing. Two of the six attire diffs in the measured story were silent no-ops, one of them `{"shift": "linen shift, hem rucked up where Elyndra's hand slipped beneath"}` — which is why that story's narration described the hem of a shift and the waistband of a pair of shorts in one paragraph, the ledger still holding the travel clothes seeded off the card. A typed `AttireDiff` now canonicalizes through `attire.coerce_diff_shape` (also run at commit, because rerunning a stage replays diffs stored before it existed) and `commit.interpret_attire_notes` reads an unrecognised key three ways: a garment she wears → what just happened to it; the wardrobe as a whole → prose the body keeps, unless it says nothing changed; a garment the ledger has never heard of → she is wearing it now, with the one-rung rule and region tables applying as normal. `AttireState` also gained `regions`, undeclared until now — so the opening turn's authored garment descriptions and `beneath` text, the richest clothing detail any story gets, were stripped by the validation round-trip on every body in every story |
| A wardrobe changes where the beat says it does, and the ledger says which garments | **Built** | The write gate: `remove`, `coverage`, `placement`, `regions` and the omission half of `replace`/`wearing` apply only on a beat whose own words are about that body's clothing (`attire.wardrobe_change_subjects`), and `resolve_garment` then says which garments the change touches. The gate exists because the write itself read nothing while every other axis read the prose — chat 78, nine turns of an interrogation in which not one beat's words name a garment, and the wardrobe was rewritten twice: a `coverage` block restating the whole wardrobe as "covers nothing", then a `remove` of two garments nobody touched, which together put a body's `beneath` prose on the page under garments that were still on. Its SUBJECT is the body, not the garment, and that is the correction: asking the beat to spell each garment as the ledger spells it makes any paraphrase of undressing unapplyable, because prose names the OCCASION of a wardrobe change and the ledger names its inventory. Measured (chat 98 t8): "She changed out of uniform" resolved to "She changes out of her duty uniform into civilian clothing", the body specialist emitted a `remove` of all five worn garments spelled exactly as the ledger held them, and four were dropped for not being named — then the omission guard put the same four back, and the body wore the outfit it changed out of AND the one it changed into for the remaining thirty turns while the scene entity said the uniform was off and the prose said it was gone. The per-garment tier could not be repaired in place: it counts a word only while ONE worn garment carries it, and that ledger carried the same coat under two authored spellings, so "uniform" named neither. Failure direction is stated rather than assumed — a phantom change destroys ledger state and never re-derives, a refused one costs a beat ONLY if the Director restates, and in the measured run it did not; subtraction is safe where a guard withholds information, and this one withholds objective state. `add` is deliberately ungated (it cannot expose a body, and gating it would refuse the first dressing of a body whose arrival is the beat), and a `remove` naming a scene ENTITY id rather than a garment is told so in those words — a garment that came off became that object, and an object id is never a wearable name. `tests/test_attire_write_gate.py`, `tests/test_attire_ledger_licence.py` |
| Establishment states one moment, not a passage | **Partial** | An opening scenario usually narrates a SEQUENCE — a character-card greeting always does, since that is what a first message is — and the `director_establish` prompt was written end to end as if it described a standing situation. Nothing said which instant is t=0, so bodies froze at different ones. Live (chat 53, "Run!", five beats: cornered / rescued / hauled inside / lever thrown / turns to look): the player stood mid-motion at the doors from beat 2 while the character stood at the console with the lever already thrown from beat 4, and the creature the whole opening was about kept a present-tense "weapon charging with a rising whine" from beat 0 — a vortex away, in no room, its charge still rising. The prompt now names the last moment as the one being established and requires positions, stations, postures, activities, entity `state` and transit phase to agree with it; anything the passage finishes is past, recorded as consequence in `world_facts`, never as an in-progress posture. **Partial** because a prompt rule is unverifiable from source: the two structural halves below are the parts a test can hold |
| An agent the opening declares has a room | **Built** | `semantic_output_errors("director_establish")` checked exactly two things — rooms non-empty, positions non-empty — so an entity could carry a full description, a live `entity_states` entry and the scene's only open `world_pressure` thread while appearing in `positions` nowhere at all. That is not cosmetic: `agents/background.py` drops an unplaced presence by construction ("cannot prove co-presence, leave out"), so it can never be perceived and never acts, while the pressure ledger keeps demanding every later beat advance a threat with no location. `schemas._unplaced_establish_entities` now fails the step. Scoped by an ALLOW-list of animate kinds, deliberately the opposite shape to `commit._INERT_ENTITY_KINDS`, because a semantic error here aborts an opening after one repair and the fallbacks: measured across all 48 live scenes, 17 carry an unplaced non-portable entity and 53 exist in total — framed diplomas, a shoe rack, a captain's chair, a bell tower, ward doors, a day-room television — so a deny-list would have killed a third of openings to tidy furniture. The allow-list yields exactly one hit in that corpus, and one in 57 stored `director_establish` variants: the creature |
| An opening can express a hold | **Built** | `DirectorEstablish` had no way to say two bodies are touching, so a greeting whose entire event was a hand seizing a wrist and hauling a body through a door committed with `contacts: []` — the grab had never happened, and the body it moved had never been anywhere else. `contact_ops` is now declared on `DirectorEstablish` (undeclared is deleted by `extra="ignore"`, the failure that kept `stations` inert for 45 scenes) and routed by the establish tail into `state_diff.contact_ops`, so it reaches `spatial.apply_contact_ops` through the same merge every later beat uses — including `derive_scene_stations`, which reads fresh contact ops. Only holds still true at the chosen moment: a hand that grabbed and then let go is a `world_fact`, not a contact |
| Arriving inside something is not entering it | **Built** | `infer_threshold_crossings`' enclosure branch fires on `was_hidden != now_hidden`, and on the opening turn `prev_scene` is empty — so every body standing in any interior read as having just climbed into it. Live, that gave BOTH occupants of a vehicle a `{from: X, to: X}` crossing, including the one whose vehicle it was and who had not moved. Requiring a previous position fixes it for the same reason mid-story: a body that joins the cast already enclosed was not watched going in, because it was not there to be watched. Inert in effect — a crossing only ever *floors* sight — but it was the engine's sole trace of anyone entering anything, and it was fabricated |
| Multi-room movement (running) | **Built** | `spatial.sprint_reach` bounded by decision (one way onward), not sight; `spatial.passable_path` reconstructs the rooms crossed so a sprinted corridor is remembered; `agents/character.sprint_offers` truncates the *offer* to known ground. Per `docs/design/DESIGN_RUNNING.md` |
| Retrieval as perturbation (unbidden recall) | **Built** | `memory.contrast_memory` surfaces one high-salience *dissimilar* memory when deterministic stuck-ness signals fire; confidence-blind, edge-triggered, substitutes for one ordinary recall slot. Adapted from SIGMA SRIP-14 §XXII — see `docs/guides/RESEARCH.md` §1.5. Dissimilarity was structural only (tokens, location, entities, turn distance) until alpha 6.3.1, correctly: on a crc32-embedded corpus cosine is a fuzzy-lexical signal and would only have restated the token penalty. Real vectors say what the token axis structurally cannot — that "the alley smelled of wet brick and chip fat" and "the backstreet stank of damp masonry and frying grease" are the SAME memory, not a perfect contrast — so a semantic term joins the structural one. Gated on 90% model coverage of the bank, and that gate is not optional: an incomparable row scores 0.0, which in ordinary recall is a silent omission but on an INVERTED axis reads as maximally contrasting, so unbidden recall would preferentially surface exactly the memories not yet rebuilt. A story mid-rebuild degrades to the old behaviour rather than to a wrong one. **And a crc32 batch is refused outright** (2026-08-20): the coverage gate passes when the sketch is the CONFIGURED embedding, since then the keys match, so the hash was feeding an axis that rewards distance -- nominating rows for unbidden recall by coin flip while reporting a semantic reason. The paragraph above already accepts the degradation this causes; `search_memories` makes the same refusal for the measured reason (289 -> 346 of 470, UNBUILT 2.21) |
| A summary keeps first-hand, hearsay and surmise apart (P8) | **Built** | Consolidation melted every provenance into ONE autobiographical string fed back wholesale each turn, so a belief the character *inferred* came back a few turns later indistinguishable from something they had *witnessed* — belief laundering into knowledge inside a single mind, the same layer collapse the engine polices between minds. `_PROVENANCE_SCOPE` routes the six provenance classes to three scopes (`witnessed`/`remembered` → autobiographical, `heard`/`told`/`read` → hearsay, `inferred` → surmise), each its own `memory_summaries` row, each reaching the character under its own label (`what_i_experienced` / `what_i_was_told` / `what_i_concluded`). Three rows rather than a provenance tag per sentence, because the summary is model-written prose and a tag inside prose is a convention the model can drop — a separate row cannot be. The first-hand row is written even for a window that produced nothing first-hand, because its `end_turn_idx` is the consolidation cursor; skipping it on a hearsay-only window re-consolidates the same memories forever |
| A character can distinguish remembered past from the present | **Built** | Present observations use `current:<perceiver>:<n>` (micro-rounds add a nonce); raw memories use `event:<hash>` and summaries `summary:<scope>:<end-turn>`. Current view/mood/goal no longer appear under `memory`. Model-visible rows are an allow-list with explicit `temporal_status`, `memory_form`, relative `when`, and `epistemic_origin`; database ids, access counters, archive/vector metadata and scores stay host-only. Output is physically split into `present_evidence_used` and `memory_evidence_used`; appraisal has separate present evidence and past `memory_modulation`. Grounding drops cross-lane/invented refs, forbids derived summaries from independently reinforcing durable state, and requires present support for nonzero somatic/goal/relationship changes. A recalled danger or pleasure can still create a realistic mild echo: grounded `somatic_echo` and `threat_bias` are capped to 0.2, persist for one beat as `active_state.memory_echo` with `temporal_source: remembered_past`, and cannot become current pain/pleasure, injury, goal impact, or proof of danger. Controlled chat-38 result after one deterministic phrase-scoring correction: semantic answered 7/7 vs lexical-only 5/7 with 100% grounded citations; semantic retrieval reached relevant evidence 5/5 vs 2/5 and earlier windows 5/5 vs 0/5. |
| A character can deliberately recall without making recall a default action | **Built** | An exceptional private `{type: "ponder", query, why}` sequence item is normalized out of the public action stream, so the Director, perception and narrator never treat thought as conduct. Commit stores one bounded query on that mind; its next character turn runs a four-item `deliberate_recall` lane on top of unchanged normal recall. Results carry `retrieval_origin: deliberate_ponder` (or both normal and deliberate origins when the same row was already reached), then the old query is consumed. A concrete reason is mandatory and ponder is absent from the default output shape. A result may raise a genuinely new query immediately; merely receiving results is explicitly not a reason to do so. |
| Psychology spends memory bandwidth honestly | **Built** | `cognitive_absorption` narrows deliberate recall from 12 recent / 16 old / 2 windows to 8/8/1 and then 4/4/0, preserving a small automatic-recognition lane rather than making pain or pleasure cause amnesia. Past can modulate familiarity and coping in bounded form, but cannot author present novelty, bodily sensation, or goal impact without current evidence. `remember_lines.why` survives minting; disputes address a stable delivered ref with present cause; `memory_effects` distinguishes retrieval from influence and feeds unbidden-recall outcome telemetry. All fields accumulate across dialogue micro-rounds. |
| The character contract asks only for what commit reads | **Built** | The 2026-08-11 output audit measured, on 401 recent-era stored calls, two dice-class transcriptions in the template and cut them. Ten stress/hedonic fields were requested where commit reads two — `hedonic.released` (the discharge declaration, rightly the character's own) and `stress.coping_mode` (a pass-through label) — and `psychology_runtime.resolve_stress`/`resolve_hedonic` recompute the rest from the appraisal plus prior state; the dicts were emitted on 71% of calls at a mean 78 tokens of which ~12 were signal. `active_state.goal` was overwritten with `wants[enacted].want` on 99.0% of calls and matched it only 16.2% of the time. The template now asks for `stress:{coping_mode}`, `hedonic:{released}` and no goal; `agents/common.declared_goal` is the single derivation every raw-variant reader goes through (the moves ledger, commit's unbidden "did the goal move" check), commit's malformed-wants fallback keeps the PREVIOUS goal rather than blanking a standing aim, and legacy emissions stay schema-valid and read identically. Proof standard: a legacy full-shape result and a slim one commit byte-identical character state (`tests/test_character_contract_slim.py`). The observation wrapper got the same treatment on the input side: six near-constant fields (intensity at its 0.35 base in 99% of 1,692 stored observations, suddenness/fidelity 99%, ambiguity 89%, `source_atom_id`/`perceiver_id` 100% redundant, ~356 wrapper tokens against ~188 of text) are omitted at their resting defaults (`composer.OBSERVATION_DEFAULTS`) — absent means the default, ids/text/channel never trimmed, non-default signal kept byte-for-byte. `considered_responses` is deliberately NOT cut: engine-unread but plausibly chain-of-thought; a `contract_bench`-style A/B is the gate (docs/UNBUILT.md §6.9) |
| A second model call says it happened | **Built** | The repair ladder (`llm_quality.complete_validated_json`: truncation re-ask, temperature-0 repair, per-candidate fallback) re-issues a full provider call, and a retry that SUCCEEDED left no stored trace — so the invisible-second-call rate could only be bounded from its failures (14 "repetition retained" notes in 401 recent-era calls, a floor of ≥3.5%, true rate unknowable, with the live benchmark's 1.25–1.50 provider calls/turn against 1.01 stored results/turn leaving ~8–15s/turn unattributed). The largest single contributor to that gap has since been REMOVED rather than instrumented: the character stage's decision-review retry no longer exists (`e629d60`), so the remaining invisible calls are the repair ladder's, the Director's player-act authority retry, and the extension campaign-violation correction — all three of which guard BROKEN output and all three of which stand. Every rung now writes one warning line — which path fired, and its duration — through `pipeline_context.current_warning_sink`, set by `agents.runtime.compute_step` beside `current_step_key` so a note raised deep inside `llm_quality` is attributed to the running step and rides the stored variant's `_engine_notes`; outside a pipeline step the sink is unset and noting is a no-op. The audit's order of operations stands: measure with this before designing any bounded-delta retry |
| Implicit prompt caching gets a replica to land on | **Built** | Explicit cache markers exist for Anthropic models only (`_anthropic_system`, `_openai_system_message`); everything else relies on provider-side implicit prefix caching — and Fireworks documents that theirs works **within one replica**, with serverless routing scattered unless the client hints placement via the standard OpenAI `user` field or an `x-session-affinity` header. Uncached, a character call re-prefills the ~15.5k-token role prompt plus ~8–11k of payload every time; cached input is discounted 50–90% (Fireworks' GLM-5.2 page: $0.14 vs $1.40/M) with TTFT reduction up to ~80%, so whether the hint lands is worth more than most output-side trims. `_apply_cache_affinity` adds `user: "sonder:<role>"` on both OpenAI-compatible body builders (sync and async; the SSE paths consume those bodies). **The value carries no content and no identity** — an engine role constant, never a chat, character, or input-derived string, because this field goes to a third party on every call. Role-only rather than chat+role, deliberately: the character prompt is name-substituted 32 chars in so cross-character sharing is nil either way, one replica caches many prefixes, a single-host engine's per-role traffic is far below one replica's capacity (measured 1.01–1.11 character calls/turn), and the retry paths inherit the same replica for free. Fail-closed opt-in via `cache_affinity_allow` (name or kind, the `prompt_cache_allow` idiom) — unset means every request is byte-identical to before — and the 400-retry strips the field rather than losing the turn to a host that rejects it. Measurable end-to-end without new code: both SSE paths request `stream_options.include_usage`, `_normalize_usage` reads `prompt_tokens_details.cached_tokens` (the Fireworks dialect) beside the Anthropic fields, and `_log_usage` logs `cached_tokens`/`cache_write_tokens` per call — writes with no subsequent reads is the signature of a prefix that is not stable. NanoGPT documents implicit caching for "OpenAI and Gemini model families plus many open-source provider/model routes" and explicit control for Claude only; whether GLM routes (TEE especially) cache is UNCONFIRMED — the hint costs nothing there and the usage log answers it empirically |
| Checkpoint / rollback | **Built** | `persist/checkpoints.py`; branching depends on it. A checkpoint is a full PRE-turn snapshot, and it used to carry every memory's two float32 vectors inline -- so the same vector was re-stored on every turn for the life of the story. Measured on a live database: checkpoints were **94.5% of a 4.4 GB file**, `memories` was 98.9% of each checkpoint, and the vectors were 96.9% of that. One story held 40,224 memory copies across 118 checkpoints and **529 distinct by content** -- 76x duplication of 1.00 GB that needs 13 MB. Vectors now live once in `memory_vectors`, addressed by `memory.vector_address` — `v1:` plus a sha1 over the vector BYTES — and the checkpoint keeps a reference. It was briefly `sha1(char_id, content)`, on the reasoning that a vector is a pure function of the memory; it is, but not of its `content`, and the compaction verifier caught the collision in production (checkpoint 855 of chat 36 held "You are in Ten Forward." at turn 42 and again at turn 44, same character, two different payloads). Byte-addressing makes a collision impossible by construction rather than by assumption and still deduplicates 69x. Nothing is re-embedded: this changes where the bytes live, not what they are. Real conversion: **448.6 MB -> 44.0 MB (10x)**, 14,820 vector copies to 300 distinct, 0 unmatched. |
| Compaction cannot lose a memory | **Built** | Rewriting rollback history is the one maintenance job where a silent error is unrecoverable, so losslessness is enforced rather than intended. Work happens per STORY on an in-memory duplicate; every checkpoint is compacted into a candidate while the stored blob is untouched; each candidate is verified field-by-field against its original -- every top-level key, every memory entry, every scalar, and every vector reference **resolved back to the exact bytes it replaced** (`_verify_no_loss`), which is the question a restore will ask. Only if EVERY checkpoint in the story verifies are vectors and blobs written, in one transaction. Any failure names the story, discards its candidates, leaves its originals byte-identical and moves on -- proved with injected loss: `cannot compact 'Corrupt Story' -- entry 2 resolves to different vector bytes`, originals unchanged, and zero vectors written for it. The duplicate is held in memory rather than as a cloned chat: same guarantee, without copying a gigabyte, and with no half-written copy to clean up if the process dies. It also refuses to start when there is no legacy data -- a no-op run still walks every checkpoint and takes the write lock per story, and on this path the safest run is the one that does not happen. |
| Consolidation, salience-weighted hybrid retrieval | **Built** | `consolidate_character_memory`; keyword + embedding search in `mind/memory.py` |
| Which mechanisms actually fire | **Built** | `tools/fire_rates.py` — per-mechanism fire rate over the corpus, `--last N` per chat, read-only. Its discipline is the DENOMINATOR: `memory_disputes` against every memory row reads 0 of 6,480 and means nothing, because the field did not exist for most of that corpus; against the beats that could have carried one it reads 0 of 181, beside a sibling introduced in the same commit, on the same 181 results, firing 78%. A mechanism with no opportunities reports `no chances`, never 0%. Four mechanisms in this engine were built, documented, tested and never ran once, and none looked dead from reading the code; three were found only because somebody went looking by hand. This is what makes that cheap. It found a fifth on its first run — see the project tier row |
| Salience discriminates the same amount in every bank | **Built** | `memory._rank_normalized_importance` rank-normalises `effective_importance` across the rows a search can see, inside their own p10-p90: ordering and influence budget both hold, only the gaps move. Measured first (`tools/salience_replay.py`, 270 real recalls, top-16 membership moved): term deleted **35.2%**, percentile-normalised to [0,1] **59.6%**, stretched 3× **47.0%**, respaced **15.2%**. So the term was never decoration, and both planned fixes for the measured compression (p10-p90 spread 0.27, 70% of rows in 0.6-0.8) moved retrieval MORE than deleting it — values in a 0.27-wide band mapped onto [0,1] gain ~3.7× the influence while reordering nothing, a weight change wearing the word normalisation. The defect actually fixed: discrimination depended on how the minting model happened to spread its numbers that day. Callers asking an absolute question (archiving, contrast) still read `effective_importance` |
| A memory that turned out to matter is found | **Built** | `commit._cited_memory_ids` reads mind-model evidence, belief evidence, and `memory_effects` with disposition `integrated`. It read only the first until 2026-08-03, which is why importance had been revised on 9 memories of 6,480: measured over the 83 results that could supply any candidate, mind-model evidence citing a stored memory fired 6, belief evidence 1, `memory_effects` **74**. The one field being read was the rarest thing a character emits. `resisted`/`dismissed` still do not count (pushed away is not turned out to matter) and `only_unrevised=True` still holds each row to one lift for its whole life, so this widens the population, never the amount |
| A mind can re-read what a memory meant | **Built, never occasioned** | The wire is proven end to end by `tests/test_dispute_reachability.py`, which builds the occasion the corpus never produced — a stranger remembered as kind, seen this beat picking a pocket — and walks a model-shaped dispute through schema coercion, grounding, the commit collector, `record_dispute` and the projection back to the mind, including its three refusals. So 0 of 181 is an absence of occasions: nobody in these stories has been deceived. The prompt was the other half — two prohibitions and one abstract permission, beside `memory_effects` at 89% which names four concrete occasions. CLAUDE.md records the same shape twice from the maze arms: bare prohibitions invert. It now names five |
| A summary clause says what stands behind it | **Built** | `memory_summaries.support` (schema v25), one entry per clause as `{claim, support_refs, epistemic_origin}`, derived host-side by content-word overlap against the window's own memories at consolidation — no model call, because an audit trail produced by the same kind of process it audits is not one. Scoped to the summary's own epistemic class so a first-hand clause cannot be supported by hearsay; `epistemic_origin` is left blank rather than defaulted when nothing supports it. Refs are `event_key`s, so rollback and branching need no remapping. An empty support set is the finding — the clause generalises, compresses, or was invented, and it is now countable. Summaries cannot reinforce durable belief, which contains most of the danger, but they move appraisal and speech and used to leave no trace when they did |
| What a character keeps, measured before it is limited | **Built** | `tools/remember_lines.py`, over 1,633 turns and 146 marks: **0 of the 125 that became rows** would have been caught by the fixed phrase list, and marked lines are retrieved later at **30.4%** against 9.3% for every non-dialogue row. No overlap with the list at all, 3.3× the baseline yield. A budget and a novelty gate were both considered and refused on those numbers. `why` is present on 146 of 146 marks and so predicts nothing — a constant is not a signal. The discriminator needs no schema change: the phrase list is a pure function of the quote, so a kept line it would have rejected is one only this character preserved |
| A traveller remembers where they have been | **Built** | `frames.is_memory_visible` honours the travellers list of the frame a memory was FORMED in as well as the one the character is standing in. It checked only the latter, and the present is the implicit frame — `frame_id is None`, no row, synthesised by `get_frame(None)` with an empty travellers list — so that clause could never fire in the present: a character who visited the year 5000 and walked home could not recall one moment of it. Everything while standing there, nothing once back where they live. Safe to widen because every caller arrives through `visible_memory_rows`, which has already filtered `char_id=?`; the only question this rule answers is whether a mind may reach its OWN experience. A native of the present is not a traveller of the future frame and still sees nothing. Five time-travel shapes covered in `tests/test_time_travel_memory.py` |
| The player's carrier envelope belongs to the era it was filled in | **Built** | A cast member's carried reports ride the frame-scoped `chat_chars`/`chat_char_frames` state. A persona has no such row, so `persona_carrier_state` was one world row answering for every era at once — the single carrier home outside `FRAME_SCOPED_WORLD_KEYS`. What the player witnessed in one era survived a rewind or a branch, and `couriers.run_couriers`' "the sender must hold the report" check passed in an era that never produced it: a firewall leak with a schema underneath it, which is why it was the one migration the 2026-08-18 repair wave authorised. Now frame-scoped and read through an EXPLICIT frame, because a bare `wget` redirects on the AMBIENT frame and is the caller's era only by accident. SCHEMA_VERSION 30; the v29→v30 migration re-keys each report by the era that acquired it, and was measured first — zero such rows on the owner's install, so the data migration is a no-op there and the guarantee is for stories written from here on |
| How much a mind holds at once is authored | **Built** | `psychology.capacity` — `narrow / focused / ordinary / broad / wide` — scales the want and intention caps (1/2 to 5/6) and narrows one further at the top of the absorption range. Measured cause: the want cap binds **79.6%** of live banks at mean 2.65, so it was not a safety valve catching an outlier, it was the shape of every character's attention, authored once by whoever picked 3. Precedent is `theory_of_mind.sheet_capacity`. `ordinary` is exactly the pair that shipped and unset is stored as `""` rather than backfilled — backfilling made "the author chose the middle" and "nobody has seen this field" the same value and silently killed the import warning written to prevent that. The character is told its own ceiling (`self.attention`) rather than having wants culled without being told the decision existed. **Projects stay off the ladder**: `PROJECT_CAP` is a dramatic limit, and six slots would lose the displacement rule that makes one mean anything |
| Commit as sole persistence boundary | **Built** | `persist/commit.py`; one outer transaction, any domain failure rolls the turn back |
| **Player action absolute** | **Built, then deliberately exceeded** | See [Player authority](#player-authority) — a considered product divergence, not drift |
| **Event-linked stance axes** | **Partial** | `trigger_event_ids` accepted but **optional**; relationships live in a `world` KV blob with no change log |
| **Canon lock** | **Partial** | `lore_entries.canon_locked` is settable via the API and chat-canon entries auto-lock after 20 turns; the specified repeated-reference lock rule is not implemented |
| A lorebook can be silenced without being detached | **Built** | `chat_lorebooks.enabled` had been in the schema since the beginning, honoured by every reader that matters — retrieval, checkpoints, the portable archive, both browser payloads — and written by nothing but the `1` five INSERTs hardcode, so the column had exactly one reachable value and the whole read path around it was dead weight. It is a different act from detaching, which is why it earns its own route rather than folding into one: detaching a story-OWNED book deletes it and its entries, while disabling it drops it out of retrieval and leaves every entry, link and uid where it is — so a host can turn a whole body of lore off for a stretch of story and back on later without losing the citations that already point into it. `PUT /api/chats/{cid}/lorebooks/{lid}` refreshes the turn's checkpoint on the way out, for the same reason attach and detach do: retrieval is snapshotted with the turn, so a reroll would otherwise restore the old reach |
| **Scene-boundary coherence pass** | **Partial** | Validation and dedup exist throughout commit; the specified retcon protocol is not implemented as such |
| **Off-screen world ticks** | **Partial** | The low tier is built as specified. The `stochastic` rung is a real seeded draw against standing intentions (`offscreen.stochastic_ticks` — `random.Random(seed)`, no model call, same seed same ticks) taken on one frame-scoped `offscreen_epoch`. The `reactive` rung stores only grounded same-beat declarations and later fires only pre-adjudicated effects. Fired mechanics rows promote into checkpoint-safe `world_events`; physical C carriers expose public surfaces only to bodies that were in a position to take them. Three kinds of body now qualify, and the clause used to name only the first: a registered character standing at the location when the surface lands; the PLAYER, whose envelope is frame-scoped because a persona has no `chat_chars` row to carry it (`carriers.persona_entry`); and a body ARRIVING later at a surface still standing — consequences fire off-screen on a clock in rooms chosen because nobody is in them, so a witness-on-the-beat rule left one measured 20-beat drive with one public surface, zero acquisitions, and a character walking into that room the next turn to look directly at a barred gate while learning nothing, forever. A standing crowd trades news both ways with a caravan at its stop on the same physics. The paid `character_agent` ceiling is built end to end: a card must explicitly opt in via `simulation.offscreen_agent`, `full_agent_candidates` selects a dormant mind only for its own active plan or carried evidence newer than its last paid tick (bounded by the actor cap, reading no player/omniscient content), and `offscreen.schedule_agent_ticks` runs one reduced turn per candidate out of band — the fail-closed `agent_context`, one character call, one Director adjudication whose consequence passes `mint_consequences` into `scheduled_events` under a stable id, and one atomic landing (own-trail movement, plan advance, log record, event-keyed autobiographical memory) guarded by epoch, base turn, and a per-subject `last_epoch_id` stamp so a reroll or restore replays rather than duplicates. Epoch, plans, carriers, and provisional logs all follow restore/frame boundaries, and fire-rate denominators are retained. Courier/letter carriers are built (`story/couriers.py`: engine-minted anonymous riders with positions on `passable_path` routes, clock-driven movement, a perception surface, and question/silence interception that actually stops delivery). Caravan and artifact carriers are built on the same physics (a `stops` list on `courier_ops` makes the body a caravan — it dwells at each stop on the clock and trades news both ways with the standing crowd, surfaces and notices; `story/artifacts.py` makes a claim physical — posted where the poster stands, acquired only by reading, stopped by tearing down, worded out of band by the rumor-ledger ceiling). Still open: re-contact settlement — `docs/UNBUILT.md` §2.8 and `docs/design/OFFSCREEN_WORLD_ARCHITECTURE.md` |
| **Player authority modes** | **Built** | Hard mode. `scene.player_authority` per chat (default `world_author` = today's behaviour), enforced in `director_interpret` by `agents.common.apply_player_authority`: an asserted effect the chosen rung does not grant is downgraded on BOTH representations at once — the claim's scope `effect`→`intent` (so `_player_claim_findings` stops holding it non-rejectable) and the sequence element's `commitment` `asserted`→`contestable` (so the reaction gate lets the character it was done to contest it). The ladder is the one the enum names: own body granted by every rung, a completed effect on the world by `explicit_outcomes` and above, an actor-less world assertion by `world_author` alone. Nothing is deleted — the declaration stays verbatim and the refusal is carried into the SAME beat's resolve payload as `downgraded_assertions`, because `tell_director` reaches the next one. `PUT /api/chats/{id}/player_authority` chooses it and records the change with its turn |
| **Predictive staging** | **Not built** | No pre-staging of lore or NPCs for likely-next locations |
| **Reactivation negotiation** | **Not built** | No gap-history / delta-summary proposal, refusal caps, or "stalemate eats canon" |
| **Story and interface language packs** | **Partial** | A story chooses an installed pack (`language_runtime`, `language_packs/<id>`), and the pack owns every engine-authored human-language surface: system prompts, deterministic recognition tables, compositor Layer-B wording, authoring defaults and browser copy. The stored protocol stays canonical English — schema keys, enum values, step ids and ledger vocabulary are never translated, which is what lets one deterministic engine read objects written by any language. Layer A is untouched: a renderer adapter receives only admitted percepts plus render state, never scene, DB or identity, so wording cannot widen the information budget. English is unchanged by the extraction, with one addition. Re-verified 2026-08-17 by diffing alpha8.4.4 against alpha9.0 in parallel checkouts, four ways: 28 of 33 prompts byte-identical and **no prompt body altered**; 20 of 20 rendered surfaces byte-identical (presence clauses, speech at every barrier by every volume, character and player views, memory episodes); 220 of 224 module text tables identical, the four differing being two `_SENTENCE_SPLIT` regexes that keep the English alternation verbatim and APPEND CJK punctuation, a `_TIER_PHRASES` fallback that was an inline literal of the same value, and a freshly generated uid; and all 74 tables that moved onto the linguistics card byte-identical when read back through `english_linguistic` (including the pronoun-group map, which `common._pronoun_to_group` now derives per story inside a function from a card table that is itself identical, rather than binding one English `_PRONOUN_TO_GROUP` at import). THE ADDITION: every prompt gained the same 506-character LANGUAGE AND SCHEMA CONTRACT suffix, which is the mechanism working rather than a regression — but it means a model receives 506 characters it did not before, so "byte-identical" holds of the authored card leaves and not of the resolved prompt. Said precisely because it is the kind of claim a later change leans on. **Partial** because Japanese, the one non-English pack, is `translation_status: model-draft` and has had no native review; because `direction: rtl` is accepted and plumbed to `dir` but no stylesheet mirrors it; and because story CONTENT authored outside the language layer (`world/spatial_prose.py` contact clauses, `scene.appearance_of` glue, `story/attire.py` ledger phrases, `world/paradox.py` hazard notes) still reaches a non-English view in English. Residuals in `docs/UNBUILT.md` §1.48; guide in `docs/guides/LANGUAGE_PACKS.md` |
| **Session digest** | **Not built** | No end-of-session synthesis for resume |
| **Third-party extensions** | **Partial** | An extension is a directory under `extensions/` with a `manifest.json`; `extension_runtime` discovers it per-item (one malformed extension never takes the tree down), computes its trust class from what it can actually do, and — only once the host enables it — imports its Python entry and calls `register(api)`. Through that facade it splices its own stages into the turn plan at named anchors, observes every saved step, keeps per-story / per-character / install-scoped state under `ext:<id>` (which is how it inherits checkpoints, archives and branches without a schema change), reads a character's settled psychology, and registers sidebar tabs, toolbar buttons, composer controls, full-window views, step renderers and stream listeners in the browser — as a classic script or, for an extension built as a module graph, as an ES module entry loaded by dynamic `import()` and handed an id-bound facade. Install is from a git repository, a directory, or an `http(s)` zip — repositories by URL with an optional `#branch`/`#tag`, cloned over http(s) or `file://` only (an ssh remote would block on a passphrase prompt inside a web request) and never with submodules; every source is held to the same ceilings on symlinks, file count and expanded size, and lands with an atomic `os.replace`. Those ceilings are applied to ONE manifest — the exact set that will be copied — because they were not: the audit walked `rglob("*")` and the copy that followed applied `ignore_patterns(".git", "__pycache__", "*.pyc")`, so a developer could not install their own checkout (an integrator's worktree of >367,000 visible files against a tracked shipping tree of 144), and, worse, the set that passed the ceilings was not the set that landed on disk. A git checkout is now audited as `ls-files --cached --others --exclude-standard` would ship it — `.git` and everything `.gitignore` names is neither installed nor counted, and modes `120000`/`160000` are read from git's index rather than inferred from a filesystem that cannot answer them — while a plain directory keeps the strict recursive walk, because a folder someone points the installer at is already an explicit package. Errors name the observed number beside the allowed one, the install record carries `file_count`/`extracted_bytes`, and `audit_extension_source()` runs the same manifest and the same ceilings with nothing written, which is what an author's CI needs and what a `pack` command would only duplicate less accurately. The host also SAYS what it offers: `api.api_version` plus `api.capabilities` — fourteen named capabilities, `ext_api` + `host_capabilities` on `GET /api/extensions` for the browser half — because a version number moves once per breaking change and everything added between two breaks is invisible to it, which is why an integrator reads internals instead. A name is added when the behaviour lands and is not removed or repurposed while `ext_api` stays 1: `frame_coherent_reads` was deliberately withheld while it was being built in a parallel branch and declared in the commit that merged the two, which is the discipline working rather than an exception to it. The stage/specialist contract is stated as a contract in `docs/guides/EXTENSIONS.md` §4a — anchor grammar, per-surface collision behaviour, per-surface ordering, and how `list_channels` affects validation and merge. A repository install records its url, ref and commit, so `GET /api/extensions/updates` can ask each remote whether it has moved without downloading anything, and an update re-clones and validates in staging before the working copy is touched. Access is deliberately NOT capped: manifest capabilities are disclosure for the consent dialog, not a sandbox, because the firewall constrains what reaches a MIND and says nothing about what a developer may observe or build (`AGENTS.md` § Information boundaries). An extension that reroutes information owns the guarantee it then makes. A developer may also add a commit domain inside the turn's own transaction, serve their own HTTP routes, make model calls on any configured role — including a model lane of their own (`api.add_model_lane`): a role namespaced `ext:<id>:<name>` that gets its own row in the host's model settings, inherits `default` when left blank exactly as a blank host row does, and logs spend under its own role string in the per-call ledger, while `providers.ROLES` stays the host's fixed vocabulary (a lane named after a host role is refused, disable empties the settings row, and the host's stored configuration for a vanished lane survives the save it was not shown in) — register a seventh Director specialist family, rewrite what one mind is about to be given, and colour what the NARRATOR is told — either as a standing per-story block (`api.narration_context`, replaced rather than appended, applied once per beat so the narrator's own retries reuse it) or as a hook. A campaign layer gets four more: standing context and a hook in front of the DIRECTOR itself (`api.director_context` per phase — `establish`/`interpret`/`resolve` — and `api.on_director_payload`), which is the difference between colouring a verdict and shaping the decision, since what the Director believes propagates into state, perception and memory while narration only reaches the page; a versioned read-only canonical story facade (`api.story_view`, `web/story_view.py`) that answers ids, clock, frame, scene, rooms, cast-with-stable-ids and committed events without an engine import or a database handle; its viewer-filtered mode (`api.player_view`), which is a security boundary built entirely out of what the engine ALREADY DELIVERED to that viewer — the perception stage's own view and observations, their own memories and relationships, the identity ledger's own answer — so no second implementation of "what does this persona know" exists to drift, and where it cannot answer it OMITS rather than defaults — including, since schema 2, a structured `people` roster for a persistent crew/people interface — re-keyed in schema 3 onto immutable identity throughout, per `docs/design/DIRECTIVE_HARDENING_REPORT.md` §1: ids never derive from names (one id scheme with `viewers()`; every bearer of a granted name is a distinct entry, so two people sharing a name — or a deliberate duplicate sharing everything — never collapse, and a rename or alias moves `display_name`, never `id`), `identity_status` from the identity ledger joined with the perception stages’ own per-observer delivered-company record (so an unrecognised body appears under the composer’s label and an opaque `body:` key that is a viewer-scoped, story-salted derivative of the immutable identity — stable for that viewer across encounters and canonical renames, shared with no other viewer, invertible by nobody — a disguise that conceals identity does NOT join to the known name, and a person neither known nor delivered is absent), plus an allowlisted `facts`/`fact_sources` pair carrying only the card’s authored-public surfaces; and atomic campaign provisioning (`api.provision_story`) over the existing chat-archive importer, seeding the extension's own namespaced state inside the same transaction and recording package provenance, so a validation failure leaves no partial story behind. A fifth persistence home serves the port whose unit of state is a FILE: `api.documents` — JSON documents at logical paths with `list`, `delete`, a damage-reporting `verify` and host routes, stored one `world` row per document under `ext:<id>:doc:<path>` (story documents therefore ride checkpoints, archives and branches exactly as the other homes do, with no new table and no carriage code) or as settings rows for the install-scoped library that must NOT follow story history; paths are validated as attacker-adjacent input and the size/count ceilings refuse rather than truncate, because a story document is re-stored in every checkpoint of the story and a truncated JSON document is a parse error, not a smaller document. The last two are unrestricted, but attributed: every top-level payload key an extension changes is recorded against its id on the durable turn, so a mind that knows what it should not — or a line of prose nothing earned — names its author in one read. The story lifecycle is a declared contract (`api.chats`, `Sonder.chats`), which had to SAY two things rather than provide them: a reroll is a ROLLBACK rather than a re-render — a committed turn has already changed the world, so an adapter porting a "replies are drafts" swipe model onto Sonder's variants corrupts state — and there is no way to post prose, deliberately and permanently, because narration is produced by the pipeline from committed state and a seam for injected text would make `persist/commit.py`'s boundary advisory. Extension state has a per-era home beside the chat-global one (`api.frame_state` → `extf:<id>`, in `FRAME_SCOPED_WORLD_PREFIXES`; `api.state` unchanged, so no story migrates), because a mission advanced in one era was advanced in every era and a rewind that took the room back left the objective ticked. `Sonder.registerSettingsSection` puts an extension's own configuration inside its own card in the 🧩 menu, which is where install-scoped config belongs and where a reader is already looking at that extension. `Sonder.notify` is a standing notice column — a toast leaves in four seconds whether or not it was read, which is the wrong shape for "your objective changed while you were reading". An extension may also REFUSE a Director result (`api.on_director_result`): standing context is model input and guides a decision without guaranteeing one, so a validator reads the merged result after every deterministic floor and returns a structured violation that buys exactly one re-resolution — which re-enters the whole stage, so the corrected answer crosses every floor and every validator again rather than being patched in place, and a campaign rule never gets the last word over the engine's own physics. Deterministic code with no model handle, a deep-copied result it cannot mutate, stable ordering by extension id, `warn` by default and `fail` only for a campaign that would rather lose the turn than be wrong. And turn zero arrives whole: `provision_story` takes the frame-scoped state, Director and narration context and opening documents as DATA, validated before the archive is touched and applied inside its transaction, because pressing Start must produce a complete campaign or none. An extension's directory is imported as a PACKAGE, so its Python may be more than one file (relative imports; a `sys.path` entry was refused because a sibling `core/db.py` would shadow the engine's and two extensions' `helper.py` would collide). `extensions/campaign-demo/` is the reference campaign that proves the five campaign contracts COMPOSE rather than each merely working — provision, gate an objective behind a fact that must reach the player legitimately, rewrite the Director's standing rule when it opens, advance mission state inside the turn's transaction, render from `player_view`, export intact — and building it is what found the multi-file gap. **Partial** because the reviewed-registry phase, declarative advisor stages, the two admission-side routing hooks, a prose-author chunk registry (an extension specialist's channel reaches the ledger and is never narrated *from there*, though the narration seam now puts context in front of the narrator directly), binary/blob storage (the document store deliberately refuses large binaries), and the scoped-client work that would make third-party frontends real for non-host players are all unbuilt. All three contracts Directive's author named against 9.3 (`docs/design/DIRECTIVE_REMAINING_GAPS.md`) are built. Residuals in `docs/UNBUILT.md` §6.2; guide in `docs/guides/EXTENSIONS.md`; argument in `docs/design/EXTENSIONS_DESIGN.md`, and what a total-conversion extension still needs in `docs/design/DIRECTIVE_HOST_SURFACE.md` |
| An extension's capabilities are disclosed BEFORE consent, not after | **Built** | Three documents said manifest capabilities are disclosure for the consent dialog and never a sandbox, and the consent dialog showed the trust sentence alone — the capability summary rendered into the extension's row, behind the dialog a host had already answered. That summary was also a second, hand-written list in the browser that had drifted from the engine's: it named six of `KNOWN_CAPABILITIES`' ten, and the four it dropped were `python` (runs code in this process), `commit_domains` (runs inside the turn's transaction and can roll the turn back), `routes` (serves HTTP under the host session) and `char_state` (writes into a mind's own state) — the four a host would most want named. `KNOWN_CAPABILITIES` itself was read by nothing: a comment wearing a tuple, so the engine could not check its own list against a manifest, a loader or the browser. It is now derived from `CAPABILITY_DISCLOSURES`, which pairs each key with the sentence a host is shown, and `Extension.disclosures()` reads it into every listing row so the browser renders what it is given rather than what it remembers. A key this engine has never heard of is disclosed BY NAME rather than dropped: tolerating a later-`ext_api` manifest must not also mean silencing it, or the tolerance becomes the way to ask for something quietly |
| Public reads are frame-coherent | **Built** | The Directive review's P0: `player_view` resolved the latest committed turn's frame while `api.frame_state(...)` and `api.char_state(...)` followed the ambient `active_frame_id` — unset on an extension HTTP route, so answering for the present — and one projection composed from both carried the future's scene and identity beside the present's mission, clock and crew state: structurally valid, semantically impossible, and worse than a hard failure because every field in it is plausible. `api.at_frame(chat_id)` now resolves the frame ONCE and returns an immutable `ExtensionFrameView` binding `story_view`/`player_view`/`frame_state`/`char_state` to it — a facade rather than parameters alone because the defect was COMPOSITION: a per-call parameter must be remembered by every read a growing DTO adds, and the call that broke had nothing to remember it with. Both web views take an explicit `frame_id` (omitted = latest turn's frame; `None` = explicitly the present, which is the engine's own spelling everywhere in the persistence layer; an integer = a declared frame verified to belong to the chat, honoured even with no turns yet, because `provision_story` legitimately seeds frame state before any turn runs there). `player_view` reports its `frame` beside `story_view`'s with no schema bump, since the key is always present once the field exists and absence therefore still means "predates the field". Writes bind exactly like reads: a read-only facade would have MANUFACTURED the write half of the same defect — read era A through the view, write era B through the only ambient channel left — and the commit gate is unchanged, because binding decides WHERE and the gate still decides WHEN. Bound reads resolve the frame into each query rather than parking it in a contextvar across arbitrary extension code, so `.get()` survives an intervening frame change and leaks nothing; a foreign frame and an unknown frame get one identical refusal, so chat A cannot probe chat B's frame ids; and `events` stays story-global under any selection, stated on the read, because `world_events` is objective record while a frame is an epistemic cursor rather than a partition of what happened. Making the views frame-selectable also flushed two in-module members of a class this document already names: `_viewer_memories` was the sixth raw memories read that `visible_memory_rows`'s own comment predicts and had forgotten the frame filter, so a past-bound view handed the consumer future-era memories; and the relationships read went to `chat_chars`, the present's row, under a held frame. Route-level proof in `tests/test_extension_frame_view.py` — through real `dispatch_route`, because the defect was separately-correct APIs composed through a real route; argument in `docs/design/DESIGN_FRAME_COHERENT_READS.md` |
| A disabled or failed extension is inert everywhere | **Built** | "Enabled" and "live" are two different states, and only some of the code read the second. An extension whose `register(api)` raises is switched ON and has registered nothing; every DISPATCH seam escaped by accident (a failed record's hook lists are empty, so iterating them does nothing) and every SERVING seam did not, because serving read the enabled set. Measured on a scratch tree: a failed extension's `ui.js` was handed out by `asset_path`, `extension_script` and `ui_bundle` alike, so its browser half went on running against routes that do not exist and state nothing writes. `failure_reason(id)` and `_serving_ids()` now say what is live, and asset serving, the per-extension script and stylesheet, and both concatenated bundles read that. A failed extension also kept its Python package and every submodule in `sys.modules` — and fixing a failure means editing exactly those files — so `_deregister` drops them whether the extension failed or was switched off. Its route dispatch reports the load failure rather than "serves no GET /x", which is true of every route it declares and explains none of them, and the reason a host reads carries the exception type and its cause chain rather than `str(exc)`, which is the least informative line in a traceback and empty for a bare `KeyError`. Every registration refusal names the extension that asked. The set is pinned together in `tests/test_extensions.py` rather than one property per file, because the failure mode is a seam that was never taught the difference, and each new thing an extension can serve is a new place to forget it |
| **Living world — the five state-producers** (`docs/design/DESIGN_LIVING_WORLD.md`) | **Floors of A–E built; C's and E's ceilings built; A/B/D ceilings open** | `world/living_world.py` carries the settings ladder for all five (declared/built split per the `OFFSCREEN_LIFE_BUILT` idiom; all default off, opt-in per chat via `/api/chats/{cid}/living_world`). A recomputes routine/entropy/occupancy at contact. B mints validated scheduled consequences and fires them deterministically. C's first physical carrier floor copies only a non-empty public `witnessed` surface to a registered character actually co-located with the fired event; the frame-specific envelope follows that holder's real movement and only their private agent receives it. Sharing a room does not grant it to another mind. D accumulates durable place obligations before arrival. E stores only same-beat, character-declared plans and deterministically fires their typed pre-adjudicated stages. C's courier/letter layer is built (`story/couriers.py` + `StateDiff.courier_ops`: dispatch from a holder's own hands, movement on the clock over the shared passable graph, interception and silencing that stop delivery); C is now complete through its ceiling: caravans ride the courier object with stops, dwelling and two-way news exchange; `story/artifacts.py` notices are posted from a holder's own hands, acquired by explicit reading (provenance `read`, a copy not a mouth), destructible so a torn-down bill informs nobody, and worded by one small out-of-band call that lands only while the bill still stands. The A/B/D model-assisted ceilings remain open; E's adaptive `character_agent` ceiling is built (`offscreen.schedule_agent_ticks`): one reduced Director-adjudicated turn per opted-in candidate, acting only on character-owned carrier evidence, landing atomically under epoch/base-turn/per-subject guards. The off-screen ladder is the single authority ceiling over all five; `effective_depth` clamps both axes at read time, and the settings UI renders the engine-owned truth |
| A gate that can be entered can be left | **Built** | Two deterministic floors were built, documented, tested — and keyed on one exact spelling of a free string that no beat ever wrote. `restraint_map` selected `kind='restraint'`; measured read-only against the live database, **zero** rows spell it, while 21 spell `physical_restraint`, 3 `restrained`, 24 active across 15 chats. `awareness_map` selected `kind='awareness'` against nine active rows spelled `unconscious`, `asleep`, `sleep` or `consciousness`. The movement block for bound bodies had never once run. Fixing the selector alone was tried and reverted, correctly: it would have immobilised 24 live bodies while restraint had **no ending path at all** — no view handing the Director a `condition_id` to close, none of the deterministic exits awareness has. A gate that can be entered and not left is worse than no gate, so the exits landed first and the entrance last. Restraint is a RELATION — an agent, a means, a subject — with two persistence classes the code had never honoured: **standing** (`bound`, `encased`, `pinned` by a mass) holds with nobody attending it and blocks self-relocation until something ends it; **a hold** (`held`, `pinned` by a body) blocks only while its named holder is co-present and conscious, ends by itself when that stops being true, and blocks NOBODY when it names no holder the scene can vouch for — a two-body relation missing one endpoint is a description, not a mechanism. The distinction is the corpus's, not the code's: a flat restrained-yes/no reading immobilises all 24 rows including six embraces whose own text says "no actual restraint", a body gripping a console lever, and a row whose entire state is the string `"active"` (10 unattended bindings / 6 live grips / 8 holds naming no holder). Endings split by who owns them: the engine ends a hold whose holder is **positively** elsewhere or below waking (an unknown position keeps it — absence of data is not departure) and encodes a release the Director's own prose asserts; the Director owns untying as a decision, working free, material failure and contested escapes, handed `active_restraints` with each `condition_id`, holder, liveness and duration under a RELEASE IS YOUR JOB contract mirroring WAKING IS YOUR JOB. Deliberately **no clock exit** — a rope stays tied all night — and deliberately **no player-declaration exit**, unlike awareness: an awareness gate takes the player's view and their next move, while a restrained player still perceives, speaks, struggles and plays. The vocabulary is answered at all three tenses, because each covers a different one: the body specialist's conditions chunk teaches future rows the rungs and the holder requirement; family predicates read standing rows (restraint by word-splitting, awareness by **whole kinds only** — its family members are level words, and word-splitting would read a live `preparing_for_sleep` row, a body awake at a futon, as `sleep`); and an ending re-emits the row's OWN kind under the same `condition_id`, so a row entered under any spelling can be left under it. `_RELEASE_CUE` precision was measured against an adversarial battery rather than a corpus, since no restraint has ever ended and there are no historical release sentences: 9 false positives before hardening, 1 after, with each false positive paired to the genuine release nearest it so precision could not be bought by dropping recall. Argument in `docs/design/DESIGN_RESTRAINT_AND_AWARENESS_EXITS.md`; the routing rows in `AGENTS.md` are the maintained authority |
| A collapse gates the body that collapsed | **Built** | The player-awareness floor guarded one mind, and the support check behind it is beat-wide by design — so once any collapse existed in a beat, gating anybody in it was licensed. Live, in a scripted run: one body went down and TWO unconscious rows committed sharing one cause, the second on the lift operator standing beside the fallen one, who then ran no character step and read as a character who had gone quiet. Two premises of the old scoping were false for the cast, which is what makes it a floor rather than a note: `agents/character.py` runs no decision for a mind at a gated level, and for `unconscious`/`sedated` none of the deterministic exits fire — rouse and clock end only `asleep` — so the cost is a mind removed from play until the Director volunteers an ending, which the corpus says it has never once done. `_unsupported_character_awareness` asks per cast mind whether any sentence in the beat ties THEM to going under: sentence co-occurrence over both cue vocabularies, deliberately looser than the omission scan's clause attribution that runs the other way, so a subject the omission scan can flag is always one this floor keeps and the two directions cannot fight. Endings are never touched and a re-assertion of a live condition is not an onset. Bodies with no run mind stay the Director's, because a condition on a background presence is descriptive state and a wrong drop leaves a knocked-out body walking. The restraint side was checked and deliberately left fail-open: the hole exists in shape and not in class, since a restrained mind still runs, perceives and speaks, a spurious restraint warns the moment the body tries to move, and a wrongly dropped one frees a body somebody is holding. Measured read-only before landing: 0 of 468 condition entries across 71 live chats would have been dropped |
| A condition can act while it lasts, and the Director can see every one standing | **Built (tick + view); the idle backstop deferred, `docs/UNBUILT.md` §1.84** | `world_conditions` shipped with a `next_tick` column and an `idx_world_conditions_due` index over it, and nothing ever wrote either. Measured read-only on the author's `engine.db` 2026-08-25: **444 rows across 50 chats, 363 active, 360 of those active rows with no `expires_at` at all**, `next_tick` NULL in all 444, and 131 active rows carrying an authored `tick_interval_seconds` — a field the body specialist's own sheet asks for on every conditions beat and no reader in the engine consumed. `llm/schemas.py` stated the open question in its own comment: build the due-tick sweep, or drop the field, the column and the index. Built. `world.mechanics` pass (c1) fires due ticks against the SIMULATION clock — batched, so a beat that skips hours fires every tick that came due — and persists cadence through `next_tick`, which the sweep owns rather than the writer (every existing row meets it uninitialised and is scheduled from the clock it is first seen on, never charged for the story that preceded the mechanism). What a tick may DO is deliberately narrow, because 106 distinct `kind` strings are active and the engine must not learn what any one condition means: a signed move of a survival vital the scene ALREADY tracks, clamped per tick and into [0,1], and one clause re-announced as an engine notice, once per sweep however many times it fired. It may never CREATE the vitals ledger — `world/survival.py` says absence is the off switch, and with 131 rows already carrying intervals a seeding tick would have started a hunger clock in every story that never enabled survival; where the ledger exists and names no such body the pass skips and SAYS so. Ticks run before expiry so a row's last due tick lands before the clock closes it, and never at or past its own `expires_at`. The second half is visibility: `active_conditions` is the general form of `_awareness_view`, carrying every live row's `condition_id`, age, whether anything will ever close it, and which floor already owns its exit (`story.scene.condition_exit_owner`) — Reason 1 of the WAKING block generalized from one family to the whole table, since a hand cannot end an id it was never shown. It adds no exit and makes nothing in `_awareness_exits` redundant. Two write-path repairs of the same nothing-ever-ends class: a re-emission's authored `expires_at_seconds` now reaches the column (the UPDATE named four fields and silently discarded it), and every write stamps `last_asserted_turn_idx`, which is what makes idleness measurable at all. `next_tick` is repaired in the other direction — the writer may not set it on either branch, because a cadence the writer can move is the never-fires class re-entered through the one channel the design says must not reach it. `tests/test_condition_ticks.py` |
| **Institutions and upkeep (Charter)** | **Social, material, institutional, generation and lifecycle vertical slices built** | `world/charter_*` is the pure institution simulator; `world/charter_runtime.py` is its production I/O seam. Explicit frame-scoped registries advance from committed epochs under revision/base-turn/epoch guards. Each body holds sparse claims and independent evidence-citing trust, warmth, fear, respect and suspicion toward particular subjects—there is no universal reputation score. Beside those numbers each body holds a directional discrete tie—close, at odds, wary, afraid of, looks up to, familiar—derived from its own stance, its own regard and its own co-presence count, revalidated against those numbers on every load and save so a label cannot contradict them, and carried into the scene ledger and promotion handoff as one word beside the figures it summarizes; ties are one head's view and never symmetric. Authored consequence rules read one objective change a window deposits and produce one objective consequence -- a situation, a temporary mark, a witnessable event -- under four independent termination bounds and a signature that is handed no head to read; a rule may never write a judgment or forge an institutional conclusion, so an opinion still forms next window from evidence its holder can cite. Grounded public promises/offers/bargains create locally recognised commitments with parties, terms, witnesses and explicit lifecycle; retelling, reporting, physical carriers, Scene Life and promotion reuse the same epistemic rail. Abstract lots make scarcity and route burden visible without counting coins; courier caravans carry real freight and exchange it only at physical stops. Authorised postholders aggregate reports they personally received, select allowlisted policies, and deliver typed orders down staffed co-present reporting edges; execution produces real stock transfers and witnessable consequences. Planned towns keep a prose-free registry skeleton composed under the live scene, materialize only the occupied fringe, and may be generated from a selected lorebook subtree in one qualitative planning call. The same opt-in is exposed in greeting launch, quick and hand-built story creation, Dialogue Config, and the lorebook workspace; greeting generation lands before turn 0, while mid-story generation adds collision-safe locations without replacing or re-aging existing Charters. Generation-time presim uses coarse history plus a recent high-resolution tail. An optional history architect may schedule only physical interventions, never minds or conclusions; the post-run historian is citation-gated to actual presim event ids and remains author diagnostics. Generated populations receive stable personal names before simulation. A greeting card character may be placed as a featured resident from public history and only those ability name/scope rows that public history corroborates; ability limits and private card fields never reach the planner. They live through that same simulation, then a bounded recent-life compiler authors 10–16 detailed, independently retrievable `turn_idx=NULL` episodes constrained to the named roster, real rooms/duties, actual anchors, card and author guidance, beside separate career and recent-life semantic summaries. Unknown people/places and thin episodes are rejected, and a sparse result aborts launch rather than becoming canon. The handoff binds the full cognition entity before turn zero and retires its Charter mind while retaining the office, service record and reporting projection. Ordinary promotion carries selected memories, commitments and social axes through the same boundary. Open: an offscreen producer for `harm_done` and a consequence rule reaching the author path, deeper prehistory eras and reciprocal generated episode memories for background participants, upkeep readings as beliefs, fractional labor, keys/permissions on planned closed routes, recovery-place requirements, richer refusal/projects, adaptive safe ensemble batching, and a dedicated graphical town editor. |
| A body decides from what has passed between the two of them | **Built (design 1 of five; all five landed 2026-08-27), residuals in `docs/UNBUILT.md` §1.97** | `charter_practice._state_of` builds everything an affordance may reason over, and it was `{bodies, figures, minds, needs, regard, blame, at}` -- so a Charter body deciding what to do could not see anything that had ever passed between it and the person in front of it, and acted from need levels and a scalar regard. Comme il Faut scores every exchange against the social facts and that is the whole of its believability (`docs/guides/RESEARCH.md` §1.7.6, design 1 of five taken). `_state_of` now carries four holder-owned stores -- `experiences`, `served_beside`, `judgments`, `commitments` -- and `_between` derives a per-pair digest (`familiar`, `affect`, `debt`, `owed`) capped by one constant, `HISTORY_WEIGHT = 0.15`: 7.5x the seeded tie-break jitter so a body decides by a reason rather than a coin flip, and below the smallest present-state swing any affordance already computes so what is happening now still outranks what happened before. Every field is the DECIDING BODY'S OWN -- its own diary, its own co-presence tally, its own five-axis stance, and a commitment only where it is a party; `served_beside[a][b] == served_beside[b][a]` because both are records of the same fact held separately, and how an occasion LANDED on the other body is refused however symmetric the occasion was (`test_a_body_never_reads_the_other_heads_record`). Terms are additive and argued one at a time: accusation may only SUBTRACT and only where affect is positive (Prom Week's Simon/Naomi -- a life with somebody is a reason to hold your tongue, never to accuse somebody else harder), `tend` may only ADD and does not read affect at all (a body does not walk past somebody on the floor because it dislikes them), and a body with no history greets at exactly the old 0.9. No new persisted state: the digest is derived per window and discarded, so no archive/checkpoint/branch handling and no second copy of `experiences` to drift. Cost is constant in charter age -- depth from the O(1) `served_beside` counter, occasions from one `PAIR_TAIL`-bounded pass per holder per window memoised in the throwaway state dict (measured: 256 row visits over a 4,000-row life against 2,048 without the memo and 4,000 without the bound). Measured on `tests/charter_worlds.big_ship(crew=40)`, 480 simulated hours onscreen, seed 3, against the identical run with the stores withheld: 86 distinct `(actor, act, other)` triples moved, asks-per-actor concentration rose 0.336 -> 0.383 Gini, and the mean `served_beside` count of the body a question was taken to rose 63.8 -> 71.3. The population did not act more; it acted toward different people. Cost, best-of-5 interleaved on the same fixture and seed: +0.6% offscreen (2.751 -> 2.767 ms/simulated hour) and +4.1% onscreen (3.319 -> 3.456), against a 5% gate. `tests/test_charter_practice.py` `TestVolitionReadsHistory` |
| Something other than a failure can be evidence about a person | **Built (design 2 of five); a healthy institution's network is still empty by design, `docs/UNBUILT.md` §1.98** | The five-axis judgment network (`world/charter_social.py`) measured EMPTY across four charters of a real story and across a simulated YEAR of `tests/charter_worlds.twin_towns(40)` -- 0 events and 0 judgment holders while the same year deposited 6,742 experience rows, so the people were living and none of it was evidence. Judgments form only from witnessed events and every event kind the coarse sim emits is an institutional failure (`docs/guides/RESEARCH.md` §1.7.6, design 2 of five taken). THREE PRODUCERS WERE MISSING, each of them three quarters of a feature already built: `report_confirmed`/`report_refuted` had weights, a `WITNESSABLE` entry and runtime phrasing and no producer anywhere in the repository; `accusation` and `apology` had weights and no producer because `quarrel` had no opener -- `opportunities`' own docstring named "a blame that has landed" as one of the three circumstances that open a situation and it opened none, so `accuse` was unreachable from any state the simulation could produce; and `institution_order_executed` was emitted, witnessed and inert because the signal table spelled the same happening `order_obeyed`. `charter_news.check_reports` is the new ordinary-evidence source and the only one that is not a disaster: a body standing where a second-hand rumour named settles it against `out_of_band` on the place its own feet are in, and comes to a view about whoever told it. Everything it reads is the deciding head's OWN -- its own claim, whom that claim says it heard from, the condition of the place it is standing in, which is the channel `charter_feel.advance_feel` already appraises a window from -- and it mints NO event, so the conclusion exists only in the head that reached it, costs no `scheduled_events` row, and adds nothing to `WITNESSABLE`. Inference is the product: what is built here is a body concluding its informant was wrong. `accusation`/`apology` DO enter `WITNESSABLE`, because a speech act between two co-present bodies is heard by the room -- the same rule `story/carriers.py` holds one tier up and the same one `aid_given` was admitted under -- and their payloads carry the speaker, the target and the place and NOTHING from the institution's blame register, since `charter_runtime._scheduled_row` puts a witnessable kind's surface onto the objective carrier rail. `news_claim` now carries `toward`: it builds a FIXED dict and dropped every other payload key, which is why the existing `aid_given` event set `subject` and no witness ever received it -- a bystander saw whom the act was directed at, so carrying it subtracts nothing. Diminishing returns were REQUIRED, not a refinement: judgments decay nowhere in this package, so every signal is permanent and `_clamp` saturates hard: measured on the famine arm, 63 of 145 stance axes sat at exactly 1.000 and the network had stopped carrying information. Each delta is now scaled by the room left toward the bound THE EVIDENCE PUSHES AT (not `1 - abs(before)`, which damps contrary evidence just as hard and would freeze a pinned stance for the life of the story), so the first evidence against a settled view still lands whole. Measured on `twin_towns(40)`, window 4.0, seed 7, before -> after: healthy simulated year off screen 0 -> 0 events and 0 -> 0 judgment holders, which is the design and not an accident; famine simulated quarter off screen 6 -> 40 judgment holders and 29 -> 149 stances, 130 of the surviving `reasons` citations naming `report_confirmed`, and no axis above 0.999994 against 63 of 145 axes sitting at exactly 1.000 before; famine quarter on screen 0 -> 58 accusations (14 when this landed; the channel row below re-measured it on the finished tree). Coarse cost, best-of-3 interleaved on the healthy year: 1.953 -> 1.979 ms/simulated hour, +1.3% against the 5% gate this package uses -- the check is guarded on a non-empty news index, so an institution with no news pays nothing for it. The experience store SHRANK 42% on the famine arm (20,866 -> 12,145 rows) because the acquaintance writer was reading every key in `minds` as somebody met and 9,284 of its 9,902 rows named a news key rather than a body. `tests/test_charter_ordinary_evidence.py` |
| A relationship has a name and not only a number | **Built (design 3 of five), residuals in `docs/UNBUILT.md` §1.99** | Comme il Faut keeps a numeric social network and a discrete relationship layer side by side on purpose: numbers drive scoring, labels drive legibility, and a label is what a narrator can state plainly and a reader can hold (`docs/guides/RESEARCH.md` §1.7.6, design 3 of five taken). Charter had the numbers and no tie. `world/charter_social.py`'s tie layer is six labels -- `close`, `at_odds`, `wary`, `afraid_of`, `looks_up_to`, `familiar` -- each a reading of an axis already there plus the holder's own directed regard and its own `served_beside` count, stored sparsely as the charter's `ties` and DERIVED rather than authored, so no label can assert something the numbers do not. DIRECTIONAL, unlike CiF's, because two people do not share a head: A may hold B `close` while B holds A merely `familiar`, and the unrequited tie is the more generative one; mutuality is computed for the AUTHOR's diagnostic (`summarize`) and reaches no mind. VALIDATED ON EVERY NORMALIZE, so a label the numbers no longer support is deleted rather than merely discouraged -- and `CLOSE_FAMILIARITY` is re-checked there too, which its own comment once denied: `served_beside` only ever rises, so a `close` that passed the gate at formation can never fall back through it, and the only rows the re-check can delete are rows this package could never have written (a hand-edited registry, an archive of another shape). The pass is gated on `judgment_movements` and the same per-place roll `served_beside` counts from, and skipped above `CO_PRESENCE_WIDTH`, so it is O(bodies) per window rather than O(pairs). Measured on `twin_towns(40)`, live against the same run with the pass swapped for a no-op, arms strictly interleaved: a famine quarter forms 27 signed rows across 6 holders (16 `close`, 11 `looks_up_to`, 11 requited) against 0 with the pass off, and `familiar` reads 341 pairs instead of 368 because the 27 upgraded; a HEALTHY simulated year forms `familiar` on 412 of 1,560 directed pairs and ZERO signed labels either way, which is design 2's remaining half re-measured and is pinned by `test_a_healthy_year_of_this_engine_forms_no_signed_tie` rather than hidden. `TIE_FORM` is the one constant here still set by argument rather than by measurement, and its own comment says so. `tests/test_charter_tie.py` |
| What a body is right now that it was not before | **Built (design 4 of five), residuals in `docs/UNBUILT.md` §1.99a** | Charter had permanent dispositions (`charter_temper`), needs, felt state and a service tally, and nothing socially TEMPORARY -- no newly raised, no lately helped, no accused to your face, no in disgrace, which are what make a beat read as motivated rather than merely caused (`docs/guides/RESEARCH.md` §1.7.6, design 4 of five taken). `world/charter_mark.py` is four marks with four lifetimes over one new charter key, `marks`, normalized in `charter_model.normalize_charter` and filtered to live bodies there exactly as `experiences` and `habit_runs` are -- so a body that leaves cannot leave a mark behind, and the filter runs at the head of every `step` rather than only where rows are written. ONE ROW PER (BODY, KIND): a re-trigger overwrites `since` rather than appending and every row is pruned at expiry, so the store is bounded by bodies x 4 and a healthy simulated year ends holding NOTHING. THE SCOPE SPLIT IS THE SPINE and it is an allowlist (`BODY_MARKS`) for the same reason `charter_news.WITNESSABLE` is one: `posted`, `aided` and `accused` each have an origin the marked body was PRESENT for -- it was handed the duty, somebody tended it in the room, somebody said it to its face -- and they reach `charter_feel.appraise_window` and `scene_ledger`'s presence slice, while `disgraced` is the register's own (`attribute_blame` follows the watch the charter merely BELIEVED it had arranged, so a body can be disgraced for a post it was never at) and reaches exactly the planner's reluctance axis and `charter_log.life_of`, which is author diagnostics no mind receives. Fresh-only into the appraisal: a 168-hour mark appraised every window would rebuild the standing-strain floor that once left 240 bodies carrying strain 0.09 after 480 quiet hours. `politics.blame` stays MONOTONE on purpose -- the counter says it happened, the mark says it happened recently, and only the mark reaches the bill, so a charter does not spend a body more grudgingly a decade after. `DISGRACE_RELUCTANCE = 0.6` was set against `pressure` rather than by a sweep, because the planner's first sort component is `criticality + standing + pressure + disgrace`: measured on a two-body works fixture, at 0.3 the disgraced hand is back on the bill once the clean one reaches need level 0.6 and at 0.9 the institution never reaches for it at all, working the clean hand down to 0.2; below 1.0 on purpose so a disgrace can never outweigh being the last body qualified for another post. Fixing this exposed a defect one line away, in `advance_feel`'s nothing-wrong branch: it tested "nothing at all" where it meant "nothing adverse", so a window whose only content was GOOD took `resolve_stress`'s 0.5 unknown and a body was strained by being pleased -- measured on the one-body office fixture, `posted` at hour 4 left the director at strain 0.0952 and unavailable at hour 48 on a run it used to finish fit. No schema change and no migration: the charter lives whole inside a frame-scoped `world` row, so archive, checkpoint, branch clone and story delete carry the key for free; `charter_runtime.bind_promoted_character` purges it beside minds/needs/feel/heard_blame, and it is deliberately absent from `promotion_handoff` because the character tier has no reader for a Charter-window scoring bias. Measured on `.venv`, `big_town(40)` healthy simulated year at window 4.0 seed 3: 13 of 40 bodies ever `posted`, 0.31% of (body, window) pairs holding it, store empty at the end of the year, and the cost is 23.14 -> 23.62 s in sequence and, with the mark writer swapped for a no-op and the arms strictly INTERLEAVED in one process, 23.64/23.67 s live against 22.71/23.16 s inert -- +2.2 % to +4.1 % depending on which pair, against the 5 % gate this package uses. The same fixture with needs seeded: 804 `aid_given` acts, 6 bodies ever `aided`, 12.39% mean held -- the plan for this design predicted `aid_given` fired only under famine and it does not. `twin_towns(240)` famine month: 48 of 240 ever `posted`, 2 ever `disgraced`, 0 `accused`, because `quarrel` is not in `COARSE_PRACTICES` and an institution nobody is looking at makes no accusations. `twin_towns(40)` famine quarter before -> after: 2013 -> 1567 events and 658 -> 524 `body_unable`, because being tended now proposes pleasure and a positive-only window no longer manufactures strain. `tests/test_charter_mark.py` |
| An act changes state and something fires off the change | **Built (design 5 of five, the last), residuals in `docs/UNBUILT.md` §1.99b** | Charter had no consequence layer: an act changed state and nothing fired off the change, so the social layer moved only when the planner or an author prodded it (`docs/guides/RESEARCH.md` §1.7.6, design 5 of five taken from Comme il Faut). `world/charter_trigger.py` is authored rules that read one objective CHANGE and produce one objective consequence -- open a practice, set a `charter_mark`, emit a witnessable event. THE PASS READS A CHANGE AND NEVER A STATE, which is what makes it free rather than merely cheap: a window deposits a capped `pending_changes` frame and the next one fires on it, so with an empty frame `fire_triggers` returns on one falsy test and a healthy simulated year of `big_town(40)` (2,190 windows) fires NOTHING. It runs between `opportunities` and `enact` and that placement is a trap rather than a preference -- a practice opened after `enact` carries `last_effect_at = at + hours` and is swept by the next window's `close_stale` at every shipped window size (`IDLE_CLOSE_HOURS` 2.0 against presim windows of 4.0 and 8.0), so it could never be acted in and would fail with no error and no failing test. THE FIREWALL IS HELD BY THE SIGNATURE: `fire_triggers` is handed change rows and a body index and nothing else, the module imports none of `charter_mind`/`charter_social`/`charter_feel`/`charter_needs`/`charter_talk`/`charter_observe`/`charter_politics`/`charter_model`, and `test_the_pass_is_not_given_a_head_to_read` pins both ends. `TRIGGER_EMITTABLE` is a TIGHTER allowlist than `charter_news.WITNESSABLE` -- only `aid_given` and `harm_done`, whose truth condition is exactly "this visibly happened between these people here" -- because minting an `institution_order_executed` or a `report_confirmed` from a rule would put a false institutional fact into every head in the room at full first-hand strength with a stable news key two witnesses would agree on; and there is no `set_judgment` op, because a stance with no evidence to cite is a leak wearing a convenience, so a rule that wants to move an opinion emits an event and the axis moves next window under `update_judgments_from_minds`' own idempotence guard. Four independent bounds terminate the cascade, each a test rather than a comment: `TRIGGER_DEPTH = 2` (measured with one self-feeding authored rule over 2,000 simulated hours -- the rule produces exactly 1/2/3/4 consequences at depth 1/2/3/4 and the quiet control emits zero at every one, so the bound is the only thing stopping it), fire-once identity, a per-pair refractory, and `TRIGGER_YIELD_CAP = 8` (the per-window consequence count is 0 over a healthy `big_town(40)` year, maximum 2 over a `twin_towns(240)` famine month, p99 zero on both -- it never binds in play and always binds on a rulebase that has gone wrong, and it is the only thing holding `_record_coarse_experiences`, whose `stood_through` loop multiplies every trigger-emitted event by the size of the room). Three new charter keys, all normalized in `charter_model.normalize_charter` because that runs at the head of every `step`: `triggers`, `pending_changes` (`PENDING_CHANGE_CAP = 32`, capped round-robin across the three change families so no family can be starved by another's volume) and `trigger_last` (pruned to the longest refractory, `TRIGGER_MEMORY_CAP = 256`, because unpruned it is rules x ordered pairs, 32 x 10^6 on `big_town(1000)`). No schema change and no migration: the charter lives whole inside a frame-scoped `world` row, so archive, checkpoint, branch clone and story delete carry the keys for free. ONE default rule ships, and the count is the argument -- the other four designs already gave every state change in the package a direct producer, and a second writer of the same fact can only disagree with the first, so `set_mark` writes into design 4's `marks` rather than into a `statuses` store of its own and ships with no default at all. `blame_opens_a_quarrel` closes the last measured dead edge: `accuse` is offered only inside a `quarrel`, `quarrel` is not in `COARSE_PRACTICES`, and the offscreen branch passes no ledger, so a blame landing where nobody was looking reached nobody -- `twin_towns(240)` driven into famine for a simulated month recorded 48 bodies ever `posted`, 2 ever `disgraced` and ZERO ever `accused`. It recorded 2 told when this landed, and `twin_towns(40)` over a famine quarter 1 -- NEITHER reproduces on the finished tree (re-measured 2026-08-27: the 240-body month tells 0 under either accusation gate, because the blamed pair have moved off the failed place by the window the rule fires, and the 40-body quarter tells 2 under both), so the demonstration that survives is the placement one: on the four-body yard fixture the whole chain runs in two windows -- blame lands at hour 20 and marks the keeper `disgraced`, the trigger fires at hour 24, opens the quarrel, somebody accuses her in that same window, and she carries `accused` with the accuser named in `by`. Cost, `big_town(40)` at 4,380 hours with the pass swapped for a no-op and three pairs of arms strictly INTERLEAVED in one process: 10.71/10.27/10.23 s live against 10.15/10.53/10.17 s inert -- +5.5 %, -2.4 %, +0.6 %, mean +1.2 %, so it is not visible above run-to-run noise; micro-profiled, 18 us per window, 0.2 % of it. `tests/test_charter_trigger.py` |
| A body accuses from what IT perceived, never from the institution's books | **Built**, residuals in `docs/UNBUILT.md` §1.97 | `charter_practice._afford_accuse` decided entirely from `state["blame"][other]` -- the institution's PRIVATE register -- with no test that the actor had heard of the failure at all, and sized its utility as `0.55 + 0.1 x blame_count`, so a monotone reading of a counter no body in a room can perceive went out through `charter_runtime.presence_view`'s `action_instances` to a scene-manager model. `charter_news.WITNESSABLE`'s own allowlist comment names the class: "a conclusion the institution reached in its own books, and a body in the room has no way to perceive it. Getting this list wrong is a firewall leak." IT WAS DEAD CODE UNTIL THE DAY IT WAS NOT: `quarrel` had no opener but `_afford_accuse`'s own effect -- zero `accuse` acts measured on screen and off, in health and in famine -- and design 2's `opportunities` opener and design 5's shipped `blame_landed` rule then gave it two live openers, one onscreen and one everywhere. A leak nobody can reach is a residual; a leak on the default path is a defect. `grievance_against` is the channel that was missing, and every field in it is the DECIDING BODY'S OWN: a claim in its own head naming `other` as the party at fault (`GRIEVANCE_KINDS`, keyed by kind because which field names the answerable party is a property of the kind -- `actor` on `harm_done` and the commitment failures, `toward` on an `accusation`, since there the actor is the person speaking), or its own claim that THIS PLACE has failed while `other` is standing in it (`PLACE_FAILURE_KINDS`; co-presence is the whole of the test, exactly as it is for `charter_news.witness`). Hearsay counts, because inference is the product. No affordance reads `blame` any longer and `_state_of` says so where it still carries it. The `opportunities` opener keeps the ledger as the institution's reason to LOOK -- bookkeeping, not conduct -- and opens nothing between a pair where the actor has no reason of its own, which also stops a register-opened quarrel occupying a `PRACTICE_CAP` slot and offering `reconcile` inside a quarrel that was never had. Accusation stays pointed at bodies rather than figures, which the register gate used to do as a side effect. IT ALSO LAPSES: a claim is deleted once it fades below `charter_mind.PERSONAL_FLOOR`, where `politics.blame` is monotone and would still be a reason to round on somebody a decade later. Measured before and after on the same tree (the before arm is this tree with the three edits reverted, so nothing else differs), `twin_towns(40)` driven into famine for a simulated quarter, window 4.0, seed 7, on screen: 64 -> 58 accusations, 2 -> 7 bodies ever told they were blamed, largest judgment axis anywhere 0.2062 -> 0.3553, axes at or above `TIE_FORM` 0 of 600 -> 2 of 520, and signed ties 0 -> 2 -- the only signed ties any arm of §1.7.6 has produced without a hand-planted store. Off screen the counts are equal (2 and 2) and the PEOPLE differ. The institution being wrong about who is answerable (`attribute_blame` follows the watch it merely BELIEVED it had arranged) is now visible as that divergence instead of being laundered into an accuser's mouth. `tests/test_charter_practice.py` `test_the_books_alone_are_not_a_reason_to_round_on_anybody`, `test_a_grievance_lapses_where_the_register_never_does` |
| A consequence rule may not reach a head from a fact nobody perceived | **Built** | `world/charter_trigger.py` allowlisted which event kinds a rule may MINT (`TRIGGER_EMITTABLE`, a deliberate subset of `charter_news.WITNESSABLE`) and left open which changes it may mint them FROM, so the same hole stayed reachable from the other end. Both of these normalized clean, emitted no `trigger_warnings` notice and fired on `.venv`: `{"on": "blame_landed", "then": [{"op": "emit", "kind": "harm_done", ...}]}` put a first-hand `harm_done` claim into every head in the room off a move of the institution's private counter, and `charter_social.DEFAULT_SIGNALS` then moved trust -0.13 / fear +0.10 / suspicion +0.08 in each of them with cited evidence no witness could have seen; `{"on": "event:post_unfilled", "then": [{"op": "set_mark", "mark": "accused", ...}]}` left a body feeling `accused` at `charter_feel`'s -0.6 / certainty 0.95 and showing it in `charter_mark.mark_view`'s presence slice with nothing said aloud, no accuser and `heard_blame` still empty -- and `post_unfilled` is the exact event `tests/test_charter_promote.py` calls "a conclusion in the institution's books that no one in a room could perceive". `perceivable_change` closes it at normalization, as a REFUSAL rather than a drop, so the row survives for `trigger_warnings` to render and an author sees it the day they write it. An `act:` change passes unconditionally (an act happens in front of whoever is standing there); an `event:` change passes only where `charter_news.WITNESSABLE` says a body could have seen it, READ from that module rather than copied so a kind admitted there tomorrow is admitted here the same day; `blame_landed` is the register's own and never passes. `open_practice` is deliberately not held to it -- opening a situation puts nothing in anybody's head, and every affordance inside one applies its own channel gate at act time -- and neither is `disgraced`, which is the register's mark wherever it comes from. `tests/test_charter_trigger.py` `test_a_rule_may_not_reach_a_head_from_the_institutions_own_books` |
| A past that is not a residence is still a past | **Built** | `story/journey_history.py` is the itinerary backend for characters Charter is the wrong simulator for, and it had been the resident path's poor sibling in four measurable ways. The event count was the constant 6/8 while the resident pass advertised 10–16 in the engine's own UI; it is now the author's, one number input beside the existing guidance box, normalized server-side by `journey_event_count` (default 12, band 3–20 — the floor is the existing usability floor, the ceiling is what one utility call can answer at 45–180 words an event without degrading into the synopsis list this generator exists to stop writing). The event carried no affect and no ranking signal while the resident episode carried six: `tone`, `lesson`, `valence`, `arousal`, `salience` and `kind` now come from `world.charter_history`'s vocabularies and clamps, imported rather than restated, so a journey row reaches the same retrieval surfaces with the same things to discriminate on — measured before the change, 59 of 91 live prestory memories sat at exactly salience 0.6. The prose carried generator scaffolding into the character's own recollection (`"<when>: Early in my travels, …"`, 12% of live prestory rows); when and place have their own embedded, FTS-mirrored fields, so the weld is gone and the content is the memory. And first person, asked for in the prompt since the generator existed and measurably not holding (15% of live episodic rows never say “I”), is now enforced by the same pack-scoped `_FIRST_PERSON_RE` the narration-person machinery reads — a grounding drop, not a second instruction, because a third-person synopsis cannot be rewritten into a perspective its author never took without inventing what was felt. The lived-location brief was computed for routing and thrown away; it now reaches the generator as `arrival_brief` so the last events may run toward the place the story opens at. Arriving is still not residing: residence is a `story/history_routing.py` topology decision this generator never makes and cannot reach. `importance` stays NULL here exactly as it does on the resident path — NULL reads as the salience, and only a consequence the engine can point at revises it. |
| A mind's theory of other minds travels with it | **Built** | `world/offscreen.py`'s `AGENT_CONTEXT_KEYS` allowlist withheld `mind_models` from an off-screen tick — and the withholding protected nothing. The field has exactly ONE writer, `apply_mind_model_updates`, fed only by that mind's own firewalled turns, so handing it back opens no path between two heads; meanwhile the commonest trigger for a paid off-screen tick is carried evidence ABOUT somebody, which is precisely the moment a mind needs what it already thinks of them. The allowlist bounds CHANNELS, not VOLUME: the question for any candidate key is never "how much does this tell the mind" but "who wrote this field, from what inputs, and does handing it back open a path between two heads the fiction says are separate". Excluding a mind's own state is not caution — it is making a mind conclude less than its own evidence supports, the one repair this document's firewall section names as wrong. What travels is the DERIVED view the on-screen step already hands the same mind: decay applied, leading claim and current confidence and competitors, frame-masked by the same `nonexistent_cast` backstop — never the raw ledger, whose `last_updated_turn`/`first_seen_turn`/`formed_under` bookkeeping is the ENGINE's rather than theirs. Conviction arrives as it stands now, not at its peak, because an off-screen tick is exactly the moment the most time has passed. Its model OF the player rides too, and the distance stays real precisely because that model can be wrong; the player's own side — position, recent action, the turn feed — remains unreachable through the signature. `docs/design/DESIGN_OFFSCREEN_MIND_MODELS.md` |
| A cost you cannot decline was never a choice | **Built** | The paradox ladder's four rungs are `warn → hazard → toll → force_restore`, and three things about the top of it were wrong in ways only a latent subsystem can be. The **ceiling is not a rung**: `paradox_policy.max_severity` clamps how far the ladder may climb, so a chat capped at `hazard` was silently answering a `force_restore`-grade contradiction with a hazard note — the clamp is now terminal, not a rung the ladder walks through. The **toll is a chosen mode, not a tax**: `toll_in_radius` defaulted true and charged every body inside the radius, so a consequence nobody agreed to landed on bystanders; a cost the player cannot decline is not a choice the design ever offered. And `force_restore` **writes the scene** rather than only marking it, because a restoration recorded but not applied leaves the world contradicting its own ledger. Made deliberately BEFORE the first story finds them: verified read-only against the owner's live database, no story has ever opened a wound — one chat holds an empty `paradoxes` dict, one holds a `paradox_policy` byte-for-byte identical to the default triple, two hold fixed points, and zero wound markers exist anywhere in the `world` table. That is the one time a consequence design can change without changing anyone's story. `docs/design/DESIGN_PARADOX_CONSEQUENCES.md`; the two boundary questions in its §5 are open choices, not unfinished work |
| An opening passage puts things inside heads | **Built** | `GreetingInterpret` had one substantive field, `knowledge_seeds`, for one person — the card's owner. A greeting is not a list of facts one person knows. Read against the live corpus in full before the schema was shaped (46 greetings across 32 cards), an opening passage routinely establishes an emotional state at the instant the story opens, what each present mind believes about the others, and how they stand toward the player — and every one of those launched at zero, so a story whose first message is a man backed against a wall with a raised wrench began with a man who felt nothing and believed nothing. Sarah Moon is the sharpest case: one of her greetings opens at week-three warmth and another at day-one ignorance, and the engine launched both identically. `GreetingMind` with `GreetingBeliefSeed`/`GreetingStanceSeed`/`GreetingAffectSeed`, carried on `GreetingInterpret.minds`, routed by `story/greetings.py`'s `_seed_minds` at `EXTRACTOR_VERSION = 2`, and claimed at promotion by `commit_background.promote_background_character` reading the chat's `greeting_minds` world key. Three refusals bound it: no `projects` (the tier is earned in play under an adoption deliberation, never assigned — see the psychology guidance in `AGENTS.md`), no `intentions` (built to be completable and swept, so seeding them writes a to-do list rather than a person), and nothing about the PLAYER's own affect, beliefs or stances, because the player's inner life is the player's. `docs/design/DESIGN_GREETING_MINDS.md` |
| A rule stated once cannot drift from itself | **Built** | Four notes — `category_note`, `book_type_note`, `transit_note`, `extra_parts_note` — were pasted into seventeen prompt bodies across two packs. `{{fragment:<name>}}`, resolved at pack load, makes a copy impossible rather than making it detectable. English renders byte-identical before and after across all 41 prompt bodies, measured rather than assumed, which is the proof the mechanism substitutes exactly what the pastes said. Japanese renders differently in 13 prompts, and that difference IS the repair: seventeen independent translations of four texts collapsing into four canonical ones, and the drift they had accumulated was not phrasing wobble. `generator_lorebook`'s copy of `book_type_note` had **inverted the vehicle rule** — 移動するものには'vehicle'ではなく'location'を使用 — teaching the Japanese lorebook generator the exact opposite of the rule every other prompt in the same pack taught. `transit_note` had lost its protocol literals to translation, splitting `entities.<id>.state.transit` in half and rendering `hatch:`/`phase:` with full-width colons, an output vocabulary the committer does not accept; the `mapping_stage` copy had kept every literal intact, which is what proves the loss was the translation pass rather than a choice. `category_note` had translated `common|scholarly|esoteric` and `local|global` to their glosses, although those are the enum values a model must emit. The drift guard now asserts the stronger property on the raw pack files — every story pack embeds each fragment by reference at exactly the paths English does, the counts hold, and no pack carries a fragment's text pasted beside the fragment itself — so a reappearing paste fails in ANY pack, where the old guard could only hold English equal by hand and could no longer even locate the Japanese copies |

---

## The engine as built

### The spine

Information flows one direction, enforced as plumbing rather than prompt:

```
        ┌──────────── player intent (up) ────────────┐
        │                                             ▼
PLAYER → PERCEPTION → CHARACTERS → DIRECTOR → PERCEPTION → NARRATION
(acts)   (filter the  (react,      (resolve   (filter the   & CHARACTERS
         player act)  blind to     to one     resolved      (render/remember)
                      each other)  state)     state)
```

**Eyes severed from hand.** Perception flows *down* to the player through the
narrator; intention flows *up* from the player to the director, never touching
the narrator. In a single model the narrator is both eyes and hand — it
describes the world *and* authors what you do — which is why such systems put
words in your mouth. Here the narrator is downstream of your eyes and has no
access to your hand. It can make you *feel* anything and *do* nothing. That
severance is what makes it safe to give the narrator a lush, interpretive voice:
flavour in the perception channel cannot leak into the action channel.

### Turn shape

Exact stage order lives in `docs/guides/PIPELINE.md`. In brief:

**Opening turn** — `mapping_stage → director_establish → perception_establish →
narrator → commit`

**Normal turn** — `director_interpret → mapping_stage|mapping_quick →
perception_act → [reaction_loop] → [interaction_loop | parallel character:<id>]
→ director_resolve → background_react → perception_outcome → narrator → commit`

Every stage's output is stored as a `steps` row plus an immutable `variants`
row, with exactly one active variant. That dual representation is what makes
reroll, rerun-from-stage, manual editing and inspection possible — and it is why
the engine can be audited at all.

Auditable is not the same as legible, and the gap between them is measurable in
diagnosis time. A stored step is a JSON blob, and the two things hardest to see
in one are the two the engine does most: a perception stage emits one view per
mind keyed by cast id, where a view missing a whole sensory channel looks
exactly like a view that is merely shorter; and three of the plan's pairings run
concurrently, which `steps.ord` cannot express, so a simultaneous pair reads as
two sequential steps that happened to be quick. The pipeline drawer therefore
reads a per-perceiver step one mind at a time (named, with that mind's derived
observations beside its prose, raw JSON one click away), and every step carries
an `_engine_notes` record of what the deterministic layer repaired in it, which
steps it ran beside, and — `llm_calls` — every provider call the step paid for
({step_key, role, requested, served, in, out, cached, duration, kind}), so a
slow stage explains itself from the stored variant instead of from a stderr
line that died with the process.

### Ownership

Authority ends sharply. No agent overrules another in-domain.

| Agent | Owns | Must never |
|---|---|---|
| **Director** (`agents/director.py`) | Objective causality: interpreting the declaration, resolving outcomes, the seeded dice | Own character psychology or narration; silently replace the player's declared content |
| **Perception** (`agents/perception.py`) | What each observer legitimately receives. Stateless by requirement | Invent intent, add meaning, contradict the event stream, or leak hidden state. It may subtract and degrade, never add |
| **Character agents** (`agents/character.py`, `agents/loops.py`) | The subjective: what I would attempt, what these signals mean to me | Decide their own success — capability is objective and lives in the world record |
| **Background presence** (`agents/background.py`) | Named unregistered presences, one stateless authored reaction per beat each. An explicitly authored Charter may supply this body's own persistent history/personality packet and exact action affordances; the call still owns no state and commit routes an accepted act back to Charter. `max_reactors` defaults to 1 and is hard-clamped to 3 | Hold or invent memory/psychology; read another Charter body's packet; mutate Charter through prose; continue voicing a promoted body |
| **Narrator** (`agents/narration.py`) | Sentence-level craft, pacing, the player-facing slice | Originate player conduct or reveal unperceived facts |
| **Mapping** (`agents/mapping.py`) | Lore routing, retrieval, canon staging | Know character interiority |
| **`persist/commit.py`** | The sole persistence boundary | Trust model output — it is provisional until deterministic code validates it |

**Perception is stateless by requirement, not thrift.** A perception layer with
memory is a *bug*: remembered context could let last turn's knowledge bleed into
this turn's seeing, the exact leak it exists to prevent. The cheapest agent is
cheap *because* it must be amnesiac.

**Character agents are predictors, not role-players.** Role-play optimises "be
interesting" and volunteers the secret because the reveal is juicy. Prediction
optimises "be accurate", which means it must be free to be *boring* — to let the
coward stay hidden and nothing happen. That freedom is what makes the eventual
drama earned. The cost is that a predictor treats every fact in its context as
true and load-bearing, so context hygiene is non-negotiable.

### Information model

The load-bearing primitive is **provenance**. Humans tag a fact at encoding with
how and when they learned it, and the tag rides along on every later retrieval,
so the access list maintains itself for free. Models flatten all of that the
moment it is in the window. Binding `witnessed/heard/told/read/inferred/
remembered + turn_idx` at commit hand-installs that faculty — so "told to me as
a secret" and "just true" become structurally different stored objects, and
belief revision becomes possible.

Memory layers, per character:

| Layer | Nature | Cadence |
|---|---|---|
| Stable core | Traits with activation/inhibition cues, values, self-image, protected beliefs, coping patterns | Rare, evidence-gated |
| Stance / relationships | Trust, warmth, fear per target | Event-triggered (see [Structural debt](#structural-debt)) |
| Active state | Mood, goals, affect, stress activation/strain/load, independent pain and pleasure, and the unresolved charge they accumulate | Every turn; relaxes using simulation time. Charge outlives the level that built it and discharges only on the character's own declared resolution |
| Learned associations | Cue, appraisal bias, response tendency, strength | Evidence-gated reinforcement/extinction |
| Episodic | Witnessed events, provenance + salience | On commit; consolidated over time |
| Summaries | Autobiographical synthesis | Post-commit; reconstructible |

`mind/affect.py` implements surface/undercurrent/baseline with exponential decay
toward baseline. `mind/psychology_runtime.py` applies the same explicit-time
principle to stress and hedonic carry-over while keeping pain and pleasure as
independent current-event signals: a comforting touch can hurt a bruise and
still feel welcome. Survival vitals can supply a pain floor but are not required.
Stance axes must not erode on a clock; the grudge does not fade unless something
fades it.

### Persistence and source of truth

When representations disagree, resolve deliberately rather than updating every
copy blindly:

1. SQLite rows and `world` keys — durable runtime state
2. Active step variants — the inspectable result of the current turn
3. `PipelineContext` — in-memory working state for one execution
4. Pydantic schemas (`llm/schemas.py`) — accepted structured model output
5. Prompts — desired behaviour, never overriding deterministic validation

**Physical-world authority.** The frame-scoped `world.scene` blob is the sole
runtime authority for live rooms, positions and entity state. `room_registry` is
the sole cross-frame ledger of room identity and retirement. `world_entities` is
a derived projection of the scene commit. `world_placements` is decommissioned;
`fiction_worlds` and `fiction_locations` are deprecated import-compatibility
tables. `transit_edges` is only ever deleted: it is in no archive, no
checkpoint and no restore path, so it compatibly imports nothing.

**Commit is atomic.** Slow provider work (lore and memory embeddings) happens
*before* the write lock; then all primary turn mutations commit inside one outer
transaction under a per-turn idempotency lock. Any domain failure rolls the whole
turn back. Only autobiographical consolidation runs afterward, because it is a
reconstructible derived cache.

### Cost

Cost scales with **dramatic density, not story length**. Turn 2000 in a quiet
room with two people costs about what turn 2 in that room costs, because nothing
conditions on the 1998 turns between except memory stores that are *reduced and
retrieved against, not replayed*. The hot path is flat; only the backing stores
grow, and they grow cold.

Every agent runs on a reduction, never a log — and the reduction is both cheaper
*and* leak-proof, because the context an agent does not need and the context it
should not have are largely the same context. Statelessness is the default;
persistence is a privilege earned only by agents modelling a continuous self.

**The contract is reduced the same way the context is.** The character prompt
explains every key the payload *may* carry, while the payload carries most of
them rarely — so `prompts.character_prompt` subtracts the paragraphs whose whole
subject is a key this beat does not have, leaving a contract about the beat that
actually happened. Measured on live banks: 61.2 KB down to 52.9 KB and 50.5 KB,
13–17%. Three rules keep it from costing anything. It is gated on **the key
being in the payload**, never on a mechanism's fire rate — `PROJECTS`,
`WANTS AND GOALS` and `ASSOCIATIVE LEARNING` are the invitation that would
create the thing, and gating those would nail their rate at zero forever. It
reads **the finished payload**, not a second derivation of the same conditions,
so it cannot explain away a field the model is actually being handed. And it
fails **open**: an unmatched heading keeps its paragraph, so a preset that
rewrites one loses the saving and never the instruction. The gate table was
audited paragraph by paragraph rather than by heading, which is how three
paragraphs that carry rules beyond their own key — how to take a bearingless
doorway *at a walk*, why a walking stride is out of character for a body whose
drive is getting there, and what `active_state.goal` is at all — were found and
left ungated.

**The most expensive token is the one spent twice.** A validation failure costs
a whole repair round trip, and a character's decision-review retry cost a
second ~30k-token call — that retry is gone (`e629d60`, on the rule that only
BROKEN output earns a redo), which removes the largest of the two but not the
first. Censusing every warning the live corpus has stored
(19,474 active variants, 1,357 warnings) found most of that spend concentrated
in three places, none of which was a model reasoning badly. `resolved_events[].
event_id` — a number the *engine* assigns and the specialist merely echoes —
was 47% of every repair call, failed over `"#1"` instead of `1`.
`lore_ops[].knowledge_locations` was another 24%, the whole mapping commit
discarded over `"the vault"` instead of `["the vault"]`. Both are now read
rather than rejected, on `LenientModel`'s standing charter that a near-miss
shape should be READ; a genuinely ambiguous value (`"1,2"`, no digits at all)
still fails, because inventing one there would hide a real error. And
`_first_verbatim_repeat` had no length floor while its sibling
`_first_repeated_move` has had one at five tokens all along — so a character
saying "Mm." twice bought the full retry, and 9.2% of the corpus's 14,365
spoken lines are three words or fewer. An interjection is not a reissued line.

The same charter's quieter failure costs no call and is worse for it: a
name-keyed map (`{"reach the tower": {"impact": 0.6}}`) had its KEY placed by
two positional guesses — first required field, else first empty-defaulted prose
field — which is right everywhere except where a subject carries a non-empty
default, and `GoalImpact.serves` does. The goal was filed as the explanation,
`serves` stayed generic, and `persist/commit.py`'s goal matching could not use either.
An item model now DECLARES its subject (`_subject_field`, read through
`schemas._subject_field_of` ahead of both guesses) rather than being guessed
at; a name list was tried once and missed `belief` and `cue`, which is why the
rule has to be structural. Reading the declaration is itself a Pydantic-major
seam — the attribute is a plain string on 1 and a `ModelPrivateAttr` on 2, so
a bare `getattr` raised on the pinned major and the declaration had never once
been honoured in a default install.

---

## Player authority

**This is the engine's largest deliberate divergence from the founding
architecture, and it should be understood as a product decision rather than
drift.**

The founding design gives the player authority over *attempts*: the action you
chose always occurs as chosen, but whether it succeeds belongs to the director,
and facts about the world belong to mapping. This engine went further. It
distinguishes:

- **Contestable declaration** — "I try to take the key", "I lunge toward Mara".
  The motion begins; reactions and circumstance may alter the result.
- **Asserted declaration** — "I take the key", "the door collapses", "three
  hours pass". The effect is treated as *true*, and the director determines its
  consequences rather than whether it happened.

Assertion authority extends to world facts and time, not just the protagonist's
body. `flow.authority_claims` and `flow.scheduled_assertions` carry these
through the pipeline, with narrow carve-outs the director still refuses — most
importantly, a player claim about **another character's interior** is rerouted
to that character as an authorial *offer* it may decline, rather than enacted as
truth. Character agency survives player assertion.

**Why this is defensible.** The founding document's own closing principle is
that the engine is *a world, not a warden* — it "has no opinion on how the user
plays, because having one would mean simulating the taste that is the user's
whole contribution", and explicitly: *"the user can shackle themselves whenever
a story wants it — chosen limits make better play than enforced ones."* An
engine that maximises authorial power by default and offers restriction as an
opt-in is a direct reading of that principle, not a departure from it.

**The cost, stated plainly.** Broad assertion authority weakens the thing the
architecture is otherwise built to guarantee. If the player can assert that the
door collapsed, the world's causal integrity is partly the player's
responsibility rather than the engine's. The firewall still holds — no character
learns anything illegitimately — but "coherence without omniscience" becomes
coherence the player can override.

### Hard mode (built)

The resolution is the mode set already named in `llm/schemas.py`, now enforced:

| Mode | The player controls |
|---|---|
| `actor_only` | The protagonist's attempts, speech, and immediate bodily conduct. Assertions become *claims* the director adjudicates and may refuse |
| `explicit_outcomes` | The above, plus declared completed effects on the protagonist's own actions |
| `world_author` | The above, plus external events, entities, time, and world assertions (**today's behaviour**) |

Hard mode is `actor_only` with the director free to say no. Per chat
(`scene.player_authority` / `set_player_authority`,
`PUT /api/chats/{id}/player_authority`), chosen in the **Genre & style** menu
beside genre, tone and weather severity — it decides what a declaration MEANS
in this story, which is a question about the fiction rather than about how much
happens off screen — and defaulting to `world_author` — so an
existing story means tomorrow exactly what it meant yesterday, and this costs
one dict lookup per beat for everyone who never touches the dial.

Enforced in `director_interpret`, after claim extraction, by
`agents.common.apply_player_authority`. That point is chosen because it is the
only one where both representations of a declaration are on the table at once,
and **both** have to move or the downgrade is invisible: the CLAIM's scope
(`effect` → `intent`, which is what `_player_claim_findings` reads to decide a
claim may not be rejected) and the sequence element's `commitment` (`asserted` →
`contestable`, which is what the reaction gate reads to decide whether the
character it was done to gets to contest it physically — hard mode without that
is hard mode the cast cannot participate in).

The two design notes, and how each was settled:

1. **A refused assertion must not silently vanish.** The player wrote it for a
   reason. Silently dropping player text is the one thing the engine's authority
   contract has never done, and hard mode did not become the exception: nothing
   is deleted and nothing is rewritten. The declaration stays in the sequence
   verbatim, the claim is downgraded rather than dropped, and the record —
   `authority_downgrades` on the step, and `downgraded_assertions` in the same
   beat's resolve payload — asks the Director to resolve it as the attempt it
   now is or refuse it visibly in the prose. In the resolve payload rather than
   through `tell_director`, because that channel lands in `engine_notices` at
   commit and reaches the NEXT beat — a beat too late for the player reading
   this one, and under a restricted mode it would repeat every beat as
   past-tense feedback about a beat already settled.
2. **Mode is per-chat, not global.** Built that way, and the change record is
   stored with it: a beat where the player asserted a world fact and got it
   reads as a defect once the story is in `actor_only`, and knowing when the
   dial moved is the only thing that can explain it.

What no mode touches is the declaration itself. What the player SAID and
ATTEMPTED is fixed in every rung, and the resolve payload's `ABSOLUTE` flag is
unchanged by hard mode — a hard mode that let the Director rewrite the player's
words would be a worse violation than the one it set out to fix.

**Played, and what that found.** The first real hard-mode story (2026-08-18,
five rooms, two characters, three lore entries, `actor_only` throughout) found
the mode INERT for every assertion in it — three beats, three different shapes,
all granted. The mode was set correctly and the claims were wrong:
`_extract_authority_claims` resolves an asserted effect to the declaring actor
when the action names no targets, and interpret typed each assertion as an
action with no targets, so every one arrived stamped `subject_id` = the player
and read as the player's own body, which every rung grants.

Two guards walked into the world; a vault door opened; the player flew. Nothing
was rolled for and nothing was refused, because a non-rejectable claim never
reaches the Director's judgement at all.

The fix is that an INFERRED subject is not evidence of a body
(`subject_inferred` on the claim). Reading the prose instead does not work and
the two live cases prove it: "Two guards come around the corner" has no first
person and "I take the west door's handle" has plenty, so any test on the
wording grants exactly the wrong one. Replayed with the fix, the same beats
downgrade to contestable intents, the cast contests them (Mireille moved to
block the vault doorway), and the Director resolves them on its own judgement.

**What the mode does not promise.** On the replay the Director still let the
player fly. That is not a hard-mode failure: the mode changes WHO decides, not
what is decided, and `Design.md`'s own wording is "with the director free to
say no" — free, not obliged. That story's `fiction_model` was
`genre: unspecified` with an empty ontology, so nothing had ever said the house
was mundane and "can people fly here" had no authored answer. Physics is the
fiction model's job; authority is this mode's.

**Authority over an EFFECT is not authority over its subject's thinghood.**
The coverage check holds an asserted completed effect against the diff, and its
referrability gate passes a subject on either of two channels: the world
already holds a record for it, or the player typed the words. The second is
satisfied by every noun in a narrated sentence, and the repair sheet then
forbade the only correct answer for that class — "you may report
'already_encoded' … but never 'rejected'" — so the only permitted answer to a
grammatical subject that is not a thing (a measure, a pattern, a rhythm, a
state of light) was to encode it, and encoding a subject the world model has no
shape for means MINTING IT AS A SCENE ENTITY. Measured over one audited 15-beat
run: that is where the minted entities came from. The verdict `no_referent`
says what is actually true — the effect stands, and there is nothing structured
to encode it as — and is bounded the way `already_true` is
(`director_reconcile._verify_no_referent`): admissible only where the world does
not already know the subject, so a subject naming a body, a place or an
existing entity is refused the refusal and non-rejectability stands unchanged.
It degrades into the same `claim_notes` metadata an unreferrable subject
already degraded into: no warning, no diff, nothing minted.

Tests: `tests/test_player_authority_mode.py`,
`tests/test_resolve_reconciliation.py`.

---

## Beyond the founding design

Subsystems the original architecture never imagined, now load-bearing:

- **Temporal frames and paradox** (`core/frames.py`, `world/paradox.py`,
  `world/spatial_frames.py`). Alternate eras, travellers, per-frame cast status, fixed
  points, paradox detection. Most `world` keys are frame-scoped; cross-frame
  contracts deliberately are not.
- **Spatial model** (`world/spatial.py`, since split into thirteen `spatial_*`
  modules behind that facade; `story/scene.py`). Rooms, adjacency with bearings,
  egocentric frames (ahead/behind/came_from), barriers, hearing and visibility
  gating, zones and carry inference. Multi-room movement (`sprint_reach`,
  `passable_path`) is bounded by decision rather than sight, and the rooms
  crossed are reconstructed deterministically so a sprinted corridor is
  remembered rather than left as a hole in the map.
- **A mind's own map** (`commit.update_place_graph`, `world/place_purpose.py`).
  Per-character nodes and edges earned by walking or seeing, with retraction,
  bounded eviction, frontier distance, and an `affords` ledger recording what a
  remembered place is *for*. Kept on `chat_chars.state`, so checkpoints,
  archives and branching carry it with no schema change.
- **Durable wanting** (`mind/affect.py`). Drives, intentions, beat wants, and the
  project tier between them — capped at two, adopted only against a
  non-circular criterion, weighed at drive strength once established, and made
  legible when the mind drifts from one rather than decayed behind its back.
  A `progress` claim is a self-report and is audited: at the ceiling, where a
  beat spent on a finished goal gains nothing, and — since the first played
  case — on a beat that repeated an earlier move, where the goal is grinding
  UP the ramp and every repeat was buying a real +0.2 and a refreshed dormancy
  clock. Live (chat 80, turns 1–3): three beats of the same three sentences
  carried `ia1` from 0.0 to 0.4 with `barren_attempts` never set, while the
  engine had already measured the repetition twice and paid 82.2s for a second
  full character call over it. The ledger was simply never told. The flag is
  CARRIED from the character stage rather than re-derived, and a repetition
  the screen judged warranted still counts as progress. Below the ceiling it
  warns and holds progress but does not stall the goal: a slow intention is not
  a stuck one.
- **A position must be reachable** (`agents/director.py`). The movement
  backstop rules on DECLARED movement — route, contested doors, companions
  held back — and a position written for a body nobody said was going anywhere
  never reached it. Live (chat 80 turn 4): `interp["movement"]` was null and
  the spatial specialist wrote `{"Hinami": "obs_room"}`, moving a restrained
  subject through a two-way mirror out of a cell whose only other edge is a
  closed door, into the room she was being observed from — while the prose
  author, in the same step, had her correctly in the cell. The floor is
  reachability and never declaration: dragged, carried and lift-borne moves are
  legitimate and undeclared, and none of them passes through a wall. Scoped by
  what three existing tests taught it — a declared mover belongs to the
  backstop, because a closed door is contested rather than impossible and the
  causality owner decides it; and an entity is not a body, because a vehicle's
  arrival is what CREATES the dock edge everyone else walks.
- **Deterministic mechanics sweep** (`world/mechanics.py`). Timed arrivals, expiry,
  dock edges, news latency — LLM-free, seeded, idempotent.
- **Weather** (`world/weather.py`). One sky per scene, with each room's share decided
  by its own `exposure` (open / sheltered / enclosed) and by how many muffling
  boundaries the room graph puts between it and open air. The Director sets it
  on a beat that changes it; between those it drifts on the simulation clock,
  seeded and idempotent, so a reroll cannot produce a different sky. Sight and
  sound are answered separately, because a cellar sees nothing of a downpour
  and hears it clearly — and walking into a cave takes the rain from present,
  to muffled, to faint, to gone. A scene acquires weather only when its fiction
  establishes one, so a starship never has any. A Director's declaration is
  read as a REPORT and written over the sky already blowing, never in place of
  it: the vocabulary is five short enums, a beat describing a storm reaches for
  the vivid word (`blizzard`, `gale-force`, `sub-zero`), and every default is
  the mildest reading of its field — so an exact-match lookup that answered
  each unread word with its default turned the worst weather in the vocabulary
  into a calm spring day and replaced the storm with it. `_SYNONYMS` reads the
  words models actually write, and a word it still cannot read keeps what the
  scene had rather than clearing it. (Live failure, "The Blizzard" turn 2: all
  five declared fields missed, the whiteout became fair/none/still/mild while
  the player stood in an open clearing, and five later beats inherited the
  calm.)
- **Weather rendering** (`static/js/weather-fx.js`). Rain and snow drawn over
  the story for rooms that can see the sky, with storm flashes and thunder
  arriving after them on a distance-shaped delay. A minority of flashes —
  `WFX_BOLT_CHANCE`, 22% — also draw the channel itself, `weatherFxBolt`
  building a fresh branched polyline per strike and tearing it down after,
  because lightning is never twice the same shape and a reused asset would
  read as a loop faster than the rain does. Gated on `sky_visible` rather than
  the wider `weather_visible`: a storm watched from under an awning still
  lights the sky, but a bolt drawn under the very cover the reader stepped
  beneath is simply wrong. It fades out well short of the ground, which is the
  answer to the objection that stalled it — where a bolt LANDS is inside a
  photograph this code cannot see, so it strikes nothing in particular. A storm sky is not
  automatically an electrical one: `weather.has_lightning` and its mirror
  `weatherFxStormy` require precipitation that is not snow or sleet, so an
  ordinary blizzard neither flashes nor puts thunder into what a room hears.
  **Thundersnow** is the exception and is a property of the SKY rather than
  something derived from what falls — derived, every blizzard would flash;
  forbidden, none ever could. `advance_weather` rolls it seeded at
  `THUNDERSNOW_ODDS`, so a squall almost never flashes and a long blizzard
  probably will once, identically on every replay; a beat may also declare it.
  `normalize_weather` clears the flag anywhere it is meaningless, so lightning
  cannot outlive the snow that earned it. Drawn as three repeating tiles moved by the compositor. Snow
  additionally drifts: each layer carries a second, composed transform swaying
  it sideways on its own period (none of the three divide each other, so the
  depths never fall back into step) and leans a few degrees off the stack's
  angle via the independent `rotate` property — three layers on one identical
  vector read as wallpaper being pulled rather than as snow. Rain is exempt,
  and falls at roughly 1.7x its first-pass speed, because rain falls hard and
  straight. Marks per tile scale by tile AREA (equal counts at unequal sizes made the
  smallest layer three times the density of the largest) and snow gets its own
  larger, mutually non-multiple tile sizes: a rain streak blurs into its
  neighbours, while a snowflake is a distinct blob whose constellation the eye
  finds repeating. Skipped entirely under `prefers-reduced-motion`, and routed
  through the ambience mute.
- **Scene backdrops** (`dressing/backdrops.py`). Generated images of the room, built from
  a whitelisted spatial projection that structurally excludes occupants. Cached
  per room-plus-visible-state; a branch reads its ancestors' images in place.
  Reading one is free and immediate; commissioning one waits until the reader
  has settled on a turn — scrolling through a story passes rooms nobody stopped
  in, and neither the picture nor the sound is bought for those. All three
  presentation layers answer the same question — WHICH TURN IS BEING READ — from
  one scroll observer and one per-turn payload built from `scene_after_turn`, so
  scrolling back through a story is chronological: the picture, the sky and the
  sound are the ones that stood while that beat happened, not the ones the story
  has since arrived at. A turn with no picture or bed of its own holds the one
  already showing rather than blanking, but only while it belongs to the same
  ROOM — held across a doorway, the transcript and the screen disagree about
  where the story is.
- **Room ambience** (`dressing/ambience.py`). A looping sound bed for the player's room,
  from the same occupant-free discipline: the query is written from a
  whitelisted projection, so a soundscape cannot report a presence perception
  did not deliver. Cached per room-plus-AUDIBLE-state — deliberately a
  different set from the visual one, since light changes the picture and not
  the sound. Two sources (a local folder, or Freesound's CC-licensed APIv2,
  credited in the panel), a per-room host pin that overrides the automatic
  pick, and a reroll that remembers what it rejected. Up to three simultaneous
  layers (room tone / weather / one detail), each with its own level, its own
  reroll, its own credit and its own IDENTITY CHECK — a pin stores a sound's id
  rather than a preview URL that would expire, that id is resolved on
  Freesound's sound endpoint (its text search has no `id` field and answers
  `id:341802` with whatever scores as text — in practice one sound named
  `file_id.diz.mp3`, for every id alike, which made a pinned two-layer
  soundscape download one unrelated recording twice), and the preview URL,
  which carries its own sound id, is checked against the id the layer claims
  before anything is written to the cache — the weather layer carrying the attenuation its
  room's depth earned, so rain two rooms in is quiet over an undiminished room
  tone. That layer is the sky and only the sky: no thunder, since the engine
  draws the lightning and times the clap to it, and no wildlife, which belongs
  to the place, sits on its own level and goes on sounding after the rain
  stops. A host can stage that mix by ear and pin it to the room. The standard
  is a bed TRUE TO THE ROOM rather than a bed at any cost: candidates are
  ranked against the room's own description instead of the library's ordering,
  what the model names in `avoid` is struck out, and a place with no continuous
  sound of its own — a sealed vault, still air — can be judged silent, which is
  cached like any other answer and overruled by the reroll. The room's named
  FIXTURES are part of that description and rank ahead of its adjectives,
  because a hearth is a sound and "warm, modest, lit" is three things no
  microphone can hear. Freesound ANDs the words of a query, so a full room
  query almost always matches nothing and has to be broadened — and a rung that
  returns results is not a rung that ANSWERS: broadening is followed until a
  recording actually of this place comes back, and the ladder reaches past
  prefixes to single terms, since English puts modifiers in front of the head
  noun and a room's name in fiction is a proper noun no library has heard of.
  A winner matching neither the room nor what was searched for is refused
  outright rather than laid down as better than nothing. (Live failure, "The
  Blizzard": a warm hall with a lit hearth was searched for as "stone hearth
  fire crackle wooden room", every rung missed until the single word `stone`,
  and the hall was given a recording titled "ambience in a large cave" —
  scoring zero against the room, like every other candidate, and winning on a
  `loopable` tag.) Each bed loops by
  overlapping itself rather than restarting at the file boundary, where an MP3's
  padding leaves an audible hole. The cache key is deliberately coarse: it is a
  function of the TERMS a search would use, and a room whose state has moved but
  not audibly changed adopts the bed already on disk instead of resolving again.
- **Lorebook hierarchy** (`mind/memory.py`, `agents/mapping.py`). Nested books,
  inheritance modes, scope by world and location, link graph, canon locking.
- **Multiplayer and guest access** (`web/guest_access.py`).
- **Obligation ledger, background claims, authored events** — bookkeeping that
  keeps promises and unregistered presences coherent.
- **Import pipeline** (`story/importers.py`, `story/character_schema.py`). External card
  formats, heuristic and AI-reinterpreted paths, damaged-sheet repair on read,
  and a non-destructive v3 psychology gap-filler for older cards.
- **Per-story character cards** (`chat_chars.sheet`, `scene.active_cast`).
  Authors can tune an attached character for one story without mutating the
  reusable library resource or resetting that story's earned interior state.
  Names/uids stay fixed because they are identity keys throughout scene,
  recognition, memory, and relationship records.
- **Portable chat archives** (`persist/chat_archive.py`). Versioned, typed export/import
  with embedded resources, reference remapping, and atomic restoration.
- **Portable pipeline traces** (`persist/pipeline_trace.py`). Hash-only diagnostics by
  default, with explicit content-bearing offline replay artifacts.
- **Host authentication routes** (`web/auth_routes.py`, `web/guest_access.py`). Typed
  request/cookie transport separated from credential/session persistence.
- **Appearance system** (`static/themes.css`). Browser-local themes, independent
  story-text sizing.
- **Provider layer** (`llm/providers.py`, `llm/prompt_cache.py`, `llm/llm_quality.py`).

---

## Structural debt

The honest account. These are not open bugs; they are places where the engine is
weaker than its own stated principles.

### 1. The positive guarantee is weaker than the negative one

The firewall is excellent at *keeping the forbidden thing out*. It is much
weaker at *making the correct thing reachable and preferred*. Both are supposed
to follow from "structure over instruction"; only the first has really been
internalised.

Two production failures found in one session, both of this shape:

- **A model authored an engine primary key.** The AI import path accepted
  `identity.uid` from model output. `story/scene.py` falls back to that field for the
  *scene entity id*, so when the model returned the character's own name, every
  import of one card collided into a single scene entity — two characters
  sharing one position, one set of clothes, one owner of the memories. Fixed by
  minting the key in code, which is what structure required from the start.
- **A character could not cite the present.** `observations_used` *instructed*
  the character to cite evidence, in a payload where only memory rows carried
  ids and the current beat was an uncitable prose string. The firewall worked
  perfectly; what failed was that the permitted information had no structural
  affordance while the stale information did. The first diagnosis — 15
  citations of a previous turn and zero of the current one across one 61-turn
  chat, "a character reliably answering the previous line" — was superseded by
  the corpus-wide count: across 1,254 stored character variants and 6,404
  citations, **4,939 named an invented label** (`view` 1,532,
  `current_perception` 505, `perception` 338, …), 1,172 used the magic string
  `"current"` the prompt asked for, 255 were blank, and **38 carried a real
  id**. Characters were citing the present beat all along; they were citing it
  under about fifteen spellings of a thing that had a real name, which is
  unusable as evidence and unverifiable as a claim.

The second fix has now landed: `perception.py` mints real observation ids
(`current:<perceiver>:<n>`), the prompt points at them, and `llm/schemas.py`
normalizes legacy spellings back to the canonical sentinel. Retrieved rows use
their durable `event_key` and carry `temporal_status`, relative `when`, and
provenance. `_ground_observation_citations` then admits only ids actually
delivered to this mind across every evidence-bearing output field. A current
citation the model supplied is moved first; omission is warned, never repaired
by inventing audit evidence. The structural affordance exists, the prompt uses
it, and the output boundary can verify it.

**Rule to apply going forward:** whenever a prompt asks a model to prefer X over
Y, check whether the payload makes X *harder to reach* than Y. If it does, the
prompt will lose. **And the corollary this one added: when the payload is later
fixed, the prompt does not update itself.** A patch written for the old payload
goes on suppressing the new one — the magic-string instruction survived the
arrival of real ids and was still collecting fifteen spellings of them.

### 2. Stance changes are not auditable

The founding commitment is that every numeric change is event-linked with a
logged trigger. Reality: `apply_relationship_updates` accepts
`trigger_event_ids` but treats them as optional, with explicit handling for "a
routine trigger-less delta". Relationships live in the `world` KV blob, so there
is no change log — only the current value plus a `salient_event` string. There
is no way to answer "why does she distrust him?" from the record.

The founding design also specifies that a normal interaction moves an axis by no
more than ~0.05; the schema clamps at ±0.2.

### 3. Two import paths of very different quality

The heuristic (non-AI) import derives psychology from the card's `personality`
field. A v2 card that puts everything in `description` — common — can still
yield a sparse first pass. The character editor now exposes **Fill psychology
gaps**, which asks for a short account of formative pressures, triggers,
conflicts, coping, sensitivities, and recurring cues, then fills empty v3
psychology fields without replacing authored identity, appearance, goals, or
non-empty psychology. The initial heuristic path still needs better automatic
description fallback and sparse-import warning.

### 4. Documentation forcing functions are uneven

`docs/CODE_MAP.md` is well maintained because `make structure` fails on
staleness. No equivalent exists for hand-written docs, which is how the previous
edition of this file drifted. `docs/guides/DATABASE.md` remains deliberately compact
for a schema with roughly 30 tables and a long migration chain, while
`AGENTS.md` routes every schema change through it.

---

## Roadmap

The roadmap now lives in [`docs/UNBUILT.md`](docs/UNBUILT.md), together with
every other list of unfinished work this repository was keeping separately —
known defects, deferred audit findings, and the residuals of each design note.
There is one register, and an entry is deleted from it in the commit that lands
it.

Items 2.2-2.3 of that register repay the structural debt above: a
`relationship_events` table (debt #2) and a `description` fallback in the
heuristic import (debt #3). Debt #1's item is gone from the register because it
landed — the present beat has a first-class id, and the record of it is in the
structural-debt entry above rather than on a worklist.

Ideas that are parked rather than scheduled — a conformance test for this
document, a leak-injection suite, salience-driven personal lore, per-character
retrieval depth, belief-revision salience, an epistemic minimap — are in that
file's final section.

---

## Failure modes

| Symptom | Cause | Fix |
|---|---|---|
| NPC says what it shouldn't | Forbidden fact in its context | Structural firewall: each mind gets only its perception object |
| NPC treats private thought as dialogue | Thought and speech entered the same window | Separate channels: speech → director event; thought → routed nowhere |
| NPC reacts to something it wasn't present for | Presence was a sentence, not a router rule | Perception routes the beat only to minds that were there |
| Narrator mentions what the player can't see | Narrator knows more than the player | Deny the narrator everything but the player's perception object |
| Character answers the *previous* line | Present beat unreachable; only the past was citable | Give the present a first-class id — `current:<perceiver>:<n>`, minted in `agents/perception.py` and grounded by `_ground_observation_citations` (see [Structural debt](#structural-debt) #1) |
| Cast feels lifeless, nobody acts | Variance too low | Raise per-character temperature |
| A character is spookily prescient | Context leak | Firewall plus strict character context hygiene |
| World heals — door un-breaks, trap vanishes | Off-screen state dropped | Commit-up plus standing intentions with triggers |
| Secret-in-a-crowd is common knowledge | Visibility posture missing | Overt/concealed plus target on every declaration |
| A character names an act it could only have *felt* | The closed channel cost modality but not resolution — the act's verb crossed into touch as a paraphrase | Sensation crosses, the act's name does not; acuity sharpens detail within its own modality and never buys knowledge of the cause |
| Betrayal reads as confusion | Flat beliefs, no provenance | Provenance tags; revision as high salience |
| Trust silently erodes | Clock-driven numeric drift | Stance axes event-linked; only mood decays |
| Two characters share one position and one set of clothes | A model authored an identity key | Mint engine keys in code; never read them from model output |
| The world cannot tell the player "no" | `world_author` authority by default | Hard mode (`docs/UNBUILT.md` §2.4) |

---

## Keeping this document honest

1. Change a status row in the same commit that changes the behaviour.
2. Prefer "Partial" with a precise gap over "Built" with a caveat buried in
   prose. The gap sentence is the useful part.
3. When something in `docs/UNBUILT.md` ships, add its row here and delete the
   entry there. Do not leave it in both.
4. If this file passes roughly 500 lines, something belongs in `AGENTS.md`,
   `docs/guides/PIPELINE.md` or `docs/guides/DATABASE.md` instead. Length is how the last
   edition died.

---

### Durable attire exposure

Once a region's authored beneath-surface has entered play, unrelated later
wardrobe changes preserve that `uncovered` history. Ordinary coverage still
hides it. Bare-surface phrases are body state and never become invented
garments.

## In one breath

The player acts; perception filters the act so the present cast can react blind;
the director collapses the player's declaration and all reactions into one
resolved state, dice optional and seeded; perception filters that outcome per
mind; the narrator renders the player's slice — coloured by a voice-setting only
it can see — while each character commits a provenance-tagged, perception-
filtered memory of what it personally registered. The narrator renders
perception and never authors action, because intent runs straight to the
director and never through it. Every mind holds only what it earned.
Statelessness is the default; persistence is reserved for the agents being a
continuous self. No agent authors the story — it is the residue of honest minds
under honest causality. Omniscience exists nowhere inside the world, and only in
the player above it, who may hold as much or as little of it as they choose.
